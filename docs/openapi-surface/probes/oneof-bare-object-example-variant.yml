openapi: 3.1.0
info:
  title: oneof-bare-object-example-variant probe
  version: 1.0.0
paths:
  /probe:
    get:
      operationId: getProbe
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Probe"
components:
  schemas:
    Probe:
      oneOf:
        - type: object
          example:
            name: sample
        - type: string

