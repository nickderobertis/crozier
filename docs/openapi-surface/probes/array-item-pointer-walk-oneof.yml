openapi: 3.1.0
info:
  title: array-item-pointer-walk-oneof probe
  version: 1.0.0
paths:
  /probe:
    get:
      operationId: getProbe
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Probe"
components:
  schemas:
    Probe:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: "#/components/schemas/Target/oneOf/0"
    Target:
      oneOf:
        - type: string
        - type: integer

