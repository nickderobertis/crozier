# OpenAPI surface coverage — Parameters, headers and examples

Classified entries for the `parameters` region, against the boundary in
`## Scope` below.

## Scope

Parameter, Header and Example objects, and `components.parameters`,
`components.headers`, `components.examples`.

## How the feature list was enumerated

From the normative OpenAPI 3.0.4 and 3.1.1 object sections, not from crozier's
source. Repeating these five steps in order reproduces the 70 rows below:

1. **Fixed fields.** Take the *Fixed Fields* table of the Parameter Object, the
   Header Object and the Example Object, and emit one row per field. That is 13 +
   11 + 4 = 28 rows. (The Header Object's table is the Parameter Object's minus
   `name` and `in`, which is why it is 11.)
2. **Closed vocabularies.** Emit one row per member of each closed value set a
   field of step 1 draws from: the four `in` values. That is 4 rows.
3. **The serialization matrix.** The specification's *Style Values* table fixes
   which `style` is legal in which `in`, and each cell is then crossed with a
   scalar, an array and an object schema — the three shapes its *Style Examples*
   table distinguishes. `matrix`/`label` are path-only, `simple` is path and
   header, `form` is query and cookie, and `spaceDelimited`/`pipeDelimited`/
   `deepObject` are query-only: 7 legal style/location cells × 3 schema shapes =
   **27 features**, carried by 26 rows. The one merge is `label` with an array and
   `label` with an object, which [`../fern-limitations.md`](../fern-limitations.md)
   rules on under the single key `label-array-or-object`; splitting it would
   invent two keys that join on nothing, so one row carries both and says so.
4. **Modifier crossings the ledger already rules on.** `explode` and `content` are
   fields (step 1), but the ledger also rules on specific crossings of them, and a
   crossing is a feature of its own. Crossing `content` with the four `in` values
   gives 4 rows; the two `explode: true` crossings the ledger names give 2.
5. **Reuse and placement.** The three Components maps this region owns, plus the
   three placement rules the specification states for a parameter: reaching one
   through a Reference Object, declaring one on a Path Item, and an Operation-level
   parameter overriding a Path Item-level one of the same `(name, in)`. That is 6
   rows.

28 + 4 + 26 + 6 + 6 = **70**. Steps 1 and 3–4 stack rather than overlap: "the field
is declared at all" and "the field is declared with *this* value, in *this*
location, over *this* schema shape" are different questions, and a `golden` answer
to the first says nothing about the second — which is the whole point of the
matrix.

Three neighbours are cross-linked rather than enumerated, so no feature lands in
two files. The Header Object rows cover Header Objects reached both ways the
specification allows — a Response Object's `headers` and an Encoding Object's
`headers` — but those two containing *fields* are
[bodies and media](bodies-media.md)'s. Reference Object semantics are
[document and paths](document-paths.md)'s; only the one case of reaching a
*Parameter* through a `$ref` is enumerated here. And `schema` is inventoried
only as the Parameter/Header field it is — every Schema Object field and JSON
Schema keyword is [schemas](schemas.md)'s, wherever it appears.

## Entries
| key | oas | spec location | category | evidence | crozier sites | why bytes could move | settlement |
|---|---|---|---|---|---|---|---|
| `parameter-name` | both | Parameter Object.name | golden | census `parameter.name`: 14,538 declaration(s) in 93 registered source(s): 6-dot-authentiqio.appspot.com, amazonaws.com-cloudformation, amazonaws.com-cloudfront, anchore.io, apache.org, apache.org-airflow, apache.org-qakka, apicurio.local-registry, apideck.com-accounting, apideck.com-ats, apideck.com-connector, apideck.com-crm, apideck.com-customer-support, apideck.com-ecommerce, apideck.com-ecosystem, apideck.com-file-storage, apideck.com-hris, apideck.com-issue-tracking, apideck.com-lead, apideck.com-pos, apideck.com-proxy, apideck.com-sms, apideck.com-vault, apideck.com-webhook, apis.guru, appng-rest-api, appwrite.io-client, appwrite.io-server, asana.com, atlassian.com-jira, axesso.de, bbci.co.uk, bintable.com, box.com, bracketed-property-names, bungie.net, bunq.com, byautomata.io, calorieninjas.com, canada-holidays.ca, codesearch.debian.net, color.pizza, conjur.local, cookie-parameters, corrently.io, discourse.local, dnd5eapi.co, electric-sql, enum-name-sanitization, enum-query-param, eozilla, error-responses, esgenterprise.com, etherpad.local, etsi.local-mec010-2_apppkgmgmt, exa-gate, exhaustive, frankfurter, free5gc-namf-communication, free5gc-pdu-session, gambitcomm.local-mimic, github.com, gov.bc.ca-news, groundhog-day.com, helios-verifiable-api, http-toolkit, integer-enums, letta, maif.local-otoroshi, med-anvisa-price, microcks.local, nested-core-imports, netbox.dev, nimisampo, openbanking.org.uk-account-info-openapi, openepcis-dpp-ready, openfigi.com, query-parameters-openapi, redhat.com-catalog_inventory, redocly.com-museum, reverb.com, sac-backend, sigstore-rekor, slurmdb-rest, squareup.com, tamoss, tlon-notes, traccar.org, twilio.com-twilio_messaging_v1, twilio.com-twilio_voice_v1, withsecure-gdpr-subject-rights, worldcoin-signup-sequencer, xero.com-xero-payroll-au |  |  |  |
| `parameter-in` | both | Parameter Object.in | golden | census `parameter.in`: 14,538 declaration(s) in 93 registered source(s): 6-dot-authentiqio.appspot.com, amazonaws.com-cloudformation, amazonaws.com-cloudfront, anchore.io, apache.org, apache.org-airflow, apache.org-qakka, apicurio.local-registry, apideck.com-accounting, apideck.com-ats, apideck.com-connector, apideck.com-crm, apideck.com-customer-support, apideck.com-ecommerce, apideck.com-ecosystem, apideck.com-file-storage, apideck.com-hris, apideck.com-issue-tracking, apideck.com-lead, apideck.com-pos, apideck.com-proxy, apideck.com-sms, apideck.com-vault, apideck.com-webhook, apis.guru, appng-rest-api, appwrite.io-client, appwrite.io-server, asana.com, atlassian.com-jira, axesso.de, bbci.co.uk, bintable.com, box.com, bracketed-property-names, bungie.net, bunq.com, byautomata.io, calorieninjas.com, canada-holidays.ca, codesearch.debian.net, color.pizza, conjur.local, cookie-parameters, corrently.io, discourse.local, dnd5eapi.co, electric-sql, enum-name-sanitization, enum-query-param, eozilla, error-responses, esgenterprise.com, etherpad.local, etsi.local-mec010-2_apppkgmgmt, exa-gate, exhaustive, frankfurter, free5gc-namf-communication, free5gc-pdu-session, gambitcomm.local-mimic, github.com, gov.bc.ca-news, groundhog-day.com, helios-verifiable-api, http-toolkit, integer-enums, letta, maif.local-otoroshi, med-anvisa-price, microcks.local, nested-core-imports, netbox.dev, nimisampo, openbanking.org.uk-account-info-openapi, openepcis-dpp-ready, openfigi.com, query-parameters-openapi, redhat.com-catalog_inventory, redocly.com-museum, reverb.com, sac-backend, sigstore-rekor, slurmdb-rest, squareup.com, tamoss, tlon-notes, traccar.org, twilio.com-twilio_messaging_v1, twilio.com-twilio_voice_v1, withsecure-gdpr-subject-rights, worldcoin-signup-sequencer, xero.com-xero-payroll-au |  |  |  |
| `parameter-description` | both | Parameter Object.description | golden | census `parameter.description`: 13,295 declaration(s) in 83 registered source(s): 6-dot-authentiqio.appspot.com, amazonaws.com-cloudformation, amazonaws.com-cloudfront, anchore.io, apache.org, apache.org-airflow, apache.org-qakka, apicurio.local-registry, apideck.com-accounting, apideck.com-ats, apideck.com-connector, apideck.com-crm, apideck.com-customer-support, apideck.com-ecommerce, apideck.com-ecosystem, apideck.com-file-storage, apideck.com-hris, apideck.com-issue-tracking, apideck.com-lead, apideck.com-pos, apideck.com-proxy, apideck.com-sms, apideck.com-vault, apideck.com-webhook, appng-rest-api, appwrite.io-client, appwrite.io-server, asana.com, atlassian.com-jira, axesso.de, bbci.co.uk, bintable.com, box.com, bungie.net, bunq.com, byautomata.io, calorieninjas.com, canada-holidays.ca, codesearch.debian.net, color.pizza, conjur.local, cookie-parameters, corrently.io, discourse.local, dnd5eapi.co, electric-sql, eozilla, esgenterprise.com, etsi.local-mec010-2_apppkgmgmt, exhaustive, frankfurter, free5gc-namf-communication, free5gc-pdu-session, gambitcomm.local-mimic, github.com, gov.bc.ca-news, groundhog-day.com, helios-verifiable-api, letta, maif.local-otoroshi, med-anvisa-price, microcks.local, netbox.dev, nimisampo, openbanking.org.uk-account-info-openapi, openepcis-dpp-ready, openfigi.com, query-parameters-openapi, redhat.com-catalog_inventory, redocly.com-museum, reverb.com, sac-backend, sigstore-rekor, slurmdb-rest, squareup.com, tamoss, tlon-notes, traccar.org, twilio.com-twilio_messaging_v1, twilio.com-twilio_voice_v1, withsecure-gdpr-subject-rights, worldcoin-signup-sequencer, xero.com-xero-payroll-au |  |  |  |
| `parameter-required` | both | Parameter Object.required | golden | census `parameter.required`: 13,060 declaration(s) in 90 registered source(s): 6-dot-authentiqio.appspot.com, amazonaws.com-cloudformation, amazonaws.com-cloudfront, anchore.io, apache.org, apache.org-airflow, apache.org-qakka, apicurio.local-registry, apideck.com-accounting, apideck.com-ats, apideck.com-connector, apideck.com-crm, apideck.com-customer-support, apideck.com-ecommerce, apideck.com-ecosystem, apideck.com-file-storage, apideck.com-hris, apideck.com-issue-tracking, apideck.com-lead, apideck.com-pos, apideck.com-proxy, apideck.com-sms, apideck.com-vault, apideck.com-webhook, apis.guru, appng-rest-api, appwrite.io-client, appwrite.io-server, asana.com, atlassian.com-jira, axesso.de, bbci.co.uk, bintable.com, box.com, bracketed-property-names, bungie.net, bunq.com, byautomata.io, calorieninjas.com, canada-holidays.ca, codesearch.debian.net, color.pizza, conjur.local, cookie-parameters, discourse.local, dnd5eapi.co, electric-sql, enum-name-sanitization, enum-query-param, eozilla, error-responses, esgenterprise.com, etsi.local-mec010-2_apppkgmgmt, exa-gate, exhaustive, frankfurter, free5gc-namf-communication, free5gc-pdu-session, gambitcomm.local-mimic, github.com, gov.bc.ca-news, groundhog-day.com, helios-verifiable-api, http-toolkit, integer-enums, letta, maif.local-otoroshi, microcks.local, nested-core-imports, netbox.dev, nimisampo, openbanking.org.uk-account-info-openapi, openepcis-dpp-ready, openfigi.com, query-parameters-openapi, redhat.com-catalog_inventory, redocly.com-museum, reverb.com, sac-backend, sigstore-rekor, slurmdb-rest, squareup.com, tamoss, tlon-notes, traccar.org, twilio.com-twilio_messaging_v1, twilio.com-twilio_voice_v1, withsecure-gdpr-subject-rights, worldcoin-signup-sequencer, xero.com-xero-payroll-au |  |  |  |
| `parameter-deprecated` | both | Parameter Object.deprecated | golden | census `parameter.deprecated`: 30 declaration(s) in 4 registered source(s): bungie.net, electric-sql, github.com, letta |  |  |  |
| `parameter-allow-empty-value` | both | Parameter Object.allowEmptyValue | golden | census `parameter.allowEmptyValue`: 13 declaration(s) in 4 registered source(s): amazonaws.com-cloudfront, asana.com, canada-holidays.ca, nimisampo |  |  |  |
| `parameter-style` | both | Parameter Object.style | golden | census `parameter.style`: 167 declaration(s) in 29 registered source(s): anchore.io, apache.org, apache.org-airflow, apideck.com-accounting, apideck.com-ats, apideck.com-connector, apideck.com-crm, apideck.com-ecommerce, apideck.com-file-storage, apideck.com-hris, apideck.com-issue-tracking, apideck.com-lead, apideck.com-pos, apideck.com-vault, apideck.com-webhook, appng-rest-api, asana.com, bintable.com, bungie.net, color.pizza, electric-sql, free5gc-namf-communication, free5gc-pdu-session, openfigi.com, query-parameters-openapi, redhat.com-catalog_inventory, reverb.com, slurmdb-rest, traccar.org |  |  |  |
| `parameter-explode` | both | Parameter Object.explode | golden | census `parameter.explode`: 240 declaration(s) in 32 registered source(s): anchore.io, apache.org, apache.org-airflow, apideck.com-accounting, apideck.com-ats, apideck.com-connector, apideck.com-crm, apideck.com-ecommerce, apideck.com-file-storage, apideck.com-hris, apideck.com-issue-tracking, apideck.com-lead, apideck.com-pos, apideck.com-vault, apideck.com-webhook, appng-rest-api, asana.com, bbci.co.uk, bintable.com, box.com, bungie.net, color.pizza, electric-sql, free5gc-namf-communication, free5gc-pdu-session, gov.bc.ca-news, nimisampo, query-parameters-openapi, redhat.com-catalog_inventory, reverb.com, slurmdb-rest, traccar.org |  |  |  |
| `parameter-allow-reserved` | both | Parameter Object.allowReserved | golden | census `parameter.allowReserved`: 19 declaration(s) in 2 registered source(s): nimisampo, tamoss |  |  |  |
| `parameter-schema` | both | Parameter Object.schema | golden | census `parameter.schema`: 14,534 declaration(s) in 93 registered source(s): 6-dot-authentiqio.appspot.com, amazonaws.com-cloudformation, amazonaws.com-cloudfront, anchore.io, apache.org, apache.org-airflow, apache.org-qakka, apicurio.local-registry, apideck.com-accounting, apideck.com-ats, apideck.com-connector, apideck.com-crm, apideck.com-customer-support, apideck.com-ecommerce, apideck.com-ecosystem, apideck.com-file-storage, apideck.com-hris, apideck.com-issue-tracking, apideck.com-lead, apideck.com-pos, apideck.com-proxy, apideck.com-sms, apideck.com-vault, apideck.com-webhook, apis.guru, appng-rest-api, appwrite.io-client, appwrite.io-server, asana.com, atlassian.com-jira, axesso.de, bbci.co.uk, bintable.com, box.com, bracketed-property-names, bungie.net, bunq.com, byautomata.io, calorieninjas.com, canada-holidays.ca, codesearch.debian.net, color.pizza, conjur.local, cookie-parameters, corrently.io, discourse.local, dnd5eapi.co, electric-sql, enum-name-sanitization, enum-query-param, eozilla, error-responses, esgenterprise.com, etherpad.local, etsi.local-mec010-2_apppkgmgmt, exa-gate, exhaustive, frankfurter, free5gc-namf-communication, free5gc-pdu-session, gambitcomm.local-mimic, github.com, gov.bc.ca-news, groundhog-day.com, helios-verifiable-api, http-toolkit, integer-enums, letta, maif.local-otoroshi, med-anvisa-price, microcks.local, nested-core-imports, netbox.dev, nimisampo, openbanking.org.uk-account-info-openapi, openepcis-dpp-ready, openfigi.com, query-parameters-openapi, redhat.com-catalog_inventory, redocly.com-museum, reverb.com, sac-backend, sigstore-rekor, slurmdb-rest, squareup.com, tamoss, tlon-notes, traccar.org, twilio.com-twilio_messaging_v1, twilio.com-twilio_voice_v1, withsecure-gdpr-subject-rights, worldcoin-signup-sequencer, xero.com-xero-payroll-au |  |  |  |
| `parameter-example` | both | Parameter Object.example | golden | census `parameter.example`: 727 declaration(s) in 26 registered source(s): apideck.com-accounting, apideck.com-ats, apideck.com-connector, apideck.com-crm, apideck.com-customer-support, apideck.com-ecommerce, apideck.com-file-storage, apideck.com-hris, apideck.com-issue-tracking, apideck.com-lead, apideck.com-pos, apideck.com-proxy, apideck.com-sms, apideck.com-vault, apideck.com-webhook, appng-rest-api, asana.com, box.com, conjur.local, discourse.local, dnd5eapi.co, electric-sql, github.com, query-parameters-openapi, withsecure-gdpr-subject-rights, xero.com-xero-payroll-au |  |  |  |
| `parameter-examples` | both | Parameter Object.examples | golden | census `parameter.examples`: 19 declaration(s) in 3 registered source(s): dnd5eapi.co, electric-sql, github.com |  |  |  |
| `parameter-content` | both | Parameter Object.content | golden | census `parameter.content`: 2 declaration(s) in 2 registered source(s): apideck.com-file-storage, nimisampo |  |  |  |
| `parameter-in-query` | both | Parameter Object.in (`query`) | golden | census `parameter.in=query`: 10,274 declaration(s) in 80 registered source(s): 6-dot-authentiqio.appspot.com, amazonaws.com-cloudformation, amazonaws.com-cloudfront, anchore.io, apache.org, apache.org-airflow, apache.org-qakka, apicurio.local-registry, apideck.com-accounting, apideck.com-ats, apideck.com-connector, apideck.com-crm, apideck.com-customer-support, apideck.com-ecommerce, apideck.com-ecosystem, apideck.com-file-storage, apideck.com-hris, apideck.com-issue-tracking, apideck.com-lead, apideck.com-pos, apideck.com-proxy, apideck.com-sms, apideck.com-vault, apideck.com-webhook, appng-rest-api, appwrite.io-client, appwrite.io-server, asana.com, atlassian.com-jira, axesso.de, bbci.co.uk, bintable.com, box.com, bracketed-property-names, bungie.net, byautomata.io, calorieninjas.com, canada-holidays.ca, codesearch.debian.net, color.pizza, conjur.local, corrently.io, discourse.local, dnd5eapi.co, electric-sql, enum-name-sanitization, enum-query-param, esgenterprise.com, etherpad.local, etsi.local-mec010-2_apppkgmgmt, exa-gate, exhaustive, frankfurter, github.com, gov.bc.ca-news, groundhog-day.com, helios-verifiable-api, integer-enums, letta, maif.local-otoroshi, med-anvisa-price, microcks.local, netbox.dev, nimisampo, openbanking.org.uk-account-info-openapi, openepcis-dpp-ready, query-parameters-openapi, redhat.com-catalog_inventory, redocly.com-museum, reverb.com, sac-backend, sigstore-rekor, slurmdb-rest, squareup.com, tamoss, tlon-notes, traccar.org, twilio.com-twilio_messaging_v1, twilio.com-twilio_voice_v1, xero.com-xero-payroll-au |  |  |  |
| `parameter-in-header` | both | Parameter Object.in (`header`) | golden | census `parameter.in=header`: 219 declaration(s) in 31 registered source(s): amazonaws.com-cloudformation, amazonaws.com-cloudfront, anchore.io, apicurio.local-registry, apideck.com-accounting, apideck.com-ats, apideck.com-connector, apideck.com-crm, apideck.com-customer-support, apideck.com-ecommerce, apideck.com-file-storage, apideck.com-hris, apideck.com-issue-tracking, apideck.com-lead, apideck.com-pos, apideck.com-proxy, apideck.com-sms, apideck.com-vault, apideck.com-webhook, atlassian.com-jira, box.com, bunq.com, conjur.local, cookie-parameters, discourse.local, electric-sql, exhaustive, letta, openbanking.org.uk-account-info-openapi, withsecure-gdpr-subject-rights, xero.com-xero-payroll-au |  |  |  |
| `parameter-in-path` | both | Parameter Object.in (`path`) | golden | census `parameter.in=path`: 4,043 declaration(s) in 76 registered source(s): 6-dot-authentiqio.appspot.com, amazonaws.com-cloudfront, anchore.io, apache.org, apache.org-airflow, apache.org-qakka, apicurio.local-registry, apideck.com-accounting, apideck.com-ats, apideck.com-connector, apideck.com-crm, apideck.com-customer-support, apideck.com-ecommerce, apideck.com-ecosystem, apideck.com-file-storage, apideck.com-hris, apideck.com-issue-tracking, apideck.com-lead, apideck.com-pos, apideck.com-proxy, apideck.com-sms, apideck.com-vault, apideck.com-webhook, apis.guru, appng-rest-api, appwrite.io-client, appwrite.io-server, asana.com, atlassian.com-jira, bbci.co.uk, bintable.com, box.com, bungie.net, bunq.com, canada-holidays.ca, conjur.local, discourse.local, dnd5eapi.co, eozilla, error-responses, etsi.local-mec010-2_apppkgmgmt, exa-gate, exhaustive, frankfurter, free5gc-namf-communication, free5gc-pdu-session, gambitcomm.local-mimic, github.com, gov.bc.ca-news, groundhog-day.com, helios-verifiable-api, http-toolkit, letta, maif.local-otoroshi, microcks.local, nested-core-imports, netbox.dev, nimisampo, openbanking.org.uk-account-info-openapi, openepcis-dpp-ready, openfigi.com, redhat.com-catalog_inventory, redocly.com-museum, reverb.com, sac-backend, sigstore-rekor, slurmdb-rest, squareup.com, tamoss, tlon-notes, traccar.org, twilio.com-twilio_messaging_v1, twilio.com-twilio_voice_v1, withsecure-gdpr-subject-rights, worldcoin-signup-sequencer, xero.com-xero-payroll-au |  |  |  |
| `cookie-parameter` | both | Parameter Object.in (`cookie`) | golden | census `parameter.in=cookie`: 2 declaration(s) in 1 registered source(s): cookie-parameters; also `../fern-limitations.md` `cookie-parameter`: discards |  |  |  |
| `parameter-style-matrix-path-scalar` | both | Parameter Object.style (`matrix`, `in: path`, scalar schema) | gap | conjunction pass (`style=matrix`, `in=path`, scalar schema), re-run for this change over the 141-source walk [`document-paths.md`](document-paths.md#snapshot-reconciliation) pins: 3 declaration(s) in 1 registered source: appng-rest-api — which carries no committed golden (`../../tests/fixtures/CORPUS.md` records why), so no golden fixture declares it; no `../fern-limitations.md` row names it. The world-wide search for a real-world document declaring it is the `parameter-style-matrix-path-scalar` row of [Witness search (issue #188)](#witness-search-issue-188) below, whose outcome is `fern-rejected`: the corpus's own declarer, `appng-rest-api`, is Apache-2.0 at commit `8d9ff98f7d3ddd3e74340bcfb322c12df2ed189b` and Fern refuses it at both stages, re-measured for this change — the generator exits **1** at `fernapi/fern-python-sdk` 5.20.0 and `fern check` exits **1** at Fern CLI 5.114.1 (and did so at 5.114.0 when an earlier change measured it), both reporting `path: appNg.yml -> service`, `issue: Service requires auth, but no auth is defined.` — because its only security scheme is `type: apiKey` with `in: cookie`, which Fern's importer drops, leaving the document's one service unauthenticated. It is recorded in [`../../tests/fixtures/AGENTS.md`](../../tests/fixtures/AGENTS.md#specs-already-tried-and-rejected-do-not-re-attempt-without-a-fix-upstream). No second real-world document declaring the conjunction was found anywhere the seven sources below reach: the further search this change ran returned 16 more declaring files and every one is a tooling fixture or a demo. | `none` — the only `p.style` read in `src/` sits in `build_endpoint`'s query-parameter arm, so a path parameter's style is read nowhere | The path segment `raw_client.py` interpolates into the request URL would differ. | FIXTURE — **the search for a second real-world declarer found none, so `appng-rest-api` is still the only one this repository knows of.** The [Witness search (issue #188)](#witness-search-issue-188) row records the further search this node ran for a *second* declarer Fern accepts: 16 further declaring files across the seven sources, every one a parser or generator test resource, an example fixture or a vendor demo, and **0** in either of the two complete document corpora. So what holds this row open is still the supply of a document rather than the shape, and the supply is now known to be one document: appNG, Apache-2.0, immutably pinned, declaring the conjunction three times, and refused by Fern at both stages re-measured for this pass. Settling it takes a `matrix` path parameter over a scalar schema in a redistributable, immutably pinned document that pairs the style with a security scheme Fern's importer supports — or a Fern that accepts appNG. What it would pin is the ledger's `matrix-array`/`matrix-object` finding one shape along: [`../fern-limitations.md`](../fern-limitations.md) measures Fern discarding `matrix` on a path parameter entirely, so the golden would pin that crozier interpolates the same plain segment. |
| `matrix-array` | both | Parameter Object.style (`matrix`, `in: path`, array schema) | limitations | `../fern-limitations.md` `matrix-array`: discards + licence |  |  |  |
| `matrix-object` | both | Parameter Object.style (`matrix`, `in: path`, object schema) | limitations | `../fern-limitations.md` `matrix-object`: discards |  |  |  |
| `parameter-style-label-path-scalar` | both | Parameter Object.style (`label`, `in: path`, scalar schema) | golden | conjunction pass (`style=label`, `in=path`, scalar schema): 1 declaration(s) in 1 registered source(s): slurmdb-rest |  |  |  |
| `label-array-or-object` | both | Parameter Object.style (`label`, `in: path`, array **or** object schema) | limitations | `../fern-limitations.md` `label-array-or-object`: discards + licence — one ledger key rules on both schema shapes with one verdict, so this row joins on it rather than splitting into two keys that would join on nothing |  |  |  |
| `parameter-style-simple-path-scalar` | both | Parameter Object.style (`simple`, `in: path`, scalar schema) | golden | conjunction pass (`style=simple`, `in=path`, scalar schema): 29 declaration(s) in 5 registered source(s): bintable.com, free5gc-namf-communication, free5gc-pdu-session, openfigi.com, slurmdb-rest |  |  |  |
| `parameter-style-simple-path-array` | both | Parameter Object.style (`simple`, `in: path`, array schema) | golden | conjunction pass (`style=simple`, `in=path`, array schema), re-run over the 153 registered sources: 15 declaration(s) in 1 registered source(s): daniweb-connect — corpus row 114, registered by this change on the witness the search below records, and byte-matching its Fern 5.20.0 golden; no `../fern-limitations.md` row names it |  |  |  |
| `parameter-style-simple-path-object` | both | Parameter Object.style (`simple`, `in: path`, object schema) | gap | conjunction pass (`style=simple`, `in=path`, object schema): 0 declaration(s) across every registered source; no `../fern-limitations.md` row names it | `none` — the only `p.style` read in `src/` sits in `build_endpoint`'s query-parameter arm, so a path parameter's style is read nowhere | The path segment `raw_client.py` interpolates into the request URL would differ. | FIXTURE — **a real-world witness exists and Fern refuses it.** The [Witness search (issue #188)](#witness-search-issue-188) row records `fern-rejected`: the Pinterest REST API, `jentic/jentic-public-apis` `apis/openapi/pinterest.com/main/5.23.0/openapi.json` at commit `eb9d12a2`, declares the conjunction twice, is MIT at an immutable ref, and is the only document a real API publishes as its own description that declares it anywhere the seven sources reach — every other declarer is a tooling fixture. Fern exits 1 on 10 errors at both stages, none of them about the style, so the refusal is a screening failure of that document rather than evidence the shape is un-goldenable. Settling it takes another such document, or a Fern that accepts this one. |
| `parameter-style-simple-header-scalar` | both | Parameter Object.style (`simple`, `in: header`, scalar schema) | golden | conjunction pass (`style=simple`, `in=header`, scalar schema), re-run over the 153 registered sources: 57 declaration(s) in 4 registered source(s): vtex-pricing (28), chaingateway-io (26), apivideo-android-uploader (2), listennotes (1). `chaingateway-io` is corpus row 115 — the witness the search below records — and `vtex-pricing` (121) and `listennotes` (120) are the two alternates recorded beside it; `apivideo-android-uploader` is corpus row 107, which the earlier conjunction pass reported as declaring none (see the method notes). All four goldens are Fern 5.20.0 and all four byte-match; no `../fern-limitations.md` row names it |  |  |  |
| `header-array` | both | Parameter Object.style (`simple`, `in: header`, array schema) | limitations | `../fern-limitations.md` `header-array`: refuses |  |  |  |
| `header-object` | both | Parameter Object.style (`simple`, `in: header`, object schema) | limitations | `../fern-limitations.md` `header-object`: crashes |  |  |  |
| `parameter-style-form-query-scalar` | both | Parameter Object.style (`form`, `in: query`, scalar schema) | golden | conjunction pass (`style=form`, `in=query`, scalar schema): 41 declaration(s) in 4 registered source(s): asana.com, bintable.com, color.pizza, slurmdb-rest |  |  |  |
| `parameter-style-form-query-array` | both | Parameter Object.style (`form`, `in: query`, array schema) | golden | conjunction pass (`style=form`, `in=query`, array schema): 47 declaration(s) in 8 registered source(s): anchore.io, apache.org, apache.org-airflow, asana.com, bungie.net, query-parameters-openapi, reverb.com, traccar.org |  |  |  |
| `parameter-style-form-query-object` | both | Parameter Object.style (`form`, `in: query`, object schema) | golden | conjunction pass (`style=form`, `in=query`, object schema), re-run over the 153 registered sources: 2 declaration(s) in 1 registered source(s): hubspot-events — corpus row 116, registered by this change on the witness the search below records, and byte-matching its Fern 5.20.0 golden; no `../fern-limitations.md` row names it |  |  |  |
| `parameter-style-form-cookie-scalar` | both | Parameter Object.style (`form`, `in: cookie`, scalar schema) | gap | conjunction pass (`style=form`, `in=cookie`, scalar schema): 0 declaration(s) across every registered source; no `../fern-limitations.md` row names it | `src/ir.rs`: 1 site — the single `ParameterLocation::Cookie` read, in `build_endpoint`'s `has_unsupported_params` matcher | The cookie `raw_client.py` sends — and whether the parameter reaches `client.py`'s signature at all — would differ. | FIXTURE — **the search did not find a real-world witness, and did not finish either.** The [Witness search (issue #188)](#witness-search-issue-188) row records `witness-blocked` on that outcome's unanswered-source reading: 18 declaring files across the seven sources and every one a tooling fixture, **0** in both complete document corpora and **0** in SwaggerHub's enumerated half — but GitHub refused one of the three queries that bear on it and SwaggerHub's 414,968-spec `openapi-3.0.x` family went unread, so the row's question stays open rather than reading as a proven absence. What the answered sources do show is a real pattern: cookie parameters are rare, `form` is their default so declaring it is redundant, and Fern discards cookie parameters anyway. Settling it takes either the two outstanding sources answered, or a redistributable, immutably pinned document Fern accepts that declares it. |
| `cookie-array` | both | Parameter Object.style (`form`, `in: cookie`, array schema) | limitations | `../fern-limitations.md` `cookie-array`: discards + supply |  |  |  |
| `cookie-object` | both | Parameter Object.style (`form`, `in: cookie`, object schema) | limitations | `../fern-limitations.md` `cookie-object`: discards + supply |  |  |  |
| `parameter-style-spacedelimited-query-scalar` | both | Parameter Object.style (`spaceDelimited`, `in: query`, scalar schema) | gap | conjunction pass (`style=spaceDelimited`, `in=query`, scalar schema): 0 declaration(s) across every registered source; no `../fern-limitations.md` row names it | `src/ir.rs`: 1 site — the single `p.style` read, in `build_endpoint`'s query-parameter arm | The query argument `client.py`/`raw_client.py` build for the request would carry a different wire form. | FIXTURE — **a real-world witness exists, Fern accepts it, and its licence is the only thing in the way.** The [Witness search (issue #188)](#witness-search-issue-188) row records `witness-blocked`: the LORIS Data Query Tool API, `aces/Loris` `modules/dataquery/static/schema.yml` at commit `3305a003`, declares the conjunction twice on `PATCH /queries/{QueryID}`, is publisher-owned and immutably pinned, and Fern accepts it at both stages — but it is `GPL-3.0`, outside the corpus's Apache-2.0/MIT/BSD/CC0 set. Settling it takes another declarer under a compatible licence; the day LORIS relicenses, this one is registrable as it stands. |
| `parameter-style-spacedelimited-query-array` | both | Parameter Object.style (`spaceDelimited`, `in: query`, array schema) | golden | conjunction pass (`style=spaceDelimited`, `in=query`, array schema): 1 declaration(s) in 1 registered source(s): apideck.com-vault |  |  |  |
| `spaceDelimited-object` | both | Parameter Object.style (`spaceDelimited`, `in: query`, object schema) | limitations | `../fern-limitations.md` `spaceDelimited-object`: discards + licence |  |  |  |
| `parameter-style-pipedelimited-query-scalar` | both | Parameter Object.style (`pipeDelimited`, `in: query`, scalar schema) | gap | conjunction pass (`style=pipeDelimited`, `in=query`, scalar schema): 0 declaration(s) across every registered source; no `../fern-limitations.md` row names it | `src/ir.rs`: 1 site — the single `p.style` read, in `build_endpoint`'s query-parameter arm | The query argument `client.py`/`raw_client.py` build for the request would carry a different wire form. | FIXTURE — **the one registrable witness the search found, Fern never parses.** The [Witness search (issue #188)](#witness-search-issue-188) row records `fern-rejected`: the AlayaCare Billable Item Management API, `jentic/jentic-public-apis` `apis/openapi/swaggerhub.alayacare/api-rating/1.0.1/openapi.json` at commit `eb9d12a2`, declares the conjunction once on `GET /service_accounting` and is CC0-1.0 at an immutable ref — but putting it to Fern for registration measured the exit-0-and-nothing-happened failure `../../tests/fixtures/AGENTS.md` names: `fern check` exits **0** with `All checks passed`, the generate at `fernapi/fern-python-sdk` **5.20.0** exits **0**, and what it writes is an *empty SDK* — 35 files, a zero-byte `README.md`, a 12-byte `reference.md`, an `__init__.py` exporting nothing but the client. Its log reports `[api]: python-sdk Failed to parse openapi document Billable Item Management API` and `Failed to resolve #/components/schemas/ApiError`, a `CliError` out of `resolveResponseReference`, because five `"400"` Response Object positions hold a `$ref` to a *Schema* Object. The document is logged in that file's REJECTED table. The other declarer the search found, LORIS, is `GPL-3.0` — the same block this region's `parameter-style-spacedelimited-query-scalar` row carries. Settling it takes another declarer Fern parses under a compatible licence. |
| `pipeDelimited-array` | both | Parameter Object.style (`pipeDelimited`, `in: query`, array schema) | limitations | `../fern-limitations.md` `pipeDelimited-array`: discards |  |  |  |
| `pipeDelimited-object` | both | Parameter Object.style (`pipeDelimited`, `in: query`, object schema) | limitations | `../fern-limitations.md` `pipeDelimited-object`: discards + supply |  |  |  |
| `parameter-style-deepobject-query-scalar` | both | Parameter Object.style (`deepObject`, `in: query`, scalar schema) | golden | conjunction pass (`style=deepObject`, `in=query`, scalar schema), re-run over the 153 registered sources: 1 declaration(s) in 1 registered source(s): openintegrationhub-secret-service — corpus row 118, registered by this change on the witness the search below records, and byte-matching its Fern 5.20.0 golden; no `../fern-limitations.md` row names it |  |  |  |
| `parameter-style-deepobject-query-array` | both | Parameter Object.style (`deepObject`, `in: query`, array schema) | golden | conjunction pass (`style=deepObject`, `in=query`, array schema), re-run over the 153 registered sources: 2 declaration(s) in 2 registered source(s): paloalto-remote-networks (1), strapi-rest-api (1) — corpus rows 117 and 119, the witness the search below records and the alternate recorded beside it, each byte-matching its Fern 5.20.0 golden; no `../fern-limitations.md` row names it |  |  |  |
| `deepObject-real-object` | both | Parameter Object.style (`deepObject`, `in: query`, object schema) | golden | conjunction pass (`style=deepObject`, `in=query`, object schema): 45 declaration(s) in 14 registered source(s): apideck.com-accounting, apideck.com-ats, apideck.com-connector, apideck.com-crm, apideck.com-ecommerce, apideck.com-file-storage, apideck.com-hris, apideck.com-issue-tracking, apideck.com-lead, apideck.com-pos, apideck.com-vault, apideck.com-webhook, electric-sql, redhat.com-catalog_inventory; also `../fern-limitations.md` `deepObject-real-object`: coincidence |  |  |  |
| `explode-true-simple-header` | both | Parameter Object.explode (`true`, `style: simple`, `in: header`) | limitations | `../fern-limitations.md` `explode-true-simple-header`: refuses |  |  |  |
| `explode-true-simple-path` | both | Parameter Object.explode (`true`, `style: simple`, `in: path`) | limitations | `../fern-limitations.md` `explode-true-simple-path`: ignores |  |  |  |
| `param-content-query` | both | Parameter Object.content (`in: query`) | golden | conjunction pass (`content`, `in=query`): 1 declaration in 1 registered source: nimisampo |  |  |  |
| `param-content-header` | both | Parameter Object.content (`in: header`) | golden | conjunction pass (`content`, `in=header`): 1 declaration in 1 registered source: apideck.com-file-storage |  |  |  |
| `param-content-path` | both | Parameter Object.content (`in: path`) | limitations | `../fern-limitations.md` `param-content-path`: discards |  |  |  |
| `param-content-cookie` | both | Parameter Object.content (`in: cookie`) | limitations | `../fern-limitations.md` `param-content-cookie`: discards |  |  |  |
| `header-description` | both | Header Object.description | golden | census `header.description`: 104 declaration(s) in 11 registered source(s): box.com, bunq.com, canada-holidays.ca, electric-sql, eozilla, esgenterprise.com, groundhog-day.com, openbanking.org.uk-account-info-openapi, openepcis-dpp-ready, sigstore-rekor, tamoss |  |  |  |
| `header-required` | both | Header Object.required | golden | census `header.required`: 1 declaration(s) in 1 registered source(s): bunq.com |  |  |  |
| `header-deprecated` | both | Header Object.deprecated | gap | census `header.deprecated`: *(declared by no registered source)* — the instrument's own phrase for a selector absent from every registered source; no `../fern-limitations.md` row names it | `none` — crozier models no Header Object at all, so no field of one is read anywhere | Nothing in the emitted SDK derives from it today, so any byte Fern derives from it — a `client.py` argument, a `raw_client.py` header, a `reference.md` line — is a divergence. | FIXTURE — **a real-world witness exists and Fern refuses it.** The [Witness search (issue #188)](#witness-search-issue-188) row records `fern-rejected`: the openEHR EHR API, `jentic/jentic-public-apis` `apis/openapi/openehr.org/main/latest/openapi.json` at commit `eb9d12a2`, declares `components.headers.Location_deprecated` and is one of only two documents a real API publishes as its own description that declare the field anywhere the seven sources reach. The other is larger and is blocked at both gates rather than one: `aptos-labs/aptos-core` `api/doc/spec.yaml` at commit `a1c2b82d` is the Aptos Node API's own description and declares the field **1,675** times, and Fern refuses it too — `[sdk] 5 errors`, `issue: Objects can only extend other objects, and NoAccountSignature is not an object.`, `Found 5 errors and 0 warnings`, at both stages — while its repository's `LICENSE` at that commit is the Aptos Foundation's `Innovation-Enabling Source Code License` (`NOASSERTION` per the GitHub API), outside the corpus's set, whatever its stale `info.license` of `Apache 2.0` still says. Every other declarer is a test resource. Fern exits 1 on 39 `Discriminant value _type` errors at both stages, and the document carries a second obstacle behind that one: `CC-BY-ND-3.0`, a no-derivatives licence outside the corpus's set. Settling it takes a different declarer. |
| `header-allow-empty-value` | both | Header Object.allowEmptyValue | golden | corpus row 94, `ndw-accessibility-map`: two `components.headers` Header Objects declare `allowEmptyValue`, and its Fern 5.20.0 golden byte-matches with an empty `unmatched` list |  |  |  |
| `header-style` | both | Header Object.style | golden | census `header.style`: 24 declaration(s) in 2 registered source(s): free5gc-namf-communication, free5gc-pdu-session |  |  |  |
| `header-explode` | both | Header Object.explode | golden | census `header.explode`: 24 declaration(s) in 2 registered source(s): free5gc-namf-communication, free5gc-pdu-session |  |  |  |
| `header-allow-reserved` | both | Header Object.allowReserved | gap | census `header.allowReserved`: *(declared by no registered source)* — the instrument's own phrase for a selector absent from every registered source; no `../fern-limitations.md` row names it | `none` — crozier models no Header Object at all, so no field of one is read anywhere | Nothing in the emitted SDK derives from it today, so any byte Fern derives from it — a `client.py` argument, a `raw_client.py` header, a `reference.md` line — is a divergence. | FIXTURE — **the only two declaring documents anywhere are synthetic, and the search did not finish.** The [Witness search (issue #188)](#witness-search-issue-188) row records `witness-blocked` on that outcome's unanswered-source reading. It is the thinnest result of this node: exactly two declaring files — `microsoft/OpenAPI.NET`'s reader test sample and a `helidon-io/helidon` `src/test/resources` fixture, both excluded by the corpus's real-world rule — and otherwise zeros: **0** across 4,090 APIs.guru documents, **0** across 32,428 jentic documents, **0** across SwaggerHub's 7,682 enumerated bodies, and **0** among the 111 GitHub documents classified, of which 100 came back from a literal query for `allowReserved` and every one puts it on a Parameter Object. That reads as a real pattern rather than thin coverage, since `allowReserved` is defined for query parameters and a Header Object has no occasion to carry one. The row nonetheless stays open rather than reading as a proven absence, because GitHub refused two of its three `allowReserved` queries, Sourcegraph's plain `allowReserved` literal query stayed refused across both passes, and SwaggerHub's 414,968-spec `openapi-3.0.x` family went unread. |
| `header-schema` | both | Header Object.schema | golden | census `header.schema`: 181 declaration(s) in 17 registered source(s): apideck.com-proxy, apideck.com-vault, box.com, bunq.com, canada-holidays.ca, electric-sql, eozilla, esgenterprise.com, free5gc-namf-communication, free5gc-pdu-session, github.com, groundhog-day.com, microcks.local, openbanking.org.uk-account-info-openapi, openepcis-dpp-ready, sigstore-rekor, tamoss |  |  |  |
| `header-example` | both | Header Object.example | golden | census `header.example`: 50 declaration(s) in 2 registered source(s): electric-sql, github.com |  |  |  |
| `header-examples` | both | Header Object.examples | golden | census `header.examples`: 1 declaration(s) in 1 registered source(s): microcks.local |  |  |  |
| `header-content` | both | Header Object.content | golden | census `header.content`, re-measured for this change over the 153 registered sources: 22 declaration(s) in 1 registered source(s): vtex-pricing — corpus row 121, registered by this change on the witness the search below records, whose 22 Response Header Objects each carry a media type instead of a `schema`, and whose Fern 5.20.0 golden crozier reproduces byte for byte; no `../fern-limitations.md` row names it |  |  |  |
| `example-summary` | both | Example Object.summary | golden | census `example.summary`: 167 declaration(s) in 7 registered source(s): buildrelay, electric-sql, eozilla, github.com, redocly.com-museum, tamoss, tlon-notes |  |  |  |
| `example-description` | both | Example Object.description | golden | census `example.description`: 208 declaration(s) in 2 registered source(s): portfoliooptimizer.io, tamoss |  |  |  |
| `example-value` | both | Example Object.value | golden | census `example.value`: 957 declaration(s) in 12 registered source(s): apicurio.local-registry, box.com, buildrelay, canada-holidays.ca, dnd5eapi.co, electric-sql, github.com, groundhog-day.com, microcks.local, portfoliooptimizer.io, redocly.com-museum, tlon-notes |  |  |  |
| `example-external-value` | both | Example Object.externalValue | golden | census `example.externalValue`: 27 declaration(s) in 2 registered source(s): eozilla, tamoss |  |  |  |
| `components-parameters` | both | Components Object.parameters | golden | census `components.parameters`: 42 declaration(s) in 42 registered source(s): 6-dot-authentiqio.appspot.com, amazonaws.com-cloudformation, amazonaws.com-cloudfront, anchore.io, apache.org, apache.org-airflow, apideck.com-accounting, apideck.com-ats, apideck.com-connector, apideck.com-crm, apideck.com-customer-support, apideck.com-ecommerce, apideck.com-ecosystem, apideck.com-file-storage, apideck.com-hris, apideck.com-issue-tracking, apideck.com-lead, apideck.com-pos, apideck.com-proxy, apideck.com-sms, apideck.com-vault, apideck.com-webhook, apis.guru, asana.com, bbci.co.uk, bunq.com, codesearch.debian.net, conjur.local, dnd5eapi.co, electric-sql, etherpad.local, exa-gate, frankfurter, github.com, openbanking.org.uk-account-info-openapi, openepcis-dpp-ready, redhat.com-catalog_inventory, redocly.com-museum, sac-backend, tamoss, traccar.org, xero.com-xero-payroll-au |  |  |  |
| `components-headers` | both | Components Object.headers | golden | census `components.headers`: 4 declaration(s) in 4 registered source(s): apache.org, apache.org-airflow, bunq.com, github.com |  |  |  |
| `components-examples` | both | Components Object.examples | golden | census `components.examples`: 4 declaration(s) in 4 registered source(s): apache.org, apache.org-airflow, github.com, redocly.com-museum |  |  |  |
| `parameter-reference` | both | Parameter Object via Reference Object.$ref | golden | conjunction pass (`$ref` entry in a `parameters` list): 8,778 declaration(s) in 39 registered source(s): 6-dot-authentiqio.appspot.com, amazonaws.com-cloudformation, amazonaws.com-cloudfront, anchore.io, apache.org, apache.org-airflow, apideck.com-accounting, apideck.com-ats, apideck.com-connector, apideck.com-crm, apideck.com-customer-support, apideck.com-ecommerce, apideck.com-ecosystem, apideck.com-file-storage, apideck.com-hris, apideck.com-issue-tracking, apideck.com-lead, apideck.com-pos, apideck.com-proxy, apideck.com-sms, apideck.com-vault, apideck.com-webhook, apis.guru, asana.com, bbci.co.uk, bunq.com, conjur.local, dnd5eapi.co, electric-sql, exa-gate, frankfurter, github.com, openbanking.org.uk-account-info-openapi, openepcis-dpp-ready, redhat.com-catalog_inventory, redocly.com-museum, sac-backend, tamoss, xero.com-xero-payroll-au |  |  |  |
| `path-item-parameters` | both | Path Item Object.parameters | golden | census `pathItem.parameters`: 609 declaration(s) in 16 registered source(s): amazonaws.com-cloudformation, amazonaws.com-cloudfront, apache.org, apache.org-airflow, apicurio.local-registry, asana.com, canada-holidays.ca, color.pizza, conjur.local, etsi.local-mec010-2_apppkgmgmt, exa-gate, groundhog-day.com, microcks.local, netbox.dev, tamoss, xero.com-xero-payroll-au |  |  |  |
| `operation-overrides-path-item-parameter` | both | Operation Object.parameters (overriding Path Item Object.parameters) | gap | conjunction pass (same `(name, in)` at both Path Item and Operation level): 3 declaration(s) in 1 registered source: asana.com — which carries no committed golden (`../../tests/fixtures/CORPUS.md` records why), so no golden fixture declares it; no `../fern-limitations.md` row names it | `src/openapi.rs`: 1 site — the override branch of `normalize_parameters` | The parameter definition `client.py`, `raw_client.py` and `reference.md` are all built from would be the losing one of the pair. | FIXTURE — **the witness exists and is registrable, and the corpus's own declarer was answered first.** The [Witness search (issue #188)](#witness-search-issue-188) row records `witness-found`: AWS Import/Export, `APIs-guru/openapi-directory` `APIs/amazonaws.com/importexport/2010-06-01/openapi.yaml` at commit `f04b8d0b`, declares the collision **24** times — each of six paths declares `Action` and `Version` and each of its two operations redeclares both — is Apache-2.0 at an immutable ref, and Fern accepts it at both stages. `asana.com`, the registered source the census already sees declaring it three times, was put to Fern first as the cheapest candidate available and refused at both stages on the same 17 errors the batch-4 ledger recorded, now re-measured. Register the witness and the byte comparison pins whether Fern honours the specification's operation-wins rule. |
## Method notes

**What was run.** `just surface-census --json`, exit 0. Every `census <selector>`
cell is that run's `(selector, fixture, count)` rows for the selector, aggregated;
every *"declared by no registered source"* cell is the phrase the instrument
itself prints for an absent selector, reproducible one selector at a time with
`just surface-census --selector header.content`.

**The conjunction pass.** The census emits one row per *field*, so it cannot on
its own answer whether a `style`, an `in` and a schema shape meet on the same
node. A read-only pass supplies that, and it re-uses the census rather than
re-implementing it: import `scripts/openapi-surface-census.py`, call its
`registered_sources()` and `load_document()` so the source set and the loader are
identical, then

- take every node that literally **is** a Parameter Object — an entry of a Path
  Item `parameters` list, an entry of an Operation `parameters` list, or a value
  of `components.parameters`. An entry that is a `$ref` is a *use*, not a
  declaration, and is not counted (counting it would multiply one shared
  parameter by its call sites);
- group those nodes by `(style, in, schema shape)`, where the shape follows the
  parameter's `schema` through local `$ref`s and reads `type` (a 3.1 nullable type
  array reduces to its non-`null` member), falling back to `items` /
  `properties` / `additionalProperties`, then to a single agreed shape across
  `allOf` / `anyOf` / `oneOf`.

The pass is self-checking, and that check is the reason to trust the matrix: its
declarations account for the census's own `parameter.style` count. The change that
settled this region's style rows re-ran it over the **153** registered sources its
eight new corpus rows leave behind, and it partitions **326** of the **328**
parameters declaring a `style` across the twelve conjunctions the corpus now
declares, with none dropped for a schema behind a `$ref` — where the 2026-09-04
walk the rest of this file's counts rest on partitioned **167** across seven, all
of them. Every style row's evidence cell below carries the number this re-run
measured for it.

**The two it does not classify are the matrix's own boundary, not a hole.** Strapi
(corpus row 119) declares `sort` as `oneOf: [string, array of string]` and
`populate` as `oneOf: [string, object]`, both `style: deepObject`, `in: query`.
The pass reads a composition's shape only where its members agree on one, and
these disagree by construction — the specification's *Style Examples* table has
three shapes and a parameter that is two of them at once belongs to no cell. They
are counted here rather than silently dropped so the two totals can be compared. The `parameter-reference` and
`operation-overrides-path-item-parameter` rows come from the same pass, counting
`$ref` entries at parameter sites and `(name, in)` collisions between a Path
Item's list and its operations' lists respectively.

**Golden-backing.** The census reads every registered *source*, and not every
registered source is a *golden*: some carry no `tests/fixtures/<name>/expected/`
tree, because Fern cannot generate them or has not yet — the status tables of
[`../../tests/fixtures/CORPUS.md`](../../tests/fixtures/CORPUS.md) own the reason
for each. The index's first category rule asks for a registered **golden**
fixture, so a row here is `golden` only when at least one golden-backed source
declares the feature, while its evidence cell still lists every declaring
registered source. Recompute the split rather than trusting a copy:

```sh
just surface-census --json \
  | python3 -c 'import json,sys,pathlib; print(*sorted(s["fixture"] for s in json.load(sys.stdin)["sources"] if not pathlib.Path("tests/fixtures",s["fixture"],"expected").is_dir()),sep="\n")'
```

Two rows turn on it, and each says so in its own evidence cell: the only source
declaring `parameter-style-matrix-path-scalar` is `appng-rest-api` and the only
one declaring `operation-overrides-path-item-parameter` is `asana.com`, neither
golden-backed, so both are `gap` rather than `golden`. Land a golden for either
and its row is re-measured, not merely re-worded.

**One row the earlier pass under-reported.** `parameter-style-simple-header-scalar`
read as *0 declarations across every registered source* while corpus row 107,
`apivideo-android-uploader`, already declared it twice — the `Content-Range` header
of `POST /videos/{videoId}/source` and of `POST /upload`, each `{in: header, style:
simple}` over `{type: string, pattern: …}`. Row 107 predates the search by a day, so
the miss is the pass's rather than the corpus's; the re-run above finds both, and
the row's evidence cell now carries them beside the witness this change registered.
Under the index's precedence that source alone would already have made the row
`golden`, which is why this is recorded rather than left as a re-worded count.

**Crozier sites.** Measured against `src/`, not assumed. `Parameter::style` is
read at exactly one place in the whole crate — inside `build_endpoint`'s
query-parameter arm in `src/ir.rs` — so every non-query style crossing scores
`none`. `ParameterLocation::Cookie` is likewise read once, in the same function's
`has_unsupported_params` matcher, and the Operation-over-Path-Item override once,
in `normalize_parameters` in `src/openapi.rs`. No Header Object exists in
crozier's model at all: neither `Response` nor `Encoding` carries a `headers`
field for one to hang off, which is why every Header Object row
classified `gap` scores `none`.

**Not settled here.** This file classifies; it runs no Fern and registers no
fixture. Whether a `FIXTURE` row's witness exists in a redistributable, immutable
form is a screening question for
[`../../tests/fixtures/AGENTS.md`](../../tests/fixtures/AGENTS.md), and a `PROBE`
row's answer belongs in [`../fern-limitations.md`](../fern-limitations.md).


### Witness search (issue #188)

This subsection was written when `parameter-style-matrix-path-scalar` was this
region's one `PROBE` row. **It is not one now:** an earlier change settles it as a
`FIXTURE` gap on the evidence below, so the region carries no `PROBE` row at all
and the index publishes zero for it. Which class a row is in is its own
`settlement` cell in the entry table above, not a count restated here.

**What this table covers now.** It began as one row and is now the record of the
world-wide search for **all fourteen** `parameters` rows of the index's ranked
`FIXTURE` backlog: the ten style conjunctions, the three Header Object fields
crozier models nothing of, and the Operation-over-Path-Item override. Ten of the
fourteen are conjunctions — a `style` crossed with an `in` crossed with a schema
kind — so a document declaring the token is a witness only for the exact crossing
its own bytes declare, which is why every candidate below is classified out of the
fetched bytes with the census's own object model rather than off a grep. One
document commonly declares several rows at once, so the search ran for all
fourteen together and every candidate says which rows it declares and how many
declarations of each it carries.

**The sources, and what "read" means for each.** Every row below was put to all
seven, and the reconciliation refuses a row that drops one:

- **APIs.guru / `openapi-directory`** — read rather than sampled.
  `git clone --depth 1 --filter=blob:none https://github.com/APIs-guru/openapi-directory`
  (HEAD `f04b8d0bcd39c52e1cf3ad7a5fe744709832ae49`), then every `.json`/`.yaml`/`.yml`
  file under `APIs/` parsed and measured for all fourteen selectors at once:
  **4,138** files, **4,090** read as OpenAPI/Swagger documents, 48 unparsed.
- **`jentic/jentic-public-apis`** — read rather than sampled, at commit
  `eb9d12a2684b0fbcb5aecf51e8ae54dba0929743`. A complete clone of this catalogue is
  ~21 GB and this host had no room for a second one, so the archive was **streamed**
  instead — `https://codeload.github.com/jentic/jentic-public-apis/tar.gz/eb9d12a2684b0fbcb5aecf51e8ae54dba0929743`
  read member by member with no unpacked copy on disk — which reads the same bytes at
  the same immutable ref: **74,629** candidate files, **32,428** read as OpenAPI
  documents, 0 unparsed. The catalogue keeps five copies of each document (an
  `openapi.json` and four `meta/import/` bundles), so a count of *files* here is
  five times its count of documents. Candidates were then lifted from a sibling
  dispatch's read-only complete clone of the **same** commit, and every one was
  verified byte-identical against its pinned `raw.githubusercontent.com` URL before
  being screened — so nothing in this table rests on a copy nobody else can fetch.
- **Sourcegraph public code search** — the one literal index reachable without a
  token, queried through `https://sourcegraph.com/.api/search/stream`. Each query's
  matches were then **fetched at the commit Sourcegraph matched** and measured with
  the same object model, in two passes. The first is the twelve style queries: 1,408
  distinct matched files, **1,216** read as OpenAPI documents, 163 not OpenAPI
  documents, 29 fetch errors. The second is the three `headers:`-block regexps the
  Header Object rows need, which the instance refused on the first attempt and
  answered on a later one: 941 further distinct matched files, **428** read as
  OpenAPI documents, 430 not OpenAPI documents, 83 fetch errors. Both passes measure
  every document for all fourteen selectors, so either can answer any row; where a
  row's count comes from the second, its cell says so. Two queries were never
  answered across both passes and are recorded as `unanswered` in the rows they bear
  on.
- **GitHub code search** — the token-based index, `gh search code`, whose 100-result
  page cap and 10-request-a-minute limit both bind here. Its 114 distinct result
  files were fetched and measured the same way: **111** read as OpenAPI documents (the
  one whose path contains a space re-fetched percent-encoded), and 3 that are Java
  source the code search returned rather than documents.
- **SwaggerHub public registry** — its `?query=` endpoint searches names and
  descriptions rather than spec bodies, so the body-level read is an enumeration:
  `https://api.swaggerhub.com/specs?specification=openapi-3.1.x&limit=100&page=N`
  and the same for `openapi-3.2.0`, every `Swagger` property URL fetched as
  `…/swagger.json` and measured — **7,688** fetched, **7,682** read, 6 fetch errors.
  Its `openapi-3.0.x` family is **414,968** specs and no body read of it was
  reachable from this dispatch; every row below records that half as `unanswered`
  rather than counting it as a zero.
- **Postman public API network** — `POST https://www.postman.com/_api/ws/proxy` with
  `{"service":"search","method":"post","path":"/search-all","body":{"queryIndices":["adp.api","apinetwork.team","runtime.collection"],"queryText":"<term>","size":25,"domain":"public"}}`.
  It indexes collection, workspace and API *metadata*, not specification bodies, so a
  non-zero total there is a word match on a name and never a declaration.
- **Vendor developer portals** — the portals each feature's own domain implies. For
  `matrix` that is JAX-RS `@MatrixParam`, recorded in that row below; for the other
  thirteen it is the publishers whose documents actually serialise this way — Stripe,
  GitHub, Cloudflare, OpenAI, Twilio, DigitalOcean, Box, Adyen, Slack, SendGrid,
  NetBox, the Swagger Petstore and the OAI's own `learn.openapis.org` examples: 20
  URLs fetched, **15** read as OpenAPI documents, 5 returning 404 at the paths this
  dispatch could reach.

**Every query is shown able to find what it is looking for.** That is a property of
the query rather than a claim about any index: two of these rows collide only after
resolution (a `(name, in)` declared twice; a `style` that must meet an `in` and a
schema kind on one node), so a query that could never see the shape would report a
zero indistinguishable from a real one. The object model every source's results are
measured with was therefore run first over a control document written for the
purpose — `openapi: 3.0.3`, one declaration of each of the fourteen — and found all
fourteen; and over the two real documents this region already knows declare
something, reproducing both counts exactly: `appng-rest-api`'s **3**
`parameter-style-matrix-path-scalar` and `asana.com`'s **3**
`operation-overrides-path-item-parameter`, the same numbers the entry table's
evidence cells carry. GitHub's index was separately shown able to return a known
declarer and the classifier shown able to read the declaration out of it: `gh search
code 'matrix' --repo appNG/appng` → 4 results, of which the classification measures
`appng-rest-api/src/main/resources/org/appng/api/rest/appng-openapi.yaml` at **3**
declarations of `parameter-style-matrix-path-scalar` — the same 3 the entry table
carries. Sourcegraph could not be shown the same way, because its public instance
indexes no branch of `appNG/appng` at all (`repo:^github\.com/appNG/appng$
count:all type:file select:repo` → 0), which is recorded here rather than left as a
silent gap in the control.

`outcome` is **exactly one of four words** — `witness-found`, `fern-rejected`,
`witness-blocked`, `none-found` — spelled as the other regions' subsections spell
them, with no synonyms and no fifth word. What each settles, and what a `FIXTURE`
row may become once its search returns one, is the index's
[settlement rule](../openapi-surface-coverage.md#the-settlement-rule-as-amended),
which this file follows rather than restating. `witness-found` — a named real-world
document declares the feature, at a credential-free HTTPS URL ending `.json`,
`.yaml` or `.yml`, pinned to an immutable ref, under a redistribution-compatible
license, accepted by Fern, and under a name that is neither registered in
[`CORPUS.md`](../../tests/fixtures/CORPUS.md) nor listed DROPPED or REJECTED there
or in [`tests/fixtures/AGENTS.md`](../../tests/fixtures/AGENTS.md).
`fern-rejected` — such a document was found and Fern refuses it at the pinned ref.
`witness-blocked` — the search is blocked from producing a registrable witness, and
it covers two ways that happens: such a document was found but the corpus may not
redistribute it or it is reachable at no immutable ref, **or** a required source did
not answer, so what stands between this row and a witness is a search still owed
rather than a shape the world lacks. Both readings leave the row's question **open**
and settle nothing; a row taking the second names the sources that did not answer
and what refused them, in its own `sources searched` cell. `none-found` — no
document reaching that bar was found anywhere, which is a claim about the world and
is therefore available **only** where every required source answered.
**No row below reads `none-found`**, because no row's sources all answered:
SwaggerHub's `openapi-3.0.x` half went unread for every row, and GitHub refused
eight of the twenty-five queries put to it. The two rows for which no real-world
declarer turned up at all therefore read `witness-blocked` on the second reading
rather than `none-found`, which is what keeps an unread source from becoming
evidence of absence.

**Fern was run at the pin the corpus's own provenance records**, not merely
`fern check`: every candidate that survived screening was put through **both** a
document check at Fern CLI **5.114.1** and a real generate at
`fernapi/fern-python-sdk` **5.20.0** — the version
`tests/fixtures/eos.local/expected/.crozier-fern-golden.json` records — because this
repository's rejected ledger is full of documents that pass the first and are
refused by the second. Where a row's `fern check` cell reports one exit status it
reports both runs' agreement; where they disagree it says so.

| key | outcome | witness | immutable ref | license | fern check | sources searched and the exact query used against each |
|---|---|---|---|---|---|---|
| `parameter-style-matrix-path-scalar` | fern-rejected | appNG REST API, `appNG/appng` `appng-rest-api/src/main/resources/org/appng/api/rest/appng-openapi.yaml` at `https://raw.githubusercontent.com/appNG/appng/8d9ff98f7d3ddd3e74340bcfb322c12df2ed189b/appng-rest-api/src/main/resources/org/appng/api/rest/appng-openapi.yaml` — 28,525 bytes fetched, and **3** `style: matrix` parameters counted in them by the census's own object model, each `in: path` over `schema: {type: string}`: the `params` parameter of `/service/{site}/{application}/rest/openapi/datasource/{id}/{params}` and of the two `…/action/{event-id}/{id}/{params}` operations. **Publisher-owned** and deployed, already [`CORPUS.md`](../../tests/fixtures/CORPUS.md) row 70, and carrying **no committed golden**. **This row's question for this pass was the second one:** whether a *further* real-world declarer exists that Fern accepts — the refusal below says such a document must pair the style with a security scheme Fern's importer supports. **It does not.** The seven sources returned **16** further declaring files and not one is a document a real API publishes as its own description: they are parser and generator test resources (`Cornutum/tcases`, `OpenAPITools/openapi-generator`, `oapi-codegen/oapi-codegen`, `networknt/light-rest-4j`, `fern-api/fern`'s own importer fixture), the `readmeio/oas` `parameters-style` example fixture in its four 3.0/3.1 spellings, and two demos — `mmontone/cl-rest-server` `demo/openapi/petstore.v3.json` and `mulesoft/api-console` `demo/models/representative-service/…`. Both complete corpora declare it **0** times. So the corpus's own declarer remains the only real-world one, and Fern refuses it | commit `8d9ff98f7d3ddd3e74340bcfb322c12df2ed189b` | Apache-2.0 (`info.license` `Apache 2.0`, and the repository's own `LICENSE`) | **exit 1**, re-measured for this pass at Fern CLI **5.114.1** — `[sdk] 1 error`, `path: appNg.yml -> service`, `issue: Service requires auth, but no auth is defined.`, `Found 1 error and 0 warnings`; the generate at `fernapi/fern-python-sdk` **5.20.0** also **exit 1**, `[error] Service requires auth, but no auth is defined.`, `Found 1 errors and 0 warnings in 0.335 seconds.` The earlier change recorded the same refusal at CLI 5.114.0, so this is a confirmation rather than a change | **APIs.guru / `openapi-directory`:** `git clone --depth 1 --filter=blob:none https://github.com/APIs-guru/openapi-directory` at HEAD `f04b8d0b`, then every `.json`/`.yaml`/`.yml` file under `APIs/` parsed and grouped by `(style, in, schema shape)` and by Header Object field in one pass over all fourteen selectors → **4,090** documents read, of which **0** files declare the conjunction. **`jentic/jentic-public-apis`:** `curl -sL https://codeload.github.com/jentic/jentic-public-apis/tar.gz/eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` streamed through the same pass, with no unpacked copy on disk → **32,428** documents read, of which **0** files declare it (its one `style: matrix` document, `opendatasoft.com/main/v2.0`, declares the style `in: query` — the candidate ledger below). **Sourcegraph public code search:** `content:"style: matrix" file:\.(yaml|yml)$ count:all type:file` → **unanswered**, refused with `HTTP Error 429: Too Many Requests` on every attempt across both passes — six in the sweep, spaced 30–150 s apart, and eight more in the retry, spaced up to 240 s; `content:"\"style\": \"matrix\"" file:\.json$ count:all type:file` → 585 matches in 53 files. Every file the twelve answered style queries matched was then fetched at its matching commit and measured for all fourteen selectors — **1,216** documents in all, of which **16** files declare this conjunction, every one of them the tooling-fixture or demo pool named in the witness cell. **GitHub code search:** `gh search code 'style matrix' --filename openapi.yaml` → 1, `… --filename openapi.json` → 0, `… --filename openapi.yml` → 0. The one hit is `Souf-F/API_prototyper` `api-prototyper-cyber/openapi.yaml`, which the classification measures at **0** declarations of this conjunction. The row's positive control is the separate repository-scoped query `gh search code 'matrix' --repo appNG/appng` → 4 results, one of them `appng-rest-api/src/main/resources/org/appng/api/rest/appng-openapi.yaml`, which the same classification measures at **3** — so the query set can see this conjunction where it exists, and GitHub's own `filename:`-scoped index simply does not carry the file. **SwaggerHub public registry:** `GET /specs?query=matrix&limit=1` → `totalCount` **802,968**, which is the registry's own unfiltered total (`GET /specs?limit=1` returns the same number), so that term selects nothing; the enumerated 7,682-body sweep declares it **0** times; the registry's `openapi-3.0.x` family, **414,968** specs, exposes no body search and no body read of it was reachable from this dispatch, so that half is **unanswered**. **Postman public API network:** the `search-all` call above with `queryText: "matrix"` → totals `api` **97**, `collection` 181, `team` 767; every one of the 97 `api` results is a Matrix chat-protocol (matrix.org) API rather than a document declaring this style, and `queryText: "MatrixParam"` → `api` **0**, `queryText: "matrix path parameter"` → `api` **0** **Vendor developer portals:** the 20 URLs listed above fetched and measured — **15** read as OpenAPI documents, 5 returning 404 (`moby/moby` `docs/api/v1.45.yaml`, `atlassian/openapi-specs`, `keycloak/keycloak` `docs/documentation/api/openapi.json`, `apache/airflow` `airflow-core/docs/stable-rest-api-ref.yaml`, `wildfly/quickstart` `microprofile-openapi/openapi.yaml`) — **0** declare it. This feature's domain is JAX-RS `@MatrixParam`, so the portals put to it are the Jakarta/JAX-RS ones plus the specification's own example catalogues — `swagger-api/swagger-petstore` `src/main/resources/openapi.yaml`, `OAI/learn.openapis.org` `examples/v3.0/petstore.yaml` and `examples/v3.1/tictactoe.json` — **0** each |
| `parameter-style-simple-path-array` | witness-found | DaniWeb Connect API, `APIs-guru/openapi-directory` `APIs/daniweb.com/4/openapi.yaml` at `https://raw.githubusercontent.com/APIs-guru/openapi-directory/f04b8d0bcd39c52e1cf3ad7a5fe744709832ae49/APIs/daniweb.com/4/openapi.yaml` — 152,755 bytes fetched (identical to the clone's bytes, MD5 `f1667ce195bb…`), and **15** `style: simple` path parameters counted in them over an array schema: the `ID` parameter of `/apps/{ID}`, `/audiences/{ID}`, `/conversations/{ID}`, `/conversations/{ID}/schedules` and eleven more, each `{in: path, style: simple, schema: {type: array, items: {type: integer}, maxItems: 100}}`. **Publisher-described** and deployed: `info.x-origin` records DaniWeb's own `https://www.daniweb.com/connect/developers/swagger` and the single server is `https://www.daniweb.com/connect/api/v4`. It declares no other row of this node. Its name is in neither `CORPUS.md` nor either DROPPED/REJECTED list | commit `f04b8d0bcd39c52e1cf3ad7a5fe744709832ae49` of `APIs-guru/openapi-directory` | CC0-1.0 (the aggregating repository's own `LICENSE`, the same standing this file's ledger already gives the `jentic` aggregation); the document itself declares no `info.license` | `All checks passed` at Fern CLI 5.114.1 (**exit 0**, `Found 0 errors and 13 warnings`) and the generate at `fernapi/fern-python-sdk` **5.20.0** also **exit 0** | **APIs.guru / `openapi-directory`:** `git clone --depth 1 --filter=blob:none https://github.com/APIs-guru/openapi-directory` at HEAD `f04b8d0b`, then every `.json`/`.yaml`/`.yml` file under `APIs/` parsed and grouped by `(style, in, schema shape)` and by Header Object field in one pass over all fourteen selectors → **4,090** documents read, of which **2** files declare it — this witness at 15, and `tfl.gov.uk/v1` at 26, which Fern refuses (the ledger below). **`jentic/jentic-public-apis`:** `curl -sL https://codeload.github.com/jentic/jentic-public-apis/tar.gz/eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` streamed through the same pass, with no unpacked copy on disk → **32,428** documents read, of which **20** files — four documents, the largest `daniweb.com/main/4` (the same document, 15) and `opensearch.org/main/0.2.0` (8), which Fern refuses. **Sourcegraph public code search:** `content:"style: simple" file:\.(yaml|yml)$ count:all type:file` → 71,544 matches in 105 files; `content:"\"style\": \"simple\"" file:\.json$ count:all type:file` → 41,763 matches in 9 files. Every file the twelve answered style queries matched was then fetched at its matching commit and measured for all fourteen selectors — **1,216** documents in all, of which **23** files declare it; besides the tooling fixtures the real ones are `sdmx-twg/sdmx-rest` `api/sdmx-rest.yaml` (7) and `CWE-CAPEC/REST-API-wg` `specifications/openapi.yaml` (4). The second classification pass — the 428 documents the three `headers:`-block regexps returned — adds one more, `onflow/flow` `openapi/go-client-generated/api/swagger.yaml` (1). **GitHub code search:** `gh search code 'style simple' --filename openapi.json` → 0, `… --filename openapi.yml` → 0; `… --filename openapi.yaml` was refused with `HTTP 403: API rate limit exceeded for user ID 19440155` while `gh api rate_limit` still reported 10 of 10 `code_search` remaining — the secondary limiter, which is not waited out by polling, so it was left **unanswered** rather than retried. The 111 classified documents declare it **0** times. **SwaggerHub public registry:** the enumerated 7,682-body sweep finds **7** declaring specs, the largest `CandyceDaly/open-search_api_specification/0.2.0` (8) and `tlqubec/spideo/2.0.1` (7); every SwaggerHub version is republishable in place, so none is at an immutable ref; the registry's `openapi-3.0.x` family, **414,968** specs, exposes no body search and no body read of it was reachable from this dispatch, so that half is **unanswered**. **Postman public API network:** the `search-all` call above with `queryText: "style simple path"` → totals `api` **0**, `collection` 0, `team` 0; nothing in any index **Vendor developer portals:** the 20 URLs listed above fetched and measured — **15** read as OpenAPI documents, 5 returning 404 (`moby/moby` `docs/api/v1.45.yaml`, `atlassian/openapi-specs`, `keycloak/keycloak` `docs/documentation/api/openapi.json`, `apache/airflow` `airflow-core/docs/stable-rest-api-ref.yaml`, `wildfly/quickstart` `microprofile-openapi/openapi.yaml`) — **0** declare it |
| `parameter-style-simple-path-object` | fern-rejected | Pinterest REST API, `jentic/jentic-public-apis` `apis/openapi/pinterest.com/main/5.23.0/openapi.json` at `https://raw.githubusercontent.com/jentic/jentic-public-apis/eb9d12a2684b0fbcb5aecf51e8ae54dba0929743/apis/openapi/pinterest.com/main/5.23.0/openapi.json` — 1,991,407 bytes fetched (MD5 `1d340291ca61…`), and **2** declarations counted in them: `components.parameters.path_scope` and `components.parameters.path_type`, each `{in: path, style: simple}` over an object schema. It is a real, deployed commercial API — `info.title` `Pinterest REST API`, `info.license` `MIT`, one server `https://api.pinterest.com/v5` — and it is the **only** document reaching that standard that declares this conjunction anywhere the seven sources reach: every one of the other 17 declaring files is a tooling fixture (`Cornutum/tcases`, `oapi-codegen`, `discord-gophers/goapi-gen`, `citrusframework/citrus`'s extended Petstore, `readmeio/oas` `parameters-style` in four spellings, `wallarm/api-firewall` `resources/test/specification/…`). **Fern refuses it**, which is why the outcome is `fern-rejected` rather than `witness-found`: the refusal is a screening failure of this document, not a fact about the shape. It declares no other row of this node | commit `eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` of `jentic/jentic-public-apis` | MIT (`info.license` `MIT`, `https://spdx.org/licenses/MIT`); the aggregating repository is CC0-1.0 | **exit 1** at Fern CLI 5.114.1 — `[sdk] 10 errors`, among them `path: __package__.yml -> types -> InviteBusinessRoleBinding`, `issue: Object has multiple properties named "user":`, and `path: audiences.yml -> service -> endpoints -> create`, `issue: Multiple request properties have the name ad_account_id. This is not suitable for code generation.`, `Found 10 errors and 16 warnings`; the generate at `fernapi/fern-python-sdk` **5.20.0** also **exit 1** on the same 10 | **APIs.guru / `openapi-directory`:** `git clone --depth 1 --filter=blob:none https://github.com/APIs-guru/openapi-directory` at HEAD `f04b8d0b`, then every `.json`/`.yaml`/`.yml` file under `APIs/` parsed and grouped by `(style, in, schema shape)` and by Header Object field in one pass over all fourteen selectors → **4,090** documents read, of which **0** files declare it. **`jentic/jentic-public-apis`:** `curl -sL https://codeload.github.com/jentic/jentic-public-apis/tar.gz/eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` streamed through the same pass, with no unpacked copy on disk → **32,428** documents read, of which **5** files — one document, this witness, at 2 declarations each. **Sourcegraph public code search:** `content:"style: simple" file:\.(yaml|yml)$ count:all type:file` → 71,544 matches in 105 files; `content:"\"style\": \"simple\"" file:\.json$ count:all type:file` → 41,763 matches in 9 files. Every file the twelve answered style queries matched was then fetched at its matching commit and measured for all fourteen selectors — **1,216** documents in all, of which **17** files declare it, every one of them a tooling fixture. **GitHub code search:** `gh search code 'style simple' --filename openapi.json` → 0, `… --filename openapi.yml` → 0; `… --filename openapi.yaml` was refused with `HTTP 403: API rate limit exceeded for user ID 19440155` while `gh api rate_limit` still reported 10 of 10 `code_search` remaining — the secondary limiter, which is not waited out by polling, so it was left **unanswered** rather than retried. The 111 classified documents declare it **0** times. **SwaggerHub public registry:** the enumerated 7,682-body sweep declares it **0** times; the registry's `openapi-3.0.x` family, **414,968** specs, exposes no body search and no body read of it was reachable from this dispatch, so that half is **unanswered**. **Postman public API network:** the `search-all` call above with `queryText: "style simple path"` → totals `api` **0**, `collection` 0, `team` 0; nothing in any index **Vendor developer portals:** the 20 URLs listed above fetched and measured — **15** read as OpenAPI documents, 5 returning 404 (`moby/moby` `docs/api/v1.45.yaml`, `atlassian/openapi-specs`, `keycloak/keycloak` `docs/documentation/api/openapi.json`, `apache/airflow` `airflow-core/docs/stable-rest-api-ref.yaml`, `wildfly/quickstart` `microprofile-openapi/openapi.yaml`) — **0** declare it |
| `parameter-style-simple-header-scalar` | witness-found | Chaingateway.io, `APIs-guru/openapi-directory` `APIs/chaingateway.io/1.0/openapi.yaml` at `https://raw.githubusercontent.com/APIs-guru/openapi-directory/f04b8d0bcd39c52e1cf3ad7a5fe744709832ae49/APIs/chaingateway.io/1.0/openapi.yaml` — 54,974 bytes fetched (MD5 `1381f2e59dd3…`), and **26** `style: simple` header parameters counted in them over a scalar schema, spread across 21 operations — `/clearAddress`, `/deleteAddress`, `/exportAddress` and `/getBlock` among them, five of the 21 declaring two. **Publisher-described** and deployed: `info.x-origin` records Chaingateway's own `https://chaingateway.io/downloads/openapiv3.json` and the server is `https://eu.eth.chaingateway.io/v1`. Two further Fern-accepted declarers were screened and are alternates rather than near-misses — Listen API (`listennotes.com/2.0`, **1** declaration, `components.parameters.apiKeyParam`, whose `x-origin` is the publisher-served `https://listen-api.listennotes.com/api/v2/openapi.yaml`) and VTEX Pricing API (`vtex.local/Pricing-API/1.0`, **28**), the latter also this node's `header-content` witness. None declares any other row of this node | commit `f04b8d0bcd39c52e1cf3ad7a5fe744709832ae49` of `APIs-guru/openapi-directory` | CC0-1.0 (the aggregating repository's own `LICENSE`); the document itself declares no `info.license` | `All checks passed` at Fern CLI 5.114.1 (**exit 0**) and the generate at `fernapi/fern-python-sdk` **5.20.0** also **exit 0**; the two alternates return the same pair of exit codes | **APIs.guru / `openapi-directory`:** `git clone --depth 1 --filter=blob:none https://github.com/APIs-guru/openapi-directory` at HEAD `f04b8d0b`, then every `.json`/`.yaml`/`.yml` file under `APIs/` parsed and grouped by `(style, in, schema shape)` and by Header Object field in one pass over all fourteen selectors → **4,090** documents read, of which **35** files declare it, the largest `vtex.local/Catalog-API/1.0` (324) and `vtex.local/Logistics-API/1.0` (106). **`jentic/jentic-public-apis`:** `curl -sL https://codeload.github.com/jentic/jentic-public-apis/tar.gz/eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` streamed through the same pass, with no unpacked copy on disk → **32,428** documents read, of which **215** files — 43 documents, the largest `setu.co/main/1.0.0` (115) and `vtex.local/Logistics-API/1.0` (104). **Sourcegraph public code search:** `content:"style: simple" file:\.(yaml|yml)$ count:all type:file` → 71,544 matches in 105 files; `content:"\"style\": \"simple\"" file:\.json$ count:all type:file` → 41,763 matches in 9 files. Every file the twelve answered style queries matched was then fetched at its matching commit and measured for all fourteen selectors — **1,216** documents in all, of which **142** files declare it, the largest `ory/kratos-client-go` `api/openapi.yaml` (27) and `NemeStats/NemeStats` (24). The second classification pass — the 428 documents the three `headers:`-block regexps returned — adds 15 more, the largest `tkestack/tke` `pkg/registry/harbor/client/api/swagger.yaml` (47). **GitHub code search:** `gh search code 'style simple' --filename openapi.json` → 0, `… --filename openapi.yml` → 0; `… --filename openapi.yaml` was refused with `HTTP 403: API rate limit exceeded for user ID 19440155` while `gh api rate_limit` still reported 10 of 10 `code_search` remaining — the secondary limiter, which is not waited out by polling, so it was left **unanswered** rather than retried. The 111 classified documents declare it in **9** files, all nine the `api-evangelist/kadence` mirror of Kadence's API. **SwaggerHub public registry:** the enumerated 7,682-body sweep finds **37** declaring specs, the largest `DAVIDSUTHERLAND/fdx-api-v6-1-0/6.1.0` (156); every SwaggerHub version is republishable in place, so none is at an immutable ref; the registry's `openapi-3.0.x` family, **414,968** specs, exposes no body search and no body read of it was reachable from this dispatch, so that half is **unanswered**. **Postman public API network:** the `search-all` call above with `queryText: "simple header parameter"` → totals `api` **0**, `collection` 5, `team` 0; the five `collection` results are word matches on "header" **Vendor developer portals:** the 20 URLs listed above fetched and measured — **15** read as OpenAPI documents, 5 returning 404 (`moby/moby` `docs/api/v1.45.yaml`, `atlassian/openapi-specs`, `keycloak/keycloak` `docs/documentation/api/openapi.json`, `apache/airflow` `airflow-core/docs/stable-rest-api-ref.yaml`, `wildfly/quickstart` `microprofile-openapi/openapi.yaml`) — **1** declares it — `Adyen/adyen-openapi` `json/CheckoutService-v71.json` at 1 declaration, which Fern refuses (the ledger below) |
| `parameter-style-form-query-object` | witness-found | HubSpot Events API v3, `APIs-guru/openapi-directory` `APIs/hubapi.com/events/v3/openapi.yaml` at `https://raw.githubusercontent.com/APIs-guru/openapi-directory/f04b8d0bcd39c52e1cf3ad7a5fe744709832ae49/APIs/hubapi.com/events/v3/openapi.yaml` — 11,169 bytes fetched (MD5 `5db7d3c8b9ac…`), and **2** `style: form` query parameters counted in them over an object schema: `objectProperty.{propname}` and `property.{propname}` on `GET /events/v3/events/`, each `{in: query, style: form, explode: true, schema: {type: object}}`. **Publisher-described** and deployed: `info.x-origin` records HubSpot's own `https://api.hubspot.com/api-catalog-public/v1/apis/events/v3/events` and the server is `https://api.hubapi.com`. It declares no other row of this node | commit `f04b8d0bcd39c52e1cf3ad7a5fe744709832ae49` of `APIs-guru/openapi-directory` | CC0-1.0 (the aggregating repository's own `LICENSE`); the document itself declares no `info.license` | `All checks passed` at Fern CLI 5.114.1 (**exit 0**) and the generate at `fernapi/fern-python-sdk` **5.20.0** also **exit 0** | **APIs.guru / `openapi-directory`:** `git clone --depth 1 --filter=blob:none https://github.com/APIs-guru/openapi-directory` at HEAD `f04b8d0b`, then every `.json`/`.yaml`/`.yml` file under `APIs/` parsed and grouped by `(style, in, schema shape)` and by Header Object field in one pass over all fourteen selectors → **4,090** documents read, of which **1** file declares it — this witness, at 2. **`jentic/jentic-public-apis`:** `curl -sL https://codeload.github.com/jentic/jentic-public-apis/tar.gz/eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` streamed through the same pass, with no unpacked copy on disk → **32,428** documents read, of which **25** files — five documents, the largest `cov-spectrum.org/main/v0` (12), which Fern refuses (the ledger below); `hubspot.com/Events-events/v3` is the same document as this witness, at the same 2. **Sourcegraph public code search:** `content:"style: form" file:\.(yaml|yml)$ count:all type:file` → 51,653 matches in 212 files; `content:"\"style\": \"form\"" file:\.json$ count:all type:file` → 71,366 matches in 240 files. Every file the twelve answered style queries matched was then fetched at its matching commit and measured for all fourteen selectors — **1,216** documents in all, of which **101** files declare it, the largest `LexPredict/lexpredict-contraxsuite` `sdk/openapi_schema.json` (105) and `preset-io/agor` (54). The second classification pass — the 428 documents the three `headers:`-block regexps returned — adds 6 more, among them `nhost/nhost` `services/auth/docs/openapi.yaml` (2). **GitHub code search:** `gh search code 'style form' --filename openapi.yaml` → 5, `… --filename openapi.json` → 3; `… --filename openapi.yml` was refused with `HTTP 403: API rate limit exceeded for user ID 19440155` while `gh api rate_limit` still reported 10 of 10 `code_search` remaining — the secondary limiter, which is not waited out by polling, so it was left **unanswered** rather than retried. The 111 classified documents declare it in **1** file, `FraunhoferISST/diva` `core/services/search-assistant/apiDoc/openapi.yml`. **SwaggerHub public registry:** the enumerated 7,682-body sweep finds **14** declaring specs, the largest `fivesoto/ignition-http-api/1.0.0` (122); every SwaggerHub version is republishable in place, so none is at an immutable ref; the registry's `openapi-3.0.x` family, **414,968** specs, exposes no body search and no body read of it was reachable from this dispatch, so that half is **unanswered**. **Postman public API network:** the `search-all` call above with `queryText: "style form query object"` → totals `api` **1**, `collection` 6, `team` 2; the one `api` result is `Data Formats: Problem/Error Object (rfc7807)`, a word match on "object" **Vendor developer portals:** the 20 URLs listed above fetched and measured — **15** read as OpenAPI documents, 5 returning 404 (`moby/moby` `docs/api/v1.45.yaml`, `atlassian/openapi-specs`, `keycloak/keycloak` `docs/documentation/api/openapi.json`, `apache/airflow` `airflow-core/docs/stable-rest-api-ref.yaml`, `wildfly/quickstart` `microprofile-openapi/openapi.yaml`) — **0** declare it |
| `parameter-style-form-cookie-scalar` | witness-blocked | none reached. **18** files declare the conjunction across the seven sources and every one is a tooling fixture — `Cornutum/tcases` in eight separate test-resource trees, the `readmeio/oas` `parameters-style` example fixture in its four 3.0/3.1 spellings, `corvus-dotnet/Corvus.JsonSchema` `TestData/canonicalization-spec-3.2.json`, `ogen-go/ogen` `openapi/parser/_testdata/negative/parameters/cookie/invalid_cookie_name.json` (a *negative* parser case), and `wallarm/api-firewall` `resources/test/specification/openapi_for_tests.json`. Both complete corpora declare it **0** times and so does SwaggerHub's enumerated half. The outcome is `witness-blocked` on its unanswered-source reading rather than `none-found`, because two required sources did not fully answer: GitHub refused `gh search code 'style form' --filename openapi.yml`, and SwaggerHub's 414,968-spec `openapi-3.0.x` family went unread. What blocks this row is therefore a search still owed, and its question stays open. What the answered sources do show is a real pattern rather than an accident of coverage — cookie parameters are rare, `form` is their default so declaring it is redundant, and Fern discards cookie parameters anyway ([`../fern-limitations.md`](../fern-limitations.md) `cookie-parameter`) | — | — | — | **APIs.guru / `openapi-directory`:** `git clone --depth 1 --filter=blob:none https://github.com/APIs-guru/openapi-directory` at HEAD `f04b8d0b`, then every `.json`/`.yaml`/`.yml` file under `APIs/` parsed and grouped by `(style, in, schema shape)` and by Header Object field in one pass over all fourteen selectors → **4,090** documents read, of which **0** files declare it. **`jentic/jentic-public-apis`:** `curl -sL https://codeload.github.com/jentic/jentic-public-apis/tar.gz/eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` streamed through the same pass, with no unpacked copy on disk → **32,428** documents read, of which **0** files declare it. **Sourcegraph public code search:** `content:"style: form" file:\.(yaml|yml)$ count:all type:file` → 51,653 matches in 212 files; `content:"\"style\": \"form\"" file:\.json$ count:all type:file` → 71,366 matches in 240 files. Every file the twelve answered style queries matched was then fetched at its matching commit and measured for all fourteen selectors — **1,216** documents in all, of which **18** files declare it, every one of them the fixture pool named in the witness cell. **GitHub code search:** `gh search code 'style form' --filename openapi.yaml` → 5, `… --filename openapi.json` → 3; `… --filename openapi.yml` was refused with `HTTP 403: API rate limit exceeded for user ID 19440155` while `gh api rate_limit` still reported 10 of 10 `code_search` remaining — the secondary limiter, which is not waited out by polling, so it was left **unanswered** rather than retried. The 111 classified documents declare it **0** times. **SwaggerHub public registry:** the enumerated 7,682-body sweep declares it **0** times; the registry's `openapi-3.0.x` family, **414,968** specs, exposes no body search and no body read of it was reachable from this dispatch, so that half is **unanswered**. **Postman public API network:** the `search-all` call above with `queryText: "style form cookie"` → totals `api` **0**, `collection` 2, `team` 0; the two `collection` results are word matches on "cookie" **Vendor developer portals:** the 20 URLs listed above fetched and measured — **15** read as OpenAPI documents, 5 returning 404 (`moby/moby` `docs/api/v1.45.yaml`, `atlassian/openapi-specs`, `keycloak/keycloak` `docs/documentation/api/openapi.json`, `apache/airflow` `airflow-core/docs/stable-rest-api-ref.yaml`, `wildfly/quickstart` `microprofile-openapi/openapi.yaml`) — **0** declare it |
| `parameter-style-spacedelimited-query-scalar` | witness-blocked | LORIS Data Query Tool API, `aces/Loris` `modules/dataquery/static/schema.yml` at `https://raw.githubusercontent.com/aces/Loris/3305a00312178ea75f135be1564beaf222b25822/modules/dataquery/static/schema.yml` — 12,810 bytes fetched, and **2** `style: spaceDelimited` query parameters counted in them over a scalar schema, both on `PATCH /queries/{QueryID}`. **Publisher-owned:** `aces/Loris` is the ACES organisation's own repository for LORIS, the deployed neuroimaging data platform, and the document is the Data Query module's own description of the endpoints that module serves. It is pinned to an immutable commit at a credential-free `.yml` URL, its name is in neither `CORPUS.md` nor either DROPPED/REJECTED list, and **Fern accepts it at both stages**. **Blocked because it is not redistributable:** the repository is `GPL-3.0` and the document declares `GNU Public License, Version 3` in `info.license`, which is outside the corpus's Apache-2.0/MIT/BSD/CC0 set. It also declares **4** `parameter-style-pipedelimited-query-scalar` parameters on the same operation, where it is the second-best candidate behind that row's own witness | commit `3305a00312178ea75f135be1564beaf222b25822` | **`GPL-3.0` — the obstacle**; `info.license` `GNU Public License, Version 3` and the repository's own licence agree | `All checks passed` at Fern CLI 5.114.1 (**exit 0**, `Found 0 errors and 2 warnings`) and the generate at `fernapi/fern-python-sdk` **5.20.0** also **exit 0** — recorded even though the row is blocked, because it tells the consuming node the licence is the only thing in the way | **APIs.guru / `openapi-directory`:** `git clone --depth 1 --filter=blob:none https://github.com/APIs-guru/openapi-directory` at HEAD `f04b8d0b`, then every `.json`/`.yaml`/`.yml` file under `APIs/` parsed and grouped by `(style, in, schema shape)` and by Header Object field in one pass over all fourteen selectors → **4,090** documents read, of which **0** files declare it. **`jentic/jentic-public-apis`:** `curl -sL https://codeload.github.com/jentic/jentic-public-apis/tar.gz/eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` streamed through the same pass, with no unpacked copy on disk → **32,428** documents read, of which **0** files declare it. **Sourcegraph public code search:** `content:"style: spaceDelimited" file:\.(yaml|yml)$ count:all type:file` → 207 matches in 187 files; `content:"\"style\": \"spaceDelimited\"" file:\.json$ count:all type:file` → 295 matches in 63 files. Every file the twelve answered style queries matched was then fetched at its matching commit and measured for all fourteen selectors — **1,216** documents in all, of which **7** files declare it: this witness at 2, and six tooling fixtures — four `Checkmarx/kics` `property_allow_empty_value_ignored` test cases, `Cornutum/tcases` `styles.json`, and `postmanlabs/openapi-to-postman` `test/data/valid_openapi/petstore_deprecated_param.json`. **GitHub code search:** `gh search code 'style spaceDelimited' --filename openapi.yml` → 0; `… --filename openapi.yaml` and `… --filename openapi.json` were refused with `HTTP 403: API rate limit exceeded for user ID 19440155` while `gh api rate_limit` still reported 10 of 10 `code_search` remaining — the secondary limiter, which is not waited out by polling, so it was left **unanswered** rather than retried. The 111 classified documents declare it **0** times. **SwaggerHub public registry:** `GET /specs?query=spaceDelimited&limit=1` → `totalCount` **103**, all name and description matches because that endpoint does not search bodies; the enumerated 7,682-body sweep declares it **0** times; the registry's `openapi-3.0.x` family, **414,968** specs, exposes no body search and no body read of it was reachable from this dispatch, so that half is **unanswered**. **Postman public API network:** the `search-all` call above with `queryText: "spaceDelimited query"` → totals `api` **0**, `collection` 0, `team` 0; nothing in any index, and `queryText: "spaceDelimited"` → every index **0** **Vendor developer portals:** the 20 URLs listed above fetched and measured — **15** read as OpenAPI documents, 5 returning 404 (`moby/moby` `docs/api/v1.45.yaml`, `atlassian/openapi-specs`, `keycloak/keycloak` `docs/documentation/api/openapi.json`, `apache/airflow` `airflow-core/docs/stable-rest-api-ref.yaml`, `wildfly/quickstart` `microprofile-openapi/openapi.yaml`) — **0** declare it |
| `parameter-style-pipedelimited-query-scalar` | fern-rejected | AlayaCare Billable Item Management API, `jentic/jentic-public-apis` `apis/openapi/swaggerhub.alayacare/api-rating/1.0.1/openapi.json` at `https://raw.githubusercontent.com/jentic/jentic-public-apis/eb9d12a2684b0fbcb5aecf51e8ae54dba0929743/apis/openapi/swaggerhub.alayacare/api-rating/1.0.1/openapi.json` — 129,950 bytes fetched (MD5 `7d23ba14ee07…`), and **1** `style: pipeDelimited` query parameter counted in them over a scalar schema, on `GET /service_accounting`. It describes a real, deployed commercial API — `info.title` `Billable Item Management API`, servers `https://{tenant}.alayacare.com/api/v1/rating/` and `…/api/v2/rating/` — and unlike the SwaggerHub original it is aggregated from, the jentic copy is pinned to an immutable commit under a redistribution-compatible licence. It declares no other row of this node. The other Fern-accepted declarer, LORIS (**4** declarations), is `GPL-3.0` and is this node's `parameter-style-spacedelimited-query-scalar` blocked witness. **Put to Fern for registration by the change that settles this region, it is refused** — not by an exit code but by the exit-0-and-nothing-happened failure [`../../tests/fixtures/AGENTS.md`](../../tests/fixtures/AGENTS.md) names, which is why this row reads `fern-rejected` rather than `witness-found` | commit `eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` of `jentic/jentic-public-apis` | CC0-1.0 (the aggregating repository's own `LICENSE`, the same standing this file's ledger already gives that aggregation); the document itself declares no `info.license` | **exit 0, and nothing generated.** `All checks passed` at Fern CLI 5.114.1 and the generate at `fernapi/fern-python-sdk` **5.20.0** also **exit 0** — over a document Fern never parsed. Its generation log carries `[api]: python-sdk Failed to parse openapi document Billable Item Management API` and `Failed to resolve #/components/schemas/ApiError`, a `CliError` out of `resolveResponseReference`, and the tree it wrote is an **empty SDK**: 35 files, a zero-byte `README.md`, a 12-byte `reference.md`, and an `__init__.py` exporting nothing but the client. Five of its Response Object positions — `/billable_item_error` and four `bill_code*` operations, each `"400"` — hold a `$ref` to a *Schema* Object rather than a Response Object, which Fern's response resolver will not follow. Logged in that file's REJECTED table | **APIs.guru / `openapi-directory`:** `git clone --depth 1 --filter=blob:none https://github.com/APIs-guru/openapi-directory` at HEAD `f04b8d0b`, then every `.json`/`.yaml`/`.yml` file under `APIs/` parsed and grouped by `(style, in, schema shape)` and by Header Object field in one pass over all fourteen selectors → **4,090** documents read, of which **0** files declare it. **`jentic/jentic-public-apis`:** `curl -sL https://codeload.github.com/jentic/jentic-public-apis/tar.gz/eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` streamed through the same pass, with no unpacked copy on disk → **32,428** documents read, of which **10** files — two documents: this witness (1) and `bloomerang.co/main/2.0.0` (3), which Fern refuses (the ledger below). **Sourcegraph public code search:** `content:"style: pipeDelimited" file:\.(yaml|yml)$ count:all type:file` → 150 matches in 86 files; `content:"\"style\": \"pipeDelimited\"" file:\.json$ count:all type:file` → 387 matches in 62 files. Every file the twelve answered style queries matched was then fetched at its matching commit and measured for all fourteen selectors — **1,216** documents in all, of which **8** files declare it: `aces/Loris` (4), this witness's five jentic copies (1 each), and `Cornutum/tcases` `styles.json`. **GitHub code search:** `gh search code 'style pipeDelimited' --filename openapi.yaml` → 0, `… --filename openapi.json` → 0, `… --filename openapi.yml` → 0. The 111 classified documents declare it **0** times. **SwaggerHub public registry:** `GET /specs?query=pipeDelimited&limit=1` → `totalCount` **55**, all name and description matches; the enumerated 7,682-body sweep finds **1** declaring spec, `Diebold-Nixdorf/dn-assist_api/1.0` (3), at no immutable ref; the registry's `openapi-3.0.x` family, **414,968** specs, exposes no body search and no body read of it was reachable from this dispatch, so that half is **unanswered**. **Postman public API network:** the `search-all` call above with `queryText: "pipeDelimited query"` → totals `api` **0**, `collection` 0, `team` 0; nothing in any index, and `queryText: "pipeDelimited"` → every index **0** **Vendor developer portals:** the 20 URLs listed above fetched and measured — **15** read as OpenAPI documents, 5 returning 404 (`moby/moby` `docs/api/v1.45.yaml`, `atlassian/openapi-specs`, `keycloak/keycloak` `docs/documentation/api/openapi.json`, `apache/airflow` `airflow-core/docs/stable-rest-api-ref.yaml`, `wildfly/quickstart` `microprofile-openapi/openapi.yaml`) — **0** declare it |
| `parameter-style-deepobject-query-array` | witness-found | Palo Alto Networks Configuration Orchestration API, `PaloAltoNetworks/pan.dev` `openapi-specs/sase/config-orch/paloaltonetworks-Remote_Networks.yaml` at `https://raw.githubusercontent.com/PaloAltoNetworks/pan.dev/b9122449813f2a5fb2e1f9e403aae3d50c9712f4/openapi-specs/sase/config-orch/paloaltonetworks-Remote_Networks.yaml` — 66,219 bytes fetched, and **1** `style: deepObject` query parameter counted in them over an array schema: `components.parameters.RemoteNetworksNames`. **Publisher-owned** and deployed: `PaloAltoNetworks/pan.dev` is Palo Alto Networks' own developer-portal repository (not a fork, not archived) and the single server is `https://api.sase.paloaltonetworks.com`. It declares no other row of this node. A second Fern-accepted declarer is an alternate rather than a near-miss: Strapi REST API, `jentic` `apis/openapi/strapi.io/strapi-rest-api/5.0.0/openapi.json`, **1** declaration on `components.parameters.fields` | commit `b9122449813f2a5fb2e1f9e403aae3d50c9712f4` | MIT (the repository's own licence, per the GitHub API); the document declares no `info.license` | `All checks passed` at Fern CLI 5.114.1 (**exit 0**) and the generate at `fernapi/fern-python-sdk` **5.20.0** also **exit 0** | **APIs.guru / `openapi-directory`:** `git clone --depth 1 --filter=blob:none https://github.com/APIs-guru/openapi-directory` at HEAD `f04b8d0b`, then every `.json`/`.yaml`/`.yml` file under `APIs/` parsed and grouped by `(style, in, schema shape)` and by Header Object field in one pass over all fourteen selectors → **4,090** documents read, of which **1** file declares it — `stripe.com/2022-11-15` at 222, which Fern refuses (the ledger below). **`jentic/jentic-public-apis`:** `curl -sL https://codeload.github.com/jentic/jentic-public-apis/tar.gz/eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` streamed through the same pass, with no unpacked copy on disk → **32,428** documents read, of which **16** files — four documents, the largest `stripe.com/main/2025-03-31.basil` (264) and `stripe.com/stripe/2026-03-25.dahlia` (270), both Fern-refused; `mangadex.org/main/5.13.1` (4) is Fern-refused too, and `strapi.io` (1) is the alternate named above. **Sourcegraph public code search:** `content:"style: deepObject" file:\.(yaml|yml)$ count:all type:file` → 1,421 matches in 293 files; `content:"\"style\": \"deepObject\"" file:\.json$ count:all type:file` → 1,889 matches in 335 files. Every file the twelve answered style queries matched was then fetched at its matching commit and measured for all fourteen selectors — **1,216** documents in all, of which **25** files declare it, this witness among them. **GitHub code search:** `gh search code 'style deepObject' --filename openapi.yaml` → 1; `… --filename openapi.json` and `… --filename openapi.yml` were refused with `HTTP 403: API rate limit exceeded for user ID 19440155` while `gh api rate_limit` still reported 10 of 10 `code_search` remaining — the secondary limiter, which is not waited out by polling, so it was left **unanswered** rather than retried. The 111 classified documents declare it **0** times. **SwaggerHub public registry:** `GET /specs?query=deepObject&limit=1` → `totalCount` **0**; the enumerated 7,682-body sweep finds **2** declaring specs, `YAGNESHMODH_1/rev-cent_api_v_2/03-02-2024` (22) and `Moira/moira-alert/v2.16.0` (1), neither at an immutable ref; the registry's `openapi-3.0.x` family, **414,968** specs, exposes no body search and no body read of it was reachable from this dispatch, so that half is **unanswered**. **Postman public API network:** the `search-all` call above with `queryText: "deepObject query array"` → totals `api` **0**, `collection` 0, `team` 662; the 662 `team` results are word matches on "query" and "array" **Vendor developer portals:** the 20 URLs listed above fetched and measured — **15** read as OpenAPI documents, 5 returning 404 (`moby/moby` `docs/api/v1.45.yaml`, `atlassian/openapi-specs`, `keycloak/keycloak` `docs/documentation/api/openapi.json`, `apache/airflow` `airflow-core/docs/stable-rest-api-ref.yaml`, `wildfly/quickstart` `microprofile-openapi/openapi.yaml`) — **1** declares it — `stripe/openapi` `openapi/spec3.json` at 273, which Fern refuses (the ledger below) |
| `parameter-style-deepobject-query-scalar` | witness-found | Open Integration Hub Secrets Service, `openintegrationhub/openintegrationhub` `services/secret-service/doc/openapi.json` at `https://raw.githubusercontent.com/openintegrationhub/openintegrationhub/f82a32f8ce88a464874c1fcd3a39afd8e7e4908a/services/secret-service/doc/openapi.json` — 37,239 bytes fetched, and **1** `style: deepObject` query parameter counted in them over a scalar schema: `components.parameters.pageSize`, `{in: query, style: deepObject, schema: {type: integer}}`. **Publisher-owned:** the repository is the Open Integration Hub project's own monorepo (not a fork) and the document is the Secrets Service's own description, shipped beside that service's source. Two properties a reader should weigh, recorded here rather than left to be found: `info.version` is `0.1.0`, and its single `servers` entry is `https://skm.example.com` rather than a production host, because each deployment runs its own. It declares no other row of this node | commit `f82a32f8ce88a464874c1fcd3a39afd8e7e4908a` | Apache-2.0 (`info.license` `Apache 2.0`, and the repository's own licence per the GitHub API) | `All checks passed` at Fern CLI 5.114.1 (**exit 0**) and the generate at `fernapi/fern-python-sdk` **5.20.0** also **exit 0** | **APIs.guru / `openapi-directory`:** `git clone --depth 1 --filter=blob:none https://github.com/APIs-guru/openapi-directory` at HEAD `f04b8d0b`, then every `.json`/`.yaml`/`.yml` file under `APIs/` parsed and grouped by `(style, in, schema shape)` and by Header Object field in one pass over all fourteen selectors → **4,090** documents read, of which **1** file declares it — `stripe.com/2022-11-15` at 6, which Fern refuses (the ledger below). **`jentic/jentic-public-apis`:** `curl -sL https://codeload.github.com/jentic/jentic-public-apis/tar.gz/eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` streamed through the same pass, with no unpacked copy on disk → **32,428** documents read, of which **6** files — two Stripe documents at 1 each, both Fern-refused. **Sourcegraph public code search:** `content:"style: deepObject" file:\.(yaml|yml)$ count:all type:file` → 1,421 matches in 293 files; `content:"\"style\": \"deepObject\"" file:\.json$ count:all type:file` → 1,889 matches in 335 files. Every file the twelve answered style queries matched was then fetched at its matching commit and measured for all fourteen selectors — **1,216** documents in all, of which **13** files declare it: this witness, four `springdoc/springdoc-openapi` `app120.json` test results, four `Checkmarx/kics` cases, `Cornutum/tcases` `styles.json`, and three `ballerina-platform` generator fixtures. **GitHub code search:** `gh search code 'style deepObject' --filename openapi.yaml` → 1; `… --filename openapi.json` and `… --filename openapi.yml` were refused with `HTTP 403: API rate limit exceeded for user ID 19440155` while `gh api rate_limit` still reported 10 of 10 `code_search` remaining — the secondary limiter, which is not waited out by polling, so it was left **unanswered** rather than retried. The 111 classified documents declare it **0** times. **SwaggerHub public registry:** `GET /specs?query=deepObject&limit=1` → `totalCount` **0**; the enumerated 7,682-body sweep finds **1** declaring spec, `keyhook/developer_api/1.0.22` (18), at no immutable ref; the registry's `openapi-3.0.x` family, **414,968** specs, exposes no body search and no body read of it was reachable from this dispatch, so that half is **unanswered**. **Postman public API network:** the `search-all` call above with `queryText: "deepObject query scalar"` → totals `api` **0**, `collection` 0, `team` 0; nothing in any index **Vendor developer portals:** the 20 URLs listed above fetched and measured — **15** read as OpenAPI documents, 5 returning 404 (`moby/moby` `docs/api/v1.45.yaml`, `atlassian/openapi-specs`, `keycloak/keycloak` `docs/documentation/api/openapi.json`, `apache/airflow` `airflow-core/docs/stable-rest-api-ref.yaml`, `wildfly/quickstart` `microprofile-openapi/openapi.yaml`) — **1** declares it — `stripe/openapi` `openapi/spec3.json` at 1, which Fern refuses |
| `header-content` | witness-found | VTEX Pricing API, `APIs-guru/openapi-directory` `APIs/vtex.local/Pricing-API/1.0/openapi.yaml` at `https://raw.githubusercontent.com/APIs-guru/openapi-directory/f04b8d0bcd39c52e1cf3ad7a5fe744709832ae49/APIs/vtex.local/Pricing-API/1.0/openapi.yaml` — 70,239 bytes fetched (MD5 `451dd6315a50…`), and **22** Response Header Objects declaring `content` counted in them, each a header carrying a media type instead of a `schema`: on `GET /pricing/config` alone, `Access-Control-Allow-Credentials`, `Access-Control-Allow-Origin`, `Connection`, `Content-Encoding` and more. **Publisher-authored:** `info.x-origin` records VTEX's own `https://raw.githubusercontent.com/vtex/openapi-schemas/master/VTEX - Pricing API.json`, and the deployed server is `https://api.vtex.com/{accountName}`. The same document is this node's alternate for `parameter-style-simple-header-scalar`, which it declares **28** times; it declares no other row of this node. It is the redistributable form of the shape: VTEX's own repository declares no licence at all, while the APIs.guru copy sits under that aggregation's CC0-1.0 at an immutable commit | commit `f04b8d0bcd39c52e1cf3ad7a5fe744709832ae49` of `APIs-guru/openapi-directory` | CC0-1.0 (the aggregating repository's own `LICENSE`); the document itself declares no `info.license`, and `vtex/openapi-schemas` declares none either — which is why the aggregation's copy rather than the publisher's is the one named here | `All checks passed` at Fern CLI 5.114.1 (**exit 0**) and the generate at `fernapi/fern-python-sdk` **5.20.0** also **exit 0** | **APIs.guru / `openapi-directory`:** `git clone --depth 1 --filter=blob:none https://github.com/APIs-guru/openapi-directory` at HEAD `f04b8d0b`, then every `.json`/`.yaml`/`.yml` file under `APIs/` parsed and grouped by `(style, in, schema shape)` and by Header Object field in one pass over all fourteen selectors → **4,090** documents read, of which **7** files declare it, all seven VTEX documents, the largest `vtex.local/Customer-Credit-API/1.0` (201). **`jentic/jentic-public-apis`:** `curl -sL https://codeload.github.com/jentic/jentic-public-apis/tar.gz/eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` streamed through the same pass, with no unpacked copy on disk → **32,428** documents read, of which **15** files — three documents: `setu.co/main/1.0.0` (156), `apify.com/main/1.0` (35, already REJECTED in `tests/fixtures/AGENTS.md`), and the VTEX copy. **Sourcegraph public code search:** `headers:\n\s+[A-Za-z0-9_-]+:\n(\s+[^\n]*\n)*?\s+content:\s file:\.(yaml|yml)$ count:all type:file` (`patternType:regexp`) → refused with `HTTP Error 429: Too Many Requests` on the first pass and **answered on the retry**: 76,905 matches in 357 files, which added three declarers, all of them a connector spec or a test resource. Across both classification passes — 1,216 documents from the style queries and 428 from this one — **17** files declare it, the largest `GraphQL-Portal/graphql-portal` `packages/datasources/twitter/src/source.json` (343) and `OWASP/crAPI` (12); the one publisher-owned real API among them, `hmrc/vat-api` (11), is Fern-refused (the ledger below). **GitHub code search:** `gh search code 'headers content schema' --filename openapi.yaml` → 0. The 111 classified documents declare it **0** times. GitHub's index tokenises away the nesting this row is about, so a literal query for it cannot separate a Header Object's `content` from any other `content`; the count above is what the nearest expressible query returned. **SwaggerHub public registry:** the enumerated 7,682-body sweep finds **8** declaring specs, the largest `DAVIDSUTHERLAND/fdx-api-v6-1-0/6.1.0` (324); every SwaggerHub version is republishable in place, so none is at an immutable ref; the registry's `openapi-3.0.x` family, **414,968** specs, exposes no body search and no body read of it was reachable from this dispatch, so that half is **unanswered**. **Postman public API network:** the `search-all` call above with `queryText: "header content schema"` → totals `api` **2**, `collection` 4, `team` 0; the two `api` results are `Patterns - Versioning: Content Header - Accept` and `… - Accept-Version`, word matches on "content header" **Vendor developer portals:** the 20 URLs listed above fetched and measured — **15** read as OpenAPI documents, 5 returning 404 (`moby/moby` `docs/api/v1.45.yaml`, `atlassian/openapi-specs`, `keycloak/keycloak` `docs/documentation/api/openapi.json`, `apache/airflow` `airflow-core/docs/stable-rest-api-ref.yaml`, `wildfly/quickstart` `microprofile-openapi/openapi.yaml`) — **0** declare it |
| `header-deprecated` | fern-rejected | openEHR EHR API, `jentic/jentic-public-apis` `apis/openapi/openehr.org/main/latest/openapi.json` at `https://raw.githubusercontent.com/jentic/jentic-public-apis/eb9d12a2684b0fbcb5aecf51e8ae54dba0929743/apis/openapi/openehr.org/main/latest/openapi.json` — 184,682 bytes fetched (MD5 `4b31a3fed1e9…`), and **1** Header Object declaring `deprecated` counted in them: `components.headers.Location_deprecated`. It is a real specification of a deployed API family — `info.title` `EHR API`, the openEHR REST API, server `https://{baseUrl}/v1` — and one of only two documents reaching that standard that declare the field anywhere the seven sources reach. The other is `aptos-labs/aptos-core` `api/doc/spec.yaml` at commit `a1c2b82d`, the Aptos Node API's own description, which declares it **1,675** times — by far the largest count anywhere — and which Fern refuses too while its repository's licence at that commit puts it outside the corpus's set; it is in the ledger below. The other eleven declaring files are test resources (`dochia-dev/dochia-cli` `src/test/resources`, `python-openapi/openapi-core` `tests/integration/data/v3.0/petstore.yaml`, `qaware/openapi-generator-for-spring` `…-test/src/test/resources`, two `smallrye/smallrye-open-api` `core/src/test/resources` filter fixtures, three `OpenAPITools/openapi-diff` response-header cases, `oapi-codegen/oapi-codegen` `internal/test/schemas/deprecated/spec.yaml`, `CreateAPI/CreateAPI` `Tests/Support/Specs`, and `Endava/cats` `src/test/resources`) or the Golem and Aptos siblings, and SwaggerHub's one declarer is in the ledger below. **Fern refuses it**, and it carries a second obstacle behind the first: `info.license` is `Creative Commons Attribution-NoDerivs 3.0 Unported`, a no-derivatives licence outside the corpus's set, so even a Fern fix would not make this document registrable. It also declares **6** `parameter-style-simple-header-scalar` parameters | commit `eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` of `jentic/jentic-public-apis` | `CC-BY-ND-3.0` (`info.license` `Creative Commons Attribution-NoDerivs 3.0 Unported`) — outside the corpus's Apache-2.0/MIT/BSD/CC0 set, and a second obstacle behind the Fern refusal; the aggregating repository is CC0-1.0 | **exit 1** at Fern CLI 5.114.1 — `[sdk] 39 errors`, all of the form `path: __package__.yml -> types -> AbstractEntry`, `issue: Discriminant value _type is not suitable for code generation. Add a "name" property that starts with a letter and contains only letters, numbers, and underscores.`, `Found 39 errors and 0 warnings`; the generate at `fernapi/fern-python-sdk` **5.20.0** also **exit 1** on the same 39 | **APIs.guru / `openapi-directory`:** `git clone --depth 1 --filter=blob:none https://github.com/APIs-guru/openapi-directory` at HEAD `f04b8d0b`, then every `.json`/`.yaml`/`.yml` file under `APIs/` parsed and grouped by `(style, in, schema shape)` and by Header Object field in one pass over all fourteen selectors → **4,090** documents read, of which **0** files declare it. **`jentic/jentic-public-apis`:** `curl -sL https://codeload.github.com/jentic/jentic-public-apis/tar.gz/eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` streamed through the same pass, with no unpacked copy on disk → **32,428** documents read, of which **5** files — one document, this witness, at 1 declaration each. **Sourcegraph public code search:** `headers:\n(\s+[^\n]*\n)*?\s+deprecated:\s file:\.(yaml|yml)$ count:all type:file` (`patternType:regexp`) → refused with `HTTP Error 429: Too Many Requests` on the first pass and **answered on the retry**: 8,459 matches in 574 files. Across both classification passes — 1,216 documents from the style queries and 428 from this one — **13** files declare the field. Twelve are test resources; the thirteenth is the largest declarer found anywhere, `aptos-labs/aptos-core` `api/doc/spec.yaml` at **1,675**, which is in the ledger below. **GitHub code search:** `gh search code 'headers deprecated' --filename openapi.yaml` → 0, `… --filename openapi.json` → 0. The 111 classified documents declare it **0** times. GitHub's index tokenises away the nesting, so a literal query cannot separate a Header Object's `deprecated` from a Parameter Object's or a schema's; the counts above are what the nearest expressible queries returned. **SwaggerHub public registry:** the enumerated 7,682-body sweep finds **1** declaring spec, `DANIELCHURCHLEY/vehicle-api/1.0.0` (6), which is in the ledger below; the registry's `openapi-3.0.x` family, **414,968** specs, exposes no body search and no body read of it was reachable from this dispatch, so that half is **unanswered**. **Postman public API network:** the `search-all` call above with `queryText: "header deprecated"` → totals `api` **0**, `collection` 0, `team` 0; nothing in any index **Vendor developer portals:** the 20 URLs listed above fetched and measured — **15** read as OpenAPI documents, 5 returning 404 (`moby/moby` `docs/api/v1.45.yaml`, `atlassian/openapi-specs`, `keycloak/keycloak` `docs/documentation/api/openapi.json`, `apache/airflow` `airflow-core/docs/stable-rest-api-ref.yaml`, `wildfly/quickstart` `microprofile-openapi/openapi.yaml`) — **0** declare it |
| `header-allow-reserved` | witness-blocked | none reached. Exactly **2** declaring files were found anywhere, and both are synthetic: `microsoft/OpenAPI.NET` `test/Microsoft.OpenApi.Readers.Tests/V3Tests/Samples/OpenApiDocument/apiWithFullHeaderComponent.yaml` (2 declarations) is a reader test sample, and `helidon-io/helidon` `openapi/openapi-32/src/test/resources/static-3.2.yaml` (1) is a `src/test/resources` fixture — the pool the corpus's real-world rule excludes, and the only pool this field turns up in. Everything else is a zero: the two complete corpora declare it **0** times across 4,090 and 32,428 documents; SwaggerHub's enumerated 7,682 bodies declare it **0** times; the 111 GitHub documents classified — 100 of which came back from a query for the literal token `allowReserved` — declare it **0** times, because every `allowReserved` in them is on a Parameter Object rather than a Header Object; and Postman and the vendor portals return nothing. The finding reads as a real pattern rather than an accident of coverage: `allowReserved` is defined for query parameters, so a Header Object has no occasion to carry one, and the specification says as much. The outcome is `witness-blocked` on its unanswered-source reading rather than `none-found`, because required queries did not answer: GitHub refused two of the three `allowReserved` queries, Sourcegraph's plain `allowReserved` literal query stayed refused across both passes, and SwaggerHub's 414,968-spec `openapi-3.0.x` family went unread. What blocks this row is therefore a search still owed, and its question stays open | — | — | — | **APIs.guru / `openapi-directory`:** `git clone --depth 1 --filter=blob:none https://github.com/APIs-guru/openapi-directory` at HEAD `f04b8d0b`, then every `.json`/`.yaml`/`.yml` file under `APIs/` parsed and grouped by `(style, in, schema shape)` and by Header Object field in one pass over all fourteen selectors → **4,090** documents read, of which **0** files declare it. **`jentic/jentic-public-apis`:** `curl -sL https://codeload.github.com/jentic/jentic-public-apis/tar.gz/eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` streamed through the same pass, with no unpacked copy on disk → **32,428** documents read, of which **0** files declare it. **Sourcegraph public code search:** `content:"allowReserved" file:\.(yaml|yml|json)$ count:all type:file` → **unanswered**, refused with `HTTP Error 429: Too Many Requests` on every attempt across both passes — six in the sweep, spaced 30–150 s apart, and eight more in the retry, spaced up to 240 s; `headers:\n(\s+[^\n]*\n)*?\s+allowReserved:\s file:\.(yaml|yml)$ count:all type:file` (`patternType:regexp`) → refused on the first pass and **answered on the retry**: 61 matches in 37 files — the narrowest result any query in this table returned, and the only one that found this field on a Header Object at all. Across both classification passes — 1,216 documents from the style queries and 428 from this one — **2** files declare it, and neither is a document a real API publishes as its own description: `microsoft/OpenAPI.NET` `test/Microsoft.OpenApi.Readers.Tests/V3Tests/Samples/OpenApiDocument/apiWithFullHeaderComponent.yaml` (2), a reader test sample whose very name says what it is, and `helidon-io/helidon` `openapi/openapi-32/src/test/resources/static-3.2.yaml` (1), a `src/test/resources` fixture. The plain literal query above is the one that stayed **unanswered** **GitHub code search:** `gh search code 'allowReserved' --filename openapi.yml` → 100 (the page cap), and all 100 were fetched and measured — the one whose path contains a space (`StefH/n8n.SDK` `resources/n8n - openapi.yml`, the n8n Public API) re-fetched percent-encoded — and **0** put `allowReserved` on a Header Object. `… --filename openapi.yaml` and `… --filename openapi.json` were refused with `HTTP 403: API rate limit exceeded for user ID 19440155` while `gh api rate_limit` still reported 10 of 10 `code_search` remaining — the secondary limiter, which is not waited out by polling, so it was left **unanswered** rather than retried. **SwaggerHub public registry:** `GET /specs?query=allowReserved&limit=1` → `totalCount` **1,270**, all name and description matches because that endpoint does not search bodies; the enumerated 7,682-body sweep declares it **0** times; the registry's `openapi-3.0.x` family, **414,968** specs, exposes no body search and no body read of it was reachable from this dispatch, so that half is **unanswered**. **Postman public API network:** the `search-all` call above with `queryText: "allowReserved header"` → totals `api` **0**, `collection` 0, `team` 0; nothing in any index, and `queryText: "allowReserved"` → every index **0** **Vendor developer portals:** the 20 URLs listed above fetched and measured — **15** read as OpenAPI documents, 5 returning 404 (`moby/moby` `docs/api/v1.45.yaml`, `atlassian/openapi-specs`, `keycloak/keycloak` `docs/documentation/api/openapi.json`, `apache/airflow` `airflow-core/docs/stable-rest-api-ref.yaml`, `wildfly/quickstart` `microprofile-openapi/openapi.yaml`) — **0** declare it |
| `operation-overrides-path-item-parameter` | witness-found | AWS Import/Export, `APIs-guru/openapi-directory` `APIs/amazonaws.com/importexport/2010-06-01/openapi.yaml` at `https://raw.githubusercontent.com/APIs-guru/openapi-directory/f04b8d0bcd39c52e1cf3ad7a5fe744709832ae49/APIs/amazonaws.com/importexport/2010-06-01/openapi.yaml` — 68,804 bytes fetched (MD5 `27159b1a6533…`), and **24** Operation-over-Path-Item collisions counted in them: each of the six paths declares `parameters` referencing `components.parameters.Action` and `components.parameters.Version`, and each path's `get` and `post` redeclare both `(name, in)` pairs inline — 6 × 2 methods × 2 names. **This row's cheapest candidate was answered first, as the task requires, and it failed.** `asana.com` is the registered source the census already sees declaring the collision three times, and Fern refuses it: `fern check` at CLI **5.114.1** exits **1** with `[sdk] 17 errors` — `AddFollowersRequest is already declared in goals.yml`, `ProjectSaveAsTemplateRequest is already declared in projects.yml`, `RemoveFollowersRequest is already declared in goals.yml` and fourteen `Expected example to be a date. Example is: "2019-09-15T02:06:58.147Z"` — and the generate at `fernapi/fern-python-sdk` **5.20.0** exits **1** on the same 17. That is the same 17 the batch-4 ledger recorded, now re-measured rather than inherited. The witness above is the document found after it. It declares no other row of this node | commit `f04b8d0bcd39c52e1cf3ad7a5fe744709832ae49` of `APIs-guru/openapi-directory` | Apache-2.0 (`info.license` `Apache 2.0 License`); the aggregating repository is CC0-1.0 | `All checks passed` at Fern CLI 5.114.1 (**exit 0**) and the generate at `fernapi/fern-python-sdk` **5.20.0** also **exit 0** | **APIs.guru / `openapi-directory`:** `git clone --depth 1 --filter=blob:none https://github.com/APIs-guru/openapi-directory` at HEAD `f04b8d0b`, then every `.json`/`.yaml`/`.yml` file under `APIs/` parsed and grouped by `(style, in, schema shape)` and by Header Object field in one pass over all fourteen selectors → **4,090** documents read, of which **16** files declare it, the largest `zoom.us/2.0.0` (149) and `spoonacular.com/1.1` (55); `asana.com/1.0` is there at the same **3** the entry table records. **`jentic/jentic-public-apis`:** `curl -sL https://codeload.github.com/jentic/jentic-public-apis/tar.gz/eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` streamed through the same pass, with no unpacked copy on disk → **32,428** documents read, of which **236** files — 48 documents, the largest `vrchat.com/main/1.20.7` (220) and `spotdraft.com/main/v1` (86), both Fern-refused (the ledger below). **Sourcegraph public code search:** this shape is a *collision between two lists* rather than a literal string, so no literal index can express it; what was put to Sourcegraph instead is the structural regexp `^  /[^\n]*:\n    parameters:\n file:\.(yaml|yml)$ count:all type:file` (`patternType:regexp`), which selects documents with a Path Item-level `parameters` list at all — the necessary half of the collision — and it returned **unanswered**, refused with `HTTP Error 429: Too Many Requests` on every attempt. The row does not rest on it: the 1,216 documents the style sweeps fetched were measured for all fourteen selectors, and **32** of them declare this collision, the largest `chatwoot/chatwoot` `swagger/swagger.json` (22) and `laxamentumtech/audnexus` `docs/spec/audnexus.yaml` (7). The second classification pass — the 428 documents the three `headers:`-block regexps returned — adds 15 more, the largest `ubicloud/ubicloud` `openapi/openapi.yml` (3). **GitHub code search:** the same inexpressibility applies; the 111 classified documents declare it in **1** file, `FraunhoferISST/diva` `core/services/entity-management/apiDoc/openapi.yml` (14). **SwaggerHub public registry:** the enumerated 7,682-body sweep finds **78** declaring specs, the largest `perso-29e/sekoiaio-api-documentation/1.0` (172); every SwaggerHub version is republishable in place, so none is at an immutable ref; the registry's `openapi-3.0.x` family, **414,968** specs, exposes no body search and no body read of it was reachable from this dispatch, so that half is **unanswered**. **Postman public API network:** the `search-all` call above with `queryText: "operation parameter override"` → totals `api` **0**, `collection` 0, `team` 0; nothing in any index **Vendor developer portals:** the 20 URLs listed above fetched and measured — **15** read as OpenAPI documents, 5 returning 404 (`moby/moby` `docs/api/v1.45.yaml`, `atlassian/openapi-specs`, `keycloak/keycloak` `docs/documentation/api/openapi.json`, `apache/airflow` `airflow-core/docs/stable-rest-api-ref.yaml`, `wildfly/quickstart` `microprofile-openapi/openapi.yaml`) — **1** declares it — `cloudflare/api-schemas` `openapi.json` at 12, which Fern refuses (the ledger below) |

#### The candidate ledger

Every candidate below was fetched at the pinned ref and its declarations counted
in the fetched bytes with the census's own object model; `fern check` **and** a
real generate at `fernapi/fern-python-sdk` 5.20.0 were run on the ones that
survived that count and were redistributable and real-world enough to be worth the
run. Each is recorded because it is what a repeat of this search finds first, and
each says which of this node's rows it declares and how many declarations of each
it carries — the input the node that registers them works from.

| candidate | ref | license | counted in the fetched bytes | what became of it |
|---|---|---|---|---|
| `hpi-schul-cloud/schulcloud-server` `src/services/fileStorage/docs/openapi.yaml` | commit `e18e409fdc370f27f9b607ded292278ae7b3c89c` | AGPL-3.0 (per the GitHub API) | `style: matrix` = 2, on the `permissions` parameter of two operations; **no** row of this node | **not a witness — it declares a different shape.** It is the one *deployed service's own* document either code search returned besides appNG, so it reads like the answer; but neither declaration carries an `in` field at all, and neither carries a `schema` — both use a non-standard parameter-level `type: Array`. So it declares neither `in: path` nor a scalar schema, and the conjunction is absent from it. Its licence would have blocked it in any case, being outside the corpus's Apache/MIT/BSD/CC0 set |
| `jentic/jentic-public-apis` `apis/openapi/opendatasoft.com/main/v2.0/openapi.json` | commit `9d36c7e3808ebe65d69e81d3c3250598927a575c` | CC0-1.0 (the aggregating repository's own `LICENSE`); the document itself declares no `info.license` | `"style": "matrix"` = 1, on `components.parameters.lang`; **no** row of this node | **not a witness — the wrong `in`.** Opendatasoft's Explore API v2 is a real, deployed API and this was the only `matrix` declaration in the whole aggregated corpus, so it is the strongest-looking candidate the complete sweeps produced. Read out of the fetched bytes with the census's object model, the parameter is `in: query` — a combination the specification does not even permit for `matrix` — so it declares no path parameter and cannot witness that row. Its four bundled/entry sibling copies in the same directory carry the same one parameter |
| `asana.com`, `APIs-guru/openapi-directory` `APIs/asana.com/1.0/openapi.yaml` | commit `f04b8d0bcd39c52e1cf3ad7a5fe744709832ae49` | Apache-2.0 (`info.license` `Apache 2.0`) | `operation-overrides-path-item-parameter` = **3** | **fern-rejected — and it is the cheapest candidate this node had, so it was answered first.** It is a registered `link-ok` source the census already sees declaring the collision, and it carries no committed golden. Re-measured rather than inherited: `fern check` at CLI **5.114.1** exits **1**, `[sdk] 17 errors` — three inline request-body collisions (`AddFollowersRequest`, `ProjectSaveAsTemplateRequest`, `RemoveFollowersRequest` "already declared in") and fourteen `Expected example to be a date. Example is: "2019-09-15T02:06:58.147Z"` — and the generate at `fernapi/fern-python-sdk` **5.20.0** exits **1** on the same 17, ending `Failed.` It is already `CORPUS.md`'s batch-4 DROPPED ledger and `tests/fixtures/AGENTS.md`'s REJECTED list at the same 17 |
| `cloudflare/api-schemas` `openapi.json` | commit `f2df0ca75c0fec9047e91a34c20dd666c2dcd1ba` | BSD-3-Clause (the repository's own licence, per the GitHub API) | `operation-overrides-path-item-parameter` = **12** | **fern-rejected.** Publisher-owned (Cloudflare's own organisation, not a fork) and 24,861,364 bytes of a live API, so it was worth the run; `fern check` at CLI 5.114.1 exits **1** and the generate at 5.20.0 exits **1** |
| `stripe/openapi` `openapi/spec3.json` | commit `58e06a3214ae1574600fba64d40b770e5da6d505` | MIT (the repository's own licence) | `parameter-style-deepobject-query-array` = **273**, `parameter-style-deepobject-query-scalar` = **1** | **fern-rejected — and the largest `deepObject` declarer found anywhere.** Publisher-owned (Stripe's own organisation) and by content the strongest candidate either `deepObject` row has: 8,028,700 bytes describing Stripe's live API. `fern check` at CLI 5.114.1 exits **1**, `[sdk] 24 errors`, the first `path: __package__.yml -> service -> endpoints -> DeleteCustomersCustomerSubscriptionsSubscriptionExposedId -> examples[0] -> response`, `issue: Expected example to be an object. Example is: "schedule"`, `Found 24 errors and 20 warnings`; the generate at 5.20.0 exits **1** on the same 24. Its APIs.guru copy, `APIs/stripe.com/2022-11-15/openapi.yaml`, is the same document one version back and declares 222 and 6 |
| `jentic/jentic-public-apis` `apis/openapi/setu.co/main/1.0.0/openapi.json` | commit `eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` | CC0-1.0 (the aggregating repository's `LICENSE`); the document declares no `info.license` | `header-content` = **156**, `parameter-style-simple-header-scalar` = **115** | **not a witness — a generated composite rather than a publisher's document.** It is the largest `header-content` declarer anywhere and **Fern accepts it at both stages** (`All checks passed`, exit 0; generate exit 0), which is why it was screened first for that row. Its own `apis.json` disqualifies it: *"This spec is generated by combining individual API reference specs from the SetuHQ documentation repository."* Setu published the parts; these bytes are jentic's join of them, and no publisher stands behind the document as a whole. VTEX Pricing API replaces it for both rows |
| `hmrc/vat-api` `resources/public/api/conf/1.0/application.yaml` | commit `23e8d5c6f02600ea45cb70072d93a2435f7898d9` | Apache-2.0 (the repository's own licence) | `header-content` = **11**, `parameter-style-simple-header-scalar` = **8** | **fern-rejected — and the closest miss `header-content` has.** It is HM Revenue & Customs' own VAT (MTD) API description in HMRC's own organisation, Apache-2.0, immutably pinned: everything the bar asks for except Fern. `fern check` at CLI 5.114.1 exits **1** on a single error — `path: __package__.yml -> errors -> UnauthorizedError -> type`, `issue: Unexpected property "message"`, `Found 1 error and 0 warnings` — and the generate at 5.20.0 exits **1** on the same one |
| `jentic/jentic-public-apis` `apis/openapi/pinterest.com/main/5.23.0/openapi.json` | commit `eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` | MIT (`info.license`); the aggregating repository is CC0-1.0 | `parameter-style-simple-path-object` = **2** | **recorded** as that row's `fern-rejected` outcome — the only document a real API publishes as its own description that declares the conjunction anywhere the seven sources reach |
| `jentic/jentic-public-apis` `apis/openapi/openehr.org/main/latest/openapi.json` | commit `eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` | `CC-BY-ND-3.0` (`info.license`); the aggregating repository is CC0-1.0 | `header-deprecated` = **1**, `parameter-style-simple-header-scalar` = **6** | **recorded** as `header-deprecated`'s `fern-rejected` outcome, and blocked twice over: 39 `Discriminant value _type is not suitable for code generation` errors at both stages, and a no-derivatives licence outside the corpus's set |
| `aces/Loris` `modules/dataquery/static/schema.yml` | commit `3305a00312178ea75f135be1564beaf222b25822` | `GPL-3.0` | `parameter-style-spacedelimited-query-scalar` = **2**, `parameter-style-pipedelimited-query-scalar` = **4** | **recorded** as `parameter-style-spacedelimited-query-scalar`'s `witness-blocked` outcome — Fern accepts it at both stages and its licence is the only thing in the way. For the `pipeDelimited` row it is the second-best candidate behind AlayaCare, blocked the same way |
| `jentic/jentic-public-apis` `apis/openapi/strapi.io/strapi-rest-api/5.0.0/openapi.json` | commit `eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` | CC0-1.0 (the aggregating repository's `LICENSE`); the document declares no `info.license` | `parameter-style-deepobject-query-array` = **1**, on `components.parameters.fields` | **an alternate, not a near-miss.** Fern accepts it at both stages, so it is a second registrable witness for that row behind Palo Alto Networks; it is recorded so the registering node has a fallback if the first document is rejected for a reason this screening did not reach |
| `APIs-guru/openapi-directory` `APIs/listennotes.com/2.0/openapi.yaml`; `APIs/vtex.local/Pricing-API/1.0/openapi.yaml` | commit `f04b8d0bcd39c52e1cf3ad7a5fe744709832ae49` | CC0-1.0 (the aggregation's `LICENSE`); neither declares an `info.license` | Listen API: `parameter-style-simple-header-scalar` = **1**. VTEX Pricing API: `parameter-style-simple-header-scalar` = **28**, `header-content` = **22** | **alternates, not near-misses.** Both are Fern-accepted at both stages. Listen API's `x-origin` is the publisher-served `https://listen-api.listennotes.com/api/v2/openapi.yaml`, which is the cleanest provenance of any candidate here; VTEX Pricing API is this node's recorded `header-content` witness and its second-largest `simple`-header declarer |
| `jentic/jentic-public-apis` `apis/openapi/vrchat.com/main/1.20.7/openapi.json`; `…/spotdraft.com/main/v1/openapi.json`; `…/cov-spectrum.org/main/v0/openapi.json`; `…/mangadex.org/main/5.13.1/openapi.json`; `…/opensearch.org/main/0.2.0/openapi.json`; `…/bloomerang.co/main/2.0.0/openapi.json` | commit `eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` | CC0-1.0 (the aggregating repository's `LICENSE`) | `operation-overrides-path-item-parameter` = 220 and 86; `parameter-style-form-query-object` = 12; `parameter-style-deepobject-query-array` = 4; `parameter-style-simple-path-array` = 8; `parameter-style-pipedelimited-query-scalar` = 3 | **all six fern-rejected**, at both stages, with `Found 1`, `10`, `2`, `4`, `85` and `13` errors respectively. VRChat is the largest override declarer found anywhere and fails on one error, which makes it the first thing to re-try after a Fern upgrade |
| `APIs-guru/openapi-directory` `APIs/tfl.gov.uk/v1/openapi.yaml`; `APIs/truora.com/1.0.0/openapi.yaml`; `APIs/api.video/1/openapi.yaml`; `APIs/apacta.com/0.0.42/openapi.yaml`; `APIs/flat.io/2.13.0/openapi.yaml` | commit `f04b8d0bcd39c52e1cf3ad7a5fe744709832ae49` | CC0-1.0 (the aggregation's `LICENSE`); `truora.com` additionally declares `MIT` | `parameter-style-simple-path-array` = 26; `operation-overrides-path-item-parameter` = 4, 3 and 4; `parameter-style-simple-header-scalar` = 2 | **all five fern-rejected**, at both stages, with `Found 14`, `22`, `5`, `4` and `2` errors respectively. Transport for London is the largest `simple`-path-array declarer found anywhere and is worth re-trying after a Fern upgrade |
| `Adyen/adyen-openapi` `json/CheckoutService-v71.json` | commit `f82d1fe674e536cc2c6b0d7946e0e827873a4fbf` | MIT (the repository's own licence) | `parameter-style-simple-header-scalar` = **1** | **fern-rejected** — `fern check` at CLI 5.114.1 exits **1**, `[sdk] 2 errors`, `path: recurring.yml -> service -> endpoints -> post-storedPaymentMethods`, `issue: Duplicate example name: Card details stored`; the generate at 5.20.0 exits **1** on the same 2 |
| SwaggerHub `DANIELCHURCHLEY/vehicle-api/1.0.0`; `DAVIDSUTHERLAND/fdx-api-v6-1-0/6.1.0`; `perso-29e/sekoiaio-api-documentation/1.0`; `fivesoto/ignition-http-api/1.0.0`; `keyhook/developer_api/1.0.22`; `Diebold-Nixdorf/dn-assist_api/1.0`; `YAGNESHMODH_1/rev-cent_api_v_2/03-02-2024` | none available | none declared | between them `header-deprecated` = 6, `header-content` = 324, `parameter-style-simple-header-scalar` = 156, `operation-overrides-path-item-parameter` = 172, `parameter-style-form-query-object` = 122, `parameter-style-deepobject-query-scalar` = 18, `parameter-style-pipedelimited-query-scalar` = 3, `parameter-style-deepobject-query-array` = 22 | reachable at a direct spec URL — `https://api.swaggerhub.com/apis/<owner>/<api>/<version>/swagger.json` resolves 200 — but at **no immutable ref**: a SwaggerHub version string is republishable in place, so the bytes behind that URL can change without the URL changing, and none declares a licence. `DANIELCHURCHLEY/vehicle-api` matters most, because it is the only `header-deprecated` declarer outside openEHR and the test resources: it is plainly not a published API, its only `servers` entry being the bare `/` and its owner an individual account |
| `aptos-labs/aptos-core` `api/doc/spec.yaml` | commit `a1c2b82d3e91ca1bbdf58fc9b413cb5890d5b444` | **proprietary — `NOASSERTION` per the GitHub API**; the repository's `LICENSE` at that commit is the Aptos Foundation's `Innovation-Enabling Source Code License`, and the document's own `info.license` of `Apache 2.0` is stale metadata from before that relicense | `header-deprecated` = **1,675** | **fern-rejected, and licence-blocked behind that.** It is the largest `header-deprecated` declarer found anywhere and a real, deployed API's own description — `info.title` `Aptos Node API`, generated into the node's own repository — which is why it was screened. `fern check` at CLI 5.114.1 exits **1**, `[sdk] 5 errors`, `path: __package__.yml -> types -> AccountSignatureNoAccountSignature -> extends -> NoAccountSignature`, `issue: Objects can only extend other objects, and NoAccountSignature is not an object.`, `Found 5 errors and 0 warnings`; the generate at 5.20.0 exits **1** on the same 5. Read the licence at the pinned commit rather than the document's `info.license`, which is what makes this one look registrable when it is not |
| `golemcloud/golem` `openapi/golem-service.yaml` | commit `d667feeddaff55b154ebddd75ab80f23952a17a2` | **`BSL-1.1` — the obstacle**; the repository's `LICENSE` is Business Source License 1.1 (`NOASSERTION` per the GitHub API) | `header-deprecated` = **1**, `parameter-style-simple-header-scalar` = **2**, `parameter-style-form-query-object` = **1** | **not screened — blocked on licence before Fern.** Golem is a real deployed service and this is its own generated description, so it is recorded to save the next search the fetch; a Business Source License is outside the corpus's Apache/MIT/BSD/CC0 set |
| `microsoft/OpenAPI.NET` `test/Microsoft.OpenApi.Readers.Tests/V3Tests/Samples/OpenApiDocument/apiWithFullHeaderComponent.yaml`; `helidon-io/helidon` `openapi/openapi-32/src/test/resources/static-3.2.yaml` | commits `ba2d2a5b`, `bbd33718` | MIT; Apache-2.0 | `header-allow-reserved` = **2** and **1**; the first also declares `header-deprecated` = 2 | **the only two documents anywhere that put `allowReserved` on a Header Object, and neither is a witness.** Both licences are inside the corpus's set and both refs are immutable, so licence and pinning are not what stops them: they are synthetic. The first is a reader test sample under `test/…/Samples/` named for the fact that it exercises every Header Object field; the second is a `src/test/resources` fixture for Helidon's 3.2 support. Neither was screened with Fern, because a document no API serves cannot become a corpus row however cleanly Fern generates it |
| `ballerina-platform/openapi-connectors` `openapi/shipstation/openapi.yaml` | commit `81158a45` | Apache-2.0 | `header-content` = **5**, `parameter-style-simple-header-scalar` = **23** | **not a witness — a third-party connector spec.** ShipStation is a real API, but these bytes are the Ballerina project's connector definition of it rather than ShipStation's own description, the same unofficial-transcription ground the schemas region's ledger already records |
| The tooling-fixture pool — `Cornutum/tcases` (eight test-resource trees), `readmeio/oas` `packages/oas-examples/3.{0,1}/{json,yaml}/parameters-style.*`, `oapi-codegen/oapi-codegen` `internal/test/parameters/roundtrip/parameters.yaml`, `discord-gophers/goapi-gen`, `OpenAPITools/openapi-generator`, `networknt/light-rest-4j`, `wallarm/api-firewall`, `Checkmarx/kics`, `springdoc/springdoc-openapi`, `smallrye/smallrye-open-api`, `citrusframework/citrus`, `postmanlabs/openapi-to-postman`, `python-openapi/openapi-core`, `dochia-dev/dochia-cli`, `qaware/openapi-generator-for-spring`, `ogen-go/ogen`, `corvus-dotnet/Corvus.JsonSchema`, `fern-api/fern`'s own importer fixture, and the demos `mmontone/cl-rest-server` and `mulesoft/api-console` | fetched at the commit each index matched | mixed | between them every one of the ten style conjunctions and `header-deprecated`; `readmeio/oas` `parameters-style` alone declares `matrix`-path-scalar, `form`-cookie-scalar and `simple`-path-object, 2 each, in four spellings | **none is a witness — none is a document a real API publishes as its own description.** This is the pool the corpus's real-world rule excludes and the same one [`../fern-limitations.md`](../fern-limitations.md) already recorded for `matrix-array`. It is named in full because it is most of what the literal indexes return for these rows, and a repeat of this search will meet it before it meets anything else |
| `GraphQL-Portal/graphql-portal` `packages/datasources/twitter/src/source.json`; `OWASP/crAPI` `services/chatbot/src/resources/crapi-openapi-spec.json` | commits `a6e970f5`, `73d309cc` | MIT; Apache-2.0 | `header-content` = **343** and **12** | **not witnesses.** The first is a GraphQL-mesh *datasource* definition of Twitter's API written by the mesh's authors, not by Twitter — a third-party transcription. The second is crAPI, OWASP's deliberately vulnerable teaching application, which describes no service anybody runs in production |
| `APIs-guru/openapi-directory` `APIs/zoom.us/2.0.0/openapi.yaml`; `APIs/spoonacular.com/1.1/openapi.yaml` | commit `f04b8d0bcd39c52e1cf3ad7a5fe744709832ae49` | CC0-1.0 (the aggregation's `LICENSE`); `spoonacular.com` declares proprietary `spoonacular API Terms` | `operation-overrides-path-item-parameter` = **149** and **55** | **not screened.** They are the two largest override declarers in APIs.guru and are recorded so a repeat of this search does not spend a Fern run rediscovering them, but the row already has a Fern-accepted witness and spoonacular's own terms are outside the corpus's set in any case |
