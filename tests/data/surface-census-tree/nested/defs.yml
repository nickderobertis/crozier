components:
  schemas:
    Reachable:
      type: object
      properties:
        insideOnly:
          type: string
    Unreachable:
      type: object
      properties:
        notReferenced:
          type: string
