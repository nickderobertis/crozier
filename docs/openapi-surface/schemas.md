# OpenAPI surface coverage — Schemas

Classified entries for the `schemas` region.

### How this region was enumerated

The feature list is copied from the two normative surfaces in specification
order, not from crozier's data model, so a future reader can repeat the walk and
get the same list:

1. **The OAS 3.0 Schema Object's own fixed-field list** — the selected, adjusted
   subset of JSON Schema Draft Wright-00 that
   [OAS 3.0.3 § Schema Object](https://spec.openapis.org/oas/v3.0.3#schema-object)
   enumerates, from `title` to `deprecated`, with `additionalProperties` taken in
   all three of its forms.
2. **Each JSON Schema 2020-12 vocabulary OAS 3.1 adopts**, one vocabulary at a
   time so none is skipped: Core, Applicator, Unevaluated, Validation, Meta-data,
   Format-Annotation and Format-Assertion, and Content. `format` is one keyword
   over an open set of values, so it is walked as **two closed lists and no
   third**: the nineteen values the
   [2020-12 format registry](https://json-schema.org/draft/2020-12/json-schema-validation#name-defined-formats)
   defines, from `date-time` to `regex`, and the values the OAS Data Types tables
   define themselves —
   [3.0.3 § Data Types](https://spec.openapis.org/oas/v3.0.3#data-types) adds
   `int32`, `int64`, `float`, `double`, `byte`, `binary` and `password` to the
   registry's `date` and `date-time`, and
   [3.1.0 § Data Types](https://spec.openapis.org/oas/v3.1.0#data-types) keeps
   all of those but `byte` and `binary`, which `contentEncoding` and
   `contentMediaType` replace. Any other value is a custom spelling neither
   specification defines, and the corpus is full of them — the census reports 50
   distinct `schema.format` values across the registered sources, of which these
   two lists name 26.
3. **OAS 3.1's own additions to a schema node** — `discriminator`, `xml`,
   `externalDocs`, and `example` beside the `examples` that supersedes it.
4. **The shapes that are structure rather than a single keyword** — a boolean
   schema (`true` and `false`) in each position one may appear, a `$ref` carrying
   sibling keywords, a recursive and a mutually recursive graph, a cycle closed
   through `additionalProperties`, and each composition keyword nested inside
   another — followed by the fixed fields of the Discriminator and XML Objects,
   which this region also owns.

A keyword appearing in more than one of those lists is one row, not several:
`items`, `properties`, `additionalProperties`, `allOf`/`anyOf`/`oneOf`/`not`,
`title`, `description`, `default`, `deprecated`, `readOnly`, `writeOnly`, `enum`,
`required` and the numeric and string bounds are 3.0 fixed fields *and* 2020-12
vocabulary members, so they carry `both` in the `oas` column. To repeat the walk:
take the four lists above in order, de-duplicate, and query each keyword as
`schema.<field>` — or `schema.format=<value>` for a format — with `just
surface-census --json`.

#### One row or two, where the two specifications differ

A keyword whose 3.0 and 3.1 spellings or semantics differ is **one row with
`oas: both` when both spellings classify the same way, and two rows when they do
not**:

- **`exclusiveMinimum` — one row.** Both spellings are declared by registered
  sources (54 boolean declarations in three 3.0 documents, 22 numeric ones in two
  3.1 documents), so both are `golden` and the evidence cell carries each.
- **`exclusiveMaximum` — two rows.** Every one of its declarations is the 3.0
  boolean form, so the boolean spelling is `golden` and the numeric 3.1 spelling
  is a `gap`. The spellings classify differently, so they cannot share a row.
- **`type` — three rows.** `type-single` (a string, either specification),
  `type-array-with-null` and `type-array-multi-nonnull` (3.1 array forms) are the
  three shapes the enumeration names, and the array forms are what a 3.0 document
  cannot express.
- **`nullable` against a `null` type member — two rows.** They are different
  keywords with different version ranges, not two spellings of one: `nullable` is
  a 3.0 fixed field, `type: [..., "null"]` is the 3.1 replacement, and both are
  `golden` on their own evidence.
- **`format: byte` and `format: binary` — one row each, `oas: 3.0`.** They are
  not two spellings of one keyword but two values 3.1 withdrew: 3.0's Data Types
  table defines both, 3.1's does not, and `contentEncoding`/`contentMediaType`
  carry the meaning there. Each is `golden` on its own evidence, and the 3.1
  replacements are the `content-encoding` and `content-media-type` rows.
- **`example` against `examples` — two rows**, for the same reason: `example` is
  a singular free-form value in both specifications, `examples` is the 3.1 array
  that supersedes it.

#### The instrument, and the one thing it cannot see

`just surface-census` is the classification instrument: it walks each registered
source document's OpenAPI object model and reports, per selector, how many
declaration sites each source carries. Every `golden` row below quotes it.

What the census records is that a *keyword* is declared, never the *kind of
value* it carries or the *shape of the graph* it sits in — `schema.type` counts a
declaration whether the value is a string or an array, `schema.additionalProperties`
counts one whether the value is `true`, `false` or a schema, and no selector
exists for a boolean schema, for a `$ref` with siblings, or for a reference cycle.
Twenty-five of this region's rows are exactly those variants, so they are measured
by a **supplementary variant scan** — a subclass of the census's own walker that
splits the value kinds it merges. Its evidence cells are marked
`variant scan <fact>`; the scan, the command that runs it, and the self-check that
proves its parts add back up to the census's totals are in
[Method notes](#method-notes).

#### What belongs to another region

Cross-linked rather than duplicated, because every feature belongs to exactly one
region file:

- `components.schemas` and the collision behaviour of the names that key it
  (`docs/fern-limitations.md` `normalization-collision`) belong to
  [`document-paths.md`](document-paths.md) with the Components Object, as does a
  Path Item `$ref` (`pathitem-ref`, `relative-file-ref`).
- The `schema` field of a Parameter, Header, Media Type, Request Body or Response
  belongs to that object's region ([`parameters.md`](parameters.md),
  [`bodies-media.md`](bodies-media.md)); the Schema Object it holds is classified
  here.
- XML *payloads* — `application/xml` request and response bodies
  (`docs/fern-limitations.md` `xml-request`, `xml-response`) — belong to
  [`bodies-media.md`](bodies-media.md). The XML Object that annotates a schema is
  classified here.
- `x-` extensions on a schema node belong to
  [`oas31-extensions.md`](oas31-extensions.md).

## Scope

Schema, Discriminator and XML objects, and every JSON Schema keyword wherever it
appears, including the ones 3.1 added.

## Entries

| key | oas | spec location | category | evidence | crozier sites | why bytes could move | settlement |
|---|---|---|---|---|---|---|---|
| title | both | Schema Object.title | golden | census `schema.title`: 9624 declarations in 52 fixtures — `6-dot-authentiqio.appspot.com` (3), `amazonaws.com-cloudformation` (66), `amazonaws.com-cloudfront` (27), `apicurio.local-registry` (19), `apideck.com-accounting` (284), `apideck.com-ats` (60), `apideck.com-connector` (8), `apideck.com-crm` (44), `apideck.com-customer-support` (41), `apideck.com-ecommerce` (123), `apideck.com-file-storage` (8), `apideck.com-hris` (83), `apideck.com-issue-tracking` (49), `apideck.com-lead` (18), `apideck.com-pos` (130), `apideck.com-sms` (29), `apideck.com-vault` (29), `apideck.com-webhook` (6), `auth-schemes` (2), `box.com` (227), `canada-holidays.ca` (3), `client-class-name` (1), `cookie-parameters` (1), `discriminated-unions` (3), `eos.local` (4), `eos.local-extra-fields-forbid` (4), `error-responses` (2), `exhaustive` (24), `form-bodies` (2), `github.com` (2073), `groundhog-day.com` (2), `inline-array-request` (1), `inline-request-response` (5), `integer-enums` (3), `khoainats` (2), `letta` (2827), `livepeer-ai-runner` (157), `microcks.local` (1), `netbox.dev` (2639), `oauth-client-credentials` (3), `openbanking.org.uk-account-info-openapi` (436), `pydantic-extra-fields` (1), `recursive-types` (3), `redhat.com-catalog_inventory` (40), `schema-constraints` (2), `servers-webhooks` (3), `sigstore-rekor` (23), `sse-streaming` (1), `tamoss` (61), `twilio.com-twilio_messaging_v1` (21), `twilio.com-twilio_voice_v1` (19), `writeonly-fields` (1) |  |  |  |
| multiple-of | both | Schema Object.multipleOf | gap | census `schema.multipleOf`: 0 declarations across all 126 registered sources; no `docs/fern-limitations.md` row names it | `none` — `src/openapi.rs`'s `Schema` declares no field for it and `grep -rF '"multipleOf"' src/` finds no production read | The numeric field annotations in the generated `types/` modules would differ if Fern renders a `multipleOf` bound crozier never reads. | FIXTURE — the search found five redistributable real-world declarers and Fern refuses every one: Cloudflare (`BSD-3-Clause`, 8), Pangea and CrowdStrike (`MIT`, 4 each), the APIs.guru conversion of the New York Times books API (`Apache-2.0` upstream, 2) and Box (`Apache-2.0`, 1). A sixth declarer Fern accepts settles it, or the blocked-witness probe route. |
| maximum | both | Schema Object.maximum | golden | census `schema.maximum`: 512 declarations in 37 fixtures — `amazonaws.com-cloudformation` (17), `apideck.com-accounting` (1), `apideck.com-ats` (1), `apideck.com-connector` (1), `apideck.com-crm` (1), `apideck.com-customer-support` (1), `apideck.com-ecommerce` (1), `apideck.com-ecosystem` (1), `apideck.com-file-storage` (2), `apideck.com-hris` (15), `apideck.com-issue-tracking` (1), `apideck.com-lead` (1), `apideck.com-pos` (2), `apideck.com-sms` (1), `apideck.com-vault` (1), `apideck.com-webhook` (1), `atlassian.com-jira` (1), `bbci.co.uk` (1), `box.com` (49), `canada-holidays.ca` (7), `dnd5eapi.co` (5), `eozilla` (1), `free5gc-namf-communication` (29), `free5gc-pdu-session` (44), `github.com` (4), `groundhog-day.com` (3), `kytos-sdntrace-cp` (2), `letta` (15), `netbox.dev` (115), `portfoliooptimizer.io` (142), `redhat.com-catalog_inventory` (1), `redocly.com-museum` (1), `sac-backend` (2), `schema-constraints` (2), `squareup.com` (25), `twilio.com-twilio_messaging_v1` (8), `twilio.com-twilio_voice_v1` (7) |  |  |  |
| exclusive-maximum-boolean | 3.0 | Schema Object.exclusiveMaximum | golden | variant scan `exclusiveMaximum=boolean`: 8 declarations in 2 fixtures — `groundhog-day.com` (2), `portfoliooptimizer.io` (6); both are 3.0 documents (census `openapi.openapi=3.0`), and they are every one of the census's 8 `schema.exclusiveMaximum` declarations |  |  |  |
| exclusive-maximum-numeric | 3.1 | Schema Object.exclusiveMaximum | gap | variant scan `exclusiveMaximum=numeric`: 0 declarations across all 126 registered sources — all 8 `schema.exclusiveMaximum` declarations the census reports carry the 3.0 boolean form; no `docs/fern-limitations.md` row names it | `none` — `src/openapi.rs`'s `Schema` declares no field for it and `grep -rF '"exclusiveMaximum"' src/` finds no production read | The upper-bound field annotations in the generated `types/` modules would differ if Fern reads the 3.1 numeric spelling of the bound. | FIXTURE — **the search found a witness**: the `ITISFoundation/osparc-simcore` document declares 8 `exclusiveMaximum` keys with numeric values inside an `openapi: 3.1.0` document, so registering it pins the 3.1 spelling beside the 3.0 boolean `exclusive-maximum-boolean` already pins. |
| minimum | both | Schema Object.minimum | golden | census `schema.minimum`: 774 declarations in 44 fixtures — `amazonaws.com-cloudformation` (26), `apache.org` (3), `apache.org-airflow` (3), `apideck.com-accounting` (1), `apideck.com-ats` (1), `apideck.com-connector` (1), `apideck.com-crm` (2), `apideck.com-customer-support` (1), `apideck.com-ecommerce` (1), `apideck.com-ecosystem` (1), `apideck.com-file-storage` (2), `apideck.com-hris` (15), `apideck.com-issue-tracking` (1), `apideck.com-lead` (1), `apideck.com-pos` (2), `apideck.com-sms` (1), `apideck.com-vault` (1), `apideck.com-webhook` (1), `apis.guru` (3), `bbci.co.uk` (1), `box.com` (14), `canada-holidays.ca` (7), `dnd5eapi.co` (5), `electric-sql` (4), `eozilla` (8), `exa-gate` (3), `frankfurter` (1), `free5gc-namf-communication` (40), `free5gc-pdu-session` (58), `github.com` (4), `groundhog-day.com` (7), `kytos-sdntrace-cp` (2), `letta` (16), `netbox.dev` (122), `openepcis-dpp-ready` (1), `portfoliooptimizer.io` (326), `redhat.com-catalog_inventory` (2), `sac-backend` (3), `schema-constraints` (2), `sigstore-rekor` (9), `squareup.com` (32), `tamoss` (10), `twilio.com-twilio_messaging_v1` (16), `twilio.com-twilio_voice_v1` (14) |  |  |  |
| exclusive-minimum | both | Schema Object.exclusiveMinimum | golden | variant scan `exclusiveMinimum=boolean`: 54 declarations in 3 fixtures — `eozilla` (1), `groundhog-day.com` (3), `portfoliooptimizer.io` (50) — the 3.0 boolean spelling, all three 3.0 documents; and variant scan `exclusiveMinimum=numeric`: 22 declarations in 2 fixtures — `frankfurter` (2), `tamoss` (20) — the 3.1 numeric spelling, both 3.1 documents; together they are the census's 76 `schema.exclusiveMinimum` declarations |  |  |  |
| max-length | both | Schema Object.maxLength | golden | census `schema.maxLength`: 1850 declarations in 21 fixtures — `amazonaws.com-cloudformation` (174), `amazonaws.com-cloudfront` (2), `apideck.com-ecommerce` (1), `apideck.com-hris` (1), `apideck.com-pos` (15), `apideck.com-sms` (1), `apis.guru` (3), `atlassian.com-jira` (62), `box.com` (51), `calorieninjas.com` (1), `free5gc-namf-communication` (4), `github.com` (23), `letta` (152), `netbox.dev` (554), `openbanking.org.uk-account-info-openapi` (277), `sac-backend` (22), `schema-constraints` (2), `squareup.com` (363), `twilio.com-twilio_messaging_v1` (93), `twilio.com-twilio_voice_v1` (43), `xero.com-xero-payroll-au` (6) |  |  |  |
| min-length | both | Schema Object.minLength | golden | census `schema.minLength`: 1335 declarations in 33 fixtures — `amazonaws.com-cloudformation` (184), `amazonaws.com-cloudfront` (2), `apache.org` (3), `apache.org-airflow` (3), `apideck.com-accounting` (8), `apideck.com-ats` (4), `apideck.com-crm` (10), `apideck.com-customer-support` (8), `apideck.com-ecommerce` (3), `apideck.com-hris` (9), `apideck.com-issue-tracking` (3), `apideck.com-lead` (5), `apideck.com-pos` (5), `apideck.com-sms` (1), `apis.guru` (5), `atlassian.com-jira` (5), `bbci.co.uk` (1), `box.com` (10), `buildrelay` (3), `calorieninjas.com` (1), `conjur.local` (14), `eozilla` (1), `free5gc-namf-communication` (4), `github.com` (4), `letta` (153), `netbox.dev` (284), `openbanking.org.uk-account-info-openapi` (276), `openepcis-dpp-ready` (1), `sac-backend` (16), `schema-constraints` (4), `squareup.com` (169), `twilio.com-twilio_messaging_v1` (93), `twilio.com-twilio_voice_v1` (43) |  |  |  |
| pattern | both | Schema Object.pattern | golden | census `schema.pattern`: 943 declarations in 34 fixtures — `amazonaws.com-cloudformation` (128), `amazonaws.com-cloudfront` (6), `anchore.io` (10), `apache.org` (1), `apache.org-airflow` (1), `apideck.com-accounting` (4), `apideck.com-ats` (1), `apideck.com-ecommerce` (2), `apideck.com-hris` (28), `apideck.com-vault` (2), `apideck.com-webhook` (3), `bbci.co.uk` (2), `box.com` (3), `canada-holidays.ca` (1), `conjur.local` (1), `dnd5eapi.co` (1), `eos.local` (4), `eos.local-extra-fields-forbid` (4), `free5gc-namf-communication` (68), `free5gc-pdu-session` (66), `github.com` (15), `letta` (176), `netbox.dev` (83), `openbanking.org.uk-account-info-openapi` (141), `redhat.com-catalog_inventory` (3), `redocly.com-museum` (2), `schema-constraints` (1), `sigstore-rekor` (30), `squareup.com` (1), `tamoss` (12), `tlon-notes` (3), `twilio.com-twilio_messaging_v1` (93), `twilio.com-twilio_voice_v1` (43), `worldcoin-signup-sequencer` (4) |  |  |  |
| max-items | both | Schema Object.maxItems | golden | census `schema.maxItems`: 73 declarations in 15 fixtures — `amazonaws.com-cloudformation` (22), `atlassian.com-jira` (6), `box.com` (6), `eozilla` (1), `etsi.local-mec010-2_apppkgmgmt` (1), `exa-gate` (1), `free5gc-namf-communication` (1), `github.com` (11), `letta` (7), `openbanking.org.uk-account-info-openapi` (2), `openfigi.com` (2), `portfoliooptimizer.io` (3), `schema-constraints` (1), `sigstore-rekor` (3), `worldcoin-signup-sequencer` (6) |  |  |  |
| min-items | both | Schema Object.minItems | golden | census `schema.minItems`: 668 declarations in 25 fixtures — `amazonaws.com-cloudformation` (6), `amazonaws.com-cloudfront` (1), `apache.org` (1), `apache.org-airflow` (1), `apideck.com-accounting` (1), `apis.guru` (2), `atlassian.com-jira` (6), `bbci.co.uk` (1), `box.com` (7), `discourse.local` (3), `eozilla` (3), `etsi.local-mec010-2_apppkgmgmt` (29), `exa-gate` (1), `free5gc-namf-communication` (51), `free5gc-pdu-session` (40), `github.com` (17), `letta` (8), `openbanking.org.uk-account-info-openapi` (53), `openepcis-dpp-ready` (1), `openfigi.com` (2), `portfoliooptimizer.io` (420), `schema-constraints` (1), `sigstore-rekor` (6), `tamoss` (1), `worldcoin-signup-sequencer` (6) |  |  |  |
| unique-items | both | Schema Object.uniqueItems | golden | census `schema.uniqueItems`: 172 declarations in 7 fixtures — `apideck.com-issue-tracking` (1), `atlassian.com-jira` (80), `bbci.co.uk` (1), `discourse.local` (3), `eozilla` (2), `netbox.dev` (62), `portfoliooptimizer.io` (23) |  |  |  |
| max-properties | both | Schema Object.maxProperties | golden | census `schema.maxProperties`: 10 declarations in 3 fixtures — `amazonaws.com-cloudformation` (1), `atlassian.com-jira` (5), `github.com` (4) |  |  |  |
| min-properties | both | Schema Object.minProperties | golden | census `schema.minProperties`: 12 declarations in 5 fixtures — `amazonaws.com-cloudformation` (1), `apis.guru` (4), `atlassian.com-jira` (5), `free5gc-pdu-session` (1), `openbanking.org.uk-account-info-openapi` (1) |  |  |  |
| required | both | Schema Object.required | golden | census `schema.required`: 6787 declarations in 95 fixtures — `6-dot-authentiqio.appspot.com` (4), `airbyte.local-config` (136), `amazonaws.com-cloudformation` (66), `amazonaws.com-cloudfront` (72), `anchore.io` (31), `apache.org` (5), `apache.org-airflow` (5), `apache.org-qakka` (2), `apicurio.local-registry` (22), `apideck.com-accounting` (82), `apideck.com-ats` (14), `apideck.com-connector` (9), `apideck.com-crm` (53), `apideck.com-customer-support` (15), `apideck.com-ecommerce` (29), `apideck.com-ecosystem` (22), `apideck.com-file-storage` (42), `apideck.com-hris` (38), `apideck.com-issue-tracking` (24), `apideck.com-lead` (12), `apideck.com-pos` (56), `apideck.com-sms` (9), `apideck.com-vault` (19), `apideck.com-webhook` (11), `apis.guru` (3), `appng-rest-api` (10), `appwrite.io-client` (53), `appwrite.io-server` (64), `asana.com` (34), `atlassian.com-jira` (231), `auth-schemes` (2), `bbci.co.uk` (69), `bintable.com` (1), `box.com` (174), `buildrelay` (3), `bunq.com` (61), `byautomata.io` (6), `canada-holidays.ca` (2), `client-class-name` (1), `codesearch.debian.net` (2), `conjur.local` (6), `cookie-parameters` (1), `discourse.local` (178), `discriminated-unions` (2), `dnd5eapi.co` (1), `electric-sql` (3), `eos.local` (3), `eos.local-extra-fields-forbid` (3), `eozilla` (14), `error-responses` (2), `etsi.local-mec010-2_apppkgmgmt` (28), `exa-gate` (5), `exhaustive` (15), `form-bodies` (4), `frankfurter` (5), `free5gc-namf-communication` (80), `free5gc-pdu-session` (51), `github.com` (2899), `groundhog-day.com` (10), `http-toolkit` (3), `inline-array-request` (2), `inline-request-response` (7), `integer-enums` (1), `khoainats` (1), `kytos-sdntrace-cp` (4), `letta` (438), `livepeer-ai-runner` (31), `maif.local-otoroshi` (81), `microcks.local` (32), `netbox.dev` (393), `oauth-client-credentials` (3), `openbanking.org.uk-account-info-openapi` (235), `openepcis-dpp-ready` (9), `openfigi.com` (1), `portfoliooptimizer.io` (288), `prometheus-x-edge-computing` (5), `pydantic-extra-fields` (1), `recursive-types` (2), `redhat.com-catalog_inventory` (1), `redocly.com-museum` (4), `reverb.com` (21), `sac-backend` (10), `schema-constraints` (2), `servers-webhooks` (3), `sigstore-rekor` (73), `squareup.com` (164), `tamoss` (61), `tlon-notes` (71), `traccar.org` (1), `twilio.com-twilio_messaging_v1` (10), `twilio.com-twilio_voice_v1` (5), `withsecure-gdpr-subject-rights` (6), `worldcoin-signup-sequencer` (5), `writeonly-fields` (1), `xero.com-xero-payroll-au` (8) |  |  |  |
| enum | both | Schema Object.enum | golden | census `schema.enum`: 5601 declarations in 76 fixtures — `airbyte.local-config` (35), `amazonaws.com-cloudformation` (362), `amazonaws.com-cloudfront` (16), `anchore.io` (30), `apache.org` (9), `apache.org-airflow` (9), `apicurio.local-registry` (11), `apideck.com-accounting` (33), `apideck.com-ats` (9), `apideck.com-connector` (17), `apideck.com-crm` (17), `apideck.com-customer-support` (9), `apideck.com-ecommerce` (13), `apideck.com-ecosystem` (8), `apideck.com-file-storage` (5), `apideck.com-hris` (21), `apideck.com-issue-tracking` (13), `apideck.com-lead` (9), `apideck.com-pos` (35), `apideck.com-sms` (5), `apideck.com-vault` (15), `apideck.com-webhook` (4), `appng-rest-api` (7), `asana.com` (43), `atlassian.com-jira` (121), `bbci.co.uk` (56), `box.com` (335), `buildrelay` (2), `bungie.net` (155), `canada-holidays.ca` (10), `codesearch.debian.net` (3), `color.pizza` (1), `conjur.local` (8), `corrently.io` (3), `discourse.local` (14), `discriminated-unions` (2), `dnd5eapi.co` (32), `electric-sql` (8), `enum-name-sanitization` (3), `enum-query-param` (1), `enum-receiver-collision` (2), `eozilla` (8), `etsi.local-mec010-2_apppkgmgmt` (13), `exa-gate` (2), `exhaustive` (5), `frankfurter` (4), `free5gc-namf-communication` (29), `free5gc-pdu-session` (28), `github.com` (3019), `groundhog-day.com` (2), `helios-verifiable-api` (2), `integer-enums` (2), `letta` (209), `livepeer-ai-runner` (1), `maif.local-otoroshi` (14), `med-anvisa-price` (1), `microcks.local` (8), `netbox.dev` (246), `openbanking.org.uk-account-info-openapi` (159), `openepcis-dpp-ready` (4), `openfigi.com` (4), `portfoliooptimizer.io` (24), `recursive-types` (2), `redhat.com-catalog_inventory` (2), `redocly.com-museum` (1), `reverb.com` (21), `sac-backend` (5), `sigstore-rekor` (17), `squareup.com` (140), `tamoss` (19), `tlon-notes` (64), `twilio.com-twilio_messaging_v1` (13), `twilio.com-twilio_voice_v1` (9), `withsecure-gdpr-subject-rights` (6), `worldcoin-signup-sequencer` (1), `xero.com-xero-payroll-au` (26); the member kinds the scan finds are string, integer, boolean and null (`enum-member-float` and `enum-member-object` are rows of their own below) |  |  |  |
| type-single | both | Schema Object.type | golden | census `schema.type`: 108256 declarations in 124 fixtures — `6-dot-authentiqio.appspot.com` (60), `airbyte.local-config` (557), `amazonaws.com-cloudformation` (1049), `amazonaws.com-cloudfront` (271), `anchore.io` (821), `apache.org` (512), `apache.org-airflow` (512), `apache.org-qakka` (35), `apicurio.local-registry` (212), `apideck.com-accounting` (907), `apideck.com-ats` (292), `apideck.com-connector` (227), `apideck.com-crm` (692), `apideck.com-customer-support` (335), `apideck.com-ecommerce` (381), `apideck.com-ecosystem` (314), `apideck.com-file-storage` (364), `apideck.com-hris` (526), `apideck.com-issue-tracking` (304), `apideck.com-lead` (220), `apideck.com-pos` (699), `apideck.com-proxy` (22), `apideck.com-sms` (155), `apideck.com-vault` (355), `apideck.com-webhook` (164), `apis.guru` (40), `appng-rest-api` (195), `appwrite.io-client` (382), `appwrite.io-server` (470), `asana.com` (1046), `atlassian.com-jira` (3774), `audience-filter` (7), `audience-filter-strict` (9), `auth-schemes` (9), `axesso.de` (52), `basic-auth` (1), `bbci.co.uk` (412), `bintable.com` (9), `box.com` (2631), `bracketed-property-names` (7), `buildrelay` (31), `bungie.net` (5304), `bunq.com` (4807), `byautomata.io` (87), `calorieninjas.com` (1), `canada-holidays.ca` (52), `client-class-name` (5), `codesearch.debian.net` (20), `color.pizza` (70), `conjur.local` (160), `cookie-parameters` (6), `corrently.io` (164), `digit-leading-property` (2), `discourse.local` (2891), `discriminated-unions` (6), `dnd5eapi.co` (473), `electric-sql` (105), `enum-name-sanitization` (4), `enum-query-param` (3), `enum-receiver-collision` (3), `eos.local` (38), `eos.local-extra-fields-forbid` (38), `eozilla` (166), `error-responses` (9), `esgenterprise.com` (8), `etherpad.local` (1860), `etsi.local-mec010-2_apppkgmgmt` (178), `exa-gate` (41), `exhaustive` (149), `form-bodies` (12), `frankfurter` (64), `free5gc-namf-communication` (408), `free5gc-pdu-session` (434), `gambitcomm.local-mimic` (1168), `github.com` (43141), `gov.bc.ca-news` (263), `groundhog-day.com` (58), `helios-verifiable-api` (32), `http-toolkit` (41), `inline-array-request` (9), `inline-request-response` (17), `integer-enums` (4), `khoainats` (4), `kytos-sdntrace-cp` (41), `letta` (5442), `livepeer-ai-runner` (160), `maif.local-otoroshi` (712), `malformed-property-schema` (3), `med-anvisa-price` (47), `microcks.local` (268), `missing-operation-id` (2), `nested-core-imports` (4), `netbox.dev` (10392), `nimisampo` (105), `oauth-client-credentials` (10), `openbanking.org.uk-account-info-openapi` (1167), `openepcis-dpp-ready` (62), `openfigi.com` (46), `operation-id-non-identifier` (4), `portfoliooptimizer.io` (1393), `prometheus-x-edge-computing` (31), `pydantic-extra-fields` (5), `query-parameters-openapi` (26), `recursive-types` (9), `redhat.com-catalog_inventory` (215), `redocly.com-museum` (33), `reverb.com` (547), `sac-backend` (106), `schema-constraints` (14), `servers-webhooks` (8), `sigstore-rekor` (231), `slurmdb-rest` (477), `squareup.com` (3065), `sse-streaming` (3), `tag-based-grouping` (8), `tamoss` (378), `tlon-notes` (306), `traccar.org` (340), `twilio.com-twilio_messaging_v1` (484), `twilio.com-twilio_voice_v1` (261), `withsecure-gdpr-subject-rights` (43), `worldcoin-signup-sequencer` (23), `writeonly-fields` (6), `xero.com-xero-payroll-au` (413); the variant scan splits those into three disjoint parts — 106386 single-string declarations in 124 fixtures, a further 1370 in the 3.1 null-only form `type: "null"` (`letta`), and 500 array-valued ones the two `type-array-*` rows own |  |  |  |
| allof | both | Schema Object.allOf | golden | census `schema.allOf`: 2105 declarations in 33 fixtures — `amazonaws.com-cloudformation` (859), `amazonaws.com-cloudfront` (317), `anchore.io` (15), `apache.org` (22), `apache.org-airflow` (22), `apicurio.local-registry` (2), `apideck.com-accounting` (1), `apideck.com-ats` (1), `apideck.com-crm` (1), `apideck.com-file-storage` (1), `apideck.com-hris` (1), `apideck.com-issue-tracking` (1), `apideck.com-lead` (1), `apideck.com-pos` (1), `appng-rest-api` (1), `asana.com` (127), `atlassian.com-jira` (136), `box.com` (261), `bungie.net` (194), `dnd5eapi.co` (36), `eozilla` (6), `exhaustive` (2), `free5gc-namf-communication` (2), `free5gc-pdu-session` (2), `github.com` (36), `gov.bc.ca-news` (17), `livepeer-ai-runner` (4), `microcks.local` (2), `openepcis-dpp-ready` (11), `redocly.com-museum` (2), `sac-backend` (2), `tamoss` (15), `tlon-notes` (4) |  |  |  |
| oneof | both | Schema Object.oneOf | golden | census `schema.oneOf`: 441 declarations in 26 fixtures — `apideck.com-webhook` (3), `atlassian.com-jira` (3), `bbci.co.uk` (3), `box.com` (15), `discriminated-unions` (1), `dnd5eapi.co` (5), `electric-sql` (1), `eozilla` (7), `exhaustive` (2), `free5gc-namf-communication` (3), `free5gc-pdu-session` (1), `github.com` (230), `helios-verifiable-api` (2), `letta` (87), `maif.local-otoroshi` (22), `microcks.local` (2), `nimisampo` (1), `openepcis-dpp-ready` (2), `openfigi.com` (2), `portfoliooptimizer.io` (15), `query-parameters-openapi` (2), `recursive-types` (1), `sigstore-rekor` (17), `tamoss` (3), `tlon-notes` (10), `worldcoin-signup-sequencer` (1) |  |  |  |
| anyof | both | Schema Object.anyOf | golden | census `schema.anyOf`: 1530 declarations in 28 fixtures — `apache.org` (1), `apache.org-airflow` (1), `apideck.com-accounting` (6), `apideck.com-ats` (5), `apideck.com-connector` (3), `apideck.com-crm` (5), `apideck.com-customer-support` (5), `apideck.com-ecommerce` (5), `apideck.com-file-storage` (4), `apideck.com-hris` (5), `apideck.com-issue-tracking` (5), `apideck.com-lead` (5), `apideck.com-pos` (5), `apideck.com-proxy` (3), `apideck.com-sms` (5), `apideck.com-vault` (9), `apideck.com-webhook` (4), `atlassian.com-jira` (3), `bbci.co.uk` (6), `box.com` (1), `dnd5eapi.co` (3), `free5gc-namf-communication` (1), `github.com` (20), `letta` (1416), `livepeer-ai-runner` (1), `openepcis-dpp-ready` (1), `tamoss` (1), `withsecure-gdpr-subject-rights` (1) |  |  |  |
| not | both | Schema Object.not | golden | census `schema.not`: 5 declarations in 2 fixtures — `free5gc-namf-communication` (3), `tamoss` (2) |  |  |  |
| items | both | Schema Object.items | golden | census `schema.items`: 6544 declarations in 103 fixtures — `airbyte.local-config` (69), `amazonaws.com-cloudformation` (98), `amazonaws.com-cloudfront` (22), `anchore.io` (92), `apache.org` (53), `apache.org-airflow` (53), `apache.org-qakka` (3), `apicurio.local-registry` (24), `apideck.com-accounting` (53), `apideck.com-ats` (18), `apideck.com-connector` (32), `apideck.com-crm` (37), `apideck.com-customer-support` (27), `apideck.com-ecommerce` (24), `apideck.com-ecosystem` (11), `apideck.com-file-storage` (7), `apideck.com-hris` (29), `apideck.com-issue-tracking` (9), `apideck.com-lead` (9), `apideck.com-pos` (35), `apideck.com-sms` (3), `apideck.com-vault` (25), `apideck.com-webhook` (8), `apis.guru` (3), `appng-rest-api` (27), `appwrite.io-client` (36), `appwrite.io-server` (49), `asana.com` (109), `atlassian.com-jira` (451), `audience-filter` (1), `audience-filter-strict` (1), `axesso.de` (5), `bbci.co.uk` (20), `bintable.com` (2), `box.com` (216), `buildrelay` (5), `bungie.net` (446), `bunq.com` (329), `byautomata.io` (11), `canada-holidays.ca` (4), `client-class-name` (1), `codesearch.debian.net` (5), `color.pizza` (3), `conjur.local` (17), `corrently.io` (14), `discourse.local` (228), `dnd5eapi.co` (78), `electric-sql` (7), `eos.local` (1), `eos.local-extra-fields-forbid` (1), `eozilla` (23), `error-responses` (1), `etherpad.local` (18), `etsi.local-mec010-2_apppkgmgmt` (33), `exa-gate` (2), `exhaustive` (16), `frankfurter` (6), `free5gc-namf-communication` (52), `free5gc-pdu-session` (40), `gambitcomm.local-mimic` (118), `github.com` (1005), `gov.bc.ca-news` (44), `groundhog-day.com` (3), `helios-verifiable-api` (8), `inline-array-request` (2), `inline-request-response` (1), `kytos-sdntrace-cp` (1), `letta` (393), `livepeer-ai-runner` (9), `maif.local-otoroshi` (71), `malformed-property-schema` (1), `med-anvisa-price` (1), `microcks.local` (26), `missing-operation-id` (1), `netbox.dev` (333), `nimisampo` (14), `oauth-client-credentials` (1), `openbanking.org.uk-account-info-openapi` (205), `openepcis-dpp-ready` (9), `openfigi.com` (6), `operation-id-non-identifier` (1), `portfoliooptimizer.io` (520), `prometheus-x-edge-computing` (2), `pydantic-extra-fields` (1), `query-parameters-openapi` (6), `recursive-types` (2), `redhat.com-catalog_inventory` (18), `redocly.com-museum` (3), `reverb.com` (28), `sac-backend` (3), `schema-constraints` (1), `sigstore-rekor` (11), `slurmdb-rest` (75), `squareup.com` (423), `tag-based-grouping` (2), `tamoss` (30), `tlon-notes` (10), `traccar.org` (41), `twilio.com-twilio_messaging_v1` (28), `twilio.com-twilio_voice_v1` (9), `withsecure-gdpr-subject-rights` (4), `worldcoin-signup-sequencer` (5), `xero.com-xero-payroll-au` (67) |  |  |  |
| properties | both | Schema Object.properties | golden | census `schema.properties`: 12547 declarations in 121 fixtures — `6-dot-authentiqio.appspot.com` (17), `airbyte.local-config` (164), `amazonaws.com-cloudformation` (207), `amazonaws.com-cloudfront` (182), `anchore.io` (130), `apache.org` (81), `apache.org-airflow` (81), `apache.org-qakka` (3), `apicurio.local-registry` (30), `apideck.com-accounting` (134), `apideck.com-ats` (40), `apideck.com-connector` (41), `apideck.com-crm` (82), `apideck.com-customer-support` (31), `apideck.com-ecommerce` (56), `apideck.com-ecosystem` (32), `apideck.com-file-storage` (66), `apideck.com-hris` (79), `apideck.com-issue-tracking` (47), `apideck.com-lead` (30), `apideck.com-pos` (106), `apideck.com-proxy` (4), `apideck.com-sms` (23), `apideck.com-vault` (56), `apideck.com-webhook` (27), `apis.guru` (6), `appng-rest-api` (27), `appwrite.io-client` (54), `appwrite.io-server` (65), `asana.com` (379), `atlassian.com-jira` (535), `audience-filter` (3), `audience-filter-strict` (4), `auth-schemes` (2), `axesso.de` (5), `bbci.co.uk` (85), `bintable.com` (1), `box.com` (513), `bracketed-property-names` (2), `buildrelay` (6), `bungie.net` (746), `bunq.com` (552), `byautomata.io` (19), `canada-holidays.ca` (15), `client-class-name` (1), `codesearch.debian.net` (2), `color.pizza` (15), `conjur.local` (21), `cookie-parameters` (1), `corrently.io` (28), `digit-leading-property` (1), `discourse.local` (274), `discriminated-unions` (2), `dnd5eapi.co` (104), `electric-sql` (13), `enum-name-sanitization` (1), `enum-query-param` (1), `enum-receiver-collision` (1), `eos.local` (8), `eos.local-extra-fields-forbid` (8), `eozilla` (23), `error-responses` (2), `esgenterprise.com` (1), `etherpad.local` (456), `etsi.local-mec010-2_apppkgmgmt` (30), `exa-gate` (7), `exhaustive` (19), `form-bodies` (4), `frankfurter` (8), `free5gc-namf-communication` (92), `free5gc-pdu-session` (86), `gambitcomm.local-mimic` (24), `github.com` (3414), `gov.bc.ca-news` (23), `groundhog-day.com` (16), `helios-verifiable-api` (15), `http-toolkit` (3), `inline-array-request` (2), `inline-request-response` (7), `integer-enums` (1), `khoainats` (2), `kytos-sdntrace-cp` (12), `letta` (534), `livepeer-ai-runner` (33), `maif.local-otoroshi` (88), `malformed-property-schema` (1), `med-anvisa-price` (2), `microcks.local` (39), `nested-core-imports` (2), `netbox.dev` (406), `nimisampo` (10), `oauth-client-credentials` (3), `openbanking.org.uk-account-info-openapi` (271), `openepcis-dpp-ready` (16), `openfigi.com` (5), `operation-id-non-identifier` (1), `portfoliooptimizer.io` (312), `prometheus-x-edge-computing` (12), `pydantic-extra-fields` (1), `query-parameters-openapi` (2), `recursive-types` (3), `redhat.com-catalog_inventory` (35), `redocly.com-museum` (7), `reverb.com` (42), `sac-backend` (20), `schema-constraints` (2), `servers-webhooks` (3), `sigstore-rekor` (76), `slurmdb-rest` (122), `squareup.com` (660), `sse-streaming` (1), `tag-based-grouping` (2), `tamoss` (71), `tlon-notes` (73), `traccar.org` (35), `twilio.com-twilio_messaging_v1` (46), `twilio.com-twilio_voice_v1` (38), `withsecure-gdpr-subject-rights` (15), `worldcoin-signup-sequencer` (7), `writeonly-fields` (1), `xero.com-xero-payroll-au` (54) |  |  |  |
| additional-properties-boolean-true | both | Schema Object.additionalProperties | golden | variant scan `additionalProperties=true`: 219 declarations in 32 fixtures — `airbyte.local-config` (5), `anchore.io` (10), `apideck.com-accounting` (3), `apideck.com-ats` (1), `apideck.com-connector` (1), `apideck.com-crm` (1), `apideck.com-customer-support` (1), `apideck.com-ecommerce` (1), `apideck.com-file-storage` (2), `apideck.com-hris` (1), `apideck.com-issue-tracking` (2), `apideck.com-lead` (1), `apideck.com-pos` (1), `apideck.com-sms` (1), `apideck.com-vault` (5), `apideck.com-webhook` (4), `appwrite.io-client` (2), `appwrite.io-server` (2), `atlassian.com-jira` (47), `discourse.local` (3), `dnd5eapi.co` (3), `eozilla` (1), `etsi.local-mec010-2_apppkgmgmt` (1), `exa-gate` (1), `github.com` (16), `letta` (93), `microcks.local` (3), `openbanking.org.uk-account-info-openapi` (1), `openepcis-dpp-ready` (1), `servers-webhooks` (1), `sigstore-rekor` (2), `tamoss` (2); `docs/fern-limitations.md` `boolean-schema-true`: coincidence (`additionalProperties`, `items`) / discards (property position) |  |  |  |
| additional-properties-boolean-false | both | Schema Object.additionalProperties | golden | variant scan `additionalProperties=false`: 1177 declarations in 27 fixtures — `airbyte.local-config` (6), `apideck.com-accounting` (32), `apideck.com-ats` (13), `apideck.com-connector` (4), `apideck.com-crm` (25), `apideck.com-customer-support` (12), `apideck.com-ecommerce` (15), `apideck.com-ecosystem` (17), `apideck.com-file-storage` (19), `apideck.com-hris` (20), `apideck.com-issue-tracking` (16), `apideck.com-lead` (10), `apideck.com-pos` (17), `apideck.com-sms` (3), `apideck.com-vault` (12), `apideck.com-webhook` (5), `apis.guru` (3), `atlassian.com-jira` (441), `bbci.co.uk` (79), `discourse.local` (185), `eozilla` (1), `exa-gate` (7), `github.com` (38), `letta` (78), `openbanking.org.uk-account-info-openapi` (99), `redhat.com-catalog_inventory` (15), `tamoss` (5) |  |  |  |
| additional-properties-schema | both | Schema Object.additionalProperties | golden | variant scan `additionalProperties=schema`: 534 declarations in 24 fixtures — `amazonaws.com-cloudformation` (1), `apicurio.local-registry` (1), `apis.guru` (2), `appng-rest-api` (7), `asana.com` (2), `atlassian.com-jira` (44), `box.com` (16), `bungie.net` (291), `dnd5eapi.co` (5), `electric-sql` (1), `eozilla` (7), `exhaustive` (10), `gambitcomm.local-mimic` (19), `github.com` (13), `helios-verifiable-api` (2), `http-toolkit` (2), `letta` (44), `livepeer-ai-runner` (2), `maif.local-otoroshi` (39), `microcks.local` (9), `query-parameters-openapi` (1), `sigstore-rekor` (3), `squareup.com` (11), `tamoss` (2); the three forms together are the census's 1930 `schema.additionalProperties` declarations |  |  |  |
| description | both | Schema Object.description | golden | census `schema.description`: 33363 declarations in 82 fixtures — `6-dot-authentiqio.appspot.com` (30), `airbyte.local-config` (117), `amazonaws.com-cloudformation` (982), `amazonaws.com-cloudfront` (444), `anchore.io` (343), `apache.org` (261), `apache.org-airflow` (261), `apache.org-qakka` (17), `apicurio.local-registry` (120), `apideck.com-accounting` (609), `apideck.com-ats` (160), `apideck.com-connector` (163), `apideck.com-crm` (370), `apideck.com-customer-support` (163), `apideck.com-ecommerce` (271), `apideck.com-ecosystem` (28), `apideck.com-file-storage` (274), `apideck.com-hris` (340), `apideck.com-issue-tracking` (197), `apideck.com-lead` (117), `apideck.com-pos` (394), `apideck.com-proxy` (7), `apideck.com-sms` (109), `apideck.com-vault` (194), `apideck.com-webhook` (111), `apis.guru` (30), `appng-rest-api` (136), `appwrite.io-client` (250), `appwrite.io-server` (285), `asana.com` (537), `atlassian.com-jira` (2470), `box.com` (2085), `buildrelay` (18), `bungie.net` (2165), `bunq.com` (2976), `byautomata.io` (6), `canada-holidays.ca` (22), `codesearch.debian.net` (8), `conjur.local` (35), `corrently.io` (98), `discourse.local` (56), `dnd5eapi.co` (258), `electric-sql` (51), `eos.local` (30), `eos.local-extra-fields-forbid` (30), `eozilla` (2), `etsi.local-mec010-2_apppkgmgmt` (119), `exhaustive` (9), `frankfurter` (34), `gambitcomm.local-mimic` (4), `github.com` (6023), `groundhog-day.com` (3), `helios-verifiable-api` (4), `http-toolkit` (3), `kytos-sdntrace-cp` (28), `letta` (2283), `livepeer-ai-runner` (102), `maif.local-otoroshi` (532), `med-anvisa-price` (1), `microcks.local` (210), `netbox.dev` (289), `nimisampo` (14), `openbanking.org.uk-account-info-openapi` (938), `openepcis-dpp-ready` (50), `openfigi.com` (4), `portfoliooptimizer.io` (555), `prometheus-x-edge-computing` (31), `redhat.com-catalog_inventory` (17), `redocly.com-museum` (23), `reverb.com` (152), `sac-backend` (99), `sigstore-rekor` (158), `slurmdb-rest` (381), `squareup.com` (2695), `tamoss` (253), `tlon-notes` (28), `traccar.org` (34), `twilio.com-twilio_messaging_v1` (271), `twilio.com-twilio_voice_v1` (114), `withsecure-gdpr-subject-rights` (20), `worldcoin-signup-sequencer` (10), `xero.com-xero-payroll-au` (242) |  |  |  |
| format | both | Schema Object.format | golden | census `schema.format`: 20862 declarations in 81 fixtures — `6-dot-authentiqio.appspot.com` (1), `airbyte.local-config` (60), `amazonaws.com-cloudformation` (4), `amazonaws.com-cloudfront` (1), `anchore.io` (41), `apache.org` (68), `apache.org-airflow` (68), `apache.org-qakka` (5), `apicurio.local-registry` (37), `apideck.com-accounting` (24), `apideck.com-ats` (10), `apideck.com-connector` (6), `apideck.com-crm` (22), `apideck.com-customer-support` (19), `apideck.com-ecommerce` (5), `apideck.com-ecosystem` (11), `apideck.com-file-storage` (6), `apideck.com-hris` (27), `apideck.com-issue-tracking` (6), `apideck.com-lead` (1), `apideck.com-pos` (17), `apideck.com-sms` (5), `apideck.com-vault` (5), `apideck.com-webhook` (6), `apis.guru` (6), `appng-rest-api` (6), `appwrite.io-client` (72), `appwrite.io-server` (82), `asana.com` (80), `atlassian.com-jira` (906), `axesso.de` (6), `bbci.co.uk` (3), `box.com` (410), `buildrelay` (2), `bungie.net` (2050), `canada-holidays.ca` (4), `codesearch.debian.net` (1), `conjur.local` (2), `cookie-parameters` (1), `corrently.io` (2), `discourse.local` (1), `discriminated-unions` (2), `electric-sql` (1), `eozilla` (15), `esgenterprise.com` (5), `etsi.local-mec010-2_apppkgmgmt` (13), `exhaustive` (22), `form-bodies` (1), `frankfurter` (12), `free5gc-namf-communication` (84), `free5gc-pdu-session` (134), `gambitcomm.local-mimic` (484), `github.com` (14325), `gov.bc.ca-news` (16), `inline-request-response` (2), `integer-enums` (1), `letta` (106), `livepeer-ai-runner` (6), `maif.local-otoroshi` (96), `microcks.local` (12), `netbox.dev` (834), `nimisampo` (2), `openbanking.org.uk-account-info-openapi` (53), `openepcis-dpp-ready` (6), `openfigi.com` (1), `query-parameters-openapi` (4), `redhat.com-catalog_inventory` (70), `redocly.com-museum` (10), `reverb.com` (18), `sac-backend` (25), `schema-constraints` (2), `sigstore-rekor` (35), `slurmdb-rest` (1), `squareup.com` (42), `tamoss` (19), `traccar.org` (28), `twilio.com-twilio_messaging_v1` (98), `twilio.com-twilio_voice_v1` (78), `worldcoin-signup-sequencer` (1), `writeonly-fields` (3), `xero.com-xero-payroll-au` (106); the per-value rows below classify each 2020-12 registry format separately |  |  |  |
| default | both | Schema Object.default | golden | census `schema.default`: 2552 declarations in 57 fixtures — `airbyte.local-config` (12), `anchore.io` (11), `apache.org` (15), `apache.org-airflow` (15), `apache.org-qakka` (2), `apicurio.local-registry` (4), `apideck.com-accounting` (3), `apideck.com-ats` (2), `apideck.com-connector` (1), `apideck.com-crm` (3), `apideck.com-customer-support` (2), `apideck.com-ecommerce` (2), `apideck.com-ecosystem` (1), `apideck.com-file-storage` (3), `apideck.com-hris` (3), `apideck.com-issue-tracking` (4), `apideck.com-lead` (3), `apideck.com-pos` (4), `apideck.com-proxy` (1), `apideck.com-sms` (2), `apideck.com-vault` (12), `apideck.com-webhook` (1), `appwrite.io-client` (56), `appwrite.io-server` (69), `asana.com` (6), `atlassian.com-jira` (237), `axesso.de` (2), `bbci.co.uk` (3), `box.com` (35), `byautomata.io` (2), `canada-holidays.ca` (8), `codesearch.debian.net` (3), `conjur.local` (5), `discourse.local` (2), `electric-sql` (2), `eozilla` (16), `frankfurter` (1), `free5gc-namf-communication` (5), `free5gc-pdu-session` (16), `github.com` (1090), `gov.bc.ca-news` (35), `http-toolkit` (6), `letta` (503), `livepeer-ai-runner` (54), `netbox.dev` (136), `nimisampo` (11), `openepcis-dpp-ready` (1), `openfigi.com` (1), `portfoliooptimizer.io` (89), `redhat.com-catalog_inventory` (5), `redocly.com-museum` (2), `reverb.com` (21), `sac-backend` (2), `schema-constraints` (2), `sigstore-rekor` (1), `tamoss` (18), `tlon-notes` (1) |  |  |  |
| nullable | 3.0 | Schema Object.nullable | golden | census `schema.nullable`: 7540 declarations in 38 fixtures — `anchore.io` (27), `apache.org` (123), `apache.org-airflow` (123), `apideck.com-accounting` (280), `apideck.com-ats` (76), `apideck.com-connector` (9), `apideck.com-crm` (201), `apideck.com-customer-support` (150), `apideck.com-ecommerce` (56), `apideck.com-ecosystem` (5), `apideck.com-file-storage` (13), `apideck.com-hris` (124), `apideck.com-issue-tracking` (61), `apideck.com-lead` (61), `apideck.com-pos` (104), `apideck.com-sms` (14), `apideck.com-vault` (33), `apideck.com-webhook` (13), `asana.com` (68), `atlassian.com-jira` (7), `bbci.co.uk` (1), `box.com` (364), `buildrelay` (2), `eozilla` (2), `exhaustive` (25), `free5gc-namf-communication` (3), `free5gc-pdu-session` (4), `github.com` (4343), `groundhog-day.com` (1), `letta` (105), `netbox.dev` (823), `nimisampo` (8), `openfigi.com` (23), `redhat.com-catalog_inventory` (1), `sac-backend` (5), `tlon-notes` (6), `twilio.com-twilio_messaging_v1` (189), `twilio.com-twilio_voice_v1` (87) |  |  |  |
| discriminator | both | Schema Object.discriminator | golden | census `schema.discriminator`: 99 declarations in 11 fixtures — `apache.org` (1), `apache.org-airflow` (1), `appng-rest-api` (1), `atlassian.com-jira` (3), `discriminated-unions` (1), `letta` (68), `microcks.local` (1), `openepcis-dpp-ready` (1), `recursive-types` (1), `sigstore-rekor` (12), `tlon-notes` (9) |  |  |  |
| read-only | both | Schema Object.readOnly | golden | census `schema.readOnly`: 6166 declarations in 29 fixtures — `apache.org` (106), `apache.org-airflow` (106), `apideck.com-accounting` (42), `apideck.com-ats` (19), `apideck.com-connector` (9), `apideck.com-crm` (54), `apideck.com-customer-support` (36), `apideck.com-ecommerce` (7), `apideck.com-ecosystem` (20), `apideck.com-file-storage` (23), `apideck.com-hris` (26), `apideck.com-issue-tracking` (11), `apideck.com-lead` (4), `apideck.com-pos` (9), `apideck.com-sms` (17), `apideck.com-vault` (44), `apideck.com-webhook` (4), `asana.com` (167), `atlassian.com-jira` (948), `box.com` (6), `bunq.com` (2974), `github.com` (64), `gov.bc.ca-news` (3), `netbox.dev` (1318), `redhat.com-catalog_inventory` (113), `schema-constraints` (1), `sigstore-rekor` (19), `writeonly-fields` (2), `xero.com-xero-payroll-au` (14) |  |  |  |
| write-only | both | Schema Object.writeOnly | golden | census `schema.writeOnly`: 3147 declarations in 10 fixtures — `apache.org` (2), `apache.org-airflow` (2), `apideck.com-crm` (1), `apideck.com-file-storage` (2), `atlassian.com-jira` (150), `box.com` (1), `bunq.com` (2974), `schema-constraints` (2), `sigstore-rekor` (12), `writeonly-fields` (1) |  |  |  |
| xml | both | Schema Object.xml | golden | census `schema.xml`: 97 declarations in 3 fixtures — `amazonaws.com-cloudfront` (33), `atlassian.com-jira` (60), `byautomata.io` (4) |  |  |  |
| externaldocs | both | Schema Object.externalDocs | golden | census `schema.externalDocs`: 1 declarations in 1 fixture — `xero.com-xero-payroll-au` (1) |  |  |  |
| example | both | Schema Object.example | golden | census `schema.example`: 10938 declarations in 57 fixtures — `airbyte.local-config` (12), `anchore.io` (1), `apicurio.local-registry` (34), `apideck.com-accounting` (722), `apideck.com-ats` (225), `apideck.com-connector` (132), `apideck.com-crm` (569), `apideck.com-customer-support` (265), `apideck.com-ecommerce` (292), `apideck.com-ecosystem` (42), `apideck.com-file-storage` (271), `apideck.com-hris` (401), `apideck.com-issue-tracking` (236), `apideck.com-lead` (170), `apideck.com-pos` (533), `apideck.com-proxy` (9), `apideck.com-sms` (119), `apideck.com-vault` (247), `apideck.com-webhook` (116), `apis.guru` (5), `appng-rest-api` (5), `asana.com` (367), `atlassian.com-jira` (66), `axesso.de` (22), `bintable.com` (2), `box.com` (1426), `buildrelay` (2), `canada-holidays.ca` (20), `codesearch.debian.net` (7), `conjur.local` (52), `corrently.io` (28), `dnd5eapi.co` (25), `electric-sql` (8), `eozilla` (6), `etherpad.local` (1117), `frankfurter` (11), `free5gc-namf-communication` (59), `free5gc-pdu-session` (58), `github.com` (1883), `groundhog-day.com` (1), `khoainats` (2), `kytos-sdntrace-cp` (25), `maif.local-otoroshi` (430), `med-anvisa-price` (43), `microcks.local` (4), `nimisampo` (21), `redhat.com-catalog_inventory` (30), `redocly.com-museum` (22), `sac-backend` (78), `schema-constraints` (2), `slurmdb-rest` (101), `squareup.com` (346), `tamoss` (3), `tlon-notes` (9), `withsecure-gdpr-subject-rights` (15), `worldcoin-signup-sequencer` (7), `xero.com-xero-payroll-au` (234) |  |  |  |
| deprecated | both | Schema Object.deprecated | golden | census `schema.deprecated`: 219 declarations in 13 fixtures — `airbyte.local-config` (2), `amazonaws.com-cloudfront` (2), `apache.org` (1), `apache.org-airflow` (1), `apideck.com-accounting` (5), `apideck.com-crm` (2), `apideck.com-customer-support` (1), `apideck.com-hris` (1), `discourse.local` (1), `github.com` (9), `letta` (189), `schema-constraints` (1), `tamoss` (4) |  |  |  |
| dollar-schema | 3.1 | Schema Object.$schema | golden | census `schema.$schema`: 23 declarations in 1 fixture — `sigstore-rekor` (23), which is a **3.0** document (census `openapi.openapi=3.0`), so the golden pins what Fern does with the keyword in a dialect that does not define it |  |  |  |
| dollar-vocabulary | 3.1 | Schema Object.$vocabulary | gap | census `schema.$vocabulary`: 0 declarations across all 126 registered sources; no `docs/fern-limitations.md` row names it. Its line of the [witness search](#witness-search-issue-188) table records `none-found`: no document a real API publishes as its own description declares `$vocabulary` anywhere the five searched sources reach — the keyword is a meta-schema keyword, and every declaration the search found is either a copy of the Swagger Petstore 3.1 sample or a JSON Schema meta-schema. | `none` — `src/openapi.rs`'s `Schema` declares no field for it and `grep -rF '"$vocabulary"' src/` finds no production read | Every module under `types/` is downstream of the dialect, so a Fern that honours a non-default `$vocabulary` could emit different models for the same document. | PROBE on **witness-supply** grounds rather than structural ones: the search recorded on this row's [witness search](#witness-search-issue-188) line found no witness at all, naming each source and the query run against it — `$vocabulary` is a meta-schema keyword, so an API description has no occasion to carry one — so no corpus row can pin it and a local probe declaring a custom dialect, recorded in `docs/fern-limitations.md`, is the only available settlement. |
| dollar-id | 3.1 | Schema Object.$id | golden | census `schema.$id`: 23 declarations in 1 fixture — `sigstore-rekor` (23), which is a **3.0** document (census `openapi.openapi=3.0`), so the golden pins what Fern does with the keyword in a dialect that does not define it |  |  |  |
| dollar-anchor | 3.1 | Schema Object.$anchor | gap | 140-source census `schema.$anchor`: 0 declarations across all 140 registered sources, 123 of them golden-bearing; no `docs/fern-limitations.md` row names it. Re-measured on the 140-source walk [`document-paths.md`](document-paths.md#snapshot-reconciliation) pins rather than the 126-source walk the rest of this file rests on, and the selector still has no witness inside the corpus. Its line of the [witness search](#witness-search-issue-188) table records `witness-blocked`, and this change re-fetched that witness at its pinned ref to settle the row: `inkeep/chat-api-openapi-schema`'s `openapi.yaml` at commit `76e79a7e60be63c5cb64fa2eefa0dadd3aa54707` — 14411 bytes, MD5 `3421b267…` — declares `$anchor` twice, at lines 350 and 468, each naming a `oneOf` branch of Inkeep's own production API description, and `fern check` accepts it. **It is not redistributable**, which is the whole of what blocks it: the repository declares no licence in any form — its entire tree at that commit is `.gitignore`, `README.md` and `openapi.yaml`, and `GET /repos/inkeep/chat-api-openapi-schema/license` answers `404 Not Found` — so nothing grants the corpus the right to vendor a golden derived from it. A witness blocked on redistribution is a screening failure of that document rather than evidence the feature has no witness at all, so this row is a fixture backlog entry on the search's own supply finding rather than a probe. | `none` — `src/openapi.rs`'s `Schema` declares no field for it and `grep -rF '"$anchor"' src/` finds no production read | A `$ref` resolved through an anchor names a different component, so the module names and cross-module imports under `types/` would differ. | FIXTURE — register a redistributable, immutably pinned real-world document declaring `$anchor` that Fern accepts; the only publisher-owned declarer the search found carries no licence at all, so the next candidate is a new document rather than that one. What such a row would pin is the **declaration**: the Inkeep document declares two anchors and `$ref`s neither of them, so whether Fern resolves a plain-name `$ref` through an anchor stays a probe question until a document carrying both turns up. This node re-asked the same question of six sources for a *further* declarer and found none: every new hit is a fixture, a test resource, a Petstore copy, a meta-schema or another `speakeasy-sdks/*` third-party distribution, so the Inkeep document is still the only publisher-owned `$anchor` declarer anywhere this search reaches. |
| dollar-dynamic-anchor | 3.1 | Schema Object.$dynamicAnchor | gap | 140-source census `schema.$dynamicAnchor`: 0 declarations across all 140 registered sources, 123 of them golden-bearing; no `docs/fern-limitations.md` row names it. Re-measured on the 140-source walk [`document-paths.md`](document-paths.md#snapshot-reconciliation) pins rather than the 126-source walk the rest of this file rests on, and the selector still has no witness inside the corpus. Its line of the [witness search](#witness-search-issue-188) table records `none-found` and names every source searched and the exact query run against each: no document a real API publishes as its own description declares `$dynamicAnchor` anywhere APIs.guru / `openapi-directory`, GitHub code search, SwaggerHub's public registry, the Postman public API network or the thirteen vendor-portal documents reach — every candidate that does is a synthetic fixture, an OpenAPI 3.1 meta-schema copy, an unofficial third-party transcription, or reachable at no immutable ref. | `none` — `src/openapi.rs`'s `Schema` declares no field for it and `grep -rF '"$dynamicAnchor"' src/` finds no production read | Dynamic resolution picks a different subschema per use site, so the field annotations in `types/` would differ from the static resolution crozier performs. | PROBE on **witness-supply** grounds rather than structural ones: the search recorded on this row's [witness search](#witness-search-issue-188) line found no witness at all, naming each source and the query run against it, so no corpus row can pin the keyword and a local probe recorded in `docs/fern-limitations.md` — a `$dynamicAnchor`/`$dynamicRef` recursion — is the only available settlement. |
| dollar-ref | both | Schema Object.$ref | golden | census `schema.$ref`: 20335 declarations in 105 fixtures — `6-dot-authentiqio.appspot.com` (28), `airbyte.local-config` (543), `amazonaws.com-cloudformation` (1352), `amazonaws.com-cloudfront` (639), `anchore.io` (292), `apache.org` (175), `apache.org-airflow` (175), `apache.org-qakka` (11), `apicurio.local-registry` (162), `apideck.com-accounting` (393), `apideck.com-ats` (90), `apideck.com-connector` (71), `apideck.com-crm` (174), `apideck.com-customer-support` (61), `apideck.com-ecommerce` (92), `apideck.com-ecosystem` (54), `apideck.com-file-storage` (144), `apideck.com-hris` (167), `apideck.com-issue-tracking` (82), `apideck.com-lead` (36), `apideck.com-pos` (269), `apideck.com-sms` (23), `apideck.com-vault` (65), `apideck.com-webhook` (56), `apis.guru` (7), `appng-rest-api` (56), `appwrite.io-client` (61), `appwrite.io-server` (82), `asana.com` (491), `atlassian.com-jira` (1121), `audience-filter` (3), `audience-filter-strict` (4), `auth-schemes` (2), `axesso.de` (4), `bbci.co.uk` (48), `bintable.com` (2), `box.com` (1139), `buildrelay` (5), `bungie.net` (992), `bunq.com` (1628), `byautomata.io` (10), `canada-holidays.ca` (9), `client-class-name` (2), `codesearch.debian.net` (3), `color.pizza` (29), `conjur.local` (92), `cookie-parameters` (1), `corrently.io` (15), `digit-leading-property` (1), `discriminated-unions` (4), `dnd5eapi.co` (217), `enum-name-sanitization` (3), `enum-receiver-collision` (3), `eos.local` (12), `eos.local-extra-fields-forbid` (12), `eozilla` (71), `error-responses` (6), `etsi.local-mec010-2_apppkgmgmt` (109), `exa-gate` (7), `exhaustive` (61), `form-bodies` (2), `frankfurter` (5), `free5gc-namf-communication` (197), `free5gc-pdu-session` (241), `gambitcomm.local-mimic` (44), `github.com` (2411), `gov.bc.ca-news` (104), `groundhog-day.com` (5), `helios-verifiable-api` (111), `http-toolkit` (17), `inline-array-request` (1), `inline-request-response` (2), `integer-enums` (4), `khoainats` (4), `letta` (1093), `livepeer-ai-runner` (79), `maif.local-otoroshi` (267), `med-anvisa-price` (1), `microcks.local` (79), `netbox.dev` (1121), `oauth-client-credentials` (3), `openbanking.org.uk-account-info-openapi` (760), `openepcis-dpp-ready` (61), `openfigi.com` (12), `prometheus-x-edge-computing` (37), `pydantic-extra-fields` (2), `query-parameters-openapi` (10), `recursive-types` (6), `redhat.com-catalog_inventory` (145), `redocly.com-museum` (38), `sac-backend` (52), `schema-constraints` (2), `servers-webhooks` (4), `sigstore-rekor` (35), `slurmdb-rest` (175), `squareup.com` (1140), `tamoss` (156), `tlon-notes` (84), `traccar.org` (91), `twilio.com-twilio_messaging_v1` (54), `twilio.com-twilio_voice_v1` (26), `withsecure-gdpr-subject-rights` (29), `worldcoin-signup-sequencer` (18), `writeonly-fields` (2), `xero.com-xero-payroll-au` (139) |  |  |  |
| dollar-dynamic-ref | 3.1 | Schema Object.$dynamicRef | gap | 140-source census `schema.$dynamicRef`: 0 declarations across all 140 registered sources, 123 of them golden-bearing; no `docs/fern-limitations.md` row names it. Re-measured on the 140-source walk [`document-paths.md`](document-paths.md#snapshot-reconciliation) pins rather than the 126-source walk the rest of this file rests on, and the selector still has no witness inside the corpus. Its line of the [witness search](#witness-search-issue-188) table records `none-found`, out of the same candidate set as `dollar-dynamic-anchor` and against the same five sources and queries it names: the two keywords are only exercisable together, and no document reaching the witness bar declares either. | `none` — `src/openapi.rs`'s `Schema` declares no field for it and `grep -rF '"$dynamicRef"' src/` finds no production read | The referenced model decides the annotation and the import a `types/` module emits, so an unresolved `$dynamicRef` changes both. | PROBE on **witness-supply** grounds rather than structural ones, exactly as `dollar-dynamic-anchor`: the search found no witness for either keyword, naming each source and the query run against it, so the pair is only exercisable from a local probe recorded in `docs/fern-limitations.md`. |
| dollar-defs | 3.1 | Schema Object.$defs | gap | census `schema.$defs`: 0 declarations across all 126 registered sources; no `docs/fern-limitations.md` row names it | `none` — `src/openapi.rs`'s `Schema` declares no field for it and `grep -rF '"$defs"' src/` finds no production read | A `$defs` subschema is a named type, so whether Fern emits a module for it decides whether `types/` carries one and what `types/__init__.py` exports. | FIXTURE — **the search found a witness**: `emergentmethods/flowdapt` `openapi.json`, `Apache-2.0`, pinned at `e8f34d41`, 7 `$defs` declarations, accepted by both halves of the Fern screen. Registering it and byte-matching its Fern golden settles it. |
| dollar-comment | 3.1 | Schema Object.$comment | golden | 141-source census `schema.$comment`: 2 declarations in 1 fixture — `volview-backend-contract` (2), on `components.schemas.TaskSpec` and `components.schemas.AnnotationsFile`. Re-measured on the 141-source walk [`document-paths.md`](document-paths.md#snapshot-reconciliation) pins rather than the 126-source walk the rest of this file rests on, because this change registered that source. It is the witness this row's line of the [witness search](#witness-search-issue-188) table records: the neutral backend contract `Kitware/VolView` publishes for its own client, at commit `496ada04c1d07235f98c92a68b5c374c7188ee87` under the repository's Apache-2.0 `LICENSE`, which `fern check` accepts (`All checks passed`, exit 0) and which is corpus row 109 — its Fern 5.20.0 golden byte-matches with no exclusions, so the row pins that crozier ignores the annotation-only keyword exactly where Fern does. No `docs/fern-limitations.md` row names it. |  |  |  |
| prefix-items | 3.1 | Schema Object.prefixItems | golden | census `schema.prefixItems`: 2 declarations in 1 fixture — `worldcoin-signup-sequencer` (2) |  |  |  |
| contains | 3.1 | Schema Object.contains | gap | census `schema.contains`: 0 declarations across all 126 registered sources; no `docs/fern-limitations.md` row names it | `none` — `src/openapi.rs`'s `Schema` declares no field for it, and the one hit `grep -rF '"contains"' src/` returns is a test document inside `src/ir.rs` | An array schema's `contains` could narrow the `typing.List[...]` element annotation a `types/` module emits. | FIXTURE — the issue #188 search found real-world declarers and Fern refuses every redistributable one it found: `grafana/grafana-asserts-public-clients` (`Apache-2.0`, 2), `pitekusu/shittim-chest` (`MIT`, 24) and `goptics/vizb` (`MIT`, 8) all fail `fern check`. What settles it is a further redistributable declarer Fern accepts — or, on the `fern-rejected` outcome the search recorded, the blocked-witness probe route the amended rule opens. |
| pattern-properties | 3.1 | Schema Object.patternProperties | golden | census `schema.patternProperties`: 1 declarations in 1 fixture — `electric-sql` (1) |  |  |  |
| dependent-schemas | 3.1 | Schema Object.dependentSchemas | gap | census `schema.dependentSchemas`: 0 declarations across all 126 registered sources; no `docs/fern-limitations.md` row names it | `none` — `src/openapi.rs`'s `Schema` declares no field for it and `grep -rF '"dependentSchemas"' src/` finds no production read | Conditional properties change which fields a generated model in `types/` declares. | FIXTURE — the search put six sources to it and found no document a real API publishes as its own description that declares `dependentSchemas`: every hit is an OpenAPI meta-schema, a parser or generator fixture, or a Petstore 3.1 sample, and the one exception Fern accepts describes an API whose only `servers` entry is an RFC 2606 `.invalid` placeholder. A 3.1 corpus row declaring it still settles the row; no document that could be one has turned up yet. |
| property-names | 3.1 | Schema Object.propertyNames | gap | census `schema.propertyNames`: 0 declarations across all 126 registered sources; no `docs/fern-limitations.md` row names it | `none` — `src/openapi.rs`'s `Schema` declares no field for it and `grep -rF '"propertyNames"' src/` finds no production read | A constrained key type could change the `typing.Dict[...]` key annotation crozier always emits as `str`. | FIXTURE — **the witness is already registered.** `just surface-census --selector schema.propertyNames` over the fetched corpus reports 7 declarations in corpus row 109, `volview-backend-contract`, whose committed Fern 5.20.0 golden byte-matches, and re-running `fern check` and `fern generate` on that document here passes both. This row's `evidence` cell and its rank in the index still read the pre-registration `0 declarations across all 126 registered sources`; reclassifying it on that measurement belongs to the chain that owns [`openapi-surface-coverage.md`](../openapi-surface-coverage.md), not to this search. A second, unregistered witness exists too — the `ITISFoundation/osparc-simcore` document, `MIT`, 30 declarations, Fern-accepted. |
| if | 3.1 | Schema Object.if | golden | census `schema.if`: 1 declarations in 1 fixture — `tamoss` (1) |  |  |  |
| then | 3.1 | Schema Object.then | golden | census `schema.then`: 1 declarations in 1 fixture — `tamoss` (1) |  |  |  |
| else | 3.1 | Schema Object.else | golden | census `schema.else`: 1 declarations in 1 fixture — `tamoss` (1) |  |  |  |
| unevaluated-items | 3.1 | Schema Object.unevaluatedItems | gap | census `schema.unevaluatedItems`: 0 declarations across all 126 registered sources; no `docs/fern-limitations.md` row names it | `none` — `src/openapi.rs`'s `Schema` declares no field for it and `grep -rF '"unevaluatedItems"' src/` finds no production read | The list element annotation in `types/` would differ if Fern applies the keyword to the tail a `prefixItems` schema leaves unevaluated. | FIXTURE — the two real-world declarers the search found are `grafana/grafana-asserts-public-clients` (`Apache-2.0`, 2) and HotCRP's own `devel/openapi.json` (`NOASSERTION`, 2), and Fern refuses both. A redistributable declarer Fern accepts settles it — a 3.1 corpus row pairing `prefixItems` with `unevaluatedItems` remains the shape to look for. |
| unevaluated-properties | 3.1 | Schema Object.unevaluatedProperties | golden | census `schema.unevaluatedProperties`: 3 declarations in 1 fixture — `tamoss` (3) |  |  |  |
| type-array-with-null | 3.1 | Schema Object.type | golden | variant scan `type=array-with-null`: 498 declarations in 3 fixtures — `discourse.local` (479), `frankfurter` (15), `tlon-notes` (4); all three are 3.1 documents (census `openapi.openapi=3.1`), and the count matches `docs/fern-limitations.md`'s own round-4 measurement of `type-array-nullable` (498 occurrences in 3 documents) |  |  |  |
| type-array-multi-nonnull | 3.1 | Schema Object.type | golden | variant scan `type=array-multi-nonnull`: 2 declarations in 1 fixture — `openepcis-dpp-ready` (2); `docs/fern-limitations.md` `type-array-multi-nonnull`: implements |  |  |  |
| const | 3.1 | Schema Object.const | golden | census `schema.const`: 147 declarations in 3 fixtures — `exa-gate` (1), `letta` (138), `tamoss` (8); every one is a string `const` (variant scan `const=string`, 147) |  |  |  |
| const-boolean | 3.1 | Schema Object.const | limitations | variant scan `const=boolean`: 0 declarations across all 126 registered sources; `docs/fern-limitations.md` `const-boolean`: discards |  |  |  |
| const-integer | 3.1 | Schema Object.const | limitations | variant scan `const=integer`: 0 declarations across all 126 registered sources; `docs/fern-limitations.md` `const-integer`: discards |  |  |  |
| enum-member-float | both | Schema Object.enum | limitations | variant scan `enum-member=float`: 0 declarations across all 126 registered sources (the member kinds declared are string, integer, boolean and null); `docs/fern-limitations.md` `enum-member-float`: discards + supply |  |  |  |
| enum-member-object | both | Schema Object.enum | limitations | variant scan `enum-member=object`: 0 declarations across all 126 registered sources; `docs/fern-limitations.md` `enum-member-object`: discards + supply |  |  |  |
| max-contains | 3.1 | Schema Object.maxContains | gap | census `schema.maxContains`: 0 declarations across all 126 registered sources; no `docs/fern-limitations.md` row names it | `none` — `src/openapi.rs`'s `Schema` declares no field for it and `grep -rF '"maxContains"' src/` finds no production read | The bound lands in the same `types/` list annotation `maxItems` does, and crozier reads neither. | FIXTURE — both declarers the search found are `MIT` and declare `contains`, `maxContains` and `minContains` together on the same array schemas — `pitekusu/shittim-chest` (24 each) and `goptics/vizb` (8 each) — and Fern refuses both. One document Fern accepts would settle three rows at once. |
| min-contains | 3.1 | Schema Object.minContains | gap | census `schema.minContains`: 0 declarations across all 126 registered sources; no `docs/fern-limitations.md` row names it | `none` — `src/openapi.rs`'s `Schema` declares no field for it and `grep -rF '"minContains"' src/` finds no production read | As `max-contains`: the `types/` list annotation is the artifact at risk. | FIXTURE — the same two `MIT` documents `max-contains` names declare `minContains` on the same schemas (`pitekusu/shittim-chest` 24, `goptics/vizb` 8) and Fern refuses both; one accepted document settles `contains`, `max-contains` and this row together. |
| dependent-required | 3.1 | Schema Object.dependentRequired | gap | census `schema.dependentRequired`: 0 declarations across all 126 registered sources; no `docs/fern-limitations.md` row names it | `none` — `src/openapi.rs`'s `Schema` declares no field for it and `grep -rF '"dependentRequired"' src/` finds no production read | Which fields a generated model in `types/` marks optional is exactly what `required` decides, and `dependentRequired` is its conditional form. | FIXTURE — **the search found a witness**: `HelixDB/helix-db` `docs/openapi.json`, `Apache-2.0`, pinned at `f19b488b`, 2 declarations, accepted by both halves of the Fern screen. At 25 KB it is the smallest registration any of these twenty rows needs. |
| examples | 3.1 | Schema Object.examples | golden | census `schema.examples`: 230 declarations in 3 fixtures — `discourse.local` (61), `letta` (165), `openepcis-dpp-ready` (4) |  |  |  |
| format-assertion-vocabulary | 3.1 | Schema Object.format | gap | census `schema.$vocabulary`: 0 declarations across all 126 registered sources — the assertion vocabulary can only be selected through `$vocabulary`, which no source declares; no `docs/fern-limitations.md` row names it | `none` — `src/openapi.rs`'s `Schema` declares no field for it and `grep -rF '"$vocabulary"' src/` finds no production read | No generated artifact: the `types/` annotation derives from the format *value*, which the per-value rows below own, and a Python SDK asserts no format at all. | UNREACHABLE — recording that the annotation-versus-assertion split has no position in a generated Python SDK is the settlement. |
| format-date-time | both | Schema Object.format | golden | census `schema.format=date-time`: 2317 declarations in 48 fixtures — `6-dot-authentiqio.appspot.com` (1), `amazonaws.com-cloudformation` (4), `amazonaws.com-cloudfront` (1), `anchore.io` (35), `apache.org` (44), `apache.org-airflow` (44), `apicurio.local-registry` (11), `apideck.com-accounting` (14), `apideck.com-ats` (6), `apideck.com-crm` (19), `apideck.com-customer-support` (16), `apideck.com-ecommerce` (2), `apideck.com-ecosystem` (11), `apideck.com-file-storage` (3), `apideck.com-hris` (2), `apideck.com-issue-tracking` (5), `apideck.com-pos` (14), `apideck.com-sms` (4), `apideck.com-vault` (3), `apideck.com-webhook` (3), `apis.guru` (3), `asana.com` (40), `atlassian.com-jira` (23), `box.com` (155), `buildrelay` (1), `bungie.net` (76), `canada-holidays.ca` (1), `cookie-parameters` (1), `eozilla` (5), `exhaustive` (4), `free5gc-namf-communication` (5), `free5gc-pdu-session` (13), `github.com` (1145), `gov.bc.ca-news` (2), `letta` (96), `maif.local-otoroshi` (1), `microcks.local` (2), `netbox.dev` (319), `openbanking.org.uk-account-info-openapi` (30), `openepcis-dpp-ready` (1), `query-parameters-openapi` (2), `redhat.com-catalog_inventory` (64), `sac-backend` (6), `tamoss` (18), `traccar.org` (27), `twilio.com-twilio_messaging_v1` (24), `twilio.com-twilio_voice_v1` (10), `writeonly-fields` (1) |  |  |  |
| format-date | both | Schema Object.format | golden | census `schema.format=date`: 88 declarations in 22 fixtures — `airbyte.local-config` (2), `apideck.com-accounting` (9), `apideck.com-ats` (2), `apideck.com-crm` (2), `apideck.com-customer-support` (2), `apideck.com-hris` (9), `apideck.com-pos` (2), `asana.com` (25), `atlassian.com-jira` (2), `canada-holidays.ca` (2), `eozilla` (1), `esgenterprise.com` (1), `exhaustive` (3), `frankfurter` (9), `github.com` (2), `netbox.dev` (6), `openfigi.com` (1), `query-parameters-openapi` (1), `redocly.com-museum` (3), `twilio.com-twilio_messaging_v1` (1), `twilio.com-twilio_voice_v1` (2), `writeonly-fields` (1) |  |  |  |
| format-time | both | Schema Object.format | golden | census `schema.format=time`: 3 declarations in 1 fixture — `maif.local-otoroshi` (3) |  |  |  |
| format-duration | both | Schema Object.format | golden | census `schema.format=duration`: 2 declarations in 1 fixture — `mosip-esignet` (2), [`CORPUS.md`](../../tests/fixtures/CORPUS.md) row 97, whose Fern 5.20.0 golden byte-matches with no exclusions, so the `str` crozier annotates for this format is now Fern's own; measured on the 129-source walk [`document-paths.md`](document-paths.md#snapshot-reconciliation) pins. No `docs/fern-limitations.md` row names it |  |  |  |
| format-email | both | Schema Object.format | golden | census `schema.format=email`: 62 declarations in 20 fixtures — `airbyte.local-config` (3), `apideck.com-accounting` (1), `apideck.com-ats` (1), `apideck.com-crm` (1), `apideck.com-customer-support` (1), `apideck.com-ecommerce` (1), `apideck.com-hris` (2), `apideck.com-issue-tracking` (1), `apideck.com-lead` (1), `apideck.com-pos` (1), `apideck.com-sms` (1), `asana.com` (1), `box.com` (2), `discourse.local` (1), `github.com` (31), `maif.local-otoroshi` (4), `netbox.dev` (4), `redocly.com-museum` (1), `sac-backend` (3), `sigstore-rekor` (1) |  |  |  |
| format-idn-email | both | Schema Object.format | gap | census `schema.format=idn-email`: 0 declarations across all 126 registered sources; no `docs/fern-limitations.md` row names it | `none` for this value — `src/ir.rs` reads `schema.format` in 10 places and names only `binary`, `byte`, `date`, `date-time`, `int64` and `uuid`; every other value falls to a catch-all that cannot tell one unnamed value from another — `_ => TypeRef::Primitive(Prim::Str)` in `base_type_ref`, `_ => return None` in `scalar_body` | A `types/` field crozier annotates `str` would differ if Fern maps this registered format to a narrower Python type, as it does for `date` and `date-time`. | FIXTURE — the search found a witness this corpus cannot use: `wttw/aboutmyemail` `ameapi.yaml` declares `format: idn-email` 4 times, is publisher-owned, is immutably pinned at `3575c0e4` and passes both halves of the Fern screen — and its repository declares no licence at all. A licence there makes it registrable with no other change; failing that, a further redistributable declarer settles it. |
| format-hostname | both | Schema Object.format | golden | census `schema.format=hostname`: 1 declarations in 1 fixture — `maif.local-otoroshi` (1) |  |  |  |
| format-idn-hostname | both | Schema Object.format | gap | census `schema.format=idn-hostname`: 0 declarations across all 126 registered sources; no `docs/fern-limitations.md` row names it | `none` for this value — `src/ir.rs` reads `schema.format` in 10 places and names only `binary`, `byte`, `date`, `date-time`, `int64` and `uuid`; every other value falls to a catch-all that cannot tell one unnamed value from another — `_ => TypeRef::Primitive(Prim::Str)` in `base_type_ref`, `_ => return None` in `scalar_body` | A `types/` field crozier annotates `str` would differ if Fern maps this registered format to a narrower Python type, as it does for `date` and `date-time`. | FIXTURE — the only real-world declarer the search found is the document Short.io serves itself, and it fails every screen but the count: no licence declared, no fixed address (the publisher republishes it in place), and `fern check` exits 1. A redistributable, immutably pinned `format: idn-hostname` declarer Fern accepts settles it. |
| format-ipv4 | both | Schema Object.format | golden | census `schema.format=ipv4`: 3 declarations in 1 fixture — `maif.local-otoroshi` (3) |  |  |  |
| format-ipv6 | both | Schema Object.format | gap | census `schema.format=ipv6`: 0 declarations across all 126 registered sources; no `docs/fern-limitations.md` row names it | `none` for this value — `src/ir.rs` reads `schema.format` in 10 places and names only `binary`, `byte`, `date`, `date-time`, `int64` and `uuid`; every other value falls to a catch-all that cannot tell one unnamed value from another — `_ => TypeRef::Primitive(Prim::Str)` in `base_type_ref`, `_ => return None` in `scalar_body` | A `types/` field crozier annotates `str` would differ if Fern maps this registered format to a narrower Python type, as it does for `date` and `date-time`. | FIXTURE — the search found four redistributable real-world declarers and Fern refuses all four: `linode/linode-api-docs` (`Apache-2.0`, 3), `cloudflare/api-schemas` (`BSD-3-Clause`, 3), `CrowdStrike/aidr-python` (`MIT`, 1) and Vercel's `openapi.vercel.sh` (1, and a mutable address besides). A fifth declarer Fern accepts settles it, or the blocked-witness probe route. |
| format-uuid | both | Schema Object.format | golden | census `schema.format=uuid`: 130 declarations in 17 fixtures — `airbyte.local-config` (21), `apache.org-qakka` (2), `atlassian.com-jira` (12), `box.com` (3), `buildrelay` (1), `exhaustive` (3), `free5gc-namf-communication` (11), `free5gc-pdu-session` (8), `gov.bc.ca-news` (1), `letta` (2), `maif.local-otoroshi` (2), `netbox.dev` (2), `redhat.com-catalog_inventory` (6), `redocly.com-museum` (4), `schema-constraints` (1), `writeonly-fields` (1), `xero.com-xero-payroll-au` (50) |  |  |  |
| format-uri | both | Schema Object.format | golden | census `schema.format=uri`: 9231 declarations in 22 fixtures — `airbyte.local-config` (4), `apideck.com-ats` (1), `apideck.com-connector` (5), `apideck.com-ecommerce` (2), `apideck.com-vault` (2), `apideck.com-webhook` (3), `asana.com` (13), `atlassian.com-jira` (152), `canada-holidays.ca` (1), `eozilla` (6), `etsi.local-mec010-2_apppkgmgmt` (5), `frankfurter` (3), `github.com` (8485), `letta` (5), `maif.local-otoroshi` (3), `netbox.dev` (416), `openbanking.org.uk-account-info-openapi` (6), `openepcis-dpp-ready` (5), `sigstore-rekor` (3), `tamoss` (1), `twilio.com-twilio_messaging_v1` (61), `twilio.com-twilio_voice_v1` (49) |  |  |  |
| format-uri-reference | both | Schema Object.format | golden | census `schema.format=uri-reference`: 1 declarations in 1 fixture — `eozilla` (1) |  |  |  |
| format-iri | both | Schema Object.format | gap | census `schema.format=iri`: 0 declarations across all 126 registered sources; no `docs/fern-limitations.md` row names it | `none` for this value — `src/ir.rs` reads `schema.format` in 10 places and names only `binary`, `byte`, `date`, `date-time`, `int64` and `uuid`; every other value falls to a catch-all that cannot tell one unnamed value from another — `_ => TypeRef::Primitive(Prim::Str)` in `base_type_ref`, `_ => return None` in `scalar_body` | A `types/` field crozier annotates `str` would differ if Fern maps this registered format to a narrower Python type, as it does for `date` and `date-time`. | FIXTURE — the two real-world declarers the search found are the Mercure protocol's own specification, which is `AGPL-3.0` and refused by `fern check`, and Short.io's own document, which declares no licence and is refused too. A redistributable `format: iri` declarer Fern accepts settles it. |
| format-iri-reference | both | Schema Object.format | gap | census `schema.format=iri-reference`: 0 declarations across all 126 registered sources; no `docs/fern-limitations.md` row names it | `none` for this value — `src/ir.rs` reads `schema.format` in 10 places and names only `binary`, `byte`, `date`, `date-time`, `int64` and `uuid`; every other value falls to a catch-all that cannot tell one unnamed value from another — `_ => TypeRef::Primitive(Prim::Str)` in `base_type_ref`, `_ => return None` in `scalar_body` | A `types/` field crozier annotates `str` would differ if Fern maps this registered format to a narrower Python type, as it does for `date` and `date-time`. | FIXTURE — the search found one redistributable declarer Fern accepts through both halves, `stryd/efulfilmentshop-go` `api/openapi.yaml` (`MIT`, 36), and it is a Go client library's unofficial transcription rather than the publisher's own document; the two publisher-owned declarers it found are `AGPL-3.0` (Mercure, 4) and unlicensed (`adrianzech/grid-time`, 8), and Fern refuses both. A publisher-owned, redistributable declarer settles it. |
| format-uri-template | both | Schema Object.format | golden | census `schema.format=uri-template`: 4653 declarations in 1 fixture — `github.com` (4653) |  |  |  |
| format-json-pointer | both | Schema Object.format | gap | census `schema.format=json-pointer`: 0 declarations across all 126 registered sources; no `docs/fern-limitations.md` row names it | `none` for this value — `src/ir.rs` reads `schema.format` in 10 places and names only `binary`, `byte`, `date`, `date-time`, `int64` and `uuid`; every other value falls to a catch-all that cannot tell one unnamed value from another — `_ => TypeRef::Primitive(Prim::Str)` in `base_type_ref`, `_ => return None` in `scalar_body` | A `types/` field crozier annotates `str` would differ if Fern maps this registered format to a narrower Python type, as it does for `date` and `date-time`. | FIXTURE — **the search found a witness**: `dcm-project/k8s-container-service-provider` `api/v1alpha1/openapi.yaml`, `Apache-2.0` in both the repository and the document's own `info.license`, pinned at `2fb9a6de`, 2 declarations on its RFC 7807 problem detail, accepted by both halves of the Fern screen. |
| format-relative-json-pointer | both | Schema Object.format | gap | census `schema.format=relative-json-pointer`: 0 declarations across all 141 registered sources, 124 of them golden-bearing; no `docs/fern-limitations.md` row names it. Re-measured on the 141-source walk [`document-paths.md`](document-paths.md#snapshot-reconciliation) pins rather than the 126-source walk the rest of this file rests on, and the selector still has no witness inside the corpus. Its line of the [witness search](#witness-search-issue-188) table records `witness-blocked`, and this change re-fetched that witness at its pinned ref to settle the row: the BioIS API, `geo-engine/BioIS`'s `openapi.json` at commit `afdcab7d25870c2843afc2b75818e25dc8ff4b98`, declares `"format": "relative-json-pointer"` once, on `components.schemas.RelativeJsonPointer`, and `fern check` accepts it. **It is not redistributable**, which is the whole of what blocks it: the `LICENSE` at that ref reads `PROPRIETARY LICENSE … The Software is not licensed under an Open Source initiative.` and the GitHub API reports the repository's licence as `NOASSERTION`, so nothing grants the corpus the right to vendor a golden derived from it. A witness blocked on redistribution is a screening failure of that document rather than evidence the feature has no witness at all, so this row is a fixture backlog entry on the search's own supply finding rather than a probe. | `none` for this value — `src/ir.rs` reads `schema.format` in 10 places and names only `binary`, `byte`, `date`, `date-time`, `int64` and `uuid`; every other value falls to a catch-all that cannot tell one unnamed value from another — `_ => TypeRef::Primitive(Prim::Str)` in `base_type_ref`, `_ => return None` in `scalar_body` | A `types/` field crozier annotates `str` would differ if Fern maps this registered format to a narrower Python type, as it does for `date` and `date-time`. | FIXTURE — register a redistributable, immutably pinned real-world document annotating a field `format: relative-json-pointer` that Fern accepts; the only publisher-owned declarer the search found is proprietary, so the next candidate is a new document rather than that one. This node re-asked the same question of six sources for a further declarer and could not finish asking: the five this region declares answered with nothing that is a document a real API publishes as its own description, and the literal index answered only half its pair — its JSON spelling returned 3 matches in 2 files, both of them conformance-test resources, while its YAML spelling stayed behind an HTTP 429 for the whole dispatch. So the further-witness question stays open rather than closed. |
| format-regex | both | Schema Object.format | golden | census `schema.format=regex`: 1 declarations in 1 fixture — `eozilla` (1) |  |  |  |
| format-int32 | both | Schema Object.format | golden | census `schema.format=int32`: 2205 declarations in 18 fixtures — `airbyte.local-config` (1), `anchore.io` (1), `apache.org-qakka` (1), `apicurio.local-registry` (1), `appng-rest-api` (6), `appwrite.io-client` (65), `appwrite.io-server` (77), `atlassian.com-jira` (215), `box.com` (7), `bungie.net` (1201), `free5gc-namf-communication` (45), `free5gc-pdu-session` (64), `gambitcomm.local-mimic` (477), `gov.bc.ca-news` (10), `maif.local-otoroshi` (31), `microcks.local` (1), `openbanking.org.uk-account-info-openapi` (1), `xero.com-xero-payroll-au` (1) |  |  |  |
| format-int64 | both | Schema Object.format | golden | census `schema.format=int64`: 1046 declarations in 20 fixtures — `airbyte.local-config` (26), `apache.org-qakka` (1), `apicurio.local-registry` (24), `atlassian.com-jira` (494), `axesso.de` (6), `bbci.co.uk` (2), `box.com` (191), `bungie.net` (189), `exhaustive` (3), `free5gc-pdu-session` (3), `github.com` (6), `gov.bc.ca-news` (1), `integer-enums` (1), `maif.local-otoroshi` (39), `microcks.local` (3), `openbanking.org.uk-account-info-openapi` (1), `sac-backend` (12), `slurmdb-rest` (1), `squareup.com` (42), `worldcoin-signup-sequencer` (1); `int_prim` in `src/ir.rs` reads this value by name and maps it to `Prim::Long` |  |  |  |
| format-float | both | Schema Object.format | golden | census `schema.format=float`: 51 declarations in 7 fixtures — `appwrite.io-client` (3), `appwrite.io-server` (3), `bungie.net` (10), `openbanking.org.uk-account-info-openapi` (15), `redocly.com-museum` (1), `reverb.com` (18), `sac-backend` (1) |  |  |  |
| format-double | both | Schema Object.format | golden | census `schema.format=double`: 102 declarations in 13 fixtures — `apideck.com-hris` (14), `atlassian.com-jira` (6), `bbci.co.uk` (1), `bungie.net` (4), `corrently.io` (2), `discriminated-unions` (2), `esgenterprise.com` (4), `exhaustive` (4), `inline-request-response` (2), `maif.local-otoroshi` (9), `microcks.local` (2), `schema-constraints` (1), `xero.com-xero-payroll-au` (51) |  |  |  |
| format-password | both | Schema Object.format | golden | census `schema.format=password`: 7 declarations in 5 fixtures — `apache.org` (1), `apache.org-airflow` (1), `conjur.local` (1), `sac-backend` (3), `traccar.org` (1) |  |  |  |
| format-byte | 3.0 | Schema Object.format | golden | census `schema.format=byte`: 61 declarations in 8 fixtures — `apache.org-qakka` (1), `atlassian.com-jira` (1), `bungie.net` (5), `exhaustive` (3), `free5gc-namf-communication` (9), `free5gc-pdu-session` (12), `gov.bc.ca-news` (2), `sigstore-rekor` (28); 3.1 keeps no such format, replacing it with the `content-encoding` row's keyword; `scalar_body` in `src/ir.rs` reads this value by name |  |  |  |
| format-binary | 3.0 | Schema Object.format | golden | census `schema.format=binary`: 88 declarations in 19 fixtures — `airbyte.local-config` (3), `anchore.io` (2), `apicurio.local-registry` (1), `apideck.com-file-storage` (3), `asana.com` (1), `atlassian.com-jira` (1), `box.com` (8), `eozilla` (1), `etsi.local-mec010-2_apppkgmgmt` (2), `exhaustive` (1), `form-bodies` (1), `free5gc-namf-communication` (14), `free5gc-pdu-session` (34), `github.com` (1), `letta` (3), `livepeer-ai-runner` (6), `microcks.local` (3), `nimisampo` (2), `redocly.com-museum` (1); 3.1 keeps no such format, replacing it with the `content-media-type` row's keyword; `src/ir.rs` reads this value by name in 8 of its 10 `schema.format` reads |  |  |  |
| content-encoding | 3.1 | Schema Object.contentEncoding | golden | census `schema.contentEncoding`: 1 declaration in 1 fixture — `marimo` (1), pinned by corpus row 95 |  |  |  |
| content-media-type | 3.1 | Schema Object.contentMediaType | gap | census `schema.contentMediaType`: 0 declarations across all 126 registered sources; no `docs/fern-limitations.md` row names it | `none` — `src/openapi.rs`'s `Schema` declares no field for it and `grep -rF '"contentMediaType"' src/` finds no production read | As `content-encoding`: the `types/` field annotation for the embedded document is the artifact at risk. | FIXTURE — **the search found a witness.** `ITISFoundation/osparc-simcore`'s web-server document is `MIT`, immutably pinned at `7fa7c73d`, declares `contentMediaType` 24 times, and Fern accepts it through both `fern check` and a real `fern generate` at 5.20.0. Registering it as a corpus row whose golden byte-matches settles this row, and `content-schema`, `exclusive-maximum-numeric` and `property-names` with it. |
| content-schema | 3.1 | Schema Object.contentSchema | gap | census `schema.contentSchema`: 0 declarations across all 126 registered sources; no `docs/fern-limitations.md` row names it | `none` — `src/openapi.rs`'s `Schema` declares no field for it and `grep -rF '"contentSchema"' src/` finds no production read | A `contentSchema` names a model for an embedded document, so `types/` could carry a module crozier never emits. | FIXTURE — **the search found a witness**, the same `ITISFoundation/osparc-simcore` document, which declares `contentSchema` 24 times on the same nodes as its 24 `contentMediaType` ones; one corpus row settles both. |
| boolean-schema-true | 3.1 | Schema Object (boolean form) | limitations | variant scan: 0 declarations of a literal `true` schema in any position other than `additionalProperties` across all 126 registered sources (the `additionalProperties` position is the `additional-properties-boolean-true` row); `docs/fern-limitations.md` `boolean-schema-true`: coincidence (`additionalProperties`, `items`) / discards (property position) |  |  |  |
| boolean-schema-false | 3.1 | Schema Object (boolean form) | golden | variant scan `boolean-schema=false@schema.unevaluatedProperties`: 3 declarations in 1 fixture — `tamoss` (3) — the only literal `false` schema any registered source declares outside the `additionalProperties` position that `additional-properties-boolean-false` owns |  |  |  |
| ref-siblings | both | Schema Object.$ref | golden | variant scan `ref-siblings`: 1080 declarations in 20 fixtures — `airbyte.local-config` (5), `apache.org` (11), `apache.org-airflow` (11), `apicurio.local-registry` (24), `apideck.com-hris` (1), `asana.com` (20), `atlassian.com-jira` (1), `bunq.com` (853), `corrently.io` (1), `dnd5eapi.co` (4), `exhaustive` (4), `helios-verifiable-api` (1), `letta` (53), `maif.local-otoroshi` (10), `microcks.local` (18), `redocly.com-museum` (3), `sac-backend` (2), `tamoss` (43), `twilio.com-twilio_messaging_v1` (14), `worldcoin-signup-sequencer` (1); the sibling keywords declared are `description` (1037), `readOnly` (874), `type` (874), `writeOnly` (853), `default` (29), `nullable` (28), `deprecated` (2), `allOf` (1) and `example` (1). OAS 3.0 defines the siblings as ignored and 3.1 allows them, so the row is `both`: `letta`, `redocly.com-museum`, `tamoss` and `worldcoin-signup-sequencer` are the 3.1 documents among the twenty |  |  |  |
| recursive-graph | both | Schema Object.$ref | golden | variant scan `self-recursive-schema`: 21 declarations in 11 fixtures — `apideck.com-connector` (1), `appng-rest-api` (4), `atlassian.com-jira` (3), `bbci.co.uk` (1), `bungie.net` (3), `bunq.com` (3), `corrently.io` (1), `dnd5eapi.co` (1), `eozilla` (2), `recursive-types` (1), `tlon-notes` (1) — component schemas that reference themselves |  |  |  |
| mutually-recursive-graph | both | Schema Object.$ref | golden | variant scan `mutually-recursive-schema`: 13 declarations in 9 fixtures — `apideck.com-accounting` (1), `atlassian.com-jira` (2), `bunq.com` (1), `canada-holidays.ca` (1), `dnd5eapi.co` (1), `groundhog-day.com` (1), `openepcis-dpp-ready` (2), `recursive-types` (1), `squareup.com` (3) — reference cycles closed through two or more distinct component schemas |  |  |  |
| cycle-via-additionalProperties | both | Schema Object.additionalProperties | golden | variant scan `cycle-via-additionalProperties`: 2 declarations in 2 fixtures — `appng-rest-api` (1), `eozilla` (1); `docs/fern-limitations.md` `cycle-via-additionalProperties`: implements |  |  |  |
| nesting-depth-ge-15 | both | Schema Object (inline nesting depth) | golden | variant scan `schema-depth>=15`: 91 declarations in 1 fixture — `openbanking.org.uk-account-info-openapi` (91); `docs/fern-limitations.md` `nesting-depth-ge-15`: implements — the same 91 nodes that file measured |  |  |  |
| all-of-nested-composition | both | Schema Object.allOf | golden | variant scan `allOf-nested-in-composition`: 6 declarations in 3 fixtures — `box.com` (3), `exhaustive` (2), `tamoss` (1) — `allOf` declared on a schema that is itself a member of an `allOf`, `anyOf`, `oneOf` or `not` |  |  |  |
| any-of-nested-composition | both | Schema Object.anyOf | golden | variant scan `anyOf-nested-in-composition`: 2 declarations in 2 fixtures — `box.com` (1), `free5gc-namf-communication` (1) — `anyOf` declared on a schema that is itself a member of an `allOf`, `anyOf`, `oneOf` or `not` |  |  |  |
| one-of-nested-composition | both | Schema Object.oneOf | golden | variant scan `oneOf-nested-in-composition`: 36 declarations in 4 fixtures — `box.com` (8), `free5gc-namf-communication` (1), `letta` (26), `sigstore-rekor` (1) — `oneOf` declared on a schema that is itself a member of an `allOf`, `anyOf`, `oneOf` or `not` |  |  |  |
| not-nested-composition | both | Schema Object.not | golden | variant scan `not-nested-in-composition`: 3 declarations in 1 fixture — `free5gc-namf-communication` (3) — `not` declared on a schema that is itself a member of an `allOf`, `anyOf`, `oneOf` or `not` |  |  |  |
| discriminator-property-name | both | Discriminator Object.propertyName | golden | census `schema.discriminator.propertyName`: 99 declarations in 11 fixtures — `apache.org` (1), `apache.org-airflow` (1), `appng-rest-api` (1), `atlassian.com-jira` (3), `discriminated-unions` (1), `letta` (68), `microcks.local` (1), `openepcis-dpp-ready` (1), `recursive-types` (1), `sigstore-rekor` (12), `tlon-notes` (9) |  |  |  |
| discriminator-mapping | both | Discriminator Object.mapping | golden | census `schema.discriminator.mapping`: 68 declarations in 7 fixtures — `atlassian.com-jira` (3), `discriminated-unions` (1), `letta` (56), `microcks.local` (1), `openepcis-dpp-ready` (1), `recursive-types` (1), `tlon-notes` (5) |  |  |  |
| xml-name | both | XML Object.name | golden | census `schema.xml.name`: 69 declarations in 3 fixtures — `amazonaws.com-cloudfront` (22), `atlassian.com-jira` (43), `byautomata.io` (4) |  |  |  |
| xml-namespace | both | XML Object.namespace | golden | census `schema.xml.namespace`: 11 declarations in 1 fixture — `amazonaws.com-cloudfront` (11) |  |  |  |
| xml-prefix | both | XML Object.prefix | gap | census `schema.xml.prefix`: 0 declarations across all 126 registered sources; no `docs/fern-limitations.md` row names it | `none` — crozier never deserializes the XML Object: `src/openapi.rs`'s `Schema` has no `xml` field and `grep -rF '"xml"' src/` finds no production read | No generated artifact: `docs/fern-limitations.md` `xml-request` and `xml-response` (both `discards`, both `bodies-media` rows) measure Fern dropping XML payloads whole, so element metadata reaches no emitted Python. | UNREACHABLE — recording that the generated Python SDK has no XML serializer for the prefix to shape is the settlement. |
| xml-attribute | both | XML Object.attribute | golden | census `schema.xml.attribute`: 28 declarations in 1 fixture — `atlassian.com-jira` (28) |  |  |  |
| xml-wrapped | both | XML Object.wrapped | gap | census `schema.xml.wrapped`: 0 declarations across all 126 registered sources; no `docs/fern-limitations.md` row names it | `none` — as `xml-prefix`; the one hit `grep -rF '"wrapped"' src/` returns is a test document inside `src/ir.rs` | No generated artifact, for the reason `xml-prefix` gives: the measured Fern behaviour is to drop the XML payload that `wrapped` would shape. | UNREACHABLE — as `xml-prefix`. |

## Method notes

### What was run

    just surface-census --json      # 407 selectors, 603390 declaration sites, 124 sources
                                    # (31 vendored + 93 fetched link-ok)

Every `census …` evidence cell is a slice of that output; a single selector can
be re-checked without the whole run:

    just surface-census --selector schema.patternProperties

The joins on the measured-Fern ledger are against the key list that

    grep -oP '^\| `\K[A-Za-z0-9._-]+(?=` \| *[0-9]+ \|)' docs/fern-limitations.md | sort -u

reports, and each `limitations` row spells its verdict the way that file's
`How to read a verdict` section spells it.

### The supplementary variant scan

Twenty-five of the rows above are value kinds or graph shapes the census grammar
merges (see [above](#the-instrument-and-the-one-thing-it-cannot-see)). This scan
splits them, and it does that by **subclassing the census's own `Census`** rather
than walking documents itself: `walk` and `descend` are overridden to observe the
schema nodes the inherited traversal already reaches and then delegate to it, so
`$ref` transparency, the alias guard, MAP/LIST descent and `x-` skipping keep
their single definition in `scripts/openapi-surface-census.py`. It reads the same
sources through the same `registered_sources()` and `load_document`, and refuses
an unfetched or non-mapping source the way the census does, so a source that
reports nothing is never confused with one that declares nothing. Save it to a
scratch file and run it from the repo root, under the interpreter the census
recipes use, after `just surface-census` has fetched the `link-ok` half:

    "$(./scripts/census-python.sh)" /tmp/variant-scan.py

```python
"""Variant scan: the value kinds and graph shapes the census does not split.

Subclasses the census's own `Census`, so the traversal contract — `$ref`
transparency, the alias guard, MAP/LIST descent, `x-` skipping — keeps its single
definition in `scripts/openapi-surface-census.py` and this scan only observes the
nodes that walk already reaches. Run from the repo root under the interpreter
`scripts/census-python.sh` names, after `just surface-census` has fetched the
link-ok half.
"""
import collections, importlib.util, sys
spec = importlib.util.spec_from_file_location("c", "scripts/openapi-surface-census.py")
c = importlib.util.module_from_spec(spec); sys.modules["c"] = c; spec.loader.exec_module(c)
COMPOSITION = ("allOf", "anyOf", "oneOf", "not")
facts, sources = collections.Counter(), collections.defaultdict(collections.Counter)

class VariantCensus(c.Census):
    """The census's walk, with the value kinds and reference edges it merges noted."""

    def __init__(self, fixture):
        super().__init__()
        self.fixture, self.position, self.owner, self.depth = fixture, "openapi", None, 0
        self.edges = collections.defaultdict(set)

    def note(self, fact, n=1):
        facts[fact] += n
        sources[fact][self.fixture] += n

    def walk(self, node, kind, prefix, seen):
        """Observe a schema node, then hand it to the census's own traversal."""
        if kind == "schema":
            if isinstance(node, bool):
                return self.note(f"boolean-schema={str(node).lower()}@{self.position}")
            if isinstance(node, dict) and id(node) not in seen:
                self.note_schema(node)
        super().walk(node, kind, prefix, seen)

    def descend(self, value, child, selector, seen):
        """Carry the position, the schema depth and the owning component name down."""
        position, depth, owner = self.position, self.depth, self.owner
        self.position = selector
        if child.kind == "schema":
            self.depth += 1
        if selector == "components.schemas" and isinstance(value, dict):
            for name, entry in value.items():
                self.owner = name
                super().descend({name: entry}, child, selector, seen)
        else:
            super().descend(value, child, selector, seen)
        self.position, self.depth, self.owner = position, depth, owner

    def note_schema(self, node):
        if self.depth >= 15:
            self.note("schema-depth>=15")
        if "additionalProperties" in node:
            v = node["additionalProperties"]
            self.note("additionalProperties=" + ("true" if v is True else "false" if v is False
                      else "schema" if isinstance(v, dict) else "other"))
        for kw in ("exclusiveMaximum", "exclusiveMinimum"):
            if kw in node:
                kind = self.kind_of(node[kw])
                self.note(f"{kw}=" + ("numeric" if kind in ("integer", "float")
                          else kind if kind == "boolean" else "other"))
        t = node.get("type")
        if isinstance(t, list):
            self.note("type=array")
            if "null" in t:
                self.note("type=array-with-null")
            if len([x for x in t if x != "null"]) >= 2:
                self.note("type=array-multi-nonnull")
        elif isinstance(t, str):
            self.note("type=scalar" + ("-null" if t == "null" else ""))
        if "const" in node:
            self.note("const=" + self.kind_of(node["const"]))
        members = node.get("enum")
        if isinstance(members, list):
            for member in members:
                self.note("enum-member=" + self.kind_of(member))
        elif members is not None:  # not a list: it has no members to classify
            self.note("enum-not-a-list")
        if "$ref" in node:
            if any(k in c._SCHEMA_FIELDS for k in node if k != "$ref"):
                self.note("ref-siblings")
            target = node["$ref"]
            if self.owner and isinstance(target, str) and target.startswith("#/components/schemas/"):
                self.edges[self.owner].add((target.rsplit("/", 1)[-1],
                                            self.position == "schema.additionalProperties"))
        if self.position.endswith(COMPOSITION):
            for kw in COMPOSITION:
                if kw in node:
                    self.note(f"{kw}-nested-in-composition")

    @staticmethod
    def kind_of(value):
        for kind, test in (("boolean", bool), ("integer", int), ("float", float),
                           ("string", str), ("object", dict), ("array", list)):
            if isinstance(value, test):
                return kind
        return "null" if value is None else "other"

    def note_cycles(self):
        """Classify the `$ref` edges between component schemas into cycle facts."""
        colour, stack = {}, []
        def visit(n):
            colour[n], _ = 1, stack.append(n)
            for target, via_ap in sorted(self.edges.get(n, ())):
                if target == n:
                    self.note("self-recursive-schema")
                    if via_ap:
                        self.note("cycle-via-additionalProperties")
                elif colour.get(target) == 1:
                    self.note("mutually-recursive-schema")
                    cycle = stack[stack.index(target):]
                    if any(t == b and ap for a, b in zip(cycle, cycle[1:] + [target])
                           for t, ap in self.edges.get(a, ())):
                        self.note("cycle-via-additionalProperties")
                elif not colour.get(target):
                    visit(target)
            stack.pop(); colour[n] = 2
        sys.setrecursionlimit(20000)
        for n in sorted(self.edges):
            if not colour.get(n):
                visit(n)

registered = c.registered_sources(c.Path("tests/fixtures"), c.Path(".local/corpus"), False)
unfetched = [s.fixture for s in registered if s.path is None]
if unfetched:  # a source reporting nothing and one declaring nothing are not the same answer
    sys.exit(f"variant-scan: {len(unfetched)} registered source(s) have not been fetched, "
             f"starting with {unfetched[0]!r}. Run 'just surface-census', which fetches first.")
for s in registered:
    try:
        document = c.load_document(s.path)
    except c.DocumentError as error:
        sys.exit(f"variant-scan: {s.fixture}: {error}")
    if not isinstance(document, dict):
        sys.exit(f"variant-scan: {s.fixture}: {s.path} is not an OpenAPI document "
                 "(its root is not a mapping)")
    scan = VariantCensus(s.fixture)
    scan.walk(document, "openapi", "openapi", frozenset())
    scan.note_cycles()
for fact, n in sorted(facts.items()):
    where = ", ".join(f"{f} ({k})" for f, k in sorted(sources[fact].items()))
    k = len(sources[fact])
    print(f"{fact}: {n} declarations in {k} fixture{'s' if k != 1 else ''} — {where}")
```

**The self-check that makes it trustworthy.** For every keyword the scan splits,
its parts must add back up to the census's own count for that keyword, because
both are counting the same declaration sites:

| keyword | census | variant scan |
|---|---|---|
| `schema.additionalProperties` | 1930 | 219 `true` + 1177 `false` + 534 schema = 1930 |
| `schema.type` | 108256 | 106386 single + 1370 `"null"` single + 500 array = 108256 |
| `schema.exclusiveMinimum` | 76 | 54 boolean + 22 numeric = 76 |
| `schema.exclusiveMaximum` | 8 | 8 boolean + 0 numeric = 8 |
| `schema.const` | 147 | 147 string + 0 boolean + 0 integer = 147 |

Two of its results are corroborated by `docs/fern-limitations.md`'s own
independent round-4 counts: `type-array-with-null` at 498 occurrences in 3
documents, and `nesting-depth-ge-15` at 91 nodes in
`openbanking.org.uk-account-info-openapi`.

**Nothing gates that table, so re-run it rather than trusting it.** `just check`
runs `just test-surface-census`, which covers the census script; the subclass
above lives in this file and is nobody's test. Subclassing removes the way the two
could disagree about *which nodes exist* — there is one traversal, and a rename in
the script fails the scan loudly on the next run rather than quietly counting
something else — but the numbers in the table are still two separate runs, so the
reconciliation is to take them again, about four minutes together:

    just surface-census --json                             # the census half
    "$(./scripts/census-python.sh)" /tmp/variant-scan.py   # the scan half

Re-running is also how a maintainer learns that the corpus itself moved: a
registered row added or refreshed changes both halves, and the table above is the
count as of the run that wrote it. The follow-up in
[Two things a future maintainer should know](#two-things-a-future-maintainer-should-know)
retires the split entirely by teaching the script to emit these selectors itself.

### What the classification rests on, row by row

- **`golden`** (85 rows) — a registered source declares the feature. The evidence
  is the census, or the variant scan for the variants it cannot see. The last of
  them is `dollar-comment`, which corpus row 109 (`volview-backend-contract`)
  moved here straight from the probe backlog, on the witness the issue #188 search
  found it; `format-duration` before it came from the `FIXTURE` backlog, on corpus
  row 97 (`mosip-esignet`).
- **`limitations`** (5 rows) — no registered source declares it and the ledger
  rules on it: `const-boolean`, `const-integer`, `enum-member-float`,
  `enum-member-object`, and `boolean-schema-true` outside the
  `additionalProperties` position.
- **`gap`** (26 rows) — neither. How those 26 split across the three settlement
  classes is not written down a second time here: it is
  [`openapi-surface-coverage.md`](../openapi-surface-coverage.md#what-the-walk-enumerated)'s
  summary table, which `RankedBacklogTests` recomputes from this file's own rows,
  so a copy in this file would be the one place a reclassification could leave
  stale. The three `UNREACHABLE` ones are worth naming because nothing else does:
  `format-assertion-vocabulary`, `xml-prefix` and `xml-wrapped`.

Every `gap` row's `crozier sites` cell is `none`, and each row states the `grep`
that measured it rather than resting on a list copied out of the source. The
whole-file version of that measurement — the set of wire keys crozier can read at
all — is re-derived, not restated:

    awk '/^pub struct Schema \{/,/^\}/' src/openapi.rs | grep -E 'serde\(|    pub '

Every field of that struct either carries a `rename` to its wire key or takes its
Rust name verbatim, and the two `#[serde(skip)]` fields are not wire keys at all.
Read the output as the closed set: `Schema` has no `deny_unknown_fields` and no
flattened catch-all, so serde silently drops every key outside it and no `src/`
file can reach one. Re-run the command after touching the struct; a keyword added
there turns some `gap` row's `none` into a real site.

The format rows are the one nuance, and the same rule applies:

    grep -n '\.format' src/ir.rs

returns eleven lines, one of them a doc comment and ten of them reads of
`schema.format`, and the values they name are `binary`, `byte`, `date`,
`date-time`, `int64` and `uuid`. An unnamed format is read as the keyword and
dropped as a value, which is why those rows say `none` *for this value*.

### Two things a future maintainer should know

- The census grammar carries `schema.definitions`, a Draft-4 leftover neither
  OpenAPI 3.0 nor 3.1 defines. It is not enumerated above and no registered
  source declares it.
- The variant scan lives in this document rather than in `scripts/` because
  extending the instrument was out of this classification's scope. Teaching
  `scripts/openapi-surface-census.py` to emit valued selectors for
  `schema.additionalProperties`, `schema.const`, `schema.enum` members and
  `schema.type` arity — and a selector for a boolean schema — would retire the
  scan and let those 25 rows quote `just surface-census` directly.

### Witness search (issue #188)

The six schema-keyword rows below reached this subsection settling on `PROBE`
because no **registered** source declares them — a statement about a 126-spec
sample, not about the world. Three no longer do. `dollar-anchor` and
`format-relative-json-pointer` are `FIXTURE` backlog entries: the search found each
a publisher-owned witness Fern accepts, blocked only by that document's licence —
absent in one case, proprietary in the other — and a witness blocked on
redistribution is a supply problem rather than a shape no corpus row could pin.
`dollar-comment` left the backlog altogether: its witness is Apache-2.0, so it is
registered as corpus row 109, `volview-backend-contract`, and the row is `golden`
on a byte-matching Fern 5.20.0 golden. Which of the rest still settle on `PROBE`
is each one's own `settlement` cell in the entry table above, not a count restated
here.

Issue #188 asks for the second statement, so each row was searched across the
same five sources, with only the searched keyword changing. Each row's last cell
gives the exact query text with that keyword substituted, and the count it
returned; every row below names all five, and the surface-census reconciliation
refuses one that drops any of them.

- **APIs.guru / `openapi-directory`** — both the served index
  (`https://api.apis.guru/v2/list.json`, every version's `swaggerUrl` fetched)
  and a `--depth 1` clone of the repository, each grepped for the JSON and the
  YAML spelling of the keyword.
- **GitHub code search** — `gh search code '"<keyword>" filename:openapi.yaml'`
  and the same query over `openapi.json`, `openapi.yml`, `swagger.yaml`,
  `swagger.json` and `path:openapi`, widened to the `language:JSON` and
  `language:YAML` caps where the narrow queries returned little.
- **SwaggerHub public registry** — `https://api.swaggerhub.com/specs?query=…`
  first, and, because that endpoint searches names and descriptions rather than
  spec bodies, the whole public 3.1/3.2 registry enumerated by page and every
  body fetched and grepped.
- **Postman public API network** — `POST https://www.postman.com/_api/ws/proxy`
  against the `search-all` service over the `apinetwork.team`,
  `runtime.collection` and `adp.api` indices.
- **Vendor developer portals** — the portals this region's own domain implies.
  That domain is the JSON Schema 2020-12 dialect and the registries that publish
  it, so the thirteen documents searched are `spec.openapis.org`,
  `json-schema.org`, the OAI's own `learn.openapis.org` 3.1 examples, Apicurio
  Registry, and four large vendor 3.1 documents (OpenAI, Stripe, Cloudflare, and
  Google Gemini via WSO2).

**A witness is a document a real API publishes as its own description.** Three
kinds of document fail that standard however faithfully they declare the keyword,
and each was recorded as a witness by an earlier pass of this search before being
demoted to a candidate below. They are set out here rather than left to the
ledger because getting them wrong is what the search kept getting wrong: each
looks more like a witness than the one before it, and the third looks exactly
like one.

The first is a **synthetic fixture** — a spec somebody checked into a repository
to exercise a parser. Registering one would put a document into the corpus that no
real API ever served, and issue #188's argument is about what the *world*
publishes, not about what a test suite can be made to contain. Three are demoted
on that ground: `eve0415/oasts` `fixtures/validators-showcase-3.1/openapi.yaml` (a
`fixtures/` document whose server is `https://validators.api.example.test/v1`),
`speakeasy-api/openapi` `openapi/testdata/petstore31.openapi.json` (the Swagger
Petstore 3.1 sample, under `testdata/`), and `justiceo/openapi-zod`
`test/fixtures/primitives/openapi.yaml` (a `paths: {}` document enumerating one
component per `format`).

The second is an **unofficial third-party transcription** — an accurate
description of a real, live API written by somebody other than its publisher. It
fails for a narrower reason than a fixture does, and it is a real distinction
rather than a technicality: the publisher never served those bytes, so nothing
about them is guaranteed to track the API, and byte-matching Fern over them
verifies crozier against a document with no owner to be right about. One is
demoted on that ground: `darai0512/payjp-rust` `openapi/docs/openapi.json`, whose
repository describes itself as an "Unofficial Pay.jp OpenAPI spec".

The third is an **unproven third-party distribution** — a document hosted by
somebody other than the publisher that neither claims nor disclaims independent
authorship. It is the hardest of the three to see, because it reads like the real
thing: the content names the publisher, and the natural inference is that the
publisher wrote it. That inference is not evidence. What would settle it is a
reference the publisher itself serves, or a recorded provenance chain ending at
one; absent either, "the publisher authored these bytes" is a guess, and a `PROBE`
is not retired on a guess. Two `speakeasy-sdks/*` documents are demoted on that
ground, and the Orb one is worth stating in full because it is the case that
looked strongest: `speakeasy-sdks/orb-python` `openapi/openapi.yaml` carries Orb's
contact, terms and production server, and an earlier commit on this branch
recorded it as the `dollar-anchor` witness on exactly that basis. The provenance
does not survive checking. Orb publishes no OpenAPI document anywhere this search
could reach — nothing in its own `orbcorp` organisation (its eight SDK
repositories carry `.stats.yml` and `api.md`, no spec), and
`docs.withorb.com/openapi.{json,yaml}`, `docs.withorb.com/api-reference/openapi.json`
and `withorb.com/openapi.json` all 404 while `api.withorb.com/openapi.json` 403s.
The Speakeasy repository's own generation workflow reads
`openapi_doc_location: ./openapi/openapi.yaml`, so it regenerates from the copy in
its own tree and records no upstream URL: the chain terminates inside the
third-party repository, and there is nothing left to check it against.

**A document being publisher-owned is therefore load-bearing, and it decides
three of the six rows outright.** `inkeep/chat-api-openapi-schema` sits in
Inkeep's own organisation and describes Inkeep's own production API, which is why
it is recorded where the Orb copy is not, and why `dollar-anchor` is
`witness-blocked` rather than `none-found` — it is blocked only because that
repository declares no licence. `geo-engine/BioIS` passes the same test for
`format-relative-json-pointer` and is blocked only by a proprietary one.
`Kitware/VolView` is publisher-owned on the same test *and* Apache-2.0, which is
why `dollar-comment` is the one `witness-found` row the table has left — and the
one row this search retired outright, as corpus row 109. The other three rows have
no publisher-owned candidate at all, and that — not a shortage of declarations —
is why they read `none-found`.

Re-searching for replacements produced a different outcome per row rather than one
verdict for the region, which is why the table records outcomes per row.

Every candidate that *looked* like a declaration was fetched at the pinned ref
recorded below and the declaration **counted in the fetched bytes**, never read
off a search-result snippet. That matters more than it sounds: GitHub code search
tokenizes away the leading `$`, so candidates match the bare word and declare
nothing, and among those that do contain the string, several carry it only inside
an `example:` value, a backticked mention in a `description:`, or an enum of
reserved keyword names — each is recorded as such below rather than counted.
Surviving candidates were screened with `fern check` in a throwaway workspace
wired the way `scripts/generate-fern-fixture.sh` wires one; the launcher on PATH
is `fern` 5.113.1 and `fern.config.json` pins the CLI it actually runs at 5.67.1,
the same `FERN_CLI_VERSION` that script defaults to.

**An `outcome` here and a `settlement` in the entry table are two different
statements, and they are allowed to disagree.** This subsection records what is
true of the world; the `settlement` cell records what class of work the row
needs. What each outcome implies for that cell is
[`openapi-surface-coverage.md`](../openapi-surface-coverage.md#the-settlement-rule-as-amended)'s
rule, not this file's, and this file follows it rather than restating it. Two of
its consequences are worth naming where a reader meets the outcomes:

- `dollar-anchor` and `format-relative-json-pointer` read `witness-blocked` and
  settle `FIXTURE`. Both settlements moved with no corpus row registered, because
  a witness found and then blocked is a screening failure of that document rather
  than evidence the feature has none. Neither blocked document's own bar is
  touched by that: nothing registers until a declarer arrives under a
  redistribution-compatible licence.
- `dollar-comment` reads `witness-found` and is `golden`, not a `gap` at all. Its
  witness is Apache-2.0 and Fern accepts it, so the rule made the row registrable
  and the change that owns it registered it — corpus row 109,
  `volview-backend-contract`, whose Fern 5.20.0 golden crozier reproduces byte for
  byte. A `witness-found` outcome is therefore not a settlement class of its own:
  it is the one outcome that can retire a row to `golden` outright.

**The twenty `schemas` rows of the ranked `FIXTURE` backlog were searched next**, and
they are the second block of the table below — `contains`, `content-media-type`,
`content-schema`, `dependent-required`, `dependent-schemas`, `dollar-defs`,
`exclusive-maximum-numeric`, the seven `format-*` rows, `max-contains`, `min-contains`,
`multiple-of`, `property-names` and `unevaluated-items`. Two of them, `dollar-anchor` and
`format-relative-json-pointer`, already had a line here; those two were not re-answered
but re-*asked*, for a second declarer beyond the blocked one, and each line records that
re-search at its end rather than restating the first one's answer.

Three things about that pass are worth stating once rather than twenty times.

**It asked a sixth source, and that source leads each cell rather than joining the
bulleted list above.** The sixth is **Sourcegraph public code search**, a credential-free
*literal* index: `content:"$anchor:"` matches the `$` and the colon that GitHub's
tokenizer drops, which is exactly the difference between finding a `$defs` declaration and
finding the English word *defs*. It is recorded at the head of each of these twenty rows'
last cell instead of as a bullet above, because a bullet above would claim the six rows
before them had been put to it, and they were not. Its refusals are recorded the same way:
Sourcegraph rate-limits a client that has asked it forty `count:all` questions, and it
refused **nine of those forty** for the rest of this dispatch — the two queries of each of
the five `format-*` rows, less one that answered after a long cool-down. Each refusal is
recorded in its own row with the HTTP status and the interstitial it returned, and no row
reads those nine as a zero. Two mechanical notes for re-running a query out of a cell: a
`\|` inside one is markdown escaping a table separator and the query itself carries a
literal `|`, and `<url>` and `<query>` are placeholders the surrounding sentence fills.

**What each source's number counts, so that a zero and a large number mean the same thing
in each row.** The two APIs.guru readings, the SwaggerHub sweep and the vendor set report
**declaring documents out of documents read whole** — 3992 served versions and 4138 clone
files, 7666 registry specs, 29 vendor documents, every one of them fetched and walked, so
their zeros are exhaustive over what they cover. The three indices cannot report that,
because an index returns hits rather than documents: **GitHub** reports its own
`total_count`, of which the first page — up to 100 items per query — is what was read, and
the candidates worth fetching out of those pages are the ledger below; **Sourcegraph**
reports its `matchCount` and the number of distinct files behind it, of which up to 60 per
query were listed and the plausible ones fetched; **Postman** searches names and
descriptions rather than spec bodies, so its numbers are matching entities per index with
`size: 3` sampled and read — which is why `iri` scores 5478 there and declares nothing.

**A declaration was counted by walking the parsed document, never by trusting a search
hit**, and for these twenty rows that is what decides the answers rather than a detail of
method. A hit is a string anywhere in a file. `contains:` matches an APIs.guru description
that ends a sentence with that word, which is what the AWS and Google documents a literal
index returns for it are doing — `APIs/googleapis.com/cloudfunctions/v1` carries it inside
an `Event types m…` description string, and `APIs/amazonaws.com/rds/2013-02-12` does not
carry the YAML spelling at all; that is why this region's two APIs.guru readings report
**0** for `contains` where Sourcegraph reports 862 matches in 338 files. `iri` matches a
Postman workspace called `api_irina`. A `multipleOf` under `example:` is data rather than
a declaration. A component *property named* `contains` is not the applicator keyword. So
each candidate was parsed and walked instead: a key counts only in schema position,
subtrees under `example`, `examples`, `enum`, `const` and `default` are values and are
skipped, and the keys of `properties`, `$defs`, `schemas`, `responses` and the other named
maps are names rather than keywords. `exclusive-maximum-numeric` needs the *value*
inspected as well as the key, because the 3.0 boolean and the 3.1 numeric spelling are the
same key — which is the whole of what separates it from `exclusive-maximum-boolean`.

**A witness had to survive both halves of the Fern screen, not just the first.** Every
surviving candidate was run through `fern check` *and* a real
`fern generate --group python-sdk --local --preview`, in a throwaway workspace wired the
way `scripts/generate-fern-fixture.sh` wires one, at the pin this corpus's own provenance
records — CLI **5.67.1** from `fern.config.json` and generator `fernapi/fern-python-sdk`
**5.20.0**. Thirty candidates were screened that way and the split is the reason the
second half is not optional: `jentic/jentic-public-apis`'s Volt.io copy passes
`fern check` with 0 errors and then fails `fern generate` with
`Unknown OAuth type: 'undefined'`.

| key | outcome | witness | immutable ref | license | fern check | sources searched and the exact query used against each |
|---|---|---|---|---|---|---|
| `dollar-anchor` | witness-blocked | Inkeep Search and Chat API, `inkeep/chat-api-openapi-schema` `openapi.yaml` at `https://raw.githubusercontent.com/inkeep/chat-api-openapi-schema/76e79a7e60be63c5cb64fa2eefa0dadd3aa54707/openapi.yaml` — 14411 bytes fetched (MD5 `3421b267…`), **2** `$anchor` declarations counted in them: `$anchor: ChatModeOptions` at line 350 and `$anchor: RecordTypeEnumerated` at line 468, each naming a `oneOf` branch. **Publisher-owned**, which is what distinguishes it from every other candidate that declares `$anchor`: the repository is Inkeep's own GitHub organisation (not a fork, not archived), and the document is `openapi` 3.1.0 titled `Inkeep Search and Chat API` against `https://api.inkeep.com` marked `Production server`. **Blocked because it is not redistributable:** the repository declares no licence at all — its whole tree at that commit is `.gitignore`, `README.md`, `openapi.yaml`, `GET /repos/inkeep/chat-api-openapi-schema/license` answers `404 Not Found`, and the README is the single line `# chat-api-openapi-schema` — so nothing grants the corpus the right to vendor a golden derived from it. It is blocked on that alone: it is pinned to an immutable commit, its raw URL is credential-free HTTPS ending `.yaml`, its name is in neither `CORPUS.md` nor either DROPPED/REJECTED list, and Fern accepts it  **This node's re-search asked whether a second, usable `$anchor` witness exists, and it does not.** Every hit the six sources returned for a further declarer is a synthetic fixture (`swagger-api/apidom`'s 40-odd `$anchor-*` dereference fixtures, `corvus-dotnet/Corvus.JsonSchema` `OpenApi-Test-Suite/remotes/locationIndependentIdentifier.json`, `scalar/scalar` `packages/openapi-parser/tests/references/one-file/specification.json`, `fern-api/fern`'s own `deeply-recursive/openapi.yml`), a Petstore 3.1 sample (`apache/camel`, `speakeasy-api/openapi` `openapi/testdata/petstore31.openapi.json`, SwaggerHub's `frank-kilcommins/Swagger-Petstore-31/1.0.9-SNAPSHOT`), an OpenAPI meta-schema, a backticked `` `$anchor` `` inside a `description` (`wso2/api-platform`'s Gemini document), or one of the `speakeasy-sdks/*` unproven third-party distributions this table already demoted — to which the re-search adds exactly one name, `speakeasy-sdks/wingspan-python` `openapi.yaml`, which fails on the same ground as the Orb copy. So the further-witness answer for this row is `none-found`, and the row's own outcome stays `witness-blocked` because the Inkeep document is still the witness the corpus cannot use.| commit `76e79a7e60be63c5cb64fa2eefa0dadd3aa54707` | **none declared — the obstacle**; no `LICENSE` in the tree and the GitHub licence endpoint 404s | `All checks passed` (exit 0) — recorded even though the row is blocked, because it tells the consuming node the licence is the only thing in the way | **APIs.guru / `openapi-directory`:** `curl -sS https://api.apis.guru/v2/list.json` (2529 APIs, 3992 versions), then every version's `swaggerUrl` fetched and grepped for `"$anchor"` and the YAML spelling `$anchor:` — **0** declarations, 0 fetch errors, the 8 `swaggerUrl`s containing a space re-fetched percent-encoded; independently, `git clone --depth 1 https://github.com/APIs-guru/openapi-directory` at `f04b8d0b` and `grep -rlF '$anchor' APIs` over its 4138 spec files under 701 provider directories — **0** files. **GitHub code search:** `gh search code '"$anchor" filename:openapi.yaml'` → 9 files, `… filename:openapi.json` → 6, `… filename:swagger.yaml` → 0, `… filename:openapi.yml` → 20; the fetched-and-counted survivors are in the candidate ledger below. A second pass re-ran and widened that net: `gh search code '"$anchor" filename:openapi.yaml'` → 9, `… filename:openapi.json` → 6, `… filename:openapi.yml` → 20, `… filename:swagger.yaml` → 1, `… filename:swagger.json` → 2, `'"$anchor" path:openapi'` → 39 (the `filename:swagger.yaml` count moved 0 → 1 between the two passes, which is GitHub's index changing under the same query rather than a disagreement to resolve), `'"$anchor" "paths"'` → 0, and the two broadest the API allows, `'"$anchor" language:JSON'` → 100 (the cap) and `'"$anchor" language:YAML'` → 100 (the cap). Those nine return 254 distinct files. The recorded document came out of them, and the four others that looked like a service's own description were fetched and are false positives — `Ira-Panasiuk-2024/Money-Guard-backend` and `OksanaSmoliar/project-CodeSamurai-backend` `docs/swagger.json` carry `$anchor:` inside an image-URL `description` string, and `solana-foundation/pay-skills` and `simonliu-ai-product/Agentimon` carry a backticked `` `$anchor` `` in Google's Gemini prose listing the JSON Schema keywords it supports. **SwaggerHub:** `https://api.swaggerhub.com/specs?query=%24anchor&limit=5` → `totalCount` 14, all title matches (`Amazon`, `defaultTitle`, `dtl`) because that endpoint searches names and descriptions, not spec bodies; so the whole public 3.1/3.2 registry was enumerated instead with `https://api.swaggerhub.com/specs?specification=openapi-3.1.x&limit=100&page=N` and `…&specification=openapi-3.2.0…` (7683 specs, 0 fetch errors), each body fetched and grepped — **2** declare `$anchor` (`frank-kilcommins/Swagger-Petstore-31/1.0.9-SNAPSHOT`, `test-2a8-f7e/qtTestSchema/1.0.1`), both blocked (see the ledger). **Postman:** `POST https://www.postman.com/_api/ws/proxy` with `{"service":"search","path":"/search-all","body":{"queryIndices":["apinetwork.team","runtime.collection","adp.api"],"queryText":"$anchor","size":25,"domain":"public"}}` → totals `team` 161, `collection` 21, `api` **0**; that index carries collection and workspace metadata rather than schema bodies, and the returned names are word matches on "anchor". **Vendor portals:** `https://spec.openapis.org/oas/3.1/dialect/base`, `…/oas/3.1/meta/base`, `https://json-schema.org/draft/2020-12/schema`, `…/meta/core`, `…/meta/format-annotation`, `https://raw.githubusercontent.com/OAI/learn.openapis.org/main/examples/v3.1/{tictactoe,webhook-example,non-oauth-scopes}.json`, Apicurio Registry `common/src/main/resources/META-INF/openapi.json`, `openai/openai-openapi@master/openapi.yaml`, `stripe/openapi@master/openapi/spec3.json`, `cloudflare/api-schemas@main/openapi.json`, `wso2/api-platform@main/llm-provider-specs/gemini/openapi.yaml` — **0** `$anchor` declarations; `json-schema.org/draft/2020-12/meta/core` declares it once but is a JSON Schema at an extensionless URL, and the one occurrence in the WSO2 Gemini document is a backticked `` `$anchor` `` inside a description string at line 5027. `…/examples/v3.1/petstore.json` now 404s — that directory holds `tictactoe`, `webhook-example` and `non-oauth-scopes` today **Re-searched for a further witness by this node.** **Sourcegraph** — the literal index the first pass never asked, which matters most here because GitHub tokenizes the leading `$` away: `content:"\"$anchor\"" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 70 matches in 54 files and `content:"$anchor:" …` → 80 in 19, every one of them a fixture, a test resource, a Petstore copy or a meta-schema. **APIs.guru** re-swept whole rather than sampled — `curl -sS https://api.apis.guru/v2/list.json`, all 3992 versions fetched and each *parsed* body walked for an `$anchor` key in schema position → **0** declaring, and the `--depth 1` clone at `f04b8d0b` walked the same way over 4138 files → **0**. **GitHub** `gh api -X GET search/code -f q='"$anchor" filename:openapi.yaml'` → 9 and `… filename:openapi.json` → 6, plus `"$anchor" path:openapi extension:yaml` → 4, `… extension:json` → 4, `"$anchor" path:swagger` → 105. **SwaggerHub** the whole public 3.1/3.2 registry re-enumerated and re-fetched — 7666 distinct specs walked → **2** declaring, both named above. **Postman** `queryText: "$anchor"` → `apinetwork.team` 167 teams, `runtime.collection` 22 collections, `adp.api` 0, and none of those is a spec body. **vendor portals** the 29 publisher-served documents in this node's vendor set, walked the same way → **0** declaring. |
| `dollar-comment` | witness-found (**registered**, corpus row 109) | VolView backend contract, `Kitware/VolView` `backend-contract/generated/openapi.json` at `https://raw.githubusercontent.com/Kitware/VolView/496ada04c1d07235f98c92a68b5c374c7188ee87/backend-contract/generated/openapi.json` — 66352 bytes fetched, **2** `"$comment"` declarations counted in them, on `components.schemas.TaskSpec` and `components.schemas.AnnotationsFile`. **Publisher-owned**, on the standard this subsection applies to every row: `Kitware/VolView` is Kitware's own organisation (not a fork, `homepage` `https://volview.kitware.com`) and the document is VolView's own "neutral backend contract" — `jsonSchemaDialect` 2020-12, 9 real paths (`/tasks`, `/jobs/{jobId}/results`, …) that VolView's client calls — generated into that repository rather than hosted there by a third party. It is **not a fixture**, which is what the three demoted candidates are: `backend-contract/README.md` at the same ref describes this tree as a draft `0.x` contract shipped in the `volview` npm package whose *"sole known consumer"* is `girder_volview`, so those nine paths are the surface a real backend serves and VolView's real client calls rather than payloads written to exercise a parser. Two properties a reader should weigh anyway, recorded here rather than left to be found: the contract declares itself a draft (`info.version` `0.2.0`), and its single `servers` entry is the template `{baseUrl}` rather than a production host, because each backend maps the neutral surface onto its own URLs | commit `496ada04c1d07235f98c92a68b5c374c7188ee87` | Apache-2.0 (repository `LICENSE`) | `All checks passed` (exit 0) | **APIs.guru / `openapi-directory`:** the same two sweeps, grepping `"$comment"` and `$comment:` — **0** declarations across the 3992 served versions; the clone's raw sources carry exactly one file containing the string, `APIs/akeneo.com/1.0.0/swagger.yaml`, where four occurrences sit inside an `example:` value that is itself a JSON Schema document in a Swagger 2.0 spec, and the converted `https://api.apis.guru/v2/specs/akeneo.com/1.0.0/swagger.json` APIs.guru actually serves contains 0. **GitHub code search:** `gh search code '"$comment" filename:openapi.yaml'` → 11, `… filename:openapi.json` → 30, `… filename:swagger.yaml` → 1, `… filename:openapi.yml` → 48. **SwaggerHub:** `https://api.swaggerhub.com/specs?query=%24comment&limit=5` → `totalCount` 81, all title matches (`Akeneo PIM REST API`, …); the enumerated 7683-spec sweep found **6** declaring specs — `PangaeaInnovations/grid-parameters-v3-unified-grid-schema-api/3.0.0` (75), `1584335/redocly-museum_api/1.0.0` (19), `frr-94c/sirene-api/3.11` (14), `sandeep.abhyankar/IDMAuthentication/1.0.0`, `Mafia-Mystery/mafia-mystery/1.0`, `test-450-85a/myparcel-api/2025-02-13` (1 each) — all blocked (see the ledger). **Postman:** the same `search-all` call with `queryText:"$comment"` → totals `team` 65, `collection` 460, `api` **4**; the four `api` results are `2. Comment`, `Comments`, `fangle-comment-api` and a blog `comment-list-create` URL, i.e. word matches on "comment", not declarations. **Vendor portals:** the same thirteen documents listed for `dollar-anchor` — **0** `$comment` in the OpenAPI ones; `https://json-schema.org/draft/2020-12/schema` declares it 5 times and `…/meta/core` twice, but both are JSON Schemas rather than OpenAPI documents and their URLs end in no `.json`/`.yaml`/`.yml`, so neither is a direct spec URL |
| `dollar-dynamic-anchor` | none-found | none. No document a real API publishes as its own description declares `$dynamicAnchor` anywhere the five sources below reach. Every candidate that does declare it is a synthetic fixture, a generator's example output, a unit-test resource, a copy of the OpenAPI 3.1 meta-schema, an unofficial third-party transcription, or reachable at no immutable ref; each is in the candidate ledger with the count read out of its fetched bytes and what became of it. The single closest miss is recorded there too: `darai0512/payjp-rust` `openapi/docs/openapi.json` describes a live commercial API and declares the keyword, but its own repository calls it an *unofficial* Pay.jp spec, so it is a transcription of a publisher's API rather than the publisher's own description of it | — | — | — | **APIs.guru / `openapi-directory`:** the same two sweeps, grepping `"$dynamicAnchor"` and `$dynamicAnchor:` — **0** declarations across the 3992 served versions and **0** files across the clone's 4138. **GitHub code search:** `gh search code '"$dynamicAnchor" filename:openapi.yaml'` → 4, `… filename:openapi.json` → 6, `… filename:swagger.yaml` → 0, `… filename:openapi.yml` → 0, `… path:*.openapi.yaml` → 0, `… path:*.openapi.json` → 0, `… filename:api.yaml` → 4, `… filename:api.json` → 6 (the `filename:` qualifier matches on a suffix, so these two re-return the `openapi.*` hits), `… filename:spec.yaml` → 0, `… filename:swagger.json` → 0, `'"$dynamicAnchor" "jsonSchemaDialect"'` → 0, `'"$dynamicAnchor" "operationId"'` → 0, `'"$dynamicAnchor" "requestBody"'` → 0, `'"$dynamicAnchor" path:openapi'` → 21 — the query that surfaced the closest miss below. Two further queries, `'"$dynamicAnchor" "openapi": "3.1" language:JSON'` and `'"$dynamicAnchor" "openapi: 3.1" language:YAML'`, were refused with `HTTP 422: ERROR_TYPE_QUERY_PARSING_FATAL unable to parse query!` and returned nothing. A second, broader pass cast the widest net GitHub allows for this key and confirmed the enumeration rather than extending it: `'"$dynamicAnchor" "paths"'` → 0, `'"$dynamicAnchor" "info" "components"'` → 0, `'"$dynamicAnchor" language:JSON'` → 100 (the cap), `'"$dynamicAnchor" language:YAML'` → 100 (the cap). Those two capped queries, joined with the `$dynamicRef` pair below, return 307 distinct files, and every one of them is a JSON Schema test-suite case, a copy of the OpenAPI 3.1 meta-schema (the `@apidevtools/openapi-schemas` copy alone recurs in six vendored `node_modules` trees), parser or generator source, or a `src/test` fixture — no API description among them. **SwaggerHub:** `https://api.swaggerhub.com/specs?query=%24dynamicAnchor&limit=5` → `totalCount` 16, all title matches (`Admin API`, `dtl`, `ionos-api`); the enumerated 7683-spec sweep found **3** declaring specs — `kokyhin/Admin/1.0.0` (3), `kokyhin/Minerva/1.0.0` (3), `1584335/redocly-museum_api/1.0.0` (17) — all blocked (see the ledger). **Postman:** the same `search-all` call with `queryText:"$dynamicAnchor"` → every index total **0**, the only key of the six for which that search returns nothing at all. **Vendor portals:** the same thirteen documents — **0** `$dynamicAnchor` in the OpenAPI ones; `https://spec.openapis.org/oas/3.1/dialect/base`, `…/oas/3.1/meta/base`, `https://json-schema.org/draft/2020-12/schema` (2), `…/meta/core` (2) and `…/meta/format-annotation` each declare it, but all five are JSON Schema documents at extensionless URLs, so none is a direct spec URL |
| `dollar-dynamic-ref` | none-found | none, and **for the same reason and out of the same candidate set** as `dollar-dynamic-anchor` — the two keywords are only exercisable together, so a document witnessing one witnesses both, and no document reaching this row's bar exists either. Every candidate declaring `$dynamicRef` is a synthetic fixture, a generator's example output, a unit-test resource, a copy of the OpenAPI 3.1 meta-schema, an unofficial third-party transcription, or reachable at no immutable ref; the closest miss is again `darai0512/payjp-rust` `openapi/docs/openapi.json`, whose `$dynamicRef: '#payjpObject'` types a real list envelope but sits in a document its own repository calls unofficial | — | — | — | **APIs.guru / `openapi-directory`:** the same two sweeps, grepping `"$dynamicRef"` and `$dynamicRef:` — **0** declarations across the 3992 served versions and **0** files across the clone's 4138. **GitHub code search:** `gh search code '"$dynamicRef" filename:openapi.yaml'` → 3, `… filename:openapi.json` → 6, `… filename:swagger.yaml` → 0, `… filename:openapi.yml` → 2, `… path:*.openapi.yaml` → 0, `… path:*.openapi.json` → 0, `… filename:api.yaml` → 3, `… filename:api.json` → 6, `… filename:spec.yaml` → 0, `… filename:swagger.json` → 0, `'"$dynamicRef" "jsonSchemaDialect"'` → 0, `'"$dynamicRef" "operationId"'` → 0, `'"$dynamicRef" "requestBody"'` → 0. The first two of those were refused once with `HTTP 403: API rate limit exceeded for user ID 19440155` — code search allows 10 requests a minute — and returned those counts when re-run at a 7-second cadence; `'"$dynamicRef" "openapi": "3.1" language:JSON'` and `'"$dynamicRef" "openapi: 3.1" language:YAML'` were refused with `HTTP 422: ERROR_TYPE_QUERY_PARSING_FATAL unable to parse query!`. The closest miss below was surfaced by the `dollar-dynamic-anchor` row's `path:openapi` query and confirmed to carry both keywords. A second, broader pass: `'"$dynamicRef" "paths"'` → 0, `'"$dynamicRef" "info" "components"'` → 0, `'"$dynamicRef" language:JSON'` → 100 (the cap), `'"$dynamicRef" language:YAML'` → 93, `'"$dynamicRef" path:openapi'` → 20. Those returned the same 307-file population described in the `dollar-dynamic-anchor` row, and the three candidates in it that looked like a product's own API document — `ongres/stackgres` `…/restapi/src/main/swagger/schema.json`, `data-fair/openapi-viewer` `api/types/OpenAPISpecs/schema.json` and `sven-victor/ez-console` `openapi/swagger.yaml` — were fetched and are not: the first two are copies of the OpenAPI 3.1 meta-schema (`$id` `https://spec.openapis.org/oas/3.1/schema/…`), and in the third `$dynamicRef` is a *property name* on a model of JSON Schema itself, not a keyword. **SwaggerHub:** `https://api.swaggerhub.com/specs?query=%24dynamicRef&limit=5` → `totalCount` 15, all title matches (`Admin API`, `dtl`, `ionos-api`); the enumerated 7683-spec sweep found **2** declaring specs — `kokyhin/Admin/1.0.0` and `kokyhin/Minerva/1.0.0`, one each — both blocked (see the ledger). **Postman:** the same `search-all` call with `queryText:"$dynamicRef"` → totals `team` 1 and nothing else; the one hit is a workspace name. **Vendor portals:** the same thirteen documents — **0** `$dynamicRef` in the OpenAPI ones; `https://json-schema.org/draft/2020-12/schema` declares it 3 times and `…/meta/core` twice, but both are JSON Schemas at extensionless URLs |
| `dollar-vocabulary` | none-found | none. Every declaration the sources below produced is either a copy of the Swagger Petstore 3.1 sample or a JSON Schema meta-schema, and neither is a document a real API publishes as its own description; each is recorded in the candidate ledger with the count read out of its fetched bytes and what became of it. The finding is not an accident of coverage: `$vocabulary` is a **meta-schema** keyword — it declares which vocabularies a *dialect* requires — so an API description has no occasion to carry one, and the OpenAPI documents that do are the Swagger Petstore 3.1 sample demonstrating that it can be written (in four copies), a Java unit-test resource, a serializer dump that emits every keyword as `null`, and a SwaggerHub `qtTestSchema` | — | — | — | **APIs.guru / `openapi-directory`:** the same two sweeps, grepping `"$vocabulary"` and `$vocabulary:` — **0** declarations across the 3992 served versions and **0** files across the clone's 4138. **GitHub code search:** `gh search code '"$vocabulary" filename:openapi.yaml'` → 2, `… filename:openapi.json` → 4, `… filename:swagger.yaml` → 0, `… filename:swagger.json` → 0, `… filename:openapi.yml` → 2, `… path:*.openapi.yaml` → 0, `… path:*.openapi.json` → 0, `… filename:api.yaml` → 2, `… filename:api.json` → 4, `… filename:spec.yaml` → 0, `'"$vocabulary" "jsonSchemaDialect"'` → 0, `'"$vocabulary" "operationId"'` → 0, `'"$vocabulary" "requestBody"'` → 0, `'"$vocabulary" "3.1.0" "info"'` → 0, `'"$vocabulary" "servers" language:JSON'` → 55, `'"$vocabulary" "servers" language:YAML'` → 3, `'"$vocabulary" "responses" language:YAML'` → 3, `'"$vocabulary" path:openapi'` → 14; `'"$vocabulary" "openapi": "3.1" language:JSON'` and `'"$vocabulary" "openapi: 3.1" language:YAML'` were refused with `HTTP 422: ERROR_TYPE_QUERY_PARSING_FATAL unable to parse query!`. The 55-hit query is the broadest net cast for this key and returned no new document type: `swagger-ui-dist`/`swagger-client` bundles, GraalVM `reflect-config.json` files, JSON Schema meta-schemas of OpenAPI itself, and further Petstore copies. **SwaggerHub:** `https://api.swaggerhub.com/specs?query=%24vocabulary&limit=5` → `totalCount` 14, all title matches (`dtl`, `ionos-api`, `openapi-module`); the enumerated 7683-spec sweep found **2** declaring specs — `frank-kilcommins/Swagger-Petstore-31/1.0.9-SNAPSHOT` and `test-2a8-f7e/qtTestSchema/1.0.1`, one each — the first a Petstore copy and both blocked (see the ledger). **Postman:** the same `search-all` call with `queryText:"$vocabulary"` → totals `team` 2, `collection` 7, `api` **0**. **Vendor portals:** the same thirteen documents — **0** `$vocabulary` in the OpenAPI ones; `https://spec.openapis.org/oas/3.1/dialect/base`, `…/oas/3.1/meta/base`, `https://json-schema.org/draft/2020-12/schema` and `…/meta/core` each declare it once, and those are exactly the dialect documents this keyword exists to define, but all four are JSON Schema documents at extensionless URLs rather than OpenAPI documents at direct spec URLs |
| `format-relative-json-pointer` | witness-blocked | BioIS API, `geo-engine/BioIS` `openapi.json` at `https://raw.githubusercontent.com/geo-engine/BioIS/afdcab7d25870c2843afc2b75818e25dc8ff4b98/openapi.json` — 58895 bytes fetched, **1** `"format": "relative-json-pointer"` counted in them, on `components.schemas.RelativeJsonPointer`. **Publisher-owned**, on the same standard the other rows are held to: `geo-engine` is Geo Engine GmbH's own GitHub organisation (`name` `Geo Engine`, `blog` `www.geoengine.de`) and `geo-engine/BioIS` is not a fork, so the publisher of the API is the publisher of the document — "BioIS API" (`info.contact` `info@geoengine.de`), `openapi` 3.1.0, 17 paths on an OGC API Processes surface (`/processes/{processID}/execution`, `/jobs/{jobId}/results`, …). **Blocked because it is not redistributable:** `https://raw.githubusercontent.com/geo-engine/BioIS/afdcab7d25870c2843afc2b75818e25dc8ff4b98/LICENSE` reads `PROPRIETARY LICENSE … The Software is not licensed under an Open Source initiative.`, and the GitHub API reports its licence as `NOASSERTION`, so nothing grants the corpus the right to vendor a golden derived from it. It is blocked on that alone — it is pinned to an immutable commit, its raw URL is credential-free HTTPS ending `.json`, its name is in neither `CORPUS.md` nor either DROPPED/REJECTED list, and Fern accepts it  **This node's re-search asked whether a second, usable `relative-json-pointer` witness exists, and it could not finish asking.** Five of the six sources answered and every one returned nothing that is a document a real API publishes as its own description; the sixth, the literal index, refused. So the further-witness answer is the outcome the index's rule calls [`search-incomplete`](../openapi-surface-coverage.md#the-settlement-rule-as-amended) rather than `none-found`, and this row's own outcome stays `witness-blocked` on the BioIS document above. What the answering five did return, and what refused the sixth, is in the last column.| commit `afdcab7d25870c2843afc2b75818e25dc8ff4b98` | **proprietary — the obstacle**; `NOASSERTION` per the GitHub API | `Found 0 errors and 5 warnings in 0.000 seconds` (exit 0) — recorded even though the row is blocked, because it tells the consuming node the licence is the only thing in the way | **APIs.guru / `openapi-directory`:** the same two sweeps, grepping `relative-json-pointer` — **0** occurrences in any position across the 3992 served versions and **0** files across the clone's 4138. **GitHub code search:** `gh search code '"relative-json-pointer" filename:openapi.yaml'` → 1, `… filename:openapi.json` → 1 (the recorded document), `… filename:swagger.yaml` → 0, `… filename:swagger.json` → 0, `… filename:openapi.yml` → 0, `… path:*.openapi.yaml` → 0, `… path:*.openapi.json` → 0, `… filename:api.yaml` → 1, `… filename:api.json` → 1, `… filename:spec.yaml` → 0, `'"relative-json-pointer" "jsonSchemaDialect"'` → 0, `'"relative-json-pointer" "operationId"'` → 0, `'"relative-json-pointer" "requestBody"'` → 0, `'"relative-json-pointer" "components" language:YAML'` → 0, `'"relative-json-pointer" "servers" language:JSON'` → 27, `'"relative-json-pointer" "servers" language:YAML'` → 0, `'"relative-json-pointer" path:openapi'` → 6; `'"relative-json-pointer" "openapi:" language:YAML'`, `'"relative-json-pointer" "openapi": "3.1" language:JSON'` and `'"relative-json-pointer" "openapi: 3.1" language:YAML'` were refused with `HTTP 422: ERROR_TYPE_QUERY_PARSING_FATAL unable to parse query!`. The 27-hit query returned only `sailpoint-oss/barrelman` and `sailpoint-oss/navigator` meta-schemas plus a `vertx-json-schema` draft-7 TCK file, none of them OpenAPI documents. **SwaggerHub:** `https://api.swaggerhub.com/specs?query=relative-json-pointer&limit=5` → `totalCount` 10, all title matches (`dtl`, `HDL_RESTAPI`, `ionos-api`); the enumerated 7683-spec sweep found **0** specs containing the string anywhere. **Postman:** the same `search-all` call with `queryText:"relative-json-pointer"` → every index total **0**. **Vendor portals:** the same thirteen documents, among them `https://json-schema.org/draft/2020-12/meta/format-annotation`, the vocabulary that registers this format — **0** `relative-json-pointer` in every one of them, including the format vocabulary itself, which registers formats without asserting them **Re-searched for a further witness by this node.** **Sourcegraph** `content:"\"format\": \"relative-json-pointer\"" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` and `content:"format: relative-json-pointer" …` → the JSON spelling **3 matches in 2 files** after a long cool-down, both of them conformance-test resources (`WebFuzzing/EvoMaster` `core-tests/…/static/openapi-bbadvancedformats.json` and `corvus-dotnet/Corvus.JsonSchema` `tests-v4/…/OpenApi30.schema.json`), and the YAML spelling **unanswered**: HTTP 429 behind a Cloudflare `Just a moment…` interstitial on every attempt, over more than two hours of 90-second and then 3-to-24-minute backoffs from an otherwise idle client. The index rate-limits a client that has asked it forty `count:all` questions, and for this query it never lifted inside this dispatch. **APIs.guru** re-swept whole — all 3992 served versions fetched and each *parsed* body walked for a `format` value of `relative-json-pointer` → **0** declaring, and the `--depth 1` clone at `f04b8d0b` walked the same way over 4138 files → **0**. **GitHub** `gh api -X GET search/code -f q='"relative-json-pointer" path:openapi extension:yaml'` → 0, `… extension:json` → 0, `"relative-json-pointer" path:swagger` → 16, and widened, `"relative-json-pointer" extension:yaml` → 61 and `… extension:json` → 4088 — whose examined head is IETF draft metadata under `relaton/relaton-data-ietf` and `ietf-tools/relaton-data-ids`, `DZakh/sury` `packages/sury/specs/relative-json-pointer.yaml`, and JSON Schema conformance suites. **SwaggerHub** the whole public 3.1/3.2 registry re-enumerated — 7666 distinct specs fetched and walked → **0** declaring. **Postman** `queryText: "relative-json-pointer"` → 0 on `apinetwork.team`, `runtime.collection` and `adp.api` alike. **vendor portals** the 29 publisher-served documents in this node's vendor set, walked the same way → **0** declaring. |
| `contains` | fern-rejected | Asserts, Inc. — `grafana/grafana-asserts-public-clients` `openapi.yaml`, 264919 bytes fetched, **2** `contains` declarations counted in the parsed document, each on an array schema under `components.schemas`. **Publisher-owned**: Grafana Labs' own organisation, describing the Asserts API its own published clients call. Two further permissively licensed real-world declarers sit behind it and Fern refuses both — `pitekusu/shittim-chest` `contracts/records/v1/openapi.json` (MIT, **24**) and `goptics/vizb` `api/openapi.yaml` (MIT, **8**) | `https://raw.githubusercontent.com/grafana/grafana-asserts-public-clients/7a5d002f5d7c40a53fc77e7156ccb8a362982f0c/openapi.yaml` | `Apache-2.0` | `fern check` exits **1** with 2 errors, `Enum name  for value % is not suitable for code generation.`; `fern generate` not reached. CLI **5.67.1** via `fern.config.json`, generator `fernapi/fern-python-sdk` **5.20.0** — the pin `scripts/generate-fern-fixture.sh` defaults to | **Sourcegraph** (the sixth source, and why it leads the cell: see the note above this table) `content:"\"contains\"" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 807 matches in 158 files; `content:"contains:" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 862 matches in 338 files. Then the five this region declares. **APIs.guru** `curl -sS https://api.apis.guru/v2/list.json`, then every version's `swaggerUrl` fetched and each parsed body walked for a `contains` key in schema position — 2529 APIs, 3992 versions, 0 fetch errors, **0** declaring; and, as a second reading, `git clone --depth 1 https://github.com/APIs-guru/openapi-directory` at `f04b8d0b` walked the same way over its 4138 spec files in 701 provider directories → **0** declaring. **GitHub** `gh api -X GET search/code -f q='<query>' -f per_page=100` with `"contains" path:openapi extension:yaml` → 15872; `"contains" path:openapi extension:json` → 1622; `"contains" path:swagger` → 7728; and, widened, `"maxContains" filename:openapi.yaml` → 8; `"maxContains" filename:openapi.json` → 6 results. **SwaggerHub** `GET https://api.swaggerhub.com/specs?specification=openapi-3.1.x&limit=100&page=N` (and `openapi-3.2.0`) paged to exhaustion → 7688 entries over 78 pages, 7666 distinct `Swagger` property URLs, every one fetched and walked → **5** declaring — `malakoffhumanis/salesforce-fasst/1.0.3` (5), `JAMIESTEWARD_1/britecore/2.0.0` (4), `Jiaheng/tni-transcript-delivery-api-for-oliver-wyman/1.0.0-discussion-preview.1` (2). **Postman** `POST https://www.postman.com/_api/ws/proxy` with `{"service":"search","method":"POST","path":"/search-all","body":{"queryIndices":["<index>"],"queryText":"contains","size":3,"from":0}}` → `apinetwork.team` team=4, `runtime.collection` collection=2, `adp.api` 0. **vendor portals** the 29 publisher-served documents listed under [the vendor set](#the-vendor-portals-this-regions-domain-implies), each fetched with `curl -sSL <url>` and its parsed body walked for `"contains"` / `contains:` → **0** declaring. |
| `content-media-type` | witness-found | oSPARC — `ITISFoundation/osparc-simcore` `services/web/server/src/simcore_service_webserver/api/v0/openapi.json`, 810142 bytes fetched, **24** `contentMediaType` declarations counted in the parsed document. **Publisher-owned**: the IT'IS Foundation's own organisation, describing the web API its own front end calls. A second, independent declarer Fern also accepts is `NVIDIA-Omniverse/content-agents` `apps/physics_agent_service/openapi.yaml` at `99b773e6` (`Apache-2.0`, **9**) | `https://raw.githubusercontent.com/ITISFoundation/osparc-simcore/7fa7c73d34bb3bf4d22477a1bc5d9970c2824607/services/web/server/src/simcore_service_webserver/api/v0/openapi.json` | `MIT` | `fern check` exits **0**; `fern generate --group python-sdk --local --preview` exits **0** and writes a packaged SDK under `fern-python-sdk/src/`. CLI **5.67.1** via `fern.config.json`, generator `fernapi/fern-python-sdk` **5.20.0** — the pin `scripts/generate-fern-fixture.sh` defaults to | **Sourcegraph** (the sixth source, and why it leads the cell: see the note above this table) `content:"\"contentMediaType\"" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 404 matches in 130 files; `content:"contentMediaType:" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 240 matches in 74 files. Then the five this region declares. **APIs.guru** `curl -sS https://api.apis.guru/v2/list.json`, then every version's `swaggerUrl` fetched and each parsed body walked for a `contentMediaType` key in schema position — 2529 APIs, 3992 versions, 0 fetch errors, **0** declaring; and, as a second reading, `git clone --depth 1 https://github.com/APIs-guru/openapi-directory` at `f04b8d0b` walked the same way over its 4138 spec files in 701 provider directories → **0** declaring. **GitHub** `gh api -X GET search/code -f q='<query>' -f per_page=100` with `"contentMediaType" path:openapi extension:yaml` → 44; `"contentMediaType" path:openapi extension:json` → 62; `"contentMediaType" path:swagger` → 94 results. **SwaggerHub** `GET https://api.swaggerhub.com/specs?specification=openapi-3.1.x&limit=100&page=N` (and `openapi-3.2.0`) paged to exhaustion → 7688 entries over 78 pages, 7666 distinct `Swagger` property URLs, every one fetched and walked → **126** declaring — `ahhshaikh/mist-api/2403.1.2` (4611), `JamesWidener/code-sonar_http_api/2.1.0` (817), `DAVIDSUTHERLAND/fdx-api-v6-1-0/6.1.0` (665). **Postman** `POST https://www.postman.com/_api/ws/proxy` with `{"service":"search","method":"POST","path":"/search-all","body":{"queryIndices":["<index>"],"queryText":"contentMediaType","size":3,"from":0}}` → `apinetwork.team` 0, `runtime.collection` 0, `adp.api` 0. **vendor portals** the 29 publisher-served documents listed under [the vendor set](#the-vendor-portals-this-regions-domain-implies), each fetched with `curl -sSL <url>` and its parsed body walked for `"contentMediaType"` / `contentMediaType:` → **0** declaring. |
| `content-schema` | witness-found | oSPARC — the same `ITISFoundation/osparc-simcore` document, whose parsed body carries **24** `contentSchema` declarations beside its 24 `contentMediaType` ones: the two keywords are declared as a pair on the same nodes, so one corpus row pins both. **Publisher-owned** on the same test | `https://raw.githubusercontent.com/ITISFoundation/osparc-simcore/7fa7c73d34bb3bf4d22477a1bc5d9970c2824607/services/web/server/src/simcore_service_webserver/api/v0/openapi.json` | `MIT` | `fern check` exits **0**; `fern generate --group python-sdk --local --preview` exits **0** and writes a packaged SDK under `fern-python-sdk/src/`. CLI **5.67.1** via `fern.config.json`, generator `fernapi/fern-python-sdk` **5.20.0** — the pin `scripts/generate-fern-fixture.sh` defaults to | **Sourcegraph** (the sixth source, and why it leads the cell: see the note above this table) `content:"\"contentSchema\"" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 52 matches in 14 files; `content:"contentSchema:" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 46 matches in 31 files. Then the five this region declares. **APIs.guru** `curl -sS https://api.apis.guru/v2/list.json`, then every version's `swaggerUrl` fetched and each parsed body walked for a `contentSchema` key in schema position — 2529 APIs, 3992 versions, 0 fetch errors, **0** declaring; and, as a second reading, `git clone --depth 1 https://github.com/APIs-guru/openapi-directory` at `f04b8d0b` walked the same way over its 4138 spec files in 701 provider directories → **0** declaring. **GitHub** `gh api -X GET search/code -f q='<query>' -f per_page=100` with `"contentSchema" path:openapi extension:yaml` → 18; `"contentSchema" path:openapi extension:json` → 5; `"contentSchema" path:swagger` → 109 results. **SwaggerHub** `GET https://api.swaggerhub.com/specs?specification=openapi-3.1.x&limit=100&page=N` (and `openapi-3.2.0`) paged to exhaustion → 7688 entries over 78 pages, 7666 distinct `Swagger` property URLs, every one fetched and walked → **11** declaring — `JamesWidener/code-sonar_http_api/2.1.0` (817), `westriverconsulting/azure-ai-foundry-models-service/v1` (33), `o.motsak/ovation-server-sync/13` (4). **Postman** `POST https://www.postman.com/_api/ws/proxy` with `{"service":"search","method":"POST","path":"/search-all","body":{"queryIndices":["<index>"],"queryText":"contentSchema","size":3,"from":0}}` → `apinetwork.team` 0, `runtime.collection` 0, `adp.api` 0. **vendor portals** the 29 publisher-served documents listed under [the vendor set](#the-vendor-portals-this-regions-domain-implies), each fetched with `curl -sSL <url>` and its parsed body walked for `"contentSchema"` / `contentSchema:` → **0** declaring. |
| `dependent-required` | witness-found | HelixDB — `HelixDB/helix-db` `docs/openapi.json`, 25352 bytes fetched, **2** `dependentRequired` declarations counted in the parsed document. **Publisher-owned**: HelixDB's own organisation, describing the HTTP API its own database serves. The smallest witness this search found for any of these twenty rows | `https://raw.githubusercontent.com/HelixDB/helix-db/f19b488b3d48ec1da90f3dfcbac40f1c43d1e596/docs/openapi.json` | `Apache-2.0` | `fern check` exits **0**; `fern generate --group python-sdk --local --preview` exits **0** and writes a packaged SDK under `fern-python-sdk/src/`. CLI **5.67.1** via `fern.config.json`, generator `fernapi/fern-python-sdk` **5.20.0** — the pin `scripts/generate-fern-fixture.sh` defaults to | **Sourcegraph** (the sixth source, and why it leads the cell: see the note above this table) `content:"\"dependentRequired\"" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 19 matches in 16 files; `content:"dependentRequired:" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 25 matches in 21 files. Then the five this region declares. **APIs.guru** `curl -sS https://api.apis.guru/v2/list.json`, then every version's `swaggerUrl` fetched and each parsed body walked for a `dependentRequired` key in schema position — 2529 APIs, 3992 versions, 0 fetch errors, **0** declaring; and, as a second reading, `git clone --depth 1 https://github.com/APIs-guru/openapi-directory` at `f04b8d0b` walked the same way over its 4138 spec files in 701 provider directories → **0** declaring. **GitHub** `gh api -X GET search/code -f q='<query>' -f per_page=100` with `"dependentRequired" path:openapi extension:yaml` → 13; `"dependentRequired" path:openapi extension:json` → 2; `"dependentRequired" path:swagger` → 118 results. **SwaggerHub** `GET https://api.swaggerhub.com/specs?specification=openapi-3.1.x&limit=100&page=N` (and `openapi-3.2.0`) paged to exhaustion → 7688 entries over 78 pages, 7666 distinct `Swagger` property URLs, every one fetched and walked → **4** declaring — `JAMIESTEWARD_1/britecore/2.0.0` (3), `mhiggins-sa/book-api_3_1_0/2.0.0` (1), `Jiaheng/tni-transcript-delivery-api-for-oliver-wyman/1.0.0-discussion-preview.1` (1). **Postman** `POST https://www.postman.com/_api/ws/proxy` with `{"service":"search","method":"POST","path":"/search-all","body":{"queryIndices":["<index>"],"queryText":"dependentRequired","size":3,"from":0}}` → `apinetwork.team` 0, `runtime.collection` 0, `adp.api` 0. **vendor portals** the 29 publisher-served documents listed under [the vendor set](#the-vendor-portals-this-regions-domain-implies), each fetched with `curl -sSL <url>` and its parsed body walked for `"dependentRequired"` / `dependentRequired:` → **0** declaring. |
| `dependent-schemas` | none-found | none. Every `dependentSchemas` declaration the six sources produced is a copy of the OpenAPI 3.1 or 3.2 meta-schema, a parser's or generator's test resource, or the Swagger Petstore 3.1 sample — each recorded in the candidate ledger with the count read out of its fetched bytes. The one candidate that is neither is `duy-tung/serving-contracts` `openapi/inference-api.yaml` (`Apache-2.0`, **1**), which Fern accepts outright and which is still not a witness: its only `servers` entry is `https://gateway.serving-contracts.invalid`, an RFC 2606 placeholder its own `description` calls one, so no API serves the document | n/a | n/a | `fern check` exits **0** and `fern generate` exits **0** on the `duy-tung/serving-contracts` candidate; it is rejected on provenance rather than on Fern. CLI **5.67.1** via `fern.config.json`, generator `fernapi/fern-python-sdk` **5.20.0** — the pin `scripts/generate-fern-fixture.sh` defaults to | **Sourcegraph** (the sixth source, and why it leads the cell: see the note above this table) `content:"\"dependentSchemas\"" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 179 matches in 61 files; `content:"dependentSchemas:" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 29 matches in 23 files. Then the five this region declares. **APIs.guru** `curl -sS https://api.apis.guru/v2/list.json`, then every version's `swaggerUrl` fetched and each parsed body walked for a `dependentSchemas` key in schema position — 2529 APIs, 3992 versions, 0 fetch errors, **0** declaring; and, as a second reading, `git clone --depth 1 https://github.com/APIs-guru/openapi-directory` at `f04b8d0b` walked the same way over its 4138 spec files in 701 provider directories → **0** declaring. **GitHub** `gh api -X GET search/code -f q='<query>' -f per_page=100` with `"dependentSchemas" path:openapi extension:yaml` → 3; `"dependentSchemas" path:openapi extension:json` → 1; `"dependentSchemas" path:swagger` → 91 results. **SwaggerHub** `GET https://api.swaggerhub.com/specs?specification=openapi-3.1.x&limit=100&page=N` (and `openapi-3.2.0`) paged to exhaustion → 7688 entries over 78 pages, 7666 distinct `Swagger` property URLs, every one fetched and walked → **0** declaring. **Postman** `POST https://www.postman.com/_api/ws/proxy` with `{"service":"search","method":"POST","path":"/search-all","body":{"queryIndices":["<index>"],"queryText":"dependentSchemas","size":3,"from":0}}` → `apinetwork.team` 0, `runtime.collection` 0, `adp.api` 0. **vendor portals** the 29 publisher-served documents listed under [the vendor set](#the-vendor-portals-this-regions-domain-implies), each fetched with `curl -sSL <url>` and its parsed body walked for `"dependentSchemas"` / `dependentSchemas:` → **1** declaring — the OAI's own `spec.openapis.org/oas/3.1/schema/2022-10-07` (3), which is the 3.1 meta-schema and not an API description. |
| `dollar-defs` | witness-found | Flowdapt — `emergentmethods/flowdapt` `openapi.json`, 162458 bytes fetched, **7** `$defs` declarations counted in the parsed document. **Publisher-owned**: Emergent Methods' own organisation, describing the Flowdapt server's own API | `https://raw.githubusercontent.com/emergentmethods/flowdapt/e8f34d411564e6c0e6542fe67d0604c23de1cf91/openapi.json` | `Apache-2.0` | `fern check` exits **0**; `fern generate --group python-sdk --local --preview` exits **0** and writes a packaged SDK under `fern-python-sdk/src/`. CLI **5.67.1** via `fern.config.json`, generator `fernapi/fern-python-sdk` **5.20.0** — the pin `scripts/generate-fern-fixture.sh` defaults to | **Sourcegraph** (the sixth source, and why it leads the cell: see the note above this table) `content:"\"$defs\"" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 655 matches in 200 files; `content:"$defs:" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 101 matches in 39 files. Then the five this region declares. **APIs.guru** `curl -sS https://api.apis.guru/v2/list.json`, then every version's `swaggerUrl` fetched and each parsed body walked for a `$defs` key in schema position — 2529 APIs, 3992 versions, 0 fetch errors, **0** declaring; and, as a second reading, `git clone --depth 1 https://github.com/APIs-guru/openapi-directory` at `f04b8d0b` walked the same way over its 4138 spec files in 701 provider directories → **0** declaring. **GitHub** `gh api -X GET search/code -f q='<query>' -f per_page=100` with `"$defs" path:openapi extension:yaml` → 64; `"$defs" path:openapi extension:json` → 46; `"$defs" path:swagger` → 110; and, widened, `"$defs" filename:openapi.yaml` → 207; `"$defs" filename:openapi.json` → 157 results. **SwaggerHub** `GET https://api.swaggerhub.com/specs?specification=openapi-3.1.x&limit=100&page=N` (and `openapi-3.2.0`) paged to exhaustion → 7688 entries over 78 pages, 7666 distinct `Swagger` property URLs, every one fetched and walked → **11** declaring — `Start-Innovation/PerformanceManagement/8.3.0` (14), `CALLUM_5/eyos-api_docs/1.0.0` (10), `fivesoto/ignition-http-api/1.0.0` (8). **Postman** `POST https://www.postman.com/_api/ws/proxy` with `{"service":"search","method":"POST","path":"/search-all","body":{"queryIndices":["<index>"],"queryText":"$defs","size":3,"from":0}}` → `apinetwork.team` team=66, `runtime.collection` 0, `adp.api` 0. **vendor portals** the 29 publisher-served documents listed under [the vendor set](#the-vendor-portals-this-regions-domain-implies), each fetched with `curl -sSL <url>` and its parsed body walked for `"$defs"` / `$defs:` → **2** declaring — `spec.openapis.org/oas/3.1/schema/2022-10-07` (4) and `json-schema.org/draft/2020-12/meta/validation` (1), both meta-schemas. |
| `exclusive-maximum-numeric` | witness-found | oSPARC — the same `ITISFoundation/osparc-simcore` document again, whose parsed body carries **8** `exclusiveMaximum` keys with a *numeric* value; it declares `openapi: 3.1.0`, so these are the 3.1 spelling and not the 3.0 boolean that `exclusive-maximum-boolean` already pins. **Publisher-owned** on the same test. A second declarer Fern refuses is `maximhq/bifrost` `docs/openapi/schemas/management/governance.yaml` at `47058e2e` (`Apache-2.0`, **3**) | `https://raw.githubusercontent.com/ITISFoundation/osparc-simcore/7fa7c73d34bb3bf4d22477a1bc5d9970c2824607/services/web/server/src/simcore_service_webserver/api/v0/openapi.json` | `MIT` | `fern check` exits **0**; `fern generate --group python-sdk --local --preview` exits **0** and writes a packaged SDK under `fern-python-sdk/src/`. CLI **5.67.1** via `fern.config.json`, generator `fernapi/fern-python-sdk` **5.20.0** — the pin `scripts/generate-fern-fixture.sh` defaults to | **Sourcegraph** (the sixth source, and why it leads the cell: see the note above this table) `content:"\"exclusiveMaximum\"" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 6248 matches in 1204 files; `content:"exclusiveMaximum:" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 2470 matches in 401 files. Then the five this region declares. **APIs.guru** `curl -sS https://api.apis.guru/v2/list.json`, then every version's `swaggerUrl` fetched and each parsed body walked for an `exclusiveMaximum` key whose value is a number rather than the 3.0 boolean — 2529 APIs, 3992 versions, 0 fetch errors, **0** declaring; and, as a second reading, `git clone --depth 1 https://github.com/APIs-guru/openapi-directory` at `f04b8d0b` walked the same way over its 4138 spec files in 701 provider directories → **0** declaring. **GitHub** `gh api -X GET search/code -f q='<query>' -f per_page=100` with `"exclusiveMaximum" path:openapi extension:yaml` → 122; `"exclusiveMaximum" path:openapi extension:json` → 72; `"exclusiveMaximum" path:swagger` → 187 results. **SwaggerHub** `GET https://api.swaggerhub.com/specs?specification=openapi-3.1.x&limit=100&page=N` (and `openapi-3.2.0`) paged to exhaustion → 7688 entries over 78 pages, 7666 distinct `Swagger` property URLs, every one fetched and walked → **35** declaring — `zhangsan2/3.1/1.0.0` (18), `airbitiot/geolocation-api/1.1.0` (10), `Neorenflar/Path-Minified/1.0.0` (9). **Postman** `POST https://www.postman.com/_api/ws/proxy` with `{"service":"search","method":"POST","path":"/search-all","body":{"queryIndices":["<index>"],"queryText":"exclusiveMaximum","size":3,"from":0}}` → `apinetwork.team` 0, `runtime.collection` 0, `adp.api` 0. **vendor portals** the 29 publisher-served documents listed under [the vendor set](#the-vendor-portals-this-regions-domain-implies), each fetched with `curl -sSL <url>` and its parsed body walked for `"exclusiveMaximum"` / `exclusiveMaximum:` → **0** declaring. |
| `format-idn-email` | witness-blocked | AboutMy.email — `wttw/aboutmyemail` `ameapi.yaml`, 8120 bytes fetched, **4** `format: idn-email` declarations counted in the parsed document, on the address fields of its deliverability-report API. **Publisher-owned**: Word to the Wise's own repository for the AboutMy.email service it runs, and the only document any of the six sources produced that declares this format outside a JSON Schema conformance suite or a Go source file. Fern accepts it outright — **blocker:** licence — the repository declares **none declared**, so the corpus's redistribution set cannot take it; a licence file makes it registrable with no other change | `https://raw.githubusercontent.com/wttw/aboutmyemail/3575c0e4ef35ef7e4b94a94f30229f5b0ce9f338/ameapi.yaml` | **none declared** | `fern check` exits **0**; `fern generate --group python-sdk --local --preview` exits **0** and writes a packaged SDK under `fern-python-sdk/src/`. CLI **5.67.1** via `fern.config.json`, generator `fernapi/fern-python-sdk` **5.20.0** — the pin `scripts/generate-fern-fixture.sh` defaults to | **Sourcegraph** (the sixth source, and why it leads the cell: see the note above this table) `content:"\"format\": \"idn-email\"" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 3 matches in 2 files; `content:"format: idn-email" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 0 matches in 0 files. Then the five this region declares. **APIs.guru** `curl -sS https://api.apis.guru/v2/list.json`, then every version's `swaggerUrl` fetched and each parsed body walked for a `format` value of `idn-email` — 2529 APIs, 3992 versions, 0 fetch errors, **0** declaring; and, as a second reading, `git clone --depth 1 https://github.com/APIs-guru/openapi-directory` at `f04b8d0b` walked the same way over its 4138 spec files in 701 provider directories → **0** declaring. **GitHub** `gh api -X GET search/code -f q='<query>' -f per_page=100` with `"idn-email" path:openapi extension:yaml` → 0; `"idn-email" path:openapi extension:json` → 0; `"idn-email" path:swagger` → 16; and, widened, `"idn-email" extension:yaml` → 63; `"idn-email" extension:json` → 1578 results. **SwaggerHub** `GET https://api.swaggerhub.com/specs?specification=openapi-3.1.x&limit=100&page=N` (and `openapi-3.2.0`) paged to exhaustion → 7688 entries over 78 pages, 7666 distinct `Swagger` property URLs, every one fetched and walked → **1** declaring — `SCOTTDORMAN_1/accelerate-membership_open_api_3_1/1.0.11` (1). **Postman** `POST https://www.postman.com/_api/ws/proxy` with `{"service":"search","method":"POST","path":"/search-all","body":{"queryIndices":["<index>"],"queryText":"idn-email","size":3,"from":0}}` → `apinetwork.team` 0, `runtime.collection` 0, `adp.api` 0. **vendor portals** the 29 publisher-served documents listed under [the vendor set](#the-vendor-portals-this-regions-domain-implies), each fetched with `curl -sSL <url>` and its parsed body walked for `"format": "idn-email"` / `format: idn-email` → **0** declaring. |
| `format-idn-hostname` | fern-rejected | Short.io — the document Short.io serves itself at `https://api.short.io/openapi.json`, 239940 bytes fetched, **1** `format: idn-hostname` declaration counted in the parsed document, on the `domain` field. **Publisher-owned** and publisher-*served*, which is the strongest provenance any of these twenty rows reached. Two things stop it: Fern refuses it, and the address is a live endpoint the publisher republishes in place. `jentic/jentic-public-apis` pins a 3.0.3 copy of an older version at `eb9d12a2`, which is a third-party distribution rather than a second witness | none — `https://api.short.io/openapi.json` is a live endpoint the publisher overwrites in place; the 3.1.0 body read here is retrievable at no fixed reference | **none declared** | `fern check` exits **1** with 3 errors, `CreateLinkRequest is already declared in linkManagement.yml`; `fern generate` not reached. CLI **5.67.1** via `fern.config.json`, generator `fernapi/fern-python-sdk` **5.20.0** — the pin `scripts/generate-fern-fixture.sh` defaults to | **Sourcegraph** (the sixth source, and why it leads the cell: see the note above this table) `content:"\"format\": \"idn-hostname\"" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 8 matches in 7 files; `content:"format: idn-hostname" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 0 matches in 0 files. Then the five this region declares. **APIs.guru** `curl -sS https://api.apis.guru/v2/list.json`, then every version's `swaggerUrl` fetched and each parsed body walked for a `format` value of `idn-hostname` — 2529 APIs, 3992 versions, 0 fetch errors, **0** declaring; and, as a second reading, `git clone --depth 1 https://github.com/APIs-guru/openapi-directory` at `f04b8d0b` walked the same way over its 4138 spec files in 701 provider directories → **0** declaring. **GitHub** `gh api -X GET search/code -f q='<query>' -f per_page=100` with `"idn-hostname" path:openapi extension:yaml` → 0; `"idn-hostname" path:openapi extension:json` → 0; `"idn-hostname" path:swagger` → 16 results. **SwaggerHub** `GET https://api.swaggerhub.com/specs?specification=openapi-3.1.x&limit=100&page=N` (and `openapi-3.2.0`) paged to exhaustion → 7688 entries over 78 pages, 7666 distinct `Swagger` property URLs, every one fetched and walked → **0** declaring. **Postman** `POST https://www.postman.com/_api/ws/proxy` with `{"service":"search","method":"POST","path":"/search-all","body":{"queryIndices":["<index>"],"queryText":"idn-hostname","size":3,"from":0}}` → `apinetwork.team` 0, `runtime.collection` 0, `adp.api` 0. **vendor portals** the 29 publisher-served documents listed under [the vendor set](#the-vendor-portals-this-regions-domain-implies), each fetched with `curl -sSL <url>` and its parsed body walked for `"format": "idn-hostname"` / `format: idn-hostname` → **0** declaring. |
| `format-ipv6` | fern-rejected | Linode — `linode/linode-api-docs` `openapi.json`, 7911272 bytes fetched, **3** `format: ipv6` declarations counted in the parsed document. **Publisher-owned**: Akamai/Linode's own organisation, and the source `APIs-guru/openapi-directory`'s `linode.com` entry converts from. Three further permissively licensed real-world declarers sit behind it and Fern refuses all three — `cloudflare/api-schemas` `openapi.json` (`BSD-3-Clause`, **3**), `CrowdStrike/aidr-python` `specs/ai-guard.openapi.json` (`MIT`, **1**) and Vercel's `openapi.vercel.sh` (**1**, and a mutable address besides) | `https://raw.githubusercontent.com/linode/linode-api-docs/235e8a909f6db6b92d6499900e95c6daf1824601/openapi.json` | `Apache-2.0` | `fern check` exits **1** with 459 errors, `Expected example to be an integer. Example is: 41.5`; `fern generate` not reached. CLI **5.67.1** via `fern.config.json`, generator `fernapi/fern-python-sdk` **5.20.0** — the pin `scripts/generate-fern-fixture.sh` defaults to | **Sourcegraph** (the sixth source, and why it leads the cell: see the note above this table) `content:"\"format\": \"ipv6\"" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → **unanswered**: HTTP 429 behind a Cloudflare `Just a moment…` interstitial; `content:"format: ipv6" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → **unanswered**: HTTP 429 behind a Cloudflare `Just a moment…` interstitial. Then the five this region declares. **APIs.guru** `curl -sS https://api.apis.guru/v2/list.json`, then every version's `swaggerUrl` fetched and each parsed body walked for a `format` value of `ipv6` — 2529 APIs, 3992 versions, 0 fetch errors, **5** declaring — `threatjammer.com/1.2.20` (5), `mist.com/0.36.1` (4), `linode.com/4.145.0` (3), …; and, as a second reading, `git clone --depth 1 https://github.com/APIs-guru/openapi-directory` at `f04b8d0b` walked the same way over its 4138 spec files in 701 provider directories → **5** declaring — `threatjammer.com/1.2.27` (5), `mist.com/0.37.7` (4), `linode.com/4.151.1` (3), …. **GitHub** `gh api -X GET search/code -f q='<query>' -f per_page=100` with `"ipv6" path:openapi extension:yaml` → 1376; `"ipv6" path:openapi extension:json` → 191; `"ipv6" path:swagger` → 250; and, widened, `"ipv6" filename:openapi.yaml` → 1490; `"ipv6" filename:openapi.json` → 749 results. **SwaggerHub** `GET https://api.swaggerhub.com/specs?specification=openapi-3.1.x&limit=100&page=N` (and `openapi-3.2.0`) paged to exhaustion → 7688 entries over 78 pages, 7666 distinct `Swagger` property URLs, every one fetched and walked → **14** declaring — `Neorenflar/Path-Minified/1.0.0` (7), `VIGILX/Public/1.0.0` (3), `VIGILX/VigilX/1.0.0` (3). **Postman** `POST https://www.postman.com/_api/ws/proxy` with `{"service":"search","method":"POST","path":"/search-all","body":{"queryIndices":["<index>"],"queryText":"ipv6","size":3,"from":0}}` → `apinetwork.team` team=6, `runtime.collection` collection=4, `adp.api` 0. **vendor portals** the 29 publisher-served documents listed under [the vendor set](#the-vendor-portals-this-regions-domain-implies), each fetched with `curl -sSL <url>` and its parsed body walked for `"format": "ipv6"` / `format: ipv6` → **2** declaring — Cloudflare `api-schemas` `openapi.json` (3) and Vercel `openapi.vercel.sh` (1). |
| `format-iri` | fern-rejected | The Mercure protocol — `dunglas/mercure`, read at the APIs.guru pin `mercure.local/0.3.2` its own `x-origin` points back to, 2 `format: iri` declarations counted in the parsed document beside 4 `iri-reference` ones. **Publisher-owned**: the protocol's own reference implementation. Fern refuses it, and its licence is `AGPL-3.0`, outside the corpus's set, so it is blocked twice over. The only other declarer any source produced is Short.io's own document (**4**), which Fern refuses too | `https://api.apis.guru/v2/specs/mercure.local/0.3.2/openapi.json` | `AGPL-3.0` | `fern check` exits **1** with 2 errors, `Multiple request properties resolve to the same generated name lastEventId after camelCase normalization.`; `fern generate` not reached. CLI **5.67.1** via `fern.config.json`, generator `fernapi/fern-python-sdk` **5.20.0** — the pin `scripts/generate-fern-fixture.sh` defaults to | **Sourcegraph** (the sixth source, and why it leads the cell: see the note above this table) `content:"\"format\": \"iri\"" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → **unanswered**: HTTP 429 behind a Cloudflare `Just a moment…` interstitial; `content:"format: iri" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → **unanswered**: HTTP 429 behind a Cloudflare `Just a moment…` interstitial. Then the five this region declares. **APIs.guru** `curl -sS https://api.apis.guru/v2/list.json`, then every version's `swaggerUrl` fetched and each parsed body walked for a `format` value of `iri` — 2529 APIs, 3992 versions, 0 fetch errors, **1** declaring — `mercure.local/0.3.2` (2); and, as a second reading, `git clone --depth 1 https://github.com/APIs-guru/openapi-directory` at `f04b8d0b` walked the same way over its 4138 spec files in 701 provider directories → **1** declaring — `mercure.local/0.3.2` (2). **GitHub** `gh api -X GET search/code -f q='<query>' -f per_page=100` with `"iri" path:openapi extension:yaml` → 1270; `"iri" path:openapi extension:json` → 507; `"iri" path:swagger` → 688 results. **SwaggerHub** `GET https://api.swaggerhub.com/specs?specification=openapi-3.1.x&limit=100&page=N` (and `openapi-3.2.0`) paged to exhaustion → 7688 entries over 78 pages, 7666 distinct `Swagger` property URLs, every one fetched and walked → **2** declaring — `SVEIPE/test/1` (16), `APAZMAND_1/latch-developer_api/0.1.0` (1). **Postman** `POST https://www.postman.com/_api/ws/proxy` with `{"service":"search","method":"POST","path":"/search-all","body":{"queryIndices":["<index>"],"queryText":"iri","size":3,"from":0}}` → `apinetwork.team` team=5478, `runtime.collection` collection=97, `adp.api` api=1. **vendor portals** the 29 publisher-served documents listed under [the vendor set](#the-vendor-portals-this-regions-domain-implies), each fetched with `curl -sSL <url>` and its parsed body walked for `"format": "iri"` / `format: iri` → **0** declaring. |
| `format-iri-reference` | fern-rejected | The Mercure protocol — the same `mercure.local/0.3.2` pin, **4** `format: iri-reference` declarations counted in the parsed document; `dunglas/mercure`'s current `spec/openapi.yaml` at `bdcca607` still carries **2** and Fern refuses that too. `adrianzech/grid-time` `docs/openapi.yaml` (**8**) is refused the same way and declares no licence. The one `iri-reference` document Fern *accepts* is `stryd/efulfilmentshop-go` `api/openapi.yaml` (`MIT`, **36**), and it is not a witness: the repository is a Go client library for e-fulfilmentshop.nl, so the document is an unofficial third-party transcription | `https://api.apis.guru/v2/specs/mercure.local/0.3.2/openapi.json` | `AGPL-3.0` | `fern check` exits **1** with 2 errors, `Multiple request properties resolve to the same generated name lastEventId after camelCase normalization.`; `fern generate` not reached. CLI **5.67.1** via `fern.config.json`, generator `fernapi/fern-python-sdk` **5.20.0** — the pin `scripts/generate-fern-fixture.sh` defaults to | **Sourcegraph** (the sixth source, and why it leads the cell: see the note above this table) `content:"\"format\": \"iri-reference\"" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → **unanswered**: HTTP 429 behind a Cloudflare `Just a moment…` interstitial; `content:"format: iri-reference" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → **unanswered**: HTTP 429 behind a Cloudflare `Just a moment…` interstitial. Then the five this region declares. **APIs.guru** `curl -sS https://api.apis.guru/v2/list.json`, then every version's `swaggerUrl` fetched and each parsed body walked for a `format` value of `iri-reference` — 2529 APIs, 3992 versions, 0 fetch errors, **2** declaring — `alertersystem.com/1.6.0` (1297), `mercure.local/0.3.2` (4); and, as a second reading, `git clone --depth 1 https://github.com/APIs-guru/openapi-directory` at `f04b8d0b` walked the same way over its 4138 spec files in 701 provider directories → **2** declaring — `alertersystem.com/1.7.0` (1375), `mercure.local/0.3.2` (4). **GitHub** `gh api -X GET search/code -f q='<query>' -f per_page=100` with `"iri-reference" path:openapi extension:yaml` → 4; `"iri-reference" path:openapi extension:json` → 3; `"iri-reference" path:swagger` → 18; and, widened, `"iri-reference" filename:openapi.yaml` → 41; `"iri-reference" filename:openapi.json` → 18 results. **SwaggerHub** `GET https://api.swaggerhub.com/specs?specification=openapi-3.1.x&limit=100&page=N` (and `openapi-3.2.0`) paged to exhaustion → 7688 entries over 78 pages, 7666 distinct `Swagger` property URLs, every one fetched and walked → **16** declaring — `SEBASTIENTOURREL_1/mrod/0.0.0` (376), `Pure-Energi/irec-api/3.46.1` (325), `JAIME_5/metickal-ia/1.0.0` (151). **Postman** `POST https://www.postman.com/_api/ws/proxy` with `{"service":"search","method":"POST","path":"/search-all","body":{"queryIndices":["<index>"],"queryText":"iri-reference","size":3,"from":0}}` → `apinetwork.team` 0, `runtime.collection` 0, `adp.api` 0. **vendor portals** the 29 publisher-served documents listed under [the vendor set](#the-vendor-portals-this-regions-domain-implies), each fetched with `curl -sSL <url>` and its parsed body walked for `"format": "iri-reference"` / `format: iri-reference` → **0** declaring. |
| `format-json-pointer` | witness-found | Kubernetes Container Service Provider — `dcm-project/k8s-container-service-provider` `api/v1alpha1/openapi.yaml`, 20809 bytes fetched, **2** `format: json-pointer` declarations counted in the parsed document, on its RFC 7807 problem detail. **Publisher-owned**: the project's own organisation, and the document carries `info.license` `Apache 2.0` itself | `https://raw.githubusercontent.com/dcm-project/k8s-container-service-provider/2fb9a6decfdb290e819a1571ecb11ca4966ec19a/api/v1alpha1/openapi.yaml` | `Apache-2.0` | `fern check` exits **0**; `fern generate --group python-sdk --local --preview` exits **0** and writes a packaged SDK under `fern-python-sdk/src/`. CLI **5.67.1** via `fern.config.json`, generator `fernapi/fern-python-sdk` **5.20.0** — the pin `scripts/generate-fern-fixture.sh` defaults to | **Sourcegraph** (the sixth source, and why it leads the cell: see the note above this table) `content:"\"format\": \"json-pointer\"" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → **unanswered**: HTTP 429 behind a Cloudflare `Just a moment…` interstitial; `content:"format: json-pointer" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → **unanswered**: HTTP 429 behind a Cloudflare `Just a moment…` interstitial. Then the five this region declares. **APIs.guru** `curl -sS https://api.apis.guru/v2/list.json`, then every version's `swaggerUrl` fetched and each parsed body walked for a `format` value of `json-pointer` — 2529 APIs, 3992 versions, 0 fetch errors, **2** declaring — `telnyx.com/2.0.0` (2), `gsmtasks.com/2.4.13` (1); and, as a second reading, `git clone --depth 1 https://github.com/APIs-guru/openapi-directory` at `f04b8d0b` walked the same way over its 4138 spec files in 701 provider directories → **2** declaring — `telnyx.com/2.0.0` (2), `gsmtasks.com/2.4.13` (1). **GitHub** `gh api -X GET search/code -f q='<query>' -f per_page=100` with `"json-pointer" path:openapi extension:yaml` → 13; `"json-pointer" path:openapi extension:json` → 18; `"json-pointer" path:swagger` → 59; and, widened, `"json-pointer" filename:openapi.yaml` → 25; `"json-pointer" filename:openapi.json` → 40 results. **SwaggerHub** `GET https://api.swaggerhub.com/specs?specification=openapi-3.1.x&limit=100&page=N` (and `openapi-3.2.0`) paged to exhaustion → 7688 entries over 78 pages, 7666 distinct `Swagger` property URLs, every one fetched and walked → **5** declaring — `mts-e56/export-hub/1.0.2` (1), `1584335/redocly-museum_api/1.0.0` (1), `mts-e56/CDVF-ICD-01/1.2.0` (1). **Postman** `POST https://www.postman.com/_api/ws/proxy` with `{"service":"search","method":"POST","path":"/search-all","body":{"queryIndices":["<index>"],"queryText":"json-pointer","size":3,"from":0}}` → `apinetwork.team` 0, `runtime.collection` 0, `adp.api` 0. **vendor portals** the 29 publisher-served documents listed under [the vendor set](#the-vendor-portals-this-regions-domain-implies), each fetched with `curl -sSL <url>` and its parsed body walked for `"format": "json-pointer"` / `format: json-pointer` → **0** declaring. |
| `max-contains` | fern-rejected | Shittim Chest Records — `pitekusu/shittim-chest` `contracts/records/v1/openapi.json`, 139123 bytes fetched, **24** `maxContains` declarations counted in the parsed document, each paired with a `contains` and a `minContains` on the same array schema. **Publisher-owned**: the project's own repository, describing the records API its own site serves. `goptics/vizb` `api/openapi.yaml` at `5cf19543` (`MIT`, **8**) sits behind it and Fern refuses that too | `https://raw.githubusercontent.com/pitekusu/shittim-chest/ec22d3587d244ebd8deba65199b3b27f31817aa2/contracts/records/v1/openapi.json` | `MIT` | `fern check` exits **1** with 18 errors, `Endpoint requires auth, but no auth is defined.`; `fern generate` not reached. CLI **5.67.1** via `fern.config.json`, generator `fernapi/fern-python-sdk` **5.20.0** — the pin `scripts/generate-fern-fixture.sh` defaults to | **Sourcegraph** (the sixth source, and why it leads the cell: see the note above this table) `content:"\"maxContains\"" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 14 matches in 14 files; `content:"maxContains:" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 17 matches in 10 files. Then the five this region declares. **APIs.guru** `curl -sS https://api.apis.guru/v2/list.json`, then every version's `swaggerUrl` fetched and each parsed body walked for a `maxContains` key in schema position — 2529 APIs, 3992 versions, 0 fetch errors, **0** declaring; and, as a second reading, `git clone --depth 1 https://github.com/APIs-guru/openapi-directory` at `f04b8d0b` walked the same way over its 4138 spec files in 701 provider directories → **0** declaring. **GitHub** `gh api -X GET search/code -f q='<query>' -f per_page=100` with `"maxContains" path:openapi extension:yaml` → 1; `"maxContains" path:openapi extension:json` → 0; `"maxContains" path:swagger` → 52; and, widened, `"maxContains" extension:yaml path:api` → 1; `"maxContains" extension:json path:api` → 13 results. **SwaggerHub** `GET https://api.swaggerhub.com/specs?specification=openapi-3.1.x&limit=100&page=N` (and `openapi-3.2.0`) paged to exhaustion → 7688 entries over 78 pages, 7666 distinct `Swagger` property URLs, every one fetched and walked → **2** declaring — `malakoffhumanis/salesforce-fasst/1.0.3` (2), `frank-kilcommins/Swagger-Petstore-31/1.0.9-SNAPSHOT` (1). **Postman** `POST https://www.postman.com/_api/ws/proxy` with `{"service":"search","method":"POST","path":"/search-all","body":{"queryIndices":["<index>"],"queryText":"maxContains","size":3,"from":0}}` → `apinetwork.team` 0, `runtime.collection` 0, `adp.api` 0. **vendor portals** the 29 publisher-served documents listed under [the vendor set](#the-vendor-portals-this-regions-domain-implies), each fetched with `curl -sSL <url>` and its parsed body walked for `"maxContains"` / `maxContains:` → **0** declaring. |
| `min-contains` | fern-rejected | Shittim Chest Records — the same `pitekusu/shittim-chest` document, **24** `minContains` declarations counted in the parsed document; the three applicator keywords are declared together on the same 24 array schemas, so one corpus row would pin `contains`, `maxContains` and `minContains` at once. `goptics/vizb` (`MIT`, **8**) is refused the same way | `https://raw.githubusercontent.com/pitekusu/shittim-chest/ec22d3587d244ebd8deba65199b3b27f31817aa2/contracts/records/v1/openapi.json` | `MIT` | `fern check` exits **1** with 18 errors, `Endpoint requires auth, but no auth is defined.`; `fern generate` not reached. CLI **5.67.1** via `fern.config.json`, generator `fernapi/fern-python-sdk` **5.20.0** — the pin `scripts/generate-fern-fixture.sh` defaults to | **Sourcegraph** (the sixth source, and why it leads the cell: see the note above this table) `content:"\"minContains\"" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 36 matches in 20 files; `content:"minContains:" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 23 matches in 12 files. Then the five this region declares. **APIs.guru** `curl -sS https://api.apis.guru/v2/list.json`, then every version's `swaggerUrl` fetched and each parsed body walked for a `minContains` key in schema position — 2529 APIs, 3992 versions, 0 fetch errors, **0** declaring; and, as a second reading, `git clone --depth 1 https://github.com/APIs-guru/openapi-directory` at `f04b8d0b` walked the same way over its 4138 spec files in 701 provider directories → **0** declaring. **GitHub** `gh api -X GET search/code -f q='<query>' -f per_page=100` with `"minContains" path:openapi extension:yaml` → 1; `"minContains" path:openapi extension:json` → 9; `"minContains" path:swagger` → 52; and, widened, `"minContains" extension:yaml path:api` → 2; `"minContains" extension:json path:api` → 13 results. **SwaggerHub** `GET https://api.swaggerhub.com/specs?specification=openapi-3.1.x&limit=100&page=N` (and `openapi-3.2.0`) paged to exhaustion → 7688 entries over 78 pages, 7666 distinct `Swagger` property URLs, every one fetched and walked → **3** declaring — `malakoffhumanis/salesforce-fasst/1.0.3` (2), `Jiaheng/tni-transcript-delivery-api-for-oliver-wyman/1.0.0-discussion-preview.1` (2), `frank-kilcommins/Swagger-Petstore-31/1.0.9-SNAPSHOT` (1). **Postman** `POST https://www.postman.com/_api/ws/proxy` with `{"service":"search","method":"POST","path":"/search-all","body":{"queryIndices":["<index>"],"queryText":"minContains","size":3,"from":0}}` → `apinetwork.team` 0, `runtime.collection` 0, `adp.api` 0. **vendor portals** the 29 publisher-served documents listed under [the vendor set](#the-vendor-portals-this-regions-domain-implies), each fetched with `curl -sSL <url>` and its parsed body walked for `"minContains"` / `minContains:` → **0** declaring. |
| `multiple-of` | fern-rejected | Cloudflare — `cloudflare/api-schemas` `openapi.json`, 24861364 bytes fetched, **8** `multipleOf` declarations counted in the parsed document. **Publisher-owned**: Cloudflare's own organisation, the schemas its own API reference is built from. Four further permissively licensed real-world declarers sit behind it and Fern refuses every one — `pangeacyber/pangea-go` `dev/specs/ai-guard.openapi.json` (`MIT`, **4**), `CrowdStrike/aidr-python` `specs/ai-guard.openapi.json` (`MIT`, **4**), the APIs.guru `nytimes.com/books_api/3.0.0` conversion of `nytimes/public_api_specs` (`Apache-2.0`, **2**) and `box/box-openapi` `openapi.json` (`Apache-2.0`, **1**) | `https://raw.githubusercontent.com/cloudflare/api-schemas/f2df0ca75c0fec9047e91a34c20dd666c2dcd1ba/openapi.json` | `BSD-3-Clause` | `fern check` exits **1** with 234 errors, `Objects can only extend other objects, and AaaAuditLogsResponseCollection is not an object.`; `fern generate` not reached. CLI **5.67.1** via `fern.config.json`, generator `fernapi/fern-python-sdk` **5.20.0** — the pin `scripts/generate-fern-fixture.sh` defaults to | **Sourcegraph** (the sixth source, and why it leads the cell: see the note above this table) `content:"\"multipleOf\"" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 6629 matches in 1185 files; `content:"multipleOf:" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 518 matches in 191 files. Then the five this region declares. **APIs.guru** `curl -sS https://api.apis.guru/v2/list.json`, then every version's `swaggerUrl` fetched and each parsed body walked for a `multipleOf` key in schema position — 2529 APIs, 3992 versions, 0 fetch errors, **7** declaring — `royalmail.com/click-and-drop/1.0.0` (6), `nytimes.com/books_api/3.0.0` (2), `amadeus.com/amadeus-trip-parser/3.0.1` (2), …; and, as a second reading, `git clone --depth 1 https://github.com/APIs-guru/openapi-directory` at `f04b8d0b` walked the same way over its 4138 spec files in 701 provider directories → **7** declaring — `royalmail.com/click-and-drop/1.0.0` (6), `nytimes.com/books_api/3.0.0` (2), `ideal-postcodes.co.uk/3.7.0` (2), …. **GitHub** `gh api -X GET search/code -f q='<query>' -f per_page=100` with `"multipleOf" path:openapi extension:yaml` → 113; `"multipleOf" path:openapi extension:json` → 31; `"multipleOf" path:swagger` → 104; and, widened, `"multipleOf" path:openapi extension:yml` → 281; `"multipleOf" filename:openapi.yaml` → 418; `"multipleOf" filename:openapi.json` → 277 results. **SwaggerHub** `GET https://api.swaggerhub.com/specs?specification=openapi-3.1.x&limit=100&page=N` (and `openapi-3.2.0`) paged to exhaustion → 7688 entries over 78 pages, 7666 distinct `Swagger` property URLs, every one fetched and walked → **33** declaring — `CapitalMarket/InformationRequestInterface/1.0.0` (158), `capitalmarket-d05/InformationRequestInterface/1.0.0` (158), `JMSANN4012/booking-com_demand_api/3.1` (135). **Postman** `POST https://www.postman.com/_api/ws/proxy` with `{"service":"search","method":"POST","path":"/search-all","body":{"queryIndices":["<index>"],"queryText":"multipleOf","size":3,"from":0}}` → `apinetwork.team` team=31, `runtime.collection` collection=118, `adp.api` 0. **vendor portals** the 29 publisher-served documents listed under [the vendor set](#the-vendor-portals-this-regions-domain-implies), each fetched with `curl -sSL <url>` and its parsed body walked for `"multipleOf"` / `multipleOf:` → **3** declaring — Cloudflare (8), Box (1) and Vercel (1). |
| `property-names` | witness-found | **the corpus already has it.** `Kitware/VolView` `backend-contract/generated/openapi.json` is registered as corpus row 109, `volview-backend-contract`, and `just surface-census --selector schema.propertyNames` run over the fetched corpus reports **7** declarations in it — the cheapest candidate available, answered rather than passed over. Its Fern 5.20.0 golden is committed and byte-matches. A second, independent witness with no corpus row is the `ITISFoundation/osparc-simcore` document above (`MIT`, **30**), which Fern also accepts | `https://raw.githubusercontent.com/Kitware/VolView/496ada04c1d07235f98c92a68b5c374c7188ee87/backend-contract/generated/openapi.json` | `Apache-2.0` | re-run here on the registered document: `fern check` exits **0**; `fern generate --group python-sdk --local --preview` exits **0** and writes a packaged SDK. CLI **5.67.1** via `fern.config.json`, generator `fernapi/fern-python-sdk` **5.20.0** — the pin `scripts/generate-fern-fixture.sh` defaults to | **Sourcegraph** (the sixth source, and why it leads the cell: see the note above this table) `content:"\"propertyNames\"" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 3476 matches in 242 files; `content:"propertyNames:" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 483 matches in 87 files. Then the five this region declares. **APIs.guru** `curl -sS https://api.apis.guru/v2/list.json`, then every version's `swaggerUrl` fetched and each parsed body walked for a `propertyNames` key in schema position — 2529 APIs, 3992 versions, 0 fetch errors, **1** declaring — `vercel.com/0.0.1` (1); and, as a second reading, `git clone --depth 1 https://github.com/APIs-guru/openapi-directory` at `f04b8d0b` walked the same way over its 4138 spec files in 701 provider directories → **1** declaring — `vercel.com/0.0.1` (1). **GitHub** `gh api -X GET search/code -f q='<query>' -f per_page=100` with `"propertyNames" path:openapi extension:yaml` → 52; `"propertyNames" path:openapi extension:json` → 76; `"propertyNames" path:swagger` → 113 results. **SwaggerHub** `GET https://api.swaggerhub.com/specs?specification=openapi-3.1.x&limit=100&page=N` (and `openapi-3.2.0`) paged to exhaustion → 7688 entries over 78 pages, 7666 distinct `Swagger` property URLs, every one fetched and walked → **46** declaring — `fivesoto/ignition-http-api/1.0.0` (122), `PAWARITKL/cp-strapi-cms/0.1.0` (118), `ANDREIBOUARIU/timehubb/0.1.0` (94). **Postman** `POST https://www.postman.com/_api/ws/proxy` with `{"service":"search","method":"POST","path":"/search-all","body":{"queryIndices":["<index>"],"queryText":"propertyNames","size":3,"from":0}}` → `apinetwork.team` 0, `runtime.collection` 0, `adp.api` 0. **vendor portals** the 29 publisher-served documents listed under [the vendor set](#the-vendor-portals-this-regions-domain-implies), each fetched with `curl -sSL <url>` and its parsed body walked for `"propertyNames"` / `propertyNames:` → **2** declaring — OpenAI `openai-openapi` `openapi.yaml` (2) and the OAI 3.1 meta-schema (1). |
| `unevaluated-items` | fern-rejected | Asserts, Inc. — `grafana/grafana-asserts-public-clients` `openapi.yaml` again, **2** `unevaluatedItems` declarations counted in the parsed document beside its 2 `contains` ones. **Publisher-owned** on the same test. The only other real-world declarer any source produced is HotCRP's own `kohler/hotcrp` `devel/openapi.json` at `9758d996` (**2**), which Fern refuses with 6 errors, `Unexpected property "ok"`, and whose repository licence reads `NOASSERTION` | `https://raw.githubusercontent.com/grafana/grafana-asserts-public-clients/7a5d002f5d7c40a53fc77e7156ccb8a362982f0c/openapi.yaml` | `Apache-2.0` | `fern check` exits **1** with 2 errors, `Enum name  for value % is not suitable for code generation.`; `fern generate` not reached. CLI **5.67.1** via `fern.config.json`, generator `fernapi/fern-python-sdk` **5.20.0** — the pin `scripts/generate-fern-fixture.sh` defaults to | **Sourcegraph** (the sixth source, and why it leads the cell: see the note above this table) `content:"\"unevaluatedItems\"" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 9 matches in 7 files; `content:"unevaluatedItems:" count:all file:(openapi\|swagger).*\.(yaml\|yml\|json)$` → 11 matches in 8 files. Then the five this region declares. **APIs.guru** `curl -sS https://api.apis.guru/v2/list.json`, then every version's `swaggerUrl` fetched and each parsed body walked for a `unevaluatedItems` key in schema position — 2529 APIs, 3992 versions, 0 fetch errors, **0** declaring; and, as a second reading, `git clone --depth 1 https://github.com/APIs-guru/openapi-directory` at `f04b8d0b` walked the same way over its 4138 spec files in 701 provider directories → **0** declaring. **GitHub** `gh api -X GET search/code -f q='<query>' -f per_page=100` with `"unevaluatedItems" path:openapi extension:yaml` → 1; `"unevaluatedItems" path:openapi extension:json` → 2; `"unevaluatedItems" path:swagger` → 80; and, widened, `"unevaluatedItems" filename:openapi.yaml` → 7; `"unevaluatedItems" filename:openapi.json` → 29; `"unevaluatedItems" extension:yaml path:api` → 0 results. **SwaggerHub** `GET https://api.swaggerhub.com/specs?specification=openapi-3.1.x&limit=100&page=N` (and `openapi-3.2.0`) paged to exhaustion → 7688 entries over 78 pages, 7666 distinct `Swagger` property URLs, every one fetched and walked → **0** declaring. **Postman** `POST https://www.postman.com/_api/ws/proxy` with `{"service":"search","method":"POST","path":"/search-all","body":{"queryIndices":["<index>"],"queryText":"unevaluatedItems","size":3,"from":0}}` → `apinetwork.team` 0, `runtime.collection` 0, `adp.api` 0. **vendor portals** the 29 publisher-served documents listed under [the vendor set](#the-vendor-portals-this-regions-domain-implies), each fetched with `curl -sSL <url>` and its parsed body walked for `"unevaluatedItems"` / `unevaluatedItems:` → **0** declaring. |

#### Every candidate the search reached, and what became of it

The outcome above is the best any candidate for that row reached, not the first
one examined. Each candidate below was fetched at the pinned ref and its
declarations counted in the fetched bytes; `fern check` was run on the ones that
survived that count and were both redistributable and real-world enough to be
worth the run.

| candidate | ref | license | counted in the fetched bytes | what became of it |
|---|---|---|---|---|
| `speakeasy-sdks/orb-python` `openapi/openapi.yaml` | `c98a5d45` | MIT | `$anchor`=24 (`subscription-status`, `external-marketplace`, `model-type`, …) | **not a witness — an unproven third-party distribution.** By content it is the strongest `$anchor` candidate anywhere: 281778 bytes describing Orb's billing API against `https://api.withorb.com/v1`, `info.contact` `Orb, Inc.`/`team@withorb.com`, 24 anchors, MIT, pinned, and `fern check` reports `Found 0 errors and 7 warnings`. What it has not got is proof that Orb published or authored those bytes. The repository is Speakeasy's, not Orb's; Orb publishes no OpenAPI document this search could reach (nothing in `orbcorp`'s eight SDK repositories, and `docs.withorb.com/openapi.{json,yaml}`, `docs.withorb.com/api-reference/openapi.json` and `withorb.com/openapi.json` 404 while `api.withorb.com/openapi.json` 403s); and the repository's own `.github/workflows/speakeasy_sdk_generation.yml` sets `openapi_doc_location: ./openapi/openapi.yaml`, regenerating from the copy in its own tree and recording no upstream URL. Named contact metadata inside a file is not provenance for the file. An earlier commit on this branch recorded it as the `dollar-anchor` witness; that row is now `witness-blocked` on Inkeep |
| `inkeep/chat-api-openapi-schema` `openapi.yaml` | `76e79a7e` | **none declared** | `$anchor`=2 — `ChatModeOptions` (line 350) and `RecordTypeEnumerated` (line 468), each naming a `oneOf` branch; no `$comment`, `$dynamicAnchor`, `$dynamicRef` or `$vocabulary` | **recorded** as `witness-blocked` for `dollar-anchor` — of everything this search reached that declares `$anchor`, the one document published by the API it describes; every other `$anchor` declaration sits in a synthetic fixture, a Petstore copy, a third-party repository, or a SwaggerHub version pinned to no immutable ref. Inkeep's own organisation, its own production API (`https://api.inkeep.com`, `openapi` 3.1.0, two `/v0/chat_sessions/…` paths), `fern check` clean, immutably pinned, at a credential-free `.yaml` URL — and unusable only because the repository declares no licence: its whole tree is `.gitignore`, `README.md`, `openapi.yaml`, and the GitHub licence endpoint 404s |
| `Kitware/VolView` `backend-contract/generated/openapi.json` | `496ada04` | Apache-2.0 | `$comment`=2 | **recorded** for `dollar-comment` — publisher-owned (Kitware's own organisation, describing the backend VolView's own client calls), Apache-2.0, and the only `$comment` candidate `fern check` accepted |
| `darai0512/payjp-rust` `openapi/docs/openapi.json` | `40015d49` | MIT | `$dynamicAnchor`=1 on `components.schemas.balance`, `$dynamicRef`=1 on `components.schemas.list.properties.data.items` | **not a witness — an unofficial third-party transcription.** It is the closest miss either dynamic row has, and worth reading before anyone repeats the search: it describes a live commercial API (`openapi` 3.1.0, server `https://api.pay.jp/v1`, 36 paths with Japanese parameter descriptions, `info.description` `https://pay.jp/docs/api`), it uses the pair for the job the pair exists for — a `$dynamicAnchor`-tagged resource schema and a shared list envelope that `$dynamicRef`s back to it — it is MIT, pinned, and `fern check` accepts it (`All checks passed`). It fails on one thing only: PAY.JP does not publish it. Its own repository describes it as an "Unofficial Pay.jp OpenAPI spec & Rust bindings", so the bytes are a third party's reading of the API rather than the publisher's description of it, and a golden byte-matched over them would verify crozier against a document no publisher stands behind. An earlier commit on this branch recorded it as the witness for both dynamic rows; both are now `none-found` |
| `geo-engine/BioIS` `openapi.json` | `afdcab7d` | proprietary (`NOASSERTION`) | `relative-json-pointer`=1 | **recorded** as `witness-blocked` for `format-relative-json-pointer` — publisher-owned (Geo Engine GmbH's own organisation, not a fork), a real `RelativeJsonPointer` component on its production API, `fern check` clean, and unusable only because its `LICENSE` reads `PROPRIETARY LICENSE … not licensed under an Open Source initiative` |
| `eve0415/oasts` `fixtures/validators-showcase-3.1/openapi.yaml` | `6b7076a5` | MIT | `$dynamicAnchor`=1, `$dynamicRef`=1 | **not a witness — a synthetic fixture.** Titled "Oasts validators showcase", served from `https://validators.api.example.test/v1`, and living under `fixtures/` beside sibling documents named for the keyword each exercises. `fern check` accepts it, and an earlier pass of this search recorded it for both dynamic rows; nothing replaces it, which is why both are `none-found` |
| `speakeasy-api/openapi` `openapi/testdata/petstore31.openapi.json` | `58b79e91` | MIT | `$anchor`=1, `$vocabulary`=1 | **not a witness — a synthetic fixture.** The Swagger Petstore 3.1 sample (`info.title` "Swagger Petstore - OpenAPI 3.1", `termsOfService` `https://swagger.io/terms/`) checked in under `openapi/testdata/`. `fern check` accepts it, and an earlier pass recorded it for `dollar-vocabulary`; nothing replaces it, which is why that row is `none-found` |
| `justiceo/openapi-zod` `test/fixtures/primitives/openapi.yaml` | `ff001507` | MIT | `relative-json-pointer`=1 | **not a witness — a synthetic fixture.** A 1683-byte `paths: {}` document under `test/fixtures/` declaring one component per JSON Schema `format`, `UnknownFormat: {format: not-a-real-format}` among them. `fern check` accepts it, and an earlier pass recorded it for `format-relative-json-pointer`; BioIS replaces it as the real-world candidate, blocked on licence |
| `crance/swagger-petstore-demo` `src/main/webapp/code-first/openapi.json` and `…/openapi.yaml` | `1b113c4b` | Apache-2.0 | `$anchor`=1, `$vocabulary`=1 in each (10905 and 7882 bytes) | the same Petstore 3.1 sample — `info.title` is `Swagger Petstore - OpenAPI 3.1` — dropped into a demo application; not screened, because a demo of a fictional pet store is not a real API's description |
| `gemyago/golang-backend-boilerplate` `internal/infrastructure/petstore/openapi.json` | `80e12340` | MIT | `$anchor`=1, `$vocabulary`=1 | byte-identical to the Speakeasy Petstore copy — same MD5 `5bacfb65…`, 10753 bytes — so it fails the same way |
| `oaplatform/oap` `oap-ws/oap-ws-openapi-ws/src/test/resources/oap/ws/openapi/OpenapiWSTest/openapi.json` | `72ff77c9` | MIT | `$dynamicAnchor`=2, `$dynamicRef`=2, `$vocabulary`=2 | a Java unit-test resource under `src/test/resources/` — `info.title` is `Test title` and its paths are `/api/`, `/api/bean`, `/api/bytes`, … ; not a real API's description, and not screened |
| `eve0415/oasts` `fixtures/anchors-3.1/openapi.yaml` | `e1abff51` | MIT | `$anchor`=1 | a 368-byte fixture from the same showcase repository, and a fixture for the same reason as its sibling above |
| `fern-api/fern` `packages/cli/api-importers/v3-importer-tests/src/__test__/fixtures/deeply-recursive/openapi.yml` | `076da817` | Apache-2.0 | `$anchor`=1, `$comment`=1, `$dynamicRef`=1 | Fern's own importer fixture — a synthetic test input twice over, and a golden generated from Fern's own test input would be weaker parity evidence than an independent document |
| `quay/clair` `httptransport/api/v1/openapi.json` | `8c540b96` | Apache-2.0 | `$comment`=6 | **fern-rejected** — `fern check`: 1 error, `__package__.yml -> types -> : Type name must begin with a letter` |
| `supabase/supabase` `apps/docs/spec/storage_v0_openapi.json` | `b0194127` | Apache-2.0 | `$comment`=4 | **fern-rejected** — `fern check`: 2 errors, `DeleteObjectBucketNameRequest is already declared in this file` and `PostObjectSignBucketNameRequest is already declared in this file` |
| `swagger-api/swagger-core` `…/comprehensiveOAS31/comprehensive-openapi.yaml` | `cb226922` | Apache-2.0 | `$anchor`=7, `$dynamicAnchor`=1, `$dynamicRef`=2 | a `src/test/resources` fixture, and **fern-rejected** anyway — `fern check`: 1 error, `Type name must begin with a letter` |
| `engine-public/protoc-gen-openapi` `examples/…/StorefrontService.openapi.yaml` | `d2073b12` | Apache-2.0 | `$anchor`=2, `$dynamicAnchor`=2, `$dynamicRef`=2 | a generator example, and **fern-rejected** anyway — `fern check`: 7 errors, `Missing discriminant property ("category")` on three response examples and `Path parameter is unreferenced in endpoint: productId` on four endpoints |
| `zaghaghi/openapi-tui` `openapi-31/openapi-in-openapi.json` (and the `tamadoge-max` fork) | `1ea673ae` | MIT | `$comment`=29, `$dynamicAnchor`=1, `$dynamicRef`=4 | the OpenAPI 3.1 meta-schema restated as an OpenAPI document — a tool's own sample, not an API — and **fern-rejected** anyway: `fern check`: 5 errors, all `Objects can only extend other objects` on `SpecificationExtensions`, `StylesForForm` and `Examples` |
| `speakeasy-sdks/wingspan-python` `openapi.yaml` | `a3f5548e` | MIT | `$anchor`=140 | the largest `$anchor` count found anywhere, and doubly unusable: it is the same **unproven third-party distribution** shape as the Orb copy above — a `speakeasy-sdks` repository, not Wingspan's — and **fern-rejected** in any case (`fern check`: 185 errors) |
| `nextcloud/server` `apps/files/openapi.json`; `openfoodfacts/food-logging` `schemas/openapi.yaml` | `2f29ad8f`, `9cfcceb8` | AGPL-3.0 | `$comment`=2 and `$comment`=1 | real API descriptions, but AGPL-3.0 is outside the corpus's Apache-2.0/MIT/BSD/CC0 rule; not screened |
| `pwharned/crassus` `openapi.json`; `cyberthing-dev/embeddingserver` `schemas/openapi.json`; `forrasz/soccer-notebook` `openapi/openapi.yaml`; `inkeep/chat-api-openapi-schema` `openapi.yaml`; `api-evangelist/polytomic`, `api-evangelist/crunchdao`, `curedao/curedao-monorepo` | fetched and counted | **no license** | between them `$anchor`, `$comment`, `$dynamicAnchor`, `$dynamicRef`, `$vocabulary` | not redistributable: the repositories declare no license at all, so nothing grants redistribution. `forrasz/soccer-notebook` is additionally a serializer dump — its 37 occurrences of each key are `$anchor: null`, `$vocabulary: null`, `$dynamicAnchor: null` emitted for every schema |
| `api-evangelist/mparticle` `openapi/mparticle-data-plan-api-openapi.yml` | `82d4d720` | **no license** | `$anchor`=1, `$comment`=1, `$vocabulary`=1 | mParticle is a real API, but the three strings are members of a YAML list of reserved keyword names (`- $recursiveRef`, `- $ref`, `- $schema`, `- $vocabulary`, `- timeLimit`, `- $comment`), not declarations — and the repository declares no license |
| `paiml/rust-mcp-sdk` `contracts/mcp-protocol-sdk-v1.yaml` | `0c8900c5` | MIT | `$vocabulary`=1 | not an OpenAPI document at all — a `metadata:`/`equations:` contract file, and the occurrence is prose inside a derivation note |
| `opengeospatial/ogcapi-processes` `openapi/schemas/cql2/cql2.yaml` | `8193f9d5` | `NOASSERTION` | `$dynamicAnchor`=1 | a 569-byte JSON Schema fragment, not an OpenAPI document, and the occurrence is a **commented-out** `#$dynamicAnchor: cql2expression` on line 2 |
| `sailpoint-oss/barrelman` `schemas/generated/openapi-3.{0,1,2}-*.json`; `sailpoint-oss/navigator` `schemas/meta/*.schema.json` | `f6b39095`, `178403a6` | MIT; navigator declares none | `relative-json-pointer`=1 in each of the two read | JSON Schema meta-schemas describing OpenAPI (top-level `$schema`/`$defs`, no `openapi`/`paths`), where the string is a member of the `format` enum; not OpenAPI documents |
| `ongres/stackgres` `stackgres-k8s/src/restapi/src/main/swagger/schema.json`; `data-fair/openapi-viewer` `api/types/OpenAPISpecs/schema.json`; `pwharned/crassus` `openapi.json`; `cyberthing-dev/embeddingserver` `schemas/openapi.json` | `0b3110fa`, `87535158`, `2c874e72`, `34ed62ce` | AGPL-3.0; MIT; none; none | `$dynamicAnchor`=1 and `$dynamicRef`=3–4 in each, beside `$comment`=29 | vendored copies of the **OpenAPI 3.1 meta-schema**, not API descriptions — each opens with `"$id": "https://spec.openapis.org/oas/3.1/schema/<date>"` and the recurring `29/1/4` count triple is that meta-schema's own. The first two are a product's repository, which is why they were fetched; the last two also declare no license |
| `sven-victor/ez-console` `openapi/swagger.yaml` | `1605edc7` | Apache-2.0 | `$dynamicRef`=2 | not a declaration: both occurrences are a **property name** — `$dynamicRef: {description: section 8.2.3.2, type: string}` on a model of JSON Schema itself, and a member of a list of reserved keyword names. The document also declares no `$dynamicAnchor` |
| `Ira-Panasiuk-2024/Money-Guard-backend` `docs/swagger.json`; `OksanaSmoliar/project-CodeSamurai-backend` `docs/swagger.json`; `solana-foundation/pay-skills` `providers/…/generativelanguage/openapi.json`; `simonliu-ai-product/Agentimon` `google-adk-server/openapi.json`; `mfzzf/astraflow` `openapi/…/gemini-v1beta-models.yaml` | `97cf74ec`, `84b88c30`, `3a53b5c6`, `6270bb96` | none; none; MIT; GPL-3.0 | `$anchor`=1–2 each, **none of them a declaration** | the five documents from the widened `$anchor` sweep that looked like a service's own description. The first two carry `$anchor:` inside an image-URL `description` string; the rest carry a backticked `` `$anchor` `` in Google's Gemini prose listing the JSON Schema keywords it supports, and are third-party copies of Google's document rather than Google's own |
| SwaggerHub `frank-kilcommins/Swagger-Petstore-31/1.0.9-SNAPSHOT`, `test-2a8-f7e/qtTestSchema/1.0.1`, `kokyhin/Admin/1.0.0`, `kokyhin/Minerva/1.0.0`, `1584335/redocly-museum_api/1.0.0`, `PangaeaInnovations/grid-parameters-v3-unified-grid-schema-api/3.0.0`, `frr-94c/sirene-api/3.11`, `sandeep.abhyankar/IDMAuthentication/1.0.0`, `Mafia-Mystery/mafia-mystery/1.0`, `test-450-85a/myparcel-api/2025-02-13` | none available | none declared | between them `$anchor`, `$comment`, `$dynamicAnchor`, `$dynamicRef`, `$vocabulary` | reachable at a direct spec URL — `https://api.swaggerhub.com/apis/<owner>/<api>/<version>/swagger.json` resolves 200 — but at **no immutable ref**: a SwaggerHub version string is republishable in place, so the bytes behind that URL can change without the URL changing, and none of the ten declares a license. Several are also plainly not real APIs (`qtTestSchema`, `mafia-mystery`, the Petstore copy) |
| `wso2/api-platform` gemini `openapi.yaml`, `atacan/GoogleGenerativeLanguage` `assets/openapi.json`, `52North/connected-systems-pygeoapi`, `api-evangelist/onshape`, `oaplatform/oap` `openapiWithoutDeprecated.json`, `AlejoTorres2001/aos_grupo9`, `pavlokamozin/talend-test`, `rmasters/mcp-openapi`, `jcjveraa/quarkus-openapi-generator-demoapp`, `Axway-API-Management-Plus/apim-cli`, `swagger-api/apidom`, `postman-cs/postman-bootstrap-action`, `clairey-zx81/json-model`, `accretional/openapi2proto`, `handrews/handrews.github.io`, `tushkum34-cloud/jslibsmeta` | fetched or read at the search result | — | **nothing, or nothing that is a declaration** | GitHub code search returned these because it tokenizes away the leading `$` and matches the bare word, or because the file is a bundled `swagger-ui-dist` asset, a GraalVM `reflect-config.json`, a search index, an OpenAPI meta-schema, or a further Petstore copy. The one occurrence in the WSO2 Gemini document is a backticked `` `$anchor` `` in a description string |

**The twenty ranked `FIXTURE` rows added the candidates below.** Same rule as above: each was fetched at the pinned reference, its declarations counted in the parsed bytes, and `fern check` plus a real `fern generate` run on the ones that survived the count and were both redistributable and real-world enough to be worth the run — thirty documents in all. Reading them in order is the point: nine of these twenty rows lose their best candidate to Fern rather than to supply, and two lose it to provenance while Fern accepts it outright.

| candidate | ref | license | counted in the fetched bytes | what became of it |
|---|---|---|---|---|
| `ITISFoundation/osparc-simcore` `services/web/server/src/simcore_service_webserver/api/v0/openapi.json` | `7fa7c73d` | `MIT` | `contentMediaType`=24, `contentSchema`=24, numeric `exclusiveMaximum`=8, `propertyNames`=30 — 810142 bytes | **recorded** as `witness-found` for four rows at once (`content-media-type`, `content-schema`, `exclusive-maximum-numeric`, `property-names`): publisher-owned (the IT'IS Foundation's own organisation, describing the API its own oSPARC front end calls), `MIT`, and Fern accepts it through both halves — `fern check` 0 errors and `fern generate` writes a packaged SDK |
| `HelixDB/helix-db` `docs/openapi.json` | `f19b488b` | `Apache-2.0` | `dependentRequired`=2, `propertyNames`=2 — 25352 bytes | **recorded** as `witness-found` for `dependent-required`; the smallest document this search accepted for any row, and Fern passes both halves |
| `emergentmethods/flowdapt` `openapi.json` | `e8f34d41` | `Apache-2.0` | `$defs`=7 — 162458 bytes | **recorded** as `witness-found` for `dollar-defs`; Fern passes both halves |
| `dcm-project/k8s-container-service-provider` `api/v1alpha1/openapi.yaml` | `2fb9a6de` | `Apache-2.0`, and the document's own `info.license` says so too | `format: json-pointer`=2 — 20809 bytes | **recorded** as `witness-found` for `format-json-pointer`; Fern passes both halves |
| `Kitware/VolView` `backend-contract/generated/openapi.json`, **already corpus row 109** | `496ada04` | `Apache-2.0` | `propertyNames`=7, measured by `just surface-census --selector schema.propertyNames` over the fetched corpus rather than by this search's own walk | **recorded** as `witness-found` for `property-names` — the cheapest candidate available and the one a search could most easily have walked past, since the row's `evidence` cell still reads the pre-registration `0 declarations across all 126 registered sources`. Re-screened here: `fern check` 0, `fern generate` 0, packaged SDK written |
| `wttw/aboutmyemail` `ameapi.yaml` | `3575c0e4` | **none declared** | `format: idn-email`=4 — 8120 bytes | **recorded** as `witness-blocked` for `format-idn-email`: publisher-owned (Word to the Wise's own repository for the AboutMy.email service), Fern passes both halves, and the licence is the only thing in the way |
| `grafana/grafana-asserts-public-clients` `openapi.yaml` | `7a5d002f` | `Apache-2.0` | `contains`=2, `unevaluatedItems`=2 — 264919 bytes | **recorded** as `fern-rejected` for both `contains` and `unevaluated-items`: publisher-owned (Grafana Labs' own organisation), redistributable, and refused by `fern check` with 2 errors, `Enum name  for value % is not suitable for code generation.` |
| `pitekusu/shittim-chest` `contracts/records/v1/openapi.json` | `ec22d358` | `MIT` | `contains`=24, `maxContains`=24, `minContains`=24 — 139123 bytes | **recorded** as `fern-rejected` for `max-contains` and `min-contains`; `fern check` exits 1 with 18 `Endpoint requires auth, but no auth is defined.` errors. One document declaring all three applicator keywords together is what a single corpus row would need |
| `goptics/vizb` `api/openapi.yaml` | `5cf19543` | `MIT` | `contains`=8, `maxContains`=8, `minContains`=8 — 31151 bytes | the same three rows' second candidate; `fern check` exits 1, `Unexpected property "errors"` |
| `cloudflare/api-schemas` `openapi.json` | `f2df0ca7` | `BSD-3-Clause` | `multipleOf`=8, `format: ipv6`=3 — 24861364 bytes | **recorded** as `fern-rejected` for `multiple-of`; `fern check` exits 1 with 234 errors, `Objects can only extend other objects, and AaaAuditLogsResponseCollection is not an object.` |
| `linode/linode-api-docs` `openapi.json` | `235e8a90` | `Apache-2.0` | `format: ipv6`=3 — 7911272 bytes | **recorded** as `fern-rejected` for `format-ipv6`; `fern check` exits 1 with 459 errors, `Expected example to be an integer. Example is: 41.5`. This is the upstream `APIs-guru/openapi-directory`'s `linode.com` entry converts from |
| `pangeacyber/pangea-go` `dev/specs/ai-guard.openapi.json` | `1c531a72` | `MIT` | `multipleOf`=4, `format: json-pointer`=2 — 155277 bytes | not a witness — Fern refuses it: `fern check` exits 1, `PangeaResponseStatus is already declared in aiGuard.yml`. Publisher-owned (the official Pangea Go monorepo), so it is a licence-clean candidate both rows lose only on Fern |
| `pangeacyber/pangea-go` `dev/specs/audit.openapi.json` | `c4ea22ab` | `MIT` | `format: json-pointer`=2, `dependentRequired`=1 — 114928 bytes | refused the same way, `PangeaResponseStatus is already declared in audit.yml` |
| `CrowdStrike/aidr-python` `specs/ai-guard.openapi.json` | `13249da6` | `MIT` | `multipleOf`=4, `format: json-pointer`=2, `format: ipv6`=1 — 209834 bytes | refused with the same diagnostic against `aidr.yml`; it is the same Pangea document under CrowdStrike's ownership, so it is not an independent candidate either |
| APIs.guru `nytimes.com/books_api/3.0.0/openapi.json`, converted from `nytimes/public_api_specs` `books_api/books_api.json` at `76c52d6f` | version `3.0.0` | `Apache-2.0` upstream; the converted document declares no `info.license` | `multipleOf`=2 in both the conversion and the upstream | not a witness — `fern check` exits 1 on the conversion with 6 `Endpoint requires auth, but no auth is defined.` errors, and on the upstream with `Failed to convert OpenAPI v2 (Swagger) spec to OpenAPI v3: (Patchable) info should not have logo property`, because what NYT publishes is Swagger 2.0 |
| `box/box-openapi` `openapi.json` | `933ec4c5` | `Apache-2.0` | `multipleOf`=1 — 1780704 bytes | not a witness — `fern check` exits 1 with 153 errors, `Object has multiple properties named "sha1":` |
| `openai/openai-openapi` `openapi.yaml` | `91cc0564` | `MIT` | `propertyNames`=2 — 3049818 bytes | not a witness — `fern check` exits 1 with 6 errors, `Object has multiple properties named "top_logprobs":`. Superseded anyway: `property-names` has a registered witness |
| `NVIDIA-Omniverse/content-agents` `apps/physics_agent_service/openapi.yaml` | `99b773e6` | `Apache-2.0` | `contentMediaType`=9 — 69842 bytes | a second `content-media-type` declarer Fern accepts through both halves; recorded here rather than in the row because the oSPARC document pins `contentSchema` beside it and this one does not |
| `mistralai/platform-docs-public` `openapi.yaml` | `ff846cf9` | `Apache-2.0` | `propertyNames`=9, `$defs`=2 — 661948 bytes | not a witness — `fern check` exits 1 with 14 errors, `Default value '-created' is not a valid enum value` |
| `RavelloH/InsightFlare` `docs/openapi.yaml` | `370ac5fc` | `MIT` | `$defs`=35, `propertyNames`=5 — 866769 bytes | not a witness, and the one failure worth naming as its own kind: `fern check` does not refuse it, it **aborts** — `FATAL ERROR: Ineffective mark-compacts near heap limit Allocation failed - JavaScript heap out of memory` after ~8 minutes at a 4 GB V8 heap. A crash is not a verdict, so the row it would have served took the smaller Flowdapt document instead |
| `kohler/hotcrp` `devel/openapi.json` | `9758d996` | `NOASSERTION` | `unevaluatedItems`=2 — 673741 bytes | not a witness twice over — HotCRP's own document, but outside the redistribution set, and `fern check` exits 1 with 6 errors, `Unexpected property "ok"` |
| `duy-tung/serving-contracts` `openapi/inference-api.yaml` | `2df9f810` | `Apache-2.0` | `dependentSchemas`=1 — 33148 bytes | not a witness — **no API serves it.** It is the only `dependentSchemas` candidate anywhere in this search that is neither a meta-schema nor a test resource, and Fern accepts it through both halves, which is why it is set out rather than listed: its single `servers` entry is `https://gateway.serving-contracts.invalid`, an RFC 2606 reserved name its own `description` calls a placeholder, in a two-day-old repository with no deployment behind it. Registering it would put a document in the corpus that no real API ever served |
| `stryd/efulfilmentshop-go` `api/openapi.yaml` | `8dc4abd4` | `MIT` | `format: iri-reference`=36 — 104956 bytes | not a witness — an **unofficial third-party transcription**, and the closest miss `format-iri-reference` has: it is redistributable and Fern accepts it through both halves. The repository describes itself as a *Go client library for interacting with e-fulfilmentshop.nl*, so the publisher of the API is not the publisher of the document |
| `dunglas/mercure`, read at APIs.guru `mercure.local/0.3.2/openapi.json` | version `0.3.2` | `AGPL-3.0` | `format: iri`=2, `format: iri-reference`=4 | **recorded** as `fern-rejected` for both `format-iri` and `format-iri-reference`: publisher-owned (the protocol's own reference implementation), blocked on licence *and* refused by `fern check` with 2 errors, `Multiple request properties resolve to the same generated name lastEventId after camelCase normalization.` The current `spec/openapi.yaml` at `bdcca607` carries 2 `iri-reference` and is refused the same way |
| `adrianzech/grid-time` `docs/openapi.yaml` | `45f51c7c` | **none declared** | `format: iri-reference`=8 — 50011 bytes | not a witness — no licence, and `fern check` exits 1 with 9 errors, `Multiple request properties resolve to the same generated name seasonSeriesCode after camelCase normalization.` |
| Short.io's own `https://api.short.io/openapi.json` | none — a live endpoint | **none declared** | `format: iri`=4, `format: idn-hostname`=1 — 239940 bytes | **recorded** as `fern-rejected` for `format-idn-hostname` and named for `format-iri`: the only publisher-*served* document this search reached, and it fails on all three screens — the address is republished in place, no licence is declared, and `fern check` exits 1 with 3 errors, `CreateLinkRequest is already declared in linkManagement.yml` |
| `jentic/jentic-public-apis` `apis/openapi/sandbox.volt.io/main/2024.04.11/openapi.json` | `eb9d12a2` | repository `CC0-1.0`; the document declares no `info.license` | `format: iri-reference`=3 — 134184 bytes | not a witness — an unproven third-party distribution, and the one candidate that shows why the Fern screen has two halves: `fern check` reports `Found 0 errors` and `fern generate` then exits 1 with `Unknown OAuth type: 'undefined'` |
| `jentic/jentic-public-apis` `apis/openapi/short.io/main/1.0.0/openapi.json` | `eb9d12a2` | as above | `format: iri`=4, `format: idn-hostname`=1 — 422431 bytes | not a witness — a third-party distribution of an older 3.0.3 version of the Short.io document above; it does not make that document immutably referenceable, because the bytes it pins are not the bytes Short.io serves |
| `open-feature/protocol` `service/openapi.yaml` | `9183895d` | `Apache-2.0` | `contentMediaType`=1, `contentSchema`=1 — 27704 bytes | not a witness — `fern check` exits 1 with 9 errors, `Objects can only extend other objects, and FlagMetadataDescription is not an object.` |
| `maximhq/bifrost` `docs/openapi/schemas/management/governance.yaml` | `47058e2e` | `Apache-2.0` | `dependentRequired`=1, numeric `exclusiveMaximum`=3 — 75635 bytes | a licence-clean second candidate for two rows that both already have an accepted witness; not screened, and named so a later search does not re-derive it |
| `oasdiff/oasdiff` `data/openapi31/base.yaml` | `a226cecc` | `Apache-2.0` | eleven of these twenty rows in one 3400-byte file — `contains`, `maxContains`, `minContains`, `propertyNames`, `dependentSchemas`, `dependentRequired`, `unevaluatedItems`, `contentMediaType`, `contentSchema`, `$defs` and numeric `exclusiveMaximum`, one declaration each | not a witness — a **synthetic fixture**: it is a diff tool's own test data, which is why one small file happens to declare eleven rarely-used keywords at once. The densest single document this search found, and the clearest illustration of why density is not provenance |
| `QutEcoacoustics/baw-server` `swagger/v2/defs/resources.json` | `9ba186c9` | `Apache-2.0` | `$defs`=1, `maxContains`=1, `minContains`=1 — 4584 bytes | not a witness — a `swagger/v2/defs/` **fragment** rather than a document: it has no `openapi`, `info` or `paths`, so nothing generates from it on its own |
| `spec.openapis.org/oas/3.1/schema/2022-10-07` and `json-schema.org/draft/2020-12/meta/validation` | the published dated schema URLs | the OAI's and the JSON Schema project's own terms | `$defs`=4 and 1, `dependentSchemas`=3, `propertyNames`=1 | not witnesses — **meta-schemas**. They are in the vendor set on purpose, because this region's domain is the 2020-12 dialect, and they are the reason `dependent-schemas` and `dollar-defs` report non-zero vendor counts while neither has a vendor witness |
| SwaggerHub, in bulk | the registry's own `Swagger` property URLs | per document; most declare none | of 7666 distinct public 3.1/3.2 specs, the declaring counts per row are in each row's last cell — 126 for `contentMediaType`, 46 for `propertyNames`, 35 for numeric `exclusiveMaximum`, 33 for `multipleOf`, 16 for `iri-reference`, down to 0 for `dependentSchemas`, `unevaluatedItems`, `idn-hostname` and `relative-json-pointer` | no witness among them. The permissively licensed ones are Petstore 3.1 copies (`frank-kilcommins/Swagger-Petstore-31`), personal re-uploads of somebody else's API (`westriverconsulting/azure-ai-foundry-models-service` re-publishing Azure's, `DJ311ROCKS/open-ai_api` and `ssb81/openai-api` re-publishing OpenAI's) or test schemas (`test-2a8-f7e/qtTestSchema`); and SwaggerHub versions are editable in place, so the registry offers no immutable reference even for the rest |
| `jentic/jentic-public-apis`, in bulk | `eb9d12a2` | repository `CC0-1.0`, per-document otherwise | the shared clone's 6439 `openapi.json` documents walked in full: `multipleOf` 33, `property-names` 12, `format: ipv6` 12, `contentMediaType` 6, numeric `exclusiveMaximum` 6, `format: iri-reference` 3, `contains` 3, `format: iri` 2, `format: json-pointer` 2, `minContains` 2, `contentSchema` 1, `format: idn-hostname` 1, and 0 for the remaining eight rows | no witness among them — the catalog is a third-party aggregation, so every hit is an unproven distribution until the publisher's own copy is found. Its value here is as a pointer: `redirection.io` (31 `iri-reference`), `gcore.com` (19 `ipv6`), `huggingface.co` (53 `propertyNames`), `runzero.com` (14 `ipv6`), `statsig.com` (37 `multipleOf`) and `clickfunnels.com` (10 `contains`) are where a next search should look for a publisher-served original. Two were chased here and neither pans out: `redirection.io` publishes no spec at any URL this search could reach, and `https://api.gcore.com/openapi.json` is live and served but declares none of these twenty rows |

#### The vendor portals this region's domain implies

The bulleted source above says the portals are "the ones this region's own domain implies",
and for these twenty rows that domain is two things at once: the JSON Schema 2020-12
dialect and its registries, which is where the applicator, unevaluated, content and
`$`-keyword rows live; and the network, identity and linked-data APIs whose own fields the
`format-*` rows name — an `ipv6` address, an `idn-hostname`, an `iri`. So the set is 29
documents each publisher serves itself, fetched with `curl -sSL` and walked the same way
every other source's documents were:

- **the dialect's own registries** — `spec.openapis.org/oas/3.1/schema/2022-10-07`,
  `spec.openapis.org/oas/3.1/dialect/base`, `json-schema.org/draft/2020-12/schema`,
  `json-schema.org/draft/2020-12/meta/validation`, and the OAI's own 3.1 examples
  `OAI/learn.openapis.org` `examples/v3.1/tictactoe.yaml` and
  `examples/v3.1/webhook-example.yaml`;
- **network, infrastructure and data-registry publishers**, where the `format-*` rows'
  own fields live — Cloudflare (both the `openapi.json` and the `openapi.yaml` of
  `cloudflare/api-schemas`), DigitalOcean, the Kubernetes
  `apis__networking.k8s.io__v1` OpenAPI v3 document, NetBox's published 2020-12
  device-type schema, Vercel's `openapi.vercel.sh`, OpenSearch's `_common` 3.1 schema
  document, Sentry, Grafana, Discourse, Crossref's Swagger 2.0 REST API description and
  the OGC's `ogcapi-features-1`;
- **large publisher documents**, where a 2020-12 keyword is likeliest to appear at all —
  OpenAI, Stripe, GitHub, Twilio, SendGrid, Adyen Checkout, Box, Asana, Ory Hydra, Ory
  Kratos and Apicurio Registry.

What they returned is each row's own cell. The set declares five of the twenty at all, and
only three of those five outside a meta-schema: `format: ipv6` (Cloudflare 3, Vercel 1),
`multipleOf` (Cloudflare 8, Box 1, Vercel 1) and `propertyNames` (OpenAI 2, plus 1 in the
OAI meta-schema). The other two, `$defs` (4 in the OAI 3.1 meta-schema, 1 in JSON Schema's
own validation vocabulary) and `dependentSchemas` (3 in the OAI meta-schema), appear
nowhere in the set but a meta-schema. Three further URLs this
set was assembled from could not be read and are not among the 29 — GitLab's
`doc/api/openapi/openapi.yaml` answered `HTTP 429` on the first attempt and `HTTP 404` on
a later one, and the IETF Datatracker's and the RIPE database's advertised
`openapi.json` endpoints both answer `HTTP 404` — and one is fetched but not walked:
`cloudflare/api-schemas`'s YAML spelling, which PyYAML refuses with `could not determine a
constructor for the tag 'tag:yaml.org,2002:value'` at line 460687. That last one costs
nothing, because it is the same document as the JSON spelling that is walked.

#### Every query, shown able to find the shape it looks for

A query that can never match its shape reports zero and reads exactly like a shape nothing
declares. Every one of these twenty queries was therefore run first against something it
must match before its zero was believed about anyone else. Two kinds of thing:

**A control document written for the purpose**, committed beside this file as
[`witness-query-control.yaml`](witness-query-control.yaml). It declares each of the twenty
exactly once, and carries the three decoys the counting rule above has to get right — a
component property literally *named* `contains`, an `example:` block spelling `multipleOf`,
`propertyNames` and `contentMediaType`, and a 3.0 boolean `exclusiveMaximum` beside the 3.1
numeric one. Walked, it yields exactly one of each of the twenty: 1 for `contains` and not
2, 1 for `exclusive-maximum-numeric` and not 2, and nothing at all from the `example:`
block. Both spellings were checked — the YAML file and a JSON rendering of it — because
half these queries are written for one spelling and half for the other. It is a valid 3.1
document in its own right: `fern check` accepts it and `fern generate --group python-sdk
--local --preview` writes a packaged SDK from it, which is the same screen every candidate
faced.

**A real declarer per row**, so that the network queries are shown returning something and
not just the local walk. Each is a document named in the ledger above, and each was found
*by* the query the row records:

| row | the declaration, in the spelling a document writes it | a document the row's own queries do match |
|---|---|---|
| `contains` | `contains:` on an array schema | `grafana/grafana-asserts-public-clients` `openapi.yaml` (2) |
| `content-media-type` | `contentMediaType: application/json` | `ITISFoundation/osparc-simcore` `…/api/v0/openapi.json` (24) |
| `content-schema` | `contentSchema:` beside it | the same oSPARC document (24) |
| `dependent-required` | `dependentRequired:` | `HelixDB/helix-db` `docs/openapi.json` (2) |
| `dependent-schemas` | `dependentSchemas:` | `duy-tung/serving-contracts` `openapi/inference-api.yaml` (1), and the OAI 3.1 meta-schema (3) |
| `dollar-anchor` | `$anchor: Name` | `inkeep/chat-api-openapi-schema` `openapi.yaml` (2), this table's own recorded witness |
| `dollar-defs` | `$defs:` | `emergentmethods/flowdapt` `openapi.json` (7) |
| `exclusive-maximum-numeric` | `exclusiveMaximum: 100` | the oSPARC document (8) |
| `format-idn-email` | `format: idn-email` | `wttw/aboutmyemail` `ameapi.yaml` (4) |
| `format-idn-hostname` | `format: idn-hostname` | Short.io's `https://api.short.io/openapi.json` (1) |
| `format-ipv6` | `format: ipv6` | `linode/linode-api-docs` `openapi.json` (3) |
| `format-iri` | `format: iri` | APIs.guru `mercure.local/0.3.2` (2) |
| `format-iri-reference` | `format: iri-reference` | `stryd/efulfilmentshop-go` `api/openapi.yaml` (36) |
| `format-json-pointer` | `format: json-pointer` | `dcm-project/k8s-container-service-provider` `api/v1alpha1/openapi.yaml` (2) |
| `format-relative-json-pointer` | `format: relative-json-pointer` | `geo-engine/BioIS` `openapi.json` (1), this table's own recorded witness |
| `max-contains` | `maxContains: 4` | `pitekusu/shittim-chest` `contracts/records/v1/openapi.json` (24) |
| `min-contains` | `minContains: 1` | the same Shittim Chest document (24) |
| `multiple-of` | `multipleOf: 0.5` | `cloudflare/api-schemas` `openapi.json` (8) |
| `property-names` | `propertyNames:` | corpus row 109 `volview-backend-contract` (7) |
| `unevaluated-items` | `unevaluatedItems: false` | `grafana/grafana-asserts-public-clients` `openapi.yaml` (2) |

Every row therefore has both: a document written to be found, and a document nobody wrote
for this search that the same query found anyway. `format-relative-json-pointer` is the one
row whose real declarer is the blocked witness this table already recorded rather than
anything the re-search turned up, which is the same statement its outcome makes.

#### Reproducing each source's sweep

The APIs.guru and SwaggerHub sweeps are the two that are not a single command.
Both are plain fetch-and-grep over a public registry index, and both are quoted
above by their result rather than by a script, so here is what to re-run:

- **APIs.guru** — `curl -sS https://api.apis.guru/v2/list.json`, take every
  version's `swaggerUrl`, fetch each (percent-encoding the eight that contain a
  space), and grep each body for `"$anchor"`, `"$comment"`, `"$dynamicAnchor"`,
  `"$dynamicRef"`, `"$vocabulary"`, `relative-json-pointer` and the five
  unquoted YAML spellings. 2529 APIs, 3992 versions, 0 fetch errors, 0 hits on
  3992 of 3992. As a second, independent reading of the same directory,
  `git clone --depth 1 https://github.com/APIs-guru/openapi-directory` (HEAD
  `f04b8d0b`, 2026-04-20) and `grep -rlF '<key>' APIs` over its 4138 spec files
  in 701 provider directories: 0 files for every key but `$comment`, whose one
  file is the `example:` value described in that row.
- **SwaggerHub** — page
  `https://api.swaggerhub.com/specs?specification=openapi-3.1.x&limit=100&page=N`
  (and `openapi-3.2.0`) until a page returns fewer than 100 `apis`, take each
  entry's `Swagger` property URL, fetch it and grep the same eleven strings.
  7683 specs, 0 fetch errors, the 10 declaring specs listed in the ledger.

**The twenty ranked `FIXTURE` rows re-ran both of those sweeps and added three more.** The
re-runs differ from the originals in one way that matters: where the first pass grepped for
a string, this one parsed every document and walked it, so its counts are declarations
rather than occurrences.

- **APIs.guru, re-swept whole.** `curl -sS https://api.apis.guru/v2/list.json` → 2529 APIs
  and 3992 versions; every `swaggerUrl` fetched (the eight containing a space re-fetched
  percent-encoded) with **0 fetch errors**; each body parsed and walked. As the second,
  independent reading, `git clone --depth 1
  https://github.com/APIs-guru/openapi-directory` at HEAD `f04b8d0b` (2026-04-20) and the
  same walk over its 4138 spec files in 701 provider directories. The two readings agree
  row for row. Both are a snapshot; re-run them rather than trusting them.
- **SwaggerHub, re-enumerated whole.** Page
  `https://api.swaggerhub.com/specs?specification=openapi-3.1.x&limit=100&page=N` (and
  `openapi-3.2.0`) until a page returns fewer than 100 `apis` — 77 pages plus 1, a stated
  `totalCount` of 7630 and 67, **7688** entries carrying **7666** distinct `Swagger`
  property URLs. All 7666 fetched and walked; six failed first time and all six recovered
  when their owner names were percent-encoded (`Jakub Kępka`, `[GD]hpsk`,
  `Thanyawit_[Palm]` and three others containing a space), so the sweep has no unread
  document in it.
- **Sourcegraph.** `curl -sS -G https://sourcegraph.com/.api/search/stream --data-urlencode
  'q=<query>' -H 'Accept: text/event-stream'`, parsing the `progress` events for
  `matchCount` and the `matches` events for the distinct files. Forty queries, two per row.
  **Thirty-one answered and nine did not**: the index serves a run of `count:all` queries
  and then answers `HTTP 429` with a Cloudflare `Just a moment…` interstitial for hours.
  The ten that first refused belong to the five `format-*` rows, which are the last five
  in alphabetical order and so the ones the run reached last — a scheduling artefact
  rather than anything about those queries. They were retried on a 90-second cadence, then
  at 3, 6, 9, 12, 15, 18, 21 and 24 minutes, and then on a 150-second cadence from an
  otherwise idle client with no other query in flight; exactly one of the ten came back
  (`format-relative-json-pointer`'s JSON spelling, 3 matches in 2 conformance-test files)
  and the other nine did not lift inside this dispatch. Anyone re-running this should pace
  at one query per two minutes from the start rather than discovering the limit at query
  thirty.
- **The Jentic public-apis catalog**, read from the shared clone at
  `/home/nick/projects/ai-orchestrator/runs/crozier-close-gap-backlog/scratch/163531-4/jentic-public-apis`
  at `eb9d12a2` rather than cloned again — a full clone is about 21 GB and this host had
  under 30 GB free. All **6439** `apis/**/openapi.json` documents parsed and walked. It is
  not one of this region's declared sources and no row rests on it; it is recorded because
  it is the largest single body of real-world OpenAPI this search read, and its hits are
  the pointer list in the ledger's last row.
- **The vendor set**, listed in
  [its own subsection](#the-vendor-portals-this-regions-domain-implies) above: 29 documents
  fetched with `curl -sSL` and walked, with three unreachable URLs recorded there rather
  than silently dropped.

Neither sweep is gated by anything, and both are a snapshot: a registry that
gains a 3.1 document tomorrow is not in the counts above. Re-run them rather
than trusting them, exactly as the [variant scan](#the-supplementary-variant-scan)
asks.
