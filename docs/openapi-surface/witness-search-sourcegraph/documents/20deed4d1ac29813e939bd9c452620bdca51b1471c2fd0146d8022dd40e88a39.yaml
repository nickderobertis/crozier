openapi: 3.1.0
info:
  title: Kernel Images API
  version: 0.1.0
paths:
  /recording/start:
    post:
      summary: Start a screen recording. Only one recording per ID can be registered at a time.
      operationId: startRecording
      x-telemetry-category: platform
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StartRecordingRequest"
      responses:
        "201":
          description: Recording started
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          description: A recording is already in progress
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/InternalError"
  /process/exec:
    post:
      summary: Execute a command synchronously
      operationId: processExec
      x-telemetry-category: platform
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProcessExecRequest"
      responses:
        "200":
          description: Execution result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProcessExecResult"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"
  /process/spawn:
    post:
      summary: Execute a command asynchronously
      operationId: processSpawn
      x-telemetry-category: platform
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProcessSpawnRequest"
      responses:
        "200":
          description: Spawned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProcessSpawnResult"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"
  /process/{process_id}/status:
    get:
      summary: Get process status
      operationId: processStatus
      x-telemetry-category: platform
      parameters:
        - name: process_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProcessStatus"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
  /process/{process_id}/stdout/stream:
    get:
      summary: Stream process stdout over SSE
      operationId: processStdoutStream
      x-telemetry-category: platform
      parameters:
        - name: process_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: SSE stream of process output and lifecycle events
          headers:
            X-SSE-Content-Type:
              description: Media type of SSE data events (application/json)
              schema:
                type: string
                const: application/json
          content:
            text/event-stream:
              schema:
                $ref: "#/components/schemas/ProcessStreamEvent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
  /process/{process_id}/stdin:
    post:
      summary: Write to process stdin
      operationId: processStdin
      x-telemetry-category: platform
      parameters:
        - name: process_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProcessStdinRequest"
      responses:
        "200":
          description: Bytes written
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProcessStdinResult"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
  /process/{process_id}/kill:
    post:
      summary: Send signal to process
      operationId: processKill
      x-telemetry-category: platform
      parameters:
        - name: process_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProcessKillRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OkResponse"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
  /process/{process_id}/resize:
    post:
      summary: Resize a PTY-backed process
      operationId: processResize
      x-telemetry-category: platform
      parameters:
        - name: process_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProcessResizeRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OkResponse"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
  /recording/stop:
    post:
      summary: Stop the recording
      operationId: stopRecording
      x-telemetry-category: platform
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StopRecordingRequest"
      responses:
        "200":
          description: Recording stopped
        "400":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"
  /recording/download:
    get:
      summary: Download the most recently recorded video file
      parameters:
        - name: id
          in: query
          description: Identifier of the recording session to download, as passed to /recording/start. When omitted, the default recording session is downloaded.
          schema:
            type: string
            pattern: "^[a-zA-Z0-9-]+$"
      operationId: downloadRecording
      x-telemetry-category: platform
      responses:
        "200":
          description: Recording file
          headers:
            # Note: using a `format: date-time` here doesn't work as intended as the generated code
            # calls a `fmt.Sprint` on the value when setting the header. time.String is a
            # non-standard format that most parses will barf on, making everyone's life harder, so
            # we're skipping the `format` in favor of an explicit description.
            X-Recording-Started-At:
              description: Timestamp of when the recording started. Guaranteed to be RFC3339.
              schema:
                type: string
            X-Recording-Finished-At:
              description: Timestamp of when the recording finished. Guaranteed to be RFC3339.
              schema:
                type: string
          content:
            video/mp4:
              schema:
                type: string
                format: binary
        "202":
          description: Recording is still in progress, please try again later
          headers:
            Retry-After:
              description: Suggested wait time in seconds before retrying
              schema:
                type: integer
                minimum: 1
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
  /recording/list:
    get:
      summary: List all recorders
      operationId: listRecorders
      x-telemetry-category: platform
      responses:
        "200":
          description: List of recorders
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/RecorderInfo"
        "500":
          $ref: "#/components/responses/InternalError"
  /recording/delete:
    post:
      summary: Delete a previously recorded video file
      operationId: deleteRecording
      x-telemetry-category: platform
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DeleteRecordingRequest"
      responses:
        "200":
          description: Recording deleted
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/InternalError"
  /recording/mark:
    post:
      summary: Add a named marker to an in-progress recording
      description: >-
        Records a named point in time during an active recording. At finalize
        the markers are injected into the output MP4 as chapter markers.
      operationId: markRecording
      x-telemetry-category: platform
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MarkRecordingRequest"
      responses:
        "201":
          description: Marker recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MarkRecordingResult"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          description: No recording session exists with the given identifier
          $ref: "#/components/responses/NotFoundError"
        "409":
          description: The recording session exists but is not actively recording
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/InternalError"
  /computer/click_mouse:
    post:
      summary: Simulate a mouse click action on the host computer
      operationId: clickMouse
      x-telemetry-category: control
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClickMouseRequest"
      responses:
        "200":
          description: Mouse action performed
        "400":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"
  /computer/move_mouse:
    post:
      summary: Move the mouse cursor to the specified coordinates on the host computer
      operationId: moveMouse
      x-telemetry-category: control
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MoveMouseRequest"
      responses:
        "200":
          description: Mouse cursor moved
        "400":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"
  /computer/screenshot:
    post:
      summary: Capture a screenshot of the host computer
      operationId: takeScreenshot
      x-telemetry-category: control
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScreenshotRequest"
      responses:
        "200":
          description: Screenshot image
          content:
            image/png:
              schema:
                type: string
                format: binary
        "400":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"
  /computer/cursor:
    post:
      summary: Hide or show the cursor
      operationId: setCursor
      x-telemetry-category: control
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetCursorRequest"
      responses:
        "200":
          description: Cursor visibility updated successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OkResponse"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"
  /computer/get_mouse_position:
    post:
      summary: Get the current mouse cursor position on the host computer
      operationId: getMousePosition
      x-telemetry-category: control
      responses:
        "200":
          description: Current mouse position
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MousePositionResponse"
        "500":
          $ref: "#/components/responses/InternalError"
  /computer/type:
    post:
      summary: Type text on the host computer
      operationId: typeText
      x-telemetry-category: control
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TypeTextRequest"
      responses:
        "200":
          description: Text typed successfully
        "400":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"
  /computer/press_key:
    post:
      summary: Press one or more keys on the host computer
      description: |
        Presses the specified keys for an optional duration. Keys should be key symbols
        supported by xdotool. For a comprehensive list of key symbols, see
        the X11 keysym definitions at https://cgit.freedesktop.org/xorg/proto/x11proto/plain/keysymdef.h
        The server honors millisecond-level durations using fractional sleeps under the hood.
      operationId: pressKey
      x-telemetry-category: control
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PressKeyRequest"
      responses:
        "200":
          description: Keys pressed successfully
        "400":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"
  /computer/scroll:
    post:
      summary: Scroll the mouse wheel at a position on the host computer
      description: |
        Scroll vertically and/or horizontally at the given coordinates, optionally while holding modifier keys.
        The scroll amounts are in logical ticks (not pixels) and application behavior may vary; tuning may be required.
        When both horizontal and vertical deltas are provided, the server applies them sequentially (vertical then horizontal)
        and batches wheel events using `xdotool click --repeat N --delay 0 <button>`.
      operationId: scroll
      x-telemetry-category: control
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScrollRequest"
      responses:
        "200":
          description: Scroll performed
        "400":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"
  /computer/drag_mouse:
    post:
      summary: Drag the mouse along a path
      description: |
        Press and hold a mouse button, optionally wait for a short delay, then move along the given path and release.
        The path is a list of [x, y] coordinate pairs.
      operationId: dragMouse
      x-telemetry-category: control
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DragMouseRequest"
      responses:
        "200":
          description: Drag performed
        "400":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"
  /computer/batch:
    post:
      summary: Execute a batch of computer actions sequentially
      description: |
        Execute an ordered list of computer actions in a single API call. Actions are performed
        sequentially while holding an exclusive lock, so no other input operations can interleave.
        Execution stops on the first error and the response indicates which action failed.
        This is useful for chaining deterministic sequences of mouse and keyboard actions
        without paying network round-trip latency for each individual action.
      operationId: batchComputerAction
      x-telemetry-category: control
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BatchComputerActionRequest"
      responses:
        "200":
          description: Batch executed successfully
        "400":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"
  /computer/clipboard/read:
    post:
      summary: Read text from the system clipboard
      operationId: readClipboard
      x-telemetry-category: control
      responses:
        "200":
          description: Clipboard content read successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClipboardContent"
        "500":
          $ref: "#/components/responses/InternalError"
  /computer/clipboard/write:
    post:
      summary: Write text to the system clipboard
      operationId: writeClipboard
      x-telemetry-category: control
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WriteClipboardRequest"
      responses:
        "200":
          description: Text written to clipboard successfully
        "400":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"
  /logs/stream:
    get:
      summary: Stream logs over SSE
      operationId: logsStream
      x-telemetry-category: platform
      parameters:
        - in: query
          name: source
          required: true
          schema:
            type: string
            enum: [path, supervisor]
        - in: query
          name: follow
          required: false
          schema:
            type: boolean
            default: true
        - in: query
          description: only required if source is path
          name: path
          required: false
          schema:
            type: string
        - in: query
          name: supervisor_process
          description: only required if source is supervisor
          required: false
          schema:
            type: string
      responses:
        "200":
          description: SSE stream of logs
          headers:
            X-SSE-Content-Type:
              description: Media type of SSE data events (application/json)
              schema:
                type: string
                const: application/json
          content:
            text/event-stream:
              schema:
                $ref: "#/components/schemas/LogEvent"
  # File system operations
  /fs/read_file:
    get:
      summary: Read file contents
      operationId: readFile
      x-telemetry-category: platform
      parameters:
        - name: path
          in: query
          required: true
          schema:
            type: string
            pattern: "^/.*"
          description: Absolute file path to read.
      responses:
        "200":
          description: File read successfully
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"

  /fs/download_dir_zip:
    get:
      summary: Download a directory as a ZIP archive
      description: Returns a ZIP file containing the contents of the specified directory.
      operationId: downloadDirZip
      x-telemetry-category: platform
      parameters:
        - name: path
          in: query
          required: true
          schema:
            type: string
            pattern: "^/.*"
          description: Absolute directory path to archive and download.
      responses:
        "200":
          description: ZIP archive of the requested directory
          content:
            application/zip:
              schema:
                type: string
                format: binary
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"

  /fs/write_file:
    put:
      summary: Write or create a file
      operationId: writeFile
      x-telemetry-category: platform
      parameters:
        - name: path
          in: query
          required: true
          schema:
            type: string
            pattern: "^/.*"
          description: Destination absolute file path.
        - name: mode
          in: query
          required: false
          schema:
            type: string
            pattern: "^[0-7]{3,4}$"
          description: Optional file mode (octal string, e.g. 644). Defaults to 644.
      requestBody:
        required: true
        content:
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        "201":
          description: File written successfully
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"

  /fs/upload:
    post:
      summary: Upload one or more files
      description: Allows uploading single or multiple files to the remote filesystem.
      operationId: uploadFiles
      x-telemetry-category: platform
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                files:
                  type: array
                  items:
                    type: object
                    properties:
                      file:
                        type: string
                        format: binary
                      dest_path:
                        type: string
                        description: Absolute destination path to write the file.
                        pattern: "^/.*"
                    required: [file, dest_path]
              required: [files]
      responses:
        "201":
          description: Files uploaded successfully
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"

  /fs/upload_zip:
    post:
      summary: Upload a zip archive and extract it
      description: Upload a zip file and extract its contents to the specified destination path.
      operationId: uploadZip
      x-telemetry-category: platform
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                zip_file:
                  type: string
                  format: binary
                dest_path:
                  type: string
                  description: Absolute destination directory to extract the archive to.
                  pattern: "^/.*"
              required: [zip_file, dest_path]
      responses:
        "201":
          description: Zip uploaded and extracted successfully
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"

  /fs/download_dir_zstd:
    get:
      summary: Download a directory as a tar.zst archive
      description: |
        Returns a tar.zst (tar archive compressed with zstd) file containing the contents
        of the specified directory. Zstd offers significantly faster compression and
        decompression compared to zip while achieving better compression ratios.
      operationId: downloadDirZstd
      x-telemetry-category: platform
      parameters:
        - name: path
          in: query
          required: true
          schema:
            type: string
            pattern: "^/.*"
          description: Absolute directory path to archive and download.
        - name: compression_level
          in: query
          required: false
          schema:
            type: string
            enum: [fastest, default, better, best]
            default: default
          description: |
            Compression level. Higher levels produce smaller archives but take longer.
            - fastest: ~zstd level 1, maximum speed (~300-500 MB/s)
            - default: ~zstd level 3, balanced speed/ratio (~150 MB/s)
            - better: ~zstd level 7, better ratio (~50-80 MB/s)
            - best: ~zstd level 11, best ratio (~20-40 MB/s)
      responses:
        "200":
          description: Tar.zst archive of the requested directory
          content:
            application/zstd:
              schema:
                type: string
                format: binary
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"

  /fs/upload_zstd:
    post:
      summary: Upload a tar.zst archive and extract it
      description: |
        Upload a tar.zst (tar archive compressed with zstd) file and extract its contents
        to the specified destination path. Zstd offers significantly faster decompression
        compared to zip.
      operationId: uploadZstd
      x-telemetry-category: platform
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                archive:
                  type: string
                  format: binary
                  description: The tar.zst archive file.
                dest_path:
                  type: string
                  description: Absolute destination directory to extract the archive to.
                  pattern: "^/.*"
                strip_components:
                  type: integer
                  minimum: 0
                  default: 0
                  description: Number of leading path components to strip during extraction (like tar --strip-components).
              required: [archive, dest_path]
      responses:
        "201":
          description: Archive uploaded and extracted successfully
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"

  /fs/list_files:
    get:
      summary: List files in a directory
      operationId: listFiles
      x-telemetry-category: platform
      parameters:
        - name: path
          in: query
          required: true
          schema:
            type: string
            pattern: "^/.*"
          description: Absolute directory path.
      responses:
        "200":
          description: Directory listing
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListFiles"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"

  /fs/create_directory:
    put:
      summary: Create a new directory
      operationId: createDirectory
      x-telemetry-category: platform
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateDirectoryRequest"
      responses:
        "201":
          description: Directory created successfully
        "400":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"

  /fs/delete_file:
    put:
      summary: Delete a file
      operationId: deleteFile
      x-telemetry-category: platform
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DeletePathRequest"
      responses:
        "200":
          description: File deleted
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"

  /fs/delete_directory:
    put:
      summary: Delete a directory
      operationId: deleteDirectory
      x-telemetry-category: platform
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DeletePathRequest"
      responses:
        "200":
          description: Directory deleted
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"

  /fs/set_file_permissions:
    put:
      summary: Set file or directory permissions/ownership
      operationId: setFilePermissions
      x-telemetry-category: platform
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetFilePermissionsRequest"
      responses:
        "200":
          description: Permissions updated
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"

  /fs/file_info:
    get:
      summary: Get information about a file or directory
      operationId: fileInfo
      x-telemetry-category: platform
      parameters:
        - name: path
          in: query
          required: true
          schema:
            type: string
            pattern: "^/.*"
          description: Absolute path of the file or directory.
      responses:
        "200":
          description: File information
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileInfo"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"

  /fs/move:
    put:
      summary: Move or rename a file or directory
      operationId: movePath
      x-telemetry-category: platform
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MovePathRequest"
      responses:
        "200":
          description: Move successful
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"

  /fs/watch:
    post:
      summary: Watch a directory for changes
      operationId: startFsWatch
      x-telemetry-category: platform
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StartFsWatchRequest"
      responses:
        "201":
          description: Watch started successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  watch_id:
                    type: string
                    description: Unique identifier for the directory watch
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"

  /fs/watch/{watch_id}/events:
    get:
      summary: Stream filesystem events for a watch
      operationId: streamFsEvents
      x-telemetry-category: platform
      parameters:
        - name: watch_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: SSE stream of filesystem events
          headers:
            X-SSE-Content-Type:
              description: Media type of SSE data events (application/json)
              schema:
                type: string
                const: application/json
          content:
            text/event-stream:
              schema:
                $ref: "#/components/schemas/FileSystemEvent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"

  /fs/watch/{watch_id}:
    delete:
      summary: Stop watching a directory
      operationId: stopFsWatch
      x-telemetry-category: platform
      parameters:
        - name: watch_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Watch stopped successfully
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"
  /display:
    patch:
      summary: Update display configuration
      operationId: patchDisplay
      x-telemetry-category: platform
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PatchDisplayRequest"
      responses:
        "200":
          description: Updated display configuration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DisplayConfig"
        "409":
          $ref: "#/components/responses/ConflictError"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"

  /chromium/upload-extensions-and-restart:
    post:
      summary: Upload one or more unpacked extensions (as zips) and restart Chromium
      description: |
        Upload one or more extension zip archives, extract them under /home/kernel/extensions/<name>,
        set runtime extension flags in /chromium/flags, restart Chromium via supervisord, and wait
        until the Chromium DevTools "listening" log line is observed before returning success.
      operationId: uploadExtensionsAndRestart
      x-telemetry-category: platform
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/ExtensionUploadMultipart"
      responses:
        "201":
          description: Extensions uploaded, Chromium restarted, and DevTools is ready
        "400":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"

  /chromium/upload-extensions:
    post:
      summary: Upload and activate one or more unpacked extensions
      description: |
        Upload one or more extension zip archives and extract them under
        /home/kernel/extensions/<name>. Ordinary unpacked extensions are activated immediately over
        CDP without restarting Chromium, while their runtime flags are persisted for future starts.
        Content scripts are applied to existing pages after their next navigation or reload.
        Extensions that require enterprise policy still restart Chromium and wait for DevTools
        readiness before returning success.
      operationId: uploadExtensions
      x-telemetry-category: platform
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/ExtensionUploadMultipart"
      responses:
        "201":
          description: Extensions uploaded and activated
        "400":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"

  /chromium/policies:
    patch:
      summary: Update Chromium enterprise policies and restart
      description: |
        Merge user-provided Chromium enterprise policy overrides into the base policy.json,
        restart Chromium via supervisord, and wait until the Chromium DevTools "listening"
        log line is observed before returning success.
        Certain policies managed by kernel infrastructure (e.g. ExtensionSettings,
        proxy-related policies, and policies required for CDP/automation) are blocked
        from override.
      operationId: patchChromiumPolicies
      x-telemetry-category: platform
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: |
                Chromium enterprise policy overrides. Keys are policy names
                (e.g. "DefaultCookiesSetting") and values are the policy values.
                See https://chromeenterprise.google/policies/ for available policies.
              additionalProperties: true
              example:
                DefaultCookiesSetting: 1
                BasicAuthOverHttpEnabled: true
                HttpsUpgradesEnabled: false
                MaxConnectionsPerProxy: 32
                PopupsAllowedForUrls:
                  - "https://example.com"
      responses:
        "200":
          description: Policies updated and Chromium restarted successfully
        "400":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"

  /chromium/flags:
    patch:
      summary: Update Chromium launch flags and restart
      description: |
        Merge new flags with existing ones, update /chromium/flags, restart Chromium via supervisord,
        and wait until the Chromium DevTools "listening" log line is observed before returning success.
      operationId: patchChromiumFlags
      x-telemetry-category: platform
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                flags:
                  type: array
                  items:
                    type: string
                  description: Chromium flags to merge (e.g., ["--kiosk", "--disable-gpu"])
              required: [flags]
      responses:
        "200":
          description: Flags updated and Chromium restarted successfully
        "400":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"

  /configure:
    post:
      summary: Apply batched session and browser configuration plus optional navigation
      description: |
        Optional multipart parts apply browser and session configuration in one serialized request.
        Profiles, nonempty policies, nonempty flags, and extensions use one Chromium stop/apply/start
        cycle by default. Display-only and start_url-only requests stay live. Set
        `extension_load_strategy=prefer_cdp` to attempt live CDP activation when extensions are present
        without a profile, nonempty policies, or nonempty flags. Enterprise-policy extensions and CDP
        activation failures fall back to one restart within the same request. The query parameter is
        ignored by older images, which retain the default restart behavior.
        Optional `start_url` dispatches a best-effort navigation after configuration and readiness
        without waiting for page load. Bare hosts are normalized to `https://`. Omit any part you do
        not need. At least one actionable part must be present. Required configuration steps run in
        this order: policies, extensions, display, flags, then profile archive. The endpoint is not
        transactional: if a later required step fails, earlier successful side effects may remain on
        the instance while the non-2xx error propagates.
        Prefer this over separate `/chromium/*` and `/display` calls when multiple restart-triggering
        steps apply in one session configure.
      operationId: chromiumConfigure
      x-telemetry-category: platform
      parameters:
        - name: extension_load_strategy
          in: query
          required: false
          description: >-
            Extension activation strategy. Omit or use `restart` to preserve unconditional restart
            behavior for requests containing extensions. `prefer_cdp` attempts live activation for
            eligible unpacked extensions and falls back to one restart when required.
          schema:
            type: string
            enum: [restart, prefer_cdp]
            default: restart
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                display:
                  description: >-
                    UTF-8 JSON object matching `#/components/schemas/PatchDisplayRequest` (width/height/etc.).
                    When combined with restart-triggering fields, the resize is applied while Chromium
                    is stopped and Chromium is started once at the end.
                  type: string
                chromium_flags:
                  description: >-
                    UTF-8 JSON object `{"flags":["--kiosk"]}` — same semantics as PATCH /chromium/flags.
                  type: string
                chrome_policies:
                  description: UTF-8 JSON policy override map — same semantics as PATCH /chromium/policies.
                  type: string
                profile_archive:
                  description: >-
                    tar.zst archive containing the desired `/home/kernel/user-data` profile contents.
                    Prefer archives whose root entries are the profile files/directories themselves
                    (for example `Default/Preferences`). Use `strip_components` only when uploading
                    an archive that includes leading wrapper directories.
                  type: string
                  format: binary
                strip_components:
                  description: Optional number of leading path components to strip from profile_archive entries (non-negative integer as text).
                  type: string
                start_url:
                  description: >-
                    URL text to navigate after configure. Bare hosts are normalized to https://,
                    length is capped at 2048 bytes, and Chrome decides which schemes are navigable.
                  type: string
                extensions:
                  type: array
                  description: Extension zips paired with consecutive extensions.name fields (same as upload-extensions-and-restart).
                  items:
                    type: object
                    properties:
                      zip_file:
                        type: string
                        format: binary
                      name:
                        type: string
                        pattern: "^[A-Za-z0-9._-]{1,255}$"
                    required: [zip_file, name]
      responses:
        "200":
          description: Configuration applied; optional navigate completed successfully.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OkResponse"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          description: Configure failure with structured phase
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChromiumConfigureError"

  /webmcp/tools:
    get:
      summary: Discover WebMCP tools across the browser
      description: |
        Returns a snapshot of WebMCP tools registered by every open tab and by embedded
        content within those tabs.

        On the first request, Kernel starts monitoring the browser. Tabs and windows that are already
        open are included, and tabs, windows, and embedded content opened later are tracked automatically.
        Tools are removed when the tab or embedded frame that registered them closes or navigates away.

        Each tool includes an opaque tool_ref that identifies its exact live registration, tool metadata,
        and information about the window, tab, and optional embedded frame that provided it.
        Use tool_ref to invoke that exact registration.
      operationId: getWebMCPTools
      x-telemetry-category: control
      parameters:
        - name: exclude_custom
          in: query
          required: false
          schema:
            type: boolean
            default: false
          description: Exclude custom tools when true.
      responses:
        "200":
          description: Current WebMCP tool snapshot.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebMCPToolsResponse"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"

  /webmcp/invoke:
    post:
      summary: Invoke a discovered WebMCP tool
      description: |
        Invokes the exact live registration identified by tool_ref. Non-autosubmit declarative form
        tools return after their fields are populated with an awaiting_submission status. Other tools
        wait for a terminal result, including across navigation. CDP-backed custom tool outputs above
        240 KiB return an error rather than a truncated result. If the request times out after invocation
        begins, the server reports outcome_unknown and never retries the tool automatically.
      operationId: invokeWebMCPTool
      x-telemetry-category: control
      requestBody:
        description: The raw JSON request body is limited to 1 MiB plus 4 KiB of envelope overhead before decoding.
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WebMCPInvokeRequest"
      responses:
        "200":
          description: Chromium accepted the invocation and either populated a non-autosubmit form or reported a terminal result.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebMCPInvocationResult"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "504":
          description: Invocation began, but its final outcome could not be observed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebMCPInvocationFailure"
        "500":
          $ref: "#/components/responses/InternalError"

  /webmcp/custom-tools:
    get:
      summary: List custom WebMCP tools
      description: Returns every registered custom tool with its generated ID, namespace, matcher, and MCP tool metadata.
      operationId: listCustomWebMCPTools
      x-telemetry-category: control
      responses:
        "200":
          description: Current custom WebMCP tools.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomWebMCPToolsResponse"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      summary: Add custom WebMCP tools
      description: |
        Add a namespaced batch of custom tools. A custom tool can be page-backed or CDP-backed.
        Page-backed tools execute in the page via JavaScript. CDP-backed tools execute via CDP and
        can use all browser REPL tools (see `/repl`). The source must evaluate to a non-empty array
        of definitions with URL matchers, tool metadata (including an optional output schema), and
        execute functions. The batch is added atomically. Matchers apply to top-level documents and
        nested frames, including out-of-process iframes; each matching tool is exposed once on the
        tab's top-level document and appears in `GET /webmcp/tools`.

        To update one tool, list the tools, delete its ID, and add its replacement. Set
        force_overwrite_namespace to replace every existing tool in the namespace atomically;
        omitted or false adds tools without replacing existing ones. Existing invocations continue.
      operationId: addCustomWebMCPTools
      x-telemetry-category: control
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddCustomWebMCPToolsRequest"
            examples:
              page_backed:
                summary: Read a title in the page
                value:
                  namespace: example.com
                  source: |
                    [{
                      kind: "page",
                      match: {url_patterns: ["https://example.com/*"]},
                      tool: {
                        name: "read_page_title",
                        description: "Read the page title.",
                        inputSchema: {type: "object", additionalProperties: false},
                        outputSchema: {type: "object", properties: {title: {type: "string"}}, required: ["title"]},
                      },
                      execute: async () => ({title: document.title}),
                    }]
              cdp_backed:
                summary: Read a title via CDP
                value:
                  namespace: example.com
                  source: |
                    [{
                      kind: "cdp",
                      match: {url_patterns: ["https://example.com/*"]},
                      tool: {
                        name: "read_cdp_title",
                        description: "Read the tab title via CDP.",
                        inputSchema: {type: "object", additionalProperties: false},
                      },
                      execute: async (_input, {matches}) => ({
                        title: await js(() => document.title, {targetId: matches[0].top_target_id}),
                      }),
                    }]
      responses:
        "201":
          description: Custom tools added. The response contains only the newly added tools.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomWebMCPToolsResponse"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/InternalError"

  /webmcp/custom-tools/{id}:
    delete:
      summary: Remove a custom WebMCP tool
      description: Removes one custom tool by generated ID. An invocation already in progress is not canceled.
      operationId: removeCustomWebMCPTool
      x-telemetry-category: control
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            pattern: "^ct_[a-z][a-z0-9]{23}$"
      responses:
        "204":
          description: Custom tool removed.
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/InternalError"

  /playwright/execute:
    post:
      summary: Execute Playwright/TypeScript code against the browser
      description: |
        Execute arbitrary Playwright code in a fresh execution context against the browser running
        on localhost:9222. The code has access to 'page', 'context', 'browser', and 'webmcp'
        variables. 'webmcp.listTools()' returns the browser-wide WebMCP tool snapshot, and
        'webmcp.invokeTool(toolRef, input?, { timeoutSec? })' invokes an exact registration through
        the image's synchronous WebMCP API. WebMCP request failures throw a WebMCPRequestError with
        statusCode, code, invocationId, and body fields. The result of the code execution is returned
        in the response.

        'page' is bound to an active tab reported by Chrome. In single-window sessions, this is
        the foreground tab. When multiple browser windows are open, Chrome reports one active tab
        per window and the selected window is unspecified. 'context' is the BrowserContext that owns
        the selected page. If active-tab resolution races with a target change, execution falls back
        to an existing page. Use 'browser.contexts()' to select a context or page explicitly.
      operationId: executePlaywrightCode
      x-telemetry-category: control
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ExecutePlaywrightRequest"
      responses:
        "200":
          description: Code executed successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExecutePlaywrightResult"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"
  /repl:
    post:
      summary: Execute JavaScript in the Browser REPL
      description: |
        Execute JavaScript in a persistent Node.js Browser REPL. Start with live help:

        ```js
        repl.help();                         // List methods
        repl.help("click");                  // Signature, behavior, defaults, and example
        repl.help("webmcp.invokeTool");
        ```

        Available methods:
        <!-- BEGIN GENERATED REPL METHOD LIST -->
        - REPL: `repl.help`, `repl.write`, `repl.emitImage`
        - Browser control: `cdp`, `drainEvents`, `waitForEvent`, `gotoUrl`, `pageInfo`, `accessibilitySnapshot`, `click`, `typeText`, `fillInput`, `pressKey`, `scroll`, `captureScreenshot`, `listTabs`, `currentTab`, `switchTab`, `newTab`, `closeTab`, `ensureRealTab`, `iframeTarget`, `waitMs`, `waitForLoad`, `waitForElement`, `waitForNetworkIdle`, `js`, `uploadFile`, `httpGet`
        - WebMCP: `webmcp.listTools`, `webmcp.invokeTool`, `webmcp.addCustomTools`, `webmcp.listCustomTools`, `webmcp.removeCustomTool`
        <!-- END GENERATED REPL METHOD LIST -->

        Browser-control methods are available both directly and on `browser`. The same frozen
        browser-wide WebMCP client is available as `webmcp` and `browser.webmcp`, including custom
        tool add, list, and remove methods. `webmcp.listTools()` discovers tools through
        `GET /webmcp/tools`, and `webmcp.invokeTool()` calls `POST /webmcp/invoke`.
        Common workflows:

        ```js
        await gotoUrl("https://example.com");
        await waitForLoad();
        const tree = await accessibilitySnapshot();
        const link = tree.nodes.find(node => node.role === "link" && node.name === "More information...");
        if (!link) throw new Error("link not found");
        await click(link);

        const path = await captureScreenshot("/tmp/page.png", true, 1600);
        await repl.emitImage({path});
        ```

        Top-level bindings and dynamically imported modules persist until reset or process
        replacement. Expression values are ignored; emit output with `repl.write(...)`, console
        methods, or `repl.emitImage(...)`. Pinned `patchright` and `playwright-core` packages are
        available through dynamic `import()`, and `cdp()` provides unrestricted DevTools access.

        The runtime starts lazily and is owned by the API process. Reset, API restart, timeout,
        crash, OOM, or protocol failure destroys its state and changes its CUID2 `repl_id`.
        Executions are serialized. This endpoint is unrestricted code execution inside the browser
        VM, equivalent in trust level to the process and Playwright execution APIs; it is not sandboxed.
      operationId: executeBrowserRepl
      x-telemetry-category: control
      requestBody:
        description: JSON request bodies are limited to 8 MiB before strict decoding. The API rejects a marshaled daemon request over the daemon's 8 MiB newline-delimited request-line limit as a non-destructive 400.
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BrowserReplRequest"
      responses:
        "200":
          description: Code executed (success or structured failure)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrowserReplResult"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "413":
          $ref: "#/components/responses/BadRequestError"
        "500":
          $ref: "#/components/responses/InternalError"
  /telemetry:
    get:
      summary: Get telemetry configuration
      description: >
        Returns the current telemetry configuration. Returns 404 if telemetry
        is not configured.
      operationId: getTelemetry
      x-telemetry-category: platform
      responses:
        "200":
          description: Telemetry configuration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TelemetryState"
        "404":
          $ref: "#/components/responses/NotFoundError"
    put:
      summary: Set telemetry configuration
      description: >
        Sets the telemetry configuration. Returns 201 if telemetry was not
        previously configured; returns 200 if it was. Selection is opt-in: a
        request that enables no categories (a browser config with nothing set to
        true, or every category disabled) clears the configuration; this is
        idempotent when telemetry is not configured. Returns 409 when
        `storage.enabled` is false on an instance that has already started
        storage; the configuration is left unchanged.
      operationId: putTelemetry
      x-telemetry-category: platform
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BrowserTelemetryConfig"
      responses:
        "200":
          description: Telemetry configuration updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TelemetryState"
        "201":
          description: Telemetry configuration set
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TelemetryState"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/InternalError"
    patch:
      summary: Update telemetry configuration
      description: >
        Partially updates the telemetry configuration. Only categories explicitly
        set in the request body are changed; omitted categories retain their
        current settings. Returns 404 if telemetry is not configured. Setting
        every configurable category to enabled: false clears the configuration.
        Returns 409 when `storage.enabled` is false on an instance that has
        already started storage; the configuration is left unchanged.
      operationId: patchTelemetry
      x-telemetry-category: platform
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BrowserTelemetryConfig"
      responses:
        "200":
          description: Telemetry configuration updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TelemetryState"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/InternalError"
  /telemetry/events:
    post:
      summary: Publish an event into the telemetry stream
      description: >
        Injects an event into the telemetry stream, gated by the active
        telemetry config. Returns 200 with the assigned envelope when the
        event is admitted, 204 when the event is filtered (no active session
        or the event's category is disabled), or 400 on validation failure.
      operationId: publishTelemetryEvent
      x-telemetry-category: platform
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PublishEventRequest"
      responses:
        "200":
          description: Event published; envelope carries the assigned sequence.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TelemetryEnvelope"
        "204":
          description: Event accepted but filtered by the active telemetry config; not published.
        "400":
          $ref: "#/components/responses/BadRequestError"
  /telemetry/stream:
    get:
      summary: Stream telemetry events as Server-Sent Events
      description: >
        Opens a live SSE stream of telemetry events.
        Each frame has the form `id: {seq}\ndata: {envelope-json}\n\n`.
        Sequence numbers are process-monotonic and persist across configuration
        changes; a Last-Event-ID from any previous connection is valid for resuming the stream.
        Drops are signaled by gaps in the `id` field: if the received `id` is more
        than 1 ahead of the last-seen `id`, events were evicted from the buffer.
        Clients should track the last-seen `id` to detect gaps.
        Fresh connections with no Last-Event-ID start from the current sequence
        and only see new events; pass `?replay=all` to start from the oldest
        retained event instead.
        The wire shape is the generic `TelemetryEvent`. For browser events
        emitted by the Kernel image (or by callers using a documented `type`),
        the event's `data` conforms to the matching `Browser*EventData`
        schema, selected by `type`.
      operationId: streamTelemetryEvents
      x-telemetry-category: platform
      parameters:
        - in: header
          name: Last-Event-ID
          schema:
            type: string
          required: false
          description: >
            Resume after this sequence number. Omit or send 0 to start from
            the current position. Sequence numbers are process-monotonic, so
            any previous value resumes correctly from that point.
            Takes precedence over `replay` when both are present, so SSE
            auto-reconnect resumes cleanly instead of re-replaying history.
        - in: query
          name: replay
          schema:
            type: string
            enum: [all]
          required: false
          description: >
            Pass `all` to start from the oldest retained event. Ring buffer
            caps at 1024; older events are evicted and surface as a first
            `id` greater than 1.
      responses:
        "200":
          description: Live SSE stream of telemetry events.
          headers:
            X-SSE-Content-Type:
              description: Media type of SSE data events (application/json)
              schema:
                type: string
                const: application/json
          content:
            text/event-stream:
              schema:
                $ref: "#/components/schemas/TelemetryEnvelope"
  /scaletozero/disable:
    post:
      summary: Idempotently disable scale to zero on this VM.
      description: >
        Prevents the VM from scaling to zero, regardless of HTTP traffic on it.
        Calling the API on this VM will wake it because Unikraft will automatically wake for any request to the VM.
      operationId: disableScaleToZero
      x-telemetry-category: platform
      responses:
        "204":
          description: Scale-to-zero disabled
        "500":
          $ref: "#/components/responses/InternalError"
  /scaletozero/enable:
    post:
      summary: Idempotently enable scale to zero on this VM.
      description: >
        Restores normal scale-to-zero behavior on this VM, allowing it to
        scale to zero again when idle. Call this when the VM no longer needs to be held awake.
      operationId: enableScaleToZero
      x-telemetry-category: platform
      responses:
        "204":
          description: Scale-to-zero enabled
        "500":
          $ref: "#/components/responses/InternalError"
components:
  schemas:
    ExtensionUploadMultipart:
      type: object
      required: [extensions]
      properties:
        extensions:
          type: array
          description: List of extensions to upload and activate
          minItems: 1
          maxItems: 20
          items:
            type: object
            required: [zip_file, name]
            properties:
              zip_file:
                type: string
                format: binary
                description: Zip archive containing an unpacked Chromium extension (maximum 50 MiB; must include manifest.json)
              name:
                type: string
                description: Folder name to place the extension under /home/kernel/extensions/<name>
                pattern: "^[A-Za-z0-9._-]{1,255}$"

    TelemetryEvent:
      type: object
      description: >
        A telemetry event. The wire-level event shape accepted by the publish
        endpoint and emitted on the SSE stream. Arbitrary `type` strings and
        `data` payloads are admitted. For browser events emitted by the Kernel
        image, `data` conforms to the per-type schema documented in the
        `Browser*Event` / `Browser*EventData` definitions, selected by `type`.
      required:
        - type
      properties:
        ts:
          type: integer
          format: int64
          description: Unix timestamp in microseconds. Defaults to the current time when omitted.
        type:
          type: string
          description: Event type identifier.
        category:
          type: string
          description: Event category.
          enum:
            - console
            - network
            - page
            - interaction
            - control
            - platform
            - connection
            - system
            - screenshot
            - captcha
            - monitor
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          description: >
            Arbitrary JSON payload. For browser events listed in
            `KnownBrowserTelemetryEvent`, the payload conforms to the
            corresponding `Browser*EventData` schema.
        truncated:
          type: boolean
          description: Set by the server when the data field was truncated to fit the size limit.
    BrowserEventSource:
      type: object
      description: Provenance metadata identifying which producer emitted the event.
      required: [kind]
      properties:
        kind:
          type: string
          description: >
            Event producer. `cdp`: Chrome DevTools Protocol events from the
            browser. `kernel_api`: Kernel API server (reserved for
            server-generated events). `extension`: injected Chrome extension.
            `local_process`: system process running alongside the browser.
          enum:
            - cdp
            - kernel_api
            - extension
            - local_process
        event:
          type: string
          description: Producer-specific event name (e.g. `Runtime.consoleAPICalled` for CDP-sourced console events).
        metadata:
          type: object
          description: Producer-specific context (e.g. CDP target/session/frame IDs).
          additionalProperties:
            type: string
      additionalProperties: false
    PublishEventRequest:
      type: object
      description: Request body for publishing an event into the telemetry stream.
      required:
        - type
      properties:
        type:
          type: string
          description: Event type identifier.
        category:
          type: string
          description: >
            Event category. Optional and advisory: for a known event `type` the
            server assigns the category authoritatively and ignores this field.
            It is only used for unknown custom types, where it is required.
          enum:
            - console
            - network
            - page
            - interaction
            - control
            - platform
            - connection
            - system
            - screenshot
            - captcha
            - monitor
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          description: Telemetry event payload.
      additionalProperties: false
    TelemetryEnvelope:
      type: object
      description: The envelope assigned to a successfully published event.
      required:
        - seq
        - event
      properties:
        seq:
          type: integer
          format: int64
          minimum: 1
          description: >
            Process-monotonic sequence number assigned across the lifetime of
            the server. Use with Last-Event-ID to resume the SSE stream from
            this point.
        event:
          $ref: "#/components/schemas/TelemetryEvent"
      additionalProperties: false
    BrowserTelemetryConfig:
      type: object
      description: >
        Telemetry configuration for a browser. Selection is opt-in. Omit the
        browser key (or send an empty object) to capture the default set:
        lightweight operational signals (control, connection, system, captcha).
        Within `browser`, only the categories you set enabled: true are
        captured; anything omitted is off. The CDP categories (console, network,
        page, interaction), `screenshot` and `platform` are off by default and
        must be opted into. A `browser` config with nothing enabled clears the
        telemetry configuration. The `monitor` category (CDP collector health)
        is not configurable here; it flows automatically whenever a CDP category
        is captured.
      properties:
        browser:
          $ref: "#/components/schemas/BrowserTelemetryCategoriesConfig"
        export:
          $ref: "#/components/schemas/BrowserTelemetryExportConfig"
        storage:
          $ref: "#/components/schemas/BrowserTelemetryStorageConfig"
      additionalProperties: false
    BrowserTelemetryExportConfig:
      type: object
      description: >
        Forwarding of captured telemetry to an external destination. Independent
        of what is captured: export is off unless explicitly enabled here, even
        when an export destination is configured. In a PUT (full replace) an
        omitted export block resets export to off, the same as omitted categories
        turn off; in a PATCH an omitted field leaves the current setting unchanged.
      properties:
        otlp:
          $ref: "#/components/schemas/BrowserTelemetryOTLPExportConfig"
      additionalProperties: false
    BrowserTelemetryOTLPExportConfig:
      type: object
      description: OTLP/HTTP export settings.
      properties:
        enabled:
          type: boolean
          description: >
            Whether captured telemetry is forwarded to the configured OTLP
            destination. Off by default. Has no effect (export stays inactive)
            when no export destination is configured.
      additionalProperties: false
    BrowserTelemetryStorageConfig:
      type: object
      description: >
        Durable storage of captured telemetry in Kernel's event store. On by
        default, so a caller that omits this block keeps today's behavior: in a
        PUT (full replace) an omitted block means enabled, unlike `export`,
        whose omitted block means off; in a PATCH an omitted field leaves the
        current setting unchanged. Only events captured while storage is enabled
        are stored. Storage opens with the first capture session that has it
        enabled and stays open for the life of the instance, so once it has
        started a request that disables it is rejected with 409.
      properties:
        enabled:
          type: boolean
          description: >
            Whether captured telemetry is written to Kernel's event store.
            Defaults to true. Set false to keep events on the live stream and
            any enabled export destination only. Cannot be set false on an
            instance that has already started storage (409).
      additionalProperties: false
    BrowserTelemetryCategoriesConfig:
      type: object
      description: Per-category telemetry capture settings for browser events.
      properties:
        console:
          $ref: "#/components/schemas/BrowserTelemetryCategoryConfig"
          description: Console output (log, warn, error) and uncaught exceptions.
        page:
          $ref: "#/components/schemas/BrowserTelemetryCategoryConfig"
          description: Page lifecycle events (navigation, load, layout shifts, LCP).
        interaction:
          $ref: "#/components/schemas/BrowserTelemetryCategoryConfig"
          description: User interaction events (clicks, keydowns, scroll).
        network:
          $ref: "#/components/schemas/BrowserTelemetryCategoryConfig"
          description: HTTP request/response metadata.
        control:
          $ref: "#/components/schemas/BrowserTelemetryControlConfig"
          description: Agent-driven actions against the browser — computer-control calls, Playwright code execution, screenshots, clipboard access, and browser-control commands sent over the CDP proxy.
        platform:
          $ref: "#/components/schemas/BrowserTelemetryCategoryConfig"
          description: Calls that manage the VM rather than drive the browser (recording, filesystem, process, telemetry and browser configuration). Mostly platform-induced; off by default and opt-in.
        connection:
          $ref: "#/components/schemas/BrowserTelemetryCategoryConfig"
          description: Client attach/detach lifecycle for the CDP proxy and live view.
        system:
          $ref: "#/components/schemas/BrowserTelemetryCategoryConfig"
          description: Browser VM health, such as out-of-memory kills and managed-service crashes.
        screenshot:
          $ref: "#/components/schemas/BrowserTelemetryCategoryConfig"
          description: Periodic base64-encoded viewport screenshots. High volume; off by default and opt-in.
        captcha:
          $ref: "#/components/schemas/BrowserTelemetryCategoryConfig"
          description: Captcha solve attempt outcomes.
      additionalProperties: false
    BrowserTelemetryCategoryConfig:
      type: object
      description: Configuration for a single telemetry category.
      properties:
        enabled:
          type: boolean
          description: >
            Whether this category is captured. In PUT requests selection is opt-in:
            omitting this field (or the whole category) leaves the category off, so
            a PUT captures exactly the categories set to true. In PATCH requests,
            omitting this field (or sending an empty object `{}`) is a no-op; the
            category retains its current state. To enable or disable a category via
            PATCH, you must send an explicit `true` or `false`.
      additionalProperties: false
    BrowserTelemetryControlConfig:
      type: object
      description: >
        Configuration for the control category. Same `enabled` semantics as any
        other category, plus settings for the browser-control commands the CDP
        proxy reports.
      properties:
        enabled:
          type: boolean
          description: >
            Whether this category is captured. In PUT requests selection is opt-in:
            omitting this field (or the whole category) leaves the category off, so
            a PUT captures exactly the categories set to true. In PATCH requests,
            omitting this field (or sending an empty object `{}`) is a no-op; the
            category retains its current state. To enable or disable a category via
            PATCH, you must send an explicit `true` or `false`.
        cdp:
          $ref: "#/components/schemas/BrowserTelemetryCdpControlConfig"
      additionalProperties: false
    BrowserTelemetryCdpControlConfig:
      type: object
      description: Settings for the `cdp_command` events the DevTools proxy reports.
      properties:
        excluded_methods:
          type: array
          description: >
            Methods to leave out of the `cdp_command` stream. Omit the list (or
            send an empty one) to report every supported method. Exclusion is a
            telemetry setting only: an excluded command is still relayed to the
            browser unchanged, it simply produces no event. Use it to drop the
            highest-volume methods — `Input.dispatchMouseEvent` during a
            humanized cursor path, or `Page.captureScreenshot` under a
            screencast — without turning the whole category off.
          items:
            $ref: "#/components/schemas/BrowserCdpCommandMethod"
      additionalProperties: false
    BrowserCallStack:
      type: object
      description: >
        CDP Runtime.StackTrace representing the JavaScript call stack at the
        time of an event. Fields use CDP naming conventions rather than
        snake_case to match the Chrome DevTools Protocol wire format.
      required: [callFrames]
      properties:
        description:
          type: string
          description: Optional label for the stack trace (e.g. async cause).
        callFrames:
          type: array
          description: Ordered list of call frames, outermost first.
          items:
            type: object
            required: [functionName, scriptId, url, lineNumber, columnNumber]
            properties:
              functionName:
                type: string
                description: JavaScript function name, or empty string for anonymous functions.
              scriptId:
                type: string
                description: CDP script identifier.
              url:
                type: string
                description: URL or name of the script file.
              lineNumber:
                type: integer
                description: Zero-based line number within the script.
              columnNumber:
                type: integer
                description: Zero-based column number within the line.
        parent:
          $ref: "#/components/schemas/BrowserCallStack"
          description: Parent stack trace for async stacks.
    BrowserHttpHeaders:
      type: object
      description: >
        HTTP headers map forwarded as-is from CDP without normalization.
        Values are typically strings but may be any JSON type.
      additionalProperties: true
    BrowserTargetType:
      type: string
      description: CDP target type of the page that produced the event.
      enum: [page, iframe, worker, background_page, service_worker, shared_worker, other]
    BrowserEventContext:
      type: object
      description: >
        Browser event context stamped by the browser monitor onto all
        CDP-sourced events. Identifies the target, frame, and navigation epoch
        in which the event occurred.
      required: [session_id, target_id, target_type, nav_seq]
      properties:
        session_id:
          type: string
          description: CDP session identifier for the target connection.
        target_id:
          type: string
          description: Browser target identifier (stable across navigations within a tab).
        target_type:
          $ref: "#/components/schemas/BrowserTargetType"
        frame_id:
          type: string
          description: CDP frame identifier within the target.
        loader_id:
          type: string
          description: CDP document loader identifier, reset on each navigation.
        url:
          type: string
          description: URL relevant to this event; page URL for navigation and page events, request URL for network events.
        nav_seq:
          type: integer
          format: int64
          description: Monotonically increasing navigation sequence number, incremented on each top-level navigation within the target.
    BrowserConsoleLogEventData:
      allOf:
        - $ref: "#/components/schemas/BrowserEventContext"
        - type: object
          required: [level, text]
          properties:
            level:
              type: string
              description: >
                CDP Runtime.consoleAPICalled type, passed through unfiltered
                from Chrome. `error` is routed to console_error events instead;
                all other CDP console types appear here. See CDP spec for the
                full enum.
            text:
              type: string
              description: First console argument coerced to string.
            args:
              type: array
              description: All console arguments coerced to strings.
              items:
                type: string
            stack_trace:
              $ref: "#/components/schemas/BrowserCallStack"
    BrowserConsoleLogEvent:
      type: object
      description: A browser console log event (console.log, console.info, console.warn, etc.).
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: console_log
        category:
          type: string
          const: console
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserConsoleLogEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserConsoleErrorEventData:
      allOf:
        - $ref: "#/components/schemas/BrowserEventContext"
        - type: object
          required: [text]
          properties:
            text:
              type: string
              description: >
                Human-readable error text, as the browser console would display it.
                For console.error() calls, the first argument coerced to a string.
                For uncaught exceptions, the prefix and error message, e.g.
                "Uncaught Error: boom" or "Uncaught (in promise) TypeError: x is not a function".
            stack_trace:
              $ref: "#/components/schemas/BrowserCallStack"
            level:
              type: string
              description: CDP console type value, always "error". Present only when sourced from Runtime.consoleAPICalled.
            args:
              type: array
              description: All console arguments coerced to strings. Present only when sourced from Runtime.consoleAPICalled.
              items:
                type: string
            line:
              type: integer
              description: Line number in the script where the exception was thrown. Present only when sourced from Runtime.exceptionThrown.
            column:
              type: integer
              description: Column number in the script where the exception was thrown. Present only when sourced from Runtime.exceptionThrown.
            source_url:
              type: string
              description: URL of the script file that threw the exception. Present only when sourced from Runtime.exceptionThrown.
    BrowserConsoleErrorEvent:
      type: object
      description: >
        A browser console error or uncaught JavaScript exception event. Emitted
        from two distinct CDP sources with different data shapes.
        Runtime.consoleAPICalled (console.error calls) produces level, text,
        args, and stack_trace. Runtime.exceptionThrown (uncaught exceptions)
        produces text, line, column, source_url, and stack_trace. Fields not
        applicable to the source are absent.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: console_error
        category:
          type: string
          const: console
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserConsoleErrorEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserNetworkRequestEventData:
      allOf:
        - $ref: "#/components/schemas/BrowserEventContext"
        - type: object
          required: [request_id, method, document_url, headers, initiator_type]
          properties:
            request_id:
              type: string
              description: CDP request identifier, unique within the session.
            method:
              type: string
              description: HTTP method as sent on the wire (e.g. GET, POST).
            document_url:
              type: string
              description: URL of the document that initiated the request.
            headers:
              $ref: "#/components/schemas/BrowserHttpHeaders"
              description: Request headers.
            resource_type:
              type: string
              description: CDP Network.ResourceType for the request, passed through as-is from Chrome. Known values include Document, Fetch, XHR, Script, Stylesheet, Image, Media, Font, TextTrack, EventSource, WebSocket, Manifest, Prefetch, Other, and more.
            initiator_type:
              type: string
              description: CDP Initiator.type indicating what caused the request, passed through as-is from Chrome. Known values include script, parser, preload, and other.
            post_data:
              type: string
              description: Request body for POST/PUT requests, if available.
            is_redirect:
              type: boolean
              description: True if this request is the result of a redirect.
            redirect_url:
              type: string
              description: Original URL before the redirect, present when is_redirect is true.
    BrowserNetworkRequestEvent:
      type: object
      description: A browser network request sent event.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: network_request
        category:
          type: string
          const: network
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserNetworkRequestEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserNetworkResponseEventData:
      allOf:
        - $ref: "#/components/schemas/BrowserEventContext"
        - type: object
          required: [request_id, method, status, headers]
          properties:
            request_id:
              type: string
              description: CDP request identifier matching the originating network_request event.
            method:
              type: string
              description: HTTP method of the original request.
            status:
              type: integer
              description: HTTP response status code.
            status_text:
              type: string
              description: HTTP response status text (e.g. OK, Not Found).
            headers:
              $ref: "#/components/schemas/BrowserHttpHeaders"
              description: Response headers.
            mime_type:
              type: string
              description: MIME type of the response (e.g. text/html, application/json).
            resource_type:
              type: string
              description: CDP Network.ResourceType for the request, passed through as-is from Chrome. Known values include Document, Fetch, XHR, Script, Stylesheet, Image, Media, Font, TextTrack, EventSource, WebSocket, Manifest, Prefetch, Other, and more.
            body:
              type: string
              description: Truncated response body, present only for text MIME types.
    BrowserNetworkResponseEvent:
      type: object
      description: A browser network response received event. Fired after the response body is fully received, not when headers arrive.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: network_response
        category:
          type: string
          const: network
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserNetworkResponseEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserNetworkLoadingFailedEventData:
      allOf:
        - $ref: "#/components/schemas/BrowserEventContext"
        - type: object
          required: [request_id, error_text, canceled]
          properties:
            request_id:
              type: string
              description: CDP request identifier matching the originating network_request event.
            error_text:
              type: string
              description: Network error description (e.g. net::ERR_CONNECTION_REFUSED).
            canceled:
              type: boolean
              description: True if the request was canceled by the browser or page script.
            resource_type:
              type: string
              description: CDP Network.ResourceType for the request, passed through as-is from Chrome. Known values include Document, Fetch, XHR, Script, Stylesheet, Image, Media, Font, TextTrack, EventSource, WebSocket, Manifest, Prefetch, Other, and more.
    BrowserNetworkLoadingFailedEvent:
      type: object
      description: >
        A browser network loading failed event. If the request was already in
        flight when CDP attached (no prior `network_request` was emitted for
        it), `url`, `frame_id`, `loader_id`, and `resource_type` are absent;
        `BrowserEventContext` is partially populated in that case.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: network_loading_failed
        category:
          type: string
          const: network
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserNetworkLoadingFailedEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserNetworkIdleEvent:
      type: object
      description: A browser network idle event emitted after a 500ms quiet period with no in-flight HTTP requests.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: network_idle
        category:
          type: string
          const: network
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserEventContext"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserProxyErrorEventData:
      allOf:
        - $ref: "#/components/schemas/BrowserEventContext"
        - type: object
          required: [request_id, code, status]
          properties:
            request_id:
              type: string
              description: CDP request identifier matching the originating request.
            code:
              type: string
              enum: [destination_blocked, provider_blacklisted, provider_unreachable, provider_rejected, origin_tls_timeout, proxy_unavailable, restricted_route_unavailable, upstream_timeout, upstream_dns_failure, upstream_connect_failed, unknown]
              x-enum-varnames:
                - DestinationBlocked
                - ProviderBlacklisted
                - ProviderUnreachable
                - ProviderRejected
                - OriginTlsTimeout
                - ProxyUnavailable
                - RestrictedRouteUnavailable
                - UpstreamTimeout
                - UpstreamDnsFailure
                - UpstreamConnectFailed
                - ProxyErrorUnknown
              description: >
                Proxy-layer error code: the `X-Kernel-Proxy-Error` response
                header value from a branded 5xx error page served by the metro
                egress host-proxy. Values mirror what the proxy emits:
                destination_blocked, provider_blacklisted, provider_unreachable,
                provider_rejected, origin_tls_timeout, proxy_unavailable,
                restricted_route_unavailable, upstream_timeout,
                upstream_dns_failure, upstream_connect_failed. A header value
                this image does not recognize is reported as unknown, with the
                header value in raw_code.
            raw_code:
              type: string
              maxLength: 64
              pattern: '^[a-z0-9_]*$'
              description: >
                Sanitized `X-Kernel-Proxy-Error` header value, present only when
                code is unknown. Lowercased, characters outside [a-z0-9_]
                replaced with _, at most 64 characters.
            status:
              type: integer
              description: HTTP response status of the branded error page (502).
            method:
              type: string
              description: HTTP method of the failed request, when known.
            resource_type:
              type: string
              description: CDP Network.ResourceType for the request, when known.
    BrowserProxyErrorEvent:
      type: object
      description: >
        A branded proxy-layer failure observed by the browser. Emitted when the
        metro egress host-proxy serves a branded 5xx error page whose response
        carries the `X-Kernel-Proxy-Error` header. Low-volume and carries a
        typed code. Its value is per-session and per-URL attribution for
        sessions that already capture the network stream: proxy failures are
        only observable while the CDP network collector is running, so this is
        an opt-in refinement of the raw network events rather than a default-on
        alerting signal.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: proxy_error
        category:
          type: string
          const: network
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserProxyErrorEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserPageNavigationEventData:
      type: object
      additionalProperties: false
      required: [session_id, target_id, target_type, url, frame_id, loader_id]
      properties:
        url:
          type: string
          description: URL navigated to.
        frame_id:
          type: string
          description: CDP frame identifier of the navigated frame.
        parent_frame_id:
          type: string
          description: Parent frame identifier for subframe navigations; absent for top-level navigations.
        loader_id:
          type: string
          description: New CDP document loader identifier assigned for this navigation.
        session_id:
          type: string
          description: CDP session identifier.
        target_id:
          type: string
          description: Browser target identifier.
        target_type:
          $ref: "#/components/schemas/BrowserTargetType"
    BrowserPageNavigationEvent:
      type: object
      description: A browser page navigation started event (CDP Page.frameNavigated). Carries nav context fields inline but not nav_seq, as this event resets the navigation epoch.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: page_navigation
        category:
          type: string
          const: page
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserPageNavigationEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserPageDomContentLoadedEventData:
      allOf:
        - $ref: "#/components/schemas/BrowserEventContext"
        - type: object
          required: [cdp_timestamp]
          properties:
            cdp_timestamp:
              type: number
              description: Chrome monotonic clock value in seconds at which DOMContentLoaded fired, relative to browser process start (not Unix epoch). Use `ts` for wall-clock time.
    BrowserPageDomContentLoadedEvent:
      type: object
      description: A browser DOMContentLoaded event (CDP Page.domContentEventFired).
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: page_dom_content_loaded
        category:
          type: string
          const: page
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserPageDomContentLoadedEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserPageLoadEventData:
      allOf:
        - $ref: "#/components/schemas/BrowserEventContext"
        - type: object
          required: [cdp_timestamp]
          properties:
            cdp_timestamp:
              type: number
              description: Chrome monotonic clock value in seconds at which the load event fired, relative to browser process start (not Unix epoch). Use `ts` for wall-clock time.
    BrowserPageLoadEvent:
      type: object
      description: A browser page load event (CDP Page.loadEventFired).
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: page_load
        category:
          type: string
          const: page
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserPageLoadEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserPageTabOpenedEventData:
      type: object
      additionalProperties: false
      required: [target_id, target_type, url]
      properties:
        target_id:
          type: string
          description: CDP target identifier for the newly opened tab.
        target_type:
          $ref: "#/components/schemas/BrowserTargetType"
        url:
          type: string
          description: Initial URL of the new tab.
        title:
          type: string
          description: Initial page title of the new tab.
        opener_id:
          type: string
          description: Target identifier of the tab that opened this one, if any.
    BrowserPageTabOpenedEvent:
      type: object
      description: >
        A new browser tab or target was opened (CDP Target.attachedToTarget
        for page targets). Fires before a CDP session is attached to the new
        target, so `session_id`, `frame_id`, `loader_id`, and `nav_seq` are
        absent; this event does not compose `BrowserEventContext`. Consumers
        reading context fields generically should treat it as a special case.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: page_tab_opened
        category:
          type: string
          const: page
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserPageTabOpenedEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserPageCrashedEventData:
      type: object
      additionalProperties: false
      required: [target_id, target_type, url]
      properties:
        target_id:
          type: string
          description: CDP target identifier of the crashed page.
        target_type:
          $ref: "#/components/schemas/BrowserTargetType"
        url:
          type: string
          description: URL the page was on when its renderer process crashed.
    BrowserPageCrashedEvent:
      type: object
      description: >
        A page's renderer process crashed (an "Aw, Snap!" failure) while the
        browser process itself stayed alive. Reported on the crashed page's
        session, with the session and target ids on `source.metadata`. Captured
        only while the `page` category is enabled.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: page_crashed
        category:
          type: string
          const: page
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserPageCrashedEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserPageLayoutShiftEventData:
      allOf:
        - $ref: "#/components/schemas/BrowserEventContext"
        - type: object
          required: [source_frame_id, time, duration]
          properties:
            source_frame_id:
              type: string
              description: CDP frame identifier of the frame where the layout shift occurred.
            time:
              type: number
              description: Performance Timeline timestamp of the layout shift in milliseconds.
            duration:
              type: number
              description: Duration of the layout shift entry in milliseconds (always 0 for layout shifts per spec).
            layout_shift_details:
              type: object
              additionalProperties: false
              description: PerformanceLayoutShift attributes from the Performance Timeline entry.
              properties:
                value:
                  type: number
                  description: Layout shift score for this entry (contribution to CLS).
                had_recent_input:
                  type: boolean
                  description: True if the layout shift was preceded by user input within 500ms, excluding it from CLS.
    BrowserPageLayoutShiftEvent:
      type: object
      description: A browser cumulative layout shift (CLS) event from the Performance Timeline API.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: page_layout_shift
        category:
          type: string
          const: page
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserPageLayoutShiftEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserPageLcpEventData:
      allOf:
        - $ref: "#/components/schemas/BrowserEventContext"
        - type: object
          required: [source_frame_id, time]
          properties:
            source_frame_id:
              type: string
              description: CDP frame identifier of the frame where the LCP element was rendered.
            time:
              type: number
              description: Performance Timeline timestamp of the LCP entry in milliseconds.
            lcp_details:
              type: object
              additionalProperties: false
              description: LargestContentfulPaint attributes from the Performance Timeline entry.
              properties:
                render_time:
                  type: number
                  description: Render time of the LCP element in milliseconds; 0 for cross-origin images without Timing-Allow-Origin.
                load_time:
                  type: number
                  description: Load time of the LCP element in milliseconds.
                size:
                  type: number
                  description: Visible area of the LCP element in pixels squared.
                element_id:
                  type: string
                  description: id attribute of the LCP element, if present.
                url:
                  type: string
                  description: URL of the LCP element for image or video elements.
                node_id:
                  type: integer
                  description: CDP DOM node identifier of the LCP element.
    BrowserPageLcpEvent:
      type: object
      description: A browser Largest Contentful Paint (LCP) event from the Performance Timeline API.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: page_lcp
        category:
          type: string
          const: page
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserPageLcpEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserPageLayoutSettledEvent:
      type: object
      description: A browser layout settled event emitted 1 second after page load with no intervening layout shifts, indicating visual stability. Each layout shift resets the 1-second timer.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: page_layout_settled
        category:
          type: string
          const: page
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserEventContext"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserPageNavigationSettledEvent:
      type: object
      description: >
        Emitted when page_dom_content_loaded and page_layout_settled have both
        fired for the same navigation, indicating the page is loaded and
        visually stable. Independent of network_idle; a single pending request
        does not block it.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: page_navigation_settled
        category:
          type: string
          const: page
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserEventContext"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserInteractionClickEventData:
      allOf:
        - $ref: "#/components/schemas/BrowserEventContext"
        - type: object
          required: [x, y, selector, tag]
          properties:
            x:
              type: integer
              description: Viewport x-coordinate of the click in CSS pixels.
            y:
              type: integer
              description: Viewport y-coordinate of the click in CSS pixels.
            selector:
              type: string
              description: CSS selector path to the clicked element.
            tag:
              type: string
              description: HTML tag name of the clicked element in uppercase (e.g. BUTTON, A, DIV).
            text:
              type: string
              description: Visible text content of the clicked element, trimmed.
    BrowserInteractionClickEvent:
      type: object
      description: A browser user click event captured via injected page script.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: interaction_click
        category:
          type: string
          const: interaction
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserInteractionClickEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserInteractionKeyEventData:
      allOf:
        - $ref: "#/components/schemas/BrowserEventContext"
        - type: object
          required: [key, selector, tag]
          properties:
            key:
              type: string
              description: Key value from the KeyboardEvent (e.g. Enter, Backspace, a).
            selector:
              type: string
              description: CSS selector path to the element that had focus when the key was pressed.
            tag:
              type: string
              description: HTML tag name of the focused element in uppercase (e.g. INPUT, TEXTAREA, DIV).
    BrowserInteractionKeyEvent:
      type: object
      description: A browser keyboard event captured via injected page script.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: interaction_key
        category:
          type: string
          const: interaction
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserInteractionKeyEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserInteractionScrollSettledEventData:
      allOf:
        - $ref: "#/components/schemas/BrowserEventContext"
        - type: object
          required: [from_x, from_y, to_x, to_y, target_selector]
          properties:
            from_x:
              type: integer
              description: Scroll x-position at the start of the scroll gesture in CSS pixels.
            from_y:
              type: integer
              description: Scroll y-position at the start of the scroll gesture in CSS pixels.
            to_x:
              type: integer
              description: Final scroll x-position after the gesture settled in CSS pixels.
            to_y:
              type: integer
              description: Final scroll y-position after the gesture settled in CSS pixels.
            target_selector:
              type: string
              description: CSS selector path to the scrolled element.
    BrowserInteractionScrollSettledEvent:
      type: object
      description: A browser scroll settled event emitted after scroll position stops changing, captured via injected page script.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: interaction_scroll_settled
        category:
          type: string
          const: interaction
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserInteractionScrollSettledEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserMonitorScreenshotEventData:
      type: object
      additionalProperties: false
      required: [png]
      properties:
        png:
          type: string
          contentEncoding: base64
          description: Base64-encoded PNG screenshot of the browser viewport.
    BrowserMonitorScreenshotEvent:
      type: object
      description: A periodic screenshot of the browser viewport.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: monitor_screenshot
        category:
          type: string
          const: screenshot
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserMonitorScreenshotEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserMonitorDisconnectedEventData:
      type: object
      additionalProperties: false
      required: [reason]
      properties:
        reason:
          type: string
          description: 'Reason for the disconnection. chrome_restarted: Chrome process restarted.'
          enum: [chrome_restarted]
    BrowserMonitorDisconnectedEvent:
      type: object
      description: The CDP connection to Chrome was lost. Telemetry events may be dropped until monitor_reconnected arrives. In-progress computed state is discarded rather than paused, so computed events still pending for the current navigation (network_idle, page_layout_settled, page_navigation_settled) never fire. monitor_reconnected does not restore them. After reattachment a fresh state machine starts, so computed events can resume before the next navigation and carry empty navigation context until one occurs.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: monitor_disconnected
        category:
          type: string
          const: monitor
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserMonitorDisconnectedEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserMonitorReconnectedEventData:
      type: object
      additionalProperties: false
      required: [reconnect_duration_ms]
      properties:
        reconnect_duration_ms:
          type: integer
          format: int64
          description: Wall-clock time in milliseconds taken to reconnect after the disconnection.
    BrowserMonitorReconnectedEvent:
      type: object
      description: The CDP connection to Chrome was successfully re-established after a disconnection. Events emitted during the gap are lost. Computed state is reset, so navigation and network tracking restart fresh from this point.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: monitor_reconnected
        category:
          type: string
          const: monitor
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserMonitorReconnectedEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserMonitorReconnectFailedEventData:
      type: object
      additionalProperties: false
      required: [reason]
      properties:
        reason:
          type: string
          description: 'Reason for the reconnection failure. reconnect_exhausted: all retry attempts were used up without successfully restoring the CDP connection.'
          enum: [reconnect_exhausted]
    BrowserMonitorReconnectFailedEvent:
      type: object
      description: The CDP connection to Chrome could not be re-established after exhausting all reconnection attempts. No further telemetry events will arrive on this session.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: monitor_reconnect_failed
        category:
          type: string
          const: monitor
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserMonitorReconnectFailedEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserMonitorInitFailedEventData:
      type: object
      additionalProperties: false
      required: [step]
      properties:
        step:
          type: string
          description: The CDP method or initialization step that failed (e.g. browsersurface.Start).
    BrowserMonitorInitFailedEvent:
      type: object
      description: The CDP session could not be initialized.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: monitor_init_failed
        category:
          type: string
          const: monitor
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserMonitorInitFailedEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserApiCallEventData:
      type: object
      description: Per-call payload for `api_call` events.
      additionalProperties: false
      required: [request_id, operation_id, status, duration_ms]
      properties:
        request_id:
          type: string
          description: Per-request identifier from the kernel-images-api request middleware.
        operation_id:
          type: string
          description: Matched route's operation, named as the server names its handler (e.g. `TakeScreenshot`, `ExecutePlaywrightCode`).
        status:
          type: integer
          description: HTTP response status code.
        duration_ms:
          type: number
          description: Wall-clock duration of the handler in milliseconds.
        code:
          type: string
          description: Source submitted to `executePlaywrightCode`, capped at 8192 bytes like every other captured string. A capped value is cut on a character boundary and ends in `...[truncated]`. Absent for every other operation.
    BrowserPlatformApiCallEventData:
      type: object
      description: >
        Per-call payload for `platform_api_call` events. Metadata only: a
        platform call carries no submitted content, so there is no `code` field
        as there is on `api_call`.
      additionalProperties: false
      required: [request_id, operation_id, status, duration_ms]
      properties:
        request_id:
          type: string
          description: Per-request identifier from the kernel-images-api request middleware.
        operation_id:
          type: string
          description: Matched route's operation, named as the server names its handler (e.g. `ProcessExec`, `StartRecording`).
        status:
          type: integer
          description: HTTP response status code.
        duration_ms:
          type: number
          description: Wall-clock duration of the handler in milliseconds.
    BrowserApiCallEvent:
      type: object
      description: >
        A call that drives the browser, handled by the kernel-images-api server:
        computer-control actions, Playwright code execution, screenshots and
        clipboard access. Calls that manage the VM instead emit
        `platform_api_call`.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: api_call
        category:
          type: string
          const: control
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserApiCallEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserPlatformApiCallEvent:
      type: object
      description: >
        A call that manages the browser VM rather than driving the browser,
        handled by the kernel-images-api server: recording lifecycle, filesystem
        and process management, telemetry and browser configuration. These are
        mostly platform-induced (e.g. profile save, replay capture) rather than
        agent actions.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: platform_api_call
        category:
          type: string
          const: platform
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserPlatformApiCallEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserCdpMouseEventType:
      type: string
      description: >
        Mouse event phase. Canonical values from devtools-protocol@2d019e73.
        `other` stands for a value outside that set, so a client cannot put
        an arbitrary string into the stream.
      enum:
        - mousePressed
        - mouseReleased
        - mouseMoved
        - mouseWheel
        - other
    BrowserCdpKeyEventType:
      type: string
      description: >
        Key event phase. Canonical values from devtools-protocol@2d019e73.
        `other` stands for a value outside that set, so a client cannot put
        an arbitrary string into the stream.
      enum:
        - keyDown
        - keyUp
        - rawKeyDown
        - char
        - other
    BrowserCdpTouchEventType:
      type: string
      description: >
        Touch event phase. Canonical values from devtools-protocol@2d019e73.
        `other` stands for a value outside that set, so a client cannot put
        an arbitrary string into the stream.
      enum:
        - touchStart
        - touchEnd
        - touchMove
        - touchCancel
        - other
    BrowserCdpDragEventType:
      type: string
      description: >
        Drag event phase. Canonical values from devtools-protocol@2d019e73.
        `other` stands for a value outside that set, so a client cannot put
        an arbitrary string into the stream.
      enum:
        - dragEnter
        - dragOver
        - drop
        - dragCancel
        - other
    BrowserCdpMouseButton:
      type: string
      description: >
        Mouse button named by a command. Canonical values from
        devtools-protocol@2d019e73. `other` stands for a value outside that
        set, so a client cannot put an arbitrary string into the stream.
      enum:
        - none
        - left
        - middle
        - right
        - back
        - forward
        - other
    BrowserCdpPointerType:
      type: string
      description: >
        Pointer that generated an input event. Canonical values from
        devtools-protocol@2d019e73. `other` stands for a value outside that
        set, so a client cannot put an arbitrary string into the stream.
      enum:
        - mouse
        - pen
        - other
    BrowserCdpGestureSourceType:
      type: string
      description: >
        Input source a synthesized gesture emulates. Canonical values from
        devtools-protocol@2d019e73. `other` stands for a value outside that
        set, so a client cannot put an arbitrary string into the stream.
      enum:
        - default
        - touch
        - mouse
        - other
    BrowserCdpScreenshotFormat:
      type: string
      description: >
        Image format requested for a screenshot. Canonical values from
        devtools-protocol@2d019e73. `other` stands for a value outside that
        set, so a client cannot put an arbitrary string into the stream.
      enum:
        - jpeg
        - png
        - webp
        - other
    BrowserCdpSnapshotFormat:
      type: string
      description: >
        Format requested for a page snapshot. Canonical values from
        devtools-protocol@2d019e73. `other` stands for a value outside that
        set, so a client cannot put an arbitrary string into the stream.
      enum:
        - mhtml
        - other
    BrowserCdpScreencastFormat:
      type: string
      description: >
        Frame format requested for a screencast. Canonical values from
        devtools-protocol@2d019e73. `other` stands for a value outside that
        set, so a client cannot put an arbitrary string into the stream.
      enum:
        - jpeg
        - png
        - other
    BrowserCdpWebLifecycleState:
      type: string
      description: >
        Page lifecycle state applied. Canonical values from
        devtools-protocol@2d019e73. `other` stands for a value outside that
        set, so a client cannot put an arbitrary string into the stream.
      enum:
        - frozen
        - active
        - other
    BrowserCdpPdfTransferMode:
      type: string
      description: >
        How a generated PDF is returned. Canonical values from
        devtools-protocol@2d019e73. `other` stands for a value outside that
        set, so a client cannot put an arbitrary string into the stream.
      enum:
        - ReturnAsBase64
        - ReturnAsStream
        - other
    BrowserCdpWindowState:
      type: string
      description: >
        Browser window state requested. Canonical values from
        devtools-protocol@2d019e73. `other` stands for a value outside that
        set, so a client cannot put an arbitrary string into the stream.
      enum:
        - normal
        - minimized
        - maximized
        - fullscreen
        - other
    BrowserCdpTransitionType:
      type: string
      description: >
        Navigation reason reported by the caller. Canonical values from
        devtools-protocol@2d019e73. `other` stands for a value outside that
        set, so a client cannot put an arbitrary string into the stream.
      enum:
        - link
        - typed
        - address_bar
        - auto_bookmark
        - auto_subframe
        - manual_subframe
        - generated
        - auto_toplevel
        - form_submit
        - reload
        - keyword
        - keyword_generated
        - other
    BrowserCdpReferrerPolicy:
      type: string
      description: >
        Referrer policy named by a navigation. Canonical values from
        devtools-protocol@2d019e73. `other` stands for a value outside that
        set, so a client cannot put an arbitrary string into the stream.
      enum:
        - noReferrer
        - noReferrerWhenDowngrade
        - origin
        - originWhenCrossOrigin
        - sameOrigin
        - strictOrigin
        - strictOriginWhenCrossOrigin
        - unsafeUrl
        - other
    BrowserCdpAutofillMode:
      type: string
      description: >
        Which kind of value autofill filled. Canonical values from
        devtools-protocol@2d019e73.
      enum:
        - card
        - address
    BrowserCdpDragMimeCategory:
      type: string
      description: >
        Top-level MIME category of a drag item, from the IANA registry
        rather than the protocol; a drag item's subtype names the file, so
        only the category is reported. `other` stands for a value outside
        that set, so a client cannot put an arbitrary string into the
        stream.
      enum:
        - text
        - image
        - audio
        - video
        - application
        - font
        - model
        - multipart
        - message
        - other
    BrowserCdpCommandMethod:
      type: string
      description: >
        A browser-control CDP method the proxy reports. The set covers the
        commands an agent drives the browser with; configuration, DOM and
        Runtime bookkeeping, and Chrome-specific UI commands are outside it.
        Canonical definitions: devtools-protocol@2d019e73.
      enum:
        - Input.dispatchMouseEvent
        - Input.dispatchKeyEvent
        - Input.insertText
        - Input.imeSetComposition
        - Input.dispatchTouchEvent
        - Input.dispatchDragEvent
        - Input.cancelDragging
        - Input.emulateTouchFromMouseEvent
        - Input.synthesizePinchGesture
        - Input.synthesizeScrollGesture
        - Input.synthesizeTapGesture
        - DOM.setFileInputFiles
        - DOM.focus
        - DOM.scrollIntoViewIfNeeded
        - Page.bringToFront
        - Page.captureScreenshot
        - Page.captureSnapshot
        - Page.handleJavaScriptDialog
        - Page.navigate
        - Page.navigateToHistoryEntry
        - Page.reload
        - Page.printToPDF
        - Page.startScreencast
        - Page.stopScreencast
        - Page.stopLoading
        - Page.close
        - Page.setWebLifecycleState
        - Target.activateTarget
        - Target.closeTarget
        - Target.createTarget
        - Target.createBrowserContext
        - Target.disposeBrowserContext
        - Target.openDevTools
        - Browser.cancelDownload
        - Browser.close
        - Browser.setWindowBounds
        - Browser.setContentsSize
        - Autofill.trigger
    BrowserCdpInputDispatchMouseEventCommandData:
      type: object
      description: >
        Sanitized `Input.dispatchMouseEvent` arguments. Canonical input:
        `Input.dispatchMouseEvent` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method, event_type]
      properties:
        method:
          type: string
          const: Input.dispatchMouseEvent
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        event_type:
          $ref: "#/components/schemas/BrowserCdpMouseEventType"
          description: >
            Mouse event phase: `mousePressed`, `mouseReleased`, `mouseMoved`
            or `mouseWheel`. A value the protocol does not define is
            reported as `other`.
        x:
          type: number
          format: double
          description: >
            Viewport x coordinate in CSS pixels.
        y:
          type: number
          format: double
          description: >
            Viewport y coordinate in CSS pixels.
        modifiers:
          type: integer
          description: >
            Bit field of held modifier keys (1=Alt, 2=Ctrl, 4=Meta,
            8=Shift).
        button:
          $ref: "#/components/schemas/BrowserCdpMouseButton"
          description: >
            Button named by the command (`none`, `left`, `middle`, `right`,
            `back`, `forward`). A value the protocol does not define is
            reported as `other`.
        buttons:
          type: integer
          description: >
            Bit field of buttons held down. Non-zero on a `mouseMoved` means
            the move is a drag path.
        click_count:
          type: integer
          description: >
            Number of times the button was clicked (2 is a double click).
        delta_x:
          type: number
          format: double
          description: >
            Horizontal scroll delta, for `mouseWheel`.
        delta_y:
          type: number
          format: double
          description: >
            Vertical scroll delta, for `mouseWheel`.
        pointer_type:
          $ref: "#/components/schemas/BrowserCdpPointerType"
          description: >
            Pointer that generated the event (`mouse` or `pen`). A value the
            protocol does not define is reported as `other`.
        force:
          type: number
          format: double
          description: >
            Normalized pressure, 0 to 1.
        tangential_pressure:
          type: number
          format: double
          description: >
            Normalized tangential pressure, -1 to 1.
        tilt_x:
          type: number
          format: double
          description: >
            Pen tilt from the Y-Z plane, in degrees.
        tilt_y:
          type: number
          format: double
          description: >
            Pen tilt from the X-Z plane, in degrees.
        twist:
          type: integer
          description: >
            Pen clockwise rotation, in degrees.
    BrowserCdpInputDispatchKeyEventCommandData:
      type: object
      description: >
        Sanitized `Input.dispatchKeyEvent` arguments. Canonical input:
        `Input.dispatchKeyEvent` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method, event_type]
      properties:
        method:
          type: string
          const: Input.dispatchKeyEvent
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        event_type:
          $ref: "#/components/schemas/BrowserCdpKeyEventType"
          description: >
            Key event phase: `keyDown`, `keyUp`, `rawKeyDown` or `char`. A
            value the protocol does not define is reported as `other`.
        modifiers:
          type: integer
          description: >
            Bit field of held modifier keys (1=Alt, 2=Ctrl, 4=Meta,
            8=Shift).
        text_length:
          type: integer
          description: >
            Number of characters the command submitted. The text itself is
            never captured.
        named_key:
          type: string
          description: >
            Key that commands the page rather than typing into it (e.g.
            `Enter`, `Tab`, `ArrowDown`, `F5`). Keys that produce a
            character are never captured; those are counted by
            `text_length`.
        location:
          type: integer
          description: >
            Keyboard location (1=left, 2=right, 3=numpad).
        auto_repeat:
          type: boolean
          description: >
            Whether the event was generated by key repeat.
        is_keypad:
          type: boolean
          description: >
            Whether the key is on the numeric keypad.
        is_system_key:
          type: boolean
          description: >
            Whether the event is a system key event.
        command_count:
          type: integer
          description: >
            Number of editing commands (e.g. `selectAll`) carried by the
            event.
    BrowserCdpInputInsertTextCommandData:
      type: object
      description: >
        Sanitized `Input.insertText` arguments. Canonical input:
        `Input.insertText` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method, text_length]
      properties:
        method:
          type: string
          const: Input.insertText
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        text_length:
          type: integer
          description: >
            Number of characters inserted. The text itself is never
            captured.
    BrowserCdpInputImeSetCompositionCommandData:
      type: object
      description: >
        Sanitized `Input.imeSetComposition` arguments. Canonical input:
        `Input.imeSetComposition` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method, text_length]
      properties:
        method:
          type: string
          const: Input.imeSetComposition
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        text_length:
          type: integer
          description: >
            Number of characters in the composition. The text itself is
            never captured.
        selection_start:
          type: integer
          description: >
            Selection start offset within the composition.
        selection_end:
          type: integer
          description: >
            Selection end offset within the composition.
        replacement_start:
          type: integer
          description: >
            Replacement range start offset.
        replacement_end:
          type: integer
          description: >
            Replacement range end offset.
    BrowserCdpInputDispatchTouchEventCommandData:
      type: object
      description: >
        Sanitized `Input.dispatchTouchEvent` arguments. Canonical input:
        `Input.dispatchTouchEvent` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method, event_type, touch_point_count]
      properties:
        method:
          type: string
          const: Input.dispatchTouchEvent
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        event_type:
          $ref: "#/components/schemas/BrowserCdpTouchEventType"
          description: >
            Touch event phase: `touchStart`, `touchEnd`, `touchMove` or
            `touchCancel`. A value the protocol does not define is reported
            as `other`.
        touch_point_count:
          type: integer
          description: >
            Number of active touch points the command carried.
        x:
          type: number
          format: double
          description: >
            Viewport x coordinate of the first touch point. Touch
            coordinates live inside `touchPoints`, so this is the primary
            point rather than a command-level argument.
        y:
          type: number
          format: double
          description: >
            Viewport y coordinate of the first touch point.
        radius_x:
          type: number
          format: double
          description: >
            Horizontal radius of the first touch point.
        radius_y:
          type: number
          format: double
          description: >
            Vertical radius of the first touch point.
        rotation_angle:
          type: number
          format: double
          description: >
            Rotation of the first touch point, in degrees.
        force:
          type: number
          format: double
          description: >
            Normalized pressure of the first touch point, 0 to 1.
        tangential_pressure:
          type: number
          format: double
          description: >
            Normalized tangential pressure of the first touch point, -1 to
            1.
        tilt_x:
          type: number
          format: double
          description: >
            Tilt of the first touch point from the Y-Z plane, in degrees.
        tilt_y:
          type: number
          format: double
          description: >
            Tilt of the first touch point from the X-Z plane, in degrees.
        twist:
          type: integer
          description: >
            Clockwise rotation of the first touch point, in degrees.
        modifiers:
          type: integer
          description: >
            Bit field of held modifier keys (1=Alt, 2=Ctrl, 4=Meta,
            8=Shift).
    BrowserCdpInputDispatchDragEventCommandData:
      type: object
      description: >
        Sanitized `Input.dispatchDragEvent` arguments. Canonical input:
        `Input.dispatchDragEvent` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method, event_type]
      properties:
        method:
          type: string
          const: Input.dispatchDragEvent
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        event_type:
          $ref: "#/components/schemas/BrowserCdpDragEventType"
          description: >
            Drag event phase: `dragEnter`, `dragOver`, `drop` or
            `dragCancel`. A value the protocol does not define is reported
            as `other`.
        x:
          type: number
          format: double
          description: >
            Viewport x coordinate in CSS pixels.
        y:
          type: number
          format: double
          description: >
            Viewport y coordinate in CSS pixels.
        modifiers:
          type: integer
          description: >
            Bit field of held modifier keys (1=Alt, 2=Ctrl, 4=Meta,
            8=Shift).
        drag_item_count:
          type: integer
          description: >
            Number of items in the drag payload. Item contents are never
            captured.
        drag_file_count:
          type: integer
          description: >
            Number of files in the drag payload. File paths are never
            captured.
        drag_mime_categories:
          type: array
          items:
            $ref: "#/components/schemas/BrowserCdpDragMimeCategory"
          description: >
            Distinct top-level MIME categories of the drag items (e.g.
            `text`, `image`, `application`). Subtypes and contents are never
            captured. A value the protocol does not define is reported as
            `other`.
        drag_operations_mask:
          type: integer
          description: >
            Bit field of allowed drag operations (1=copy, 2=link, 16=move).
    BrowserCdpInputCancelDraggingCommandData:
      type: object
      description: >
        Sanitized `Input.cancelDragging` arguments. Canonical input:
        `Input.cancelDragging` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method]
      properties:
        method:
          type: string
          const: Input.cancelDragging
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
    BrowserCdpInputEmulateTouchFromMouseEventCommandData:
      type: object
      description: >
        Sanitized `Input.emulateTouchFromMouseEvent` arguments. Canonical
        input: `Input.emulateTouchFromMouseEvent` in
        devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method, event_type]
      properties:
        method:
          type: string
          const: Input.emulateTouchFromMouseEvent
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        event_type:
          $ref: "#/components/schemas/BrowserCdpMouseEventType"
          description: >
            Mouse event phase being emulated as touch. A value the protocol
            does not define is reported as `other`.
        x:
          type: number
          format: double
          description: >
            Viewport x coordinate in CSS pixels.
        y:
          type: number
          format: double
          description: >
            Viewport y coordinate in CSS pixels.
        button:
          $ref: "#/components/schemas/BrowserCdpMouseButton"
          description: >
            Button named by the command. A value the protocol does not
            define is reported as `other`.
        modifiers:
          type: integer
          description: >
            Bit field of held modifier keys (1=Alt, 2=Ctrl, 4=Meta,
            8=Shift).
        click_count:
          type: integer
          description: >
            Number of times the button was clicked.
        delta_x:
          type: number
          format: double
          description: >
            Horizontal scroll delta.
        delta_y:
          type: number
          format: double
          description: >
            Vertical scroll delta.
    BrowserCdpInputSynthesizePinchGestureCommandData:
      type: object
      description: >
        Sanitized `Input.synthesizePinchGesture` arguments. Canonical input:
        `Input.synthesizePinchGesture` in devtools-protocol@2d019e73, pinned
        at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method]
      properties:
        method:
          type: string
          const: Input.synthesizePinchGesture
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        x:
          type: number
          format: double
          description: >
            Viewport x coordinate in CSS pixels.
        y:
          type: number
          format: double
          description: >
            Viewport y coordinate in CSS pixels.
        scale_factor:
          type: number
          format: double
          description: >
            Relative scale of the pinch (>1 zooms in).
        relative_speed:
          type: integer
          description: >
            Relative pointer speed, in pixels per second.
        gesture_source_type:
          $ref: "#/components/schemas/BrowserCdpGestureSourceType"
          description: >
            Input source the synthesized gesture emulates. A value the
            protocol does not define is reported as `other`.
    BrowserCdpInputSynthesizeScrollGestureCommandData:
      type: object
      description: >
        Sanitized `Input.synthesizeScrollGesture` arguments. Canonical
        input: `Input.synthesizeScrollGesture` in
        devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method]
      properties:
        method:
          type: string
          const: Input.synthesizeScrollGesture
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        x:
          type: number
          format: double
          description: >
            Viewport x coordinate in CSS pixels.
        y:
          type: number
          format: double
          description: >
            Viewport y coordinate in CSS pixels.
        x_distance:
          type: number
          format: double
          description: >
            Horizontal scroll distance in CSS pixels; positive scrolls left.
        y_distance:
          type: number
          format: double
          description: >
            Vertical scroll distance in CSS pixels; positive scrolls up.
        x_overscroll:
          type: number
          format: double
          description: >
            Additional horizontal distance scrolled past the end.
        y_overscroll:
          type: number
          format: double
          description: >
            Additional vertical distance scrolled past the end.
        prevent_fling:
          type: boolean
          description: >
            Whether fling was suppressed.
        speed:
          type: integer
          description: >
            Swipe speed in pixels per second.
        gesture_source_type:
          $ref: "#/components/schemas/BrowserCdpGestureSourceType"
          description: >
            Input source the synthesized gesture emulates. A value the
            protocol does not define is reported as `other`.
        repeat_count:
          type: integer
          description: >
            Number of additional repeats of the scroll.
        repeat_delay_ms:
          type: integer
          description: >
            Delay between repeats, in milliseconds.
    BrowserCdpInputSynthesizeTapGestureCommandData:
      type: object
      description: >
        Sanitized `Input.synthesizeTapGesture` arguments. Canonical input:
        `Input.synthesizeTapGesture` in devtools-protocol@2d019e73, pinned
        at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method]
      properties:
        method:
          type: string
          const: Input.synthesizeTapGesture
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        x:
          type: number
          format: double
          description: >
            Viewport x coordinate in CSS pixels.
        y:
          type: number
          format: double
          description: >
            Viewport y coordinate in CSS pixels.
        duration:
          type: integer
          description: >
            Duration between touchdown and touchup, in milliseconds.
        tap_count:
          type: integer
          description: >
            Number of times to tap (2 is a double tap).
        gesture_source_type:
          $ref: "#/components/schemas/BrowserCdpGestureSourceType"
          description: >
            Input source the synthesized gesture emulates. A value the
            protocol does not define is reported as `other`.
    BrowserCdpDomSetFileInputFilesCommandData:
      type: object
      description: >
        Sanitized `DOM.setFileInputFiles` arguments. Canonical input:
        `DOM.setFileInputFiles` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method, file_count]
      properties:
        method:
          type: string
          const: DOM.setFileInputFiles
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        file_count:
          type: integer
          description: >
            Number of files handed to the input. File paths are never
            captured.
        node_id:
          type: integer
          description: >
            Opaque DOM node identifier the command targeted.
        backend_node_id:
          type: integer
          description: >
            Opaque backend DOM node identifier the command targeted.
        object_id:
          type: string
          maxLength: 128
          description: >
            Opaque Runtime remote object identifier the command targeted.
            Clipped to 128 characters; a longer value is not a real
            identifier.
    BrowserCdpDomFocusCommandData:
      type: object
      description: >
        Sanitized `DOM.focus` arguments. Canonical input: `DOM.focus` in
        devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method]
      properties:
        method:
          type: string
          const: DOM.focus
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        node_id:
          type: integer
          description: >
            Opaque DOM node identifier the command targeted.
        backend_node_id:
          type: integer
          description: >
            Opaque backend DOM node identifier the command targeted.
        object_id:
          type: string
          maxLength: 128
          description: >
            Opaque Runtime remote object identifier the command targeted.
            Clipped to 128 characters; a longer value is not a real
            identifier.
    BrowserCdpDomScrollIntoViewIfNeededCommandData:
      type: object
      description: >
        Sanitized `DOM.scrollIntoViewIfNeeded` arguments. Canonical input:
        `DOM.scrollIntoViewIfNeeded` in devtools-protocol@2d019e73, pinned
        at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method]
      properties:
        method:
          type: string
          const: DOM.scrollIntoViewIfNeeded
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        node_id:
          type: integer
          description: >
            Opaque DOM node identifier the command targeted.
        backend_node_id:
          type: integer
          description: >
            Opaque backend DOM node identifier the command targeted.
        object_id:
          type: string
          maxLength: 128
          description: >
            Opaque Runtime remote object identifier the command targeted.
            Clipped to 128 characters; a longer value is not a real
            identifier.
        rect_x:
          type: number
          format: double
          description: >
            X offset of the rect the command scrolled to, relative to the
            node.
        rect_y:
          type: number
          format: double
          description: >
            Y offset of the rect the command scrolled to, relative to the
            node.
        rect_width:
          type: number
          format: double
          description: >
            Width of the rect the command scrolled to.
        rect_height:
          type: number
          format: double
          description: >
            Height of the rect the command scrolled to.
    BrowserCdpPageBringToFrontCommandData:
      type: object
      description: >
        Sanitized `Page.bringToFront` arguments. Canonical input:
        `Page.bringToFront` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method]
      properties:
        method:
          type: string
          const: Page.bringToFront
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
    BrowserCdpPageCaptureScreenshotCommandData:
      type: object
      description: >
        Sanitized `Page.captureScreenshot` arguments. Canonical input:
        `Page.captureScreenshot` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method]
      properties:
        method:
          type: string
          const: Page.captureScreenshot
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        format:
          $ref: "#/components/schemas/BrowserCdpScreenshotFormat"
          description: >
            Image format requested (`jpeg`, `png` or `webp`). A value the
            protocol does not define is reported as `other`.
        quality:
          type: integer
          description: >
            Compression quality, 0 to 100, for lossy formats.
        from_surface:
          type: boolean
          description: >
            Whether the capture was taken from the surface rather than the
            view.
        capture_beyond_viewport:
          type: boolean
          description: >
            Whether the capture extended past the viewport.
        optimize_for_speed:
          type: boolean
          description: >
            Whether encoding favored speed over size.
        clip_x:
          type: number
          format: double
          description: >
            Clip region x offset in CSS pixels.
        clip_y:
          type: number
          format: double
          description: >
            Clip region y offset in CSS pixels.
        clip_width:
          type: number
          format: double
          description: >
            Clip region width in CSS pixels.
        clip_height:
          type: number
          format: double
          description: >
            Clip region height in CSS pixels.
        clip_scale:
          type: number
          format: double
          description: >
            Clip region page scale factor.
    BrowserCdpPageCaptureSnapshotCommandData:
      type: object
      description: >
        Sanitized `Page.captureSnapshot` arguments. Canonical input:
        `Page.captureSnapshot` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method]
      properties:
        method:
          type: string
          const: Page.captureSnapshot
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        format:
          $ref: "#/components/schemas/BrowserCdpSnapshotFormat"
          description: >
            Snapshot format requested (`mhtml`). A value the protocol does
            not define is reported as `other`.
    BrowserCdpPageHandleJavaScriptDialogCommandData:
      type: object
      description: >
        Sanitized `Page.handleJavaScriptDialog` arguments. Canonical input:
        `Page.handleJavaScriptDialog` in devtools-protocol@2d019e73, pinned
        at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method, accept]
      properties:
        method:
          type: string
          const: Page.handleJavaScriptDialog
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        accept:
          type: boolean
          description: >
            Whether the dialog was accepted or dismissed.
        prompt_text_length:
          type: integer
          description: >
            Number of characters entered into a prompt dialog. The text
            itself is never captured.
    BrowserCdpPageNavigateCommandData:
      type: object
      description: >
        Sanitized `Page.navigate` arguments. Canonical input:
        `Page.navigate` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method]
      properties:
        method:
          type: string
          const: Page.navigate
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        url_scheme:
          type: string
          maxLength: 32
          description: >
            Scheme of the destination URL (e.g. `https`, `about`, `data`).
            The rest of the URL is never captured.
        transition_type:
          $ref: "#/components/schemas/BrowserCdpTransitionType"
          description: >
            Navigation reason reported by the caller (e.g. `link`, `typed`,
            `reload`). A value the protocol does not define is reported as
            `other`.
        referrer_present:
          type: boolean
          description: >
            Whether the command carried a referrer. The referrer itself is
            never captured.
        referrer_policy:
          $ref: "#/components/schemas/BrowserCdpReferrerPolicy"
          description: >
            Referrer policy named by the command. A value the protocol does
            not define is reported as `other`.
        frame_id:
          type: string
          maxLength: 128
          description: >
            Opaque frame identifier. Clipped to 128 characters; a longer
            value is not a real identifier.
    BrowserCdpPageNavigateToHistoryEntryCommandData:
      type: object
      description: >
        Sanitized `Page.navigateToHistoryEntry` arguments. Canonical input:
        `Page.navigateToHistoryEntry` in devtools-protocol@2d019e73, pinned
        at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method, entry_id]
      properties:
        method:
          type: string
          const: Page.navigateToHistoryEntry
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        entry_id:
          type: integer
          description: >
            History entry the command navigated to.
    BrowserCdpPageReloadCommandData:
      type: object
      description: >
        Sanitized `Page.reload` arguments. Canonical input: `Page.reload` in
        devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method]
      properties:
        method:
          type: string
          const: Page.reload
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        ignore_cache:
          type: boolean
          description: >
            Whether the reload bypassed the cache.
        script_length:
          type: integer
          description: >
            Number of characters in the injected script.
        loader_id:
          type: string
          maxLength: 128
          description: >
            Opaque document loader identifier. Clipped to 128 characters; a
            longer value is not a real identifier.
    BrowserCdpPagePrintToPdfCommandData:
      type: object
      description: >
        Sanitized `Page.printToPDF` arguments. Canonical input:
        `Page.printToPDF` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method]
      properties:
        method:
          type: string
          const: Page.printToPDF
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        landscape:
          type: boolean
          description: >
            Whether the page was laid out in landscape.
        scale:
          type: number
          format: double
          description: >
            Page render scale.
        paper_width:
          type: number
          format: double
          description: >
            Paper width in inches.
        paper_height:
          type: number
          format: double
          description: >
            Paper height in inches.
        display_header_footer:
          type: boolean
          description: >
            Whether a header and footer were rendered.
        print_background:
          type: boolean
          description: >
            Whether background graphics were printed.
        margin_top:
          type: number
          format: double
          description: >
            Top margin in inches.
        margin_bottom:
          type: number
          format: double
          description: >
            Bottom margin in inches.
        margin_left:
          type: number
          format: double
          description: >
            Left margin in inches.
        margin_right:
          type: number
          format: double
          description: >
            Right margin in inches.
        prefer_css_page_size:
          type: boolean
          description: >
            Whether the CSS page size was preferred over the paper size.
        generate_tagged_pdf:
          type: boolean
          description: >
            Whether a tagged (accessible) PDF was requested.
        generate_document_outline:
          type: boolean
          description: >
            Whether a document outline was embedded.
        transfer_mode:
          $ref: "#/components/schemas/BrowserCdpPdfTransferMode"
          description: >
            How the PDF was returned (`ReturnAsBase64` or `ReturnAsStream`).
            A value the protocol does not define is reported as `other`.
        page_ranges_present:
          type: boolean
          description: >
            Whether a page range was supplied.
        header_template_present:
          type: boolean
          description: >
            Whether a header template was supplied. The template itself is
            never captured.
        footer_template_present:
          type: boolean
          description: >
            Whether a footer template was supplied. The template itself is
            never captured.
    BrowserCdpPageStartScreencastCommandData:
      type: object
      description: >
        Sanitized `Page.startScreencast` arguments. Canonical input:
        `Page.startScreencast` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method]
      properties:
        method:
          type: string
          const: Page.startScreencast
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        format:
          $ref: "#/components/schemas/BrowserCdpScreencastFormat"
          description: >
            Frame format requested (`jpeg` or `png`). A value the protocol
            does not define is reported as `other`.
        quality:
          type: integer
          description: >
            Compression quality, 0 to 100.
        max_width:
          type: integer
          description: >
            Maximum frame width in pixels.
        max_height:
          type: integer
          description: >
            Maximum frame height in pixels.
        every_nth_frame:
          type: integer
          description: >
            Frame sampling interval.
    BrowserCdpPageStopScreencastCommandData:
      type: object
      description: >
        Sanitized `Page.stopScreencast` arguments. Canonical input:
        `Page.stopScreencast` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method]
      properties:
        method:
          type: string
          const: Page.stopScreencast
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
    BrowserCdpPageStopLoadingCommandData:
      type: object
      description: >
        Sanitized `Page.stopLoading` arguments. Canonical input:
        `Page.stopLoading` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method]
      properties:
        method:
          type: string
          const: Page.stopLoading
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
    BrowserCdpPageCloseCommandData:
      type: object
      description: >
        Sanitized `Page.close` arguments. Canonical input: `Page.close` in
        devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method]
      properties:
        method:
          type: string
          const: Page.close
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
    BrowserCdpPageSetWebLifecycleStateCommandData:
      type: object
      description: >
        Sanitized `Page.setWebLifecycleState` arguments. Canonical input:
        `Page.setWebLifecycleState` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method, state]
      properties:
        method:
          type: string
          const: Page.setWebLifecycleState
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        state:
          $ref: "#/components/schemas/BrowserCdpWebLifecycleState"
          description: >
            Lifecycle state applied (`frozen` or `active`). A value the
            protocol does not define is reported as `other`.
    BrowserCdpTargetActivateTargetCommandData:
      type: object
      description: >
        Sanitized `Target.activateTarget` arguments. Canonical input:
        `Target.activateTarget` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method, target_id]
      properties:
        method:
          type: string
          const: Target.activateTarget
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        target_id:
          type: string
          maxLength: 128
          description: >
            Opaque target identifier. Clipped to 128 characters; a longer
            value is not a real identifier.
    BrowserCdpTargetCloseTargetCommandData:
      type: object
      description: >
        Sanitized `Target.closeTarget` arguments. Canonical input:
        `Target.closeTarget` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method, target_id]
      properties:
        method:
          type: string
          const: Target.closeTarget
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        target_id:
          type: string
          maxLength: 128
          description: >
            Opaque target identifier. Clipped to 128 characters; a longer
            value is not a real identifier.
    BrowserCdpTargetCreateTargetCommandData:
      type: object
      description: >
        Sanitized `Target.createTarget` arguments. Canonical input:
        `Target.createTarget` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method]
      properties:
        method:
          type: string
          const: Target.createTarget
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        url_scheme:
          type: string
          maxLength: 32
          description: >
            Scheme of the destination URL (e.g. `https`, `about`, `data`).
            The rest of the URL is never captured.
        left:
          type: integer
          description: >
            Window x position in screen coordinates.
        top:
          type: integer
          description: >
            Window y position in screen coordinates.
        width:
          type: integer
          description: >
            Window width in DIP.
        height:
          type: integer
          description: >
            Window height in DIP.
        window_state:
          $ref: "#/components/schemas/BrowserCdpWindowState"
          description: >
            Window state requested (`normal`, `minimized`, `maximized`,
            `fullscreen`). A value the protocol does not define is reported
            as `other`.
        browser_context_id:
          type: string
          maxLength: 128
          description: >
            Opaque browser context identifier. Clipped to 128 characters; a
            longer value is not a real identifier.
        new_window:
          type: boolean
          description: >
            Whether a new window was requested.
        background:
          type: boolean
          description: >
            Whether the target was created in the background.
        for_tab:
          type: boolean
          description: >
            Whether a tab target rather than a page target was created.
        hidden:
          type: boolean
          description: >
            Whether the target was created hidden.
        enable_begin_frame_control:
          type: boolean
          description: >
            Whether BeginFrame control was enabled (headless only).
        focus:
          type: boolean
          description: >
            Whether the new target was focused.
    BrowserCdpTargetCreateBrowserContextCommandData:
      type: object
      description: >
        Sanitized `Target.createBrowserContext` arguments. Canonical input:
        `Target.createBrowserContext` in devtools-protocol@2d019e73, pinned
        at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method]
      properties:
        method:
          type: string
          const: Target.createBrowserContext
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        dispose_on_detach:
          type: boolean
          description: >
            Whether the context is disposed when the debugging session
            detaches.
        proxy_server_present:
          type: boolean
          description: >
            Whether a proxy was configured. The proxy address is never
            captured.
        proxy_bypass_list_present:
          type: boolean
          description: >
            Whether a proxy bypass list was configured.
        universal_network_access_origin_count:
          type: integer
          description: >
            Number of origins granted universal network access. The origins
            themselves are never captured.
    BrowserCdpTargetDisposeBrowserContextCommandData:
      type: object
      description: >
        Sanitized `Target.disposeBrowserContext` arguments. Canonical input:
        `Target.disposeBrowserContext` in devtools-protocol@2d019e73, pinned
        at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method, browser_context_id]
      properties:
        method:
          type: string
          const: Target.disposeBrowserContext
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        browser_context_id:
          type: string
          maxLength: 128
          description: >
            Opaque browser context identifier. Clipped to 128 characters; a
            longer value is not a real identifier.
    BrowserCdpTargetOpenDevToolsCommandData:
      type: object
      description: >
        Sanitized `Target.openDevTools` arguments. Canonical input:
        `Target.openDevTools` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method, target_id]
      properties:
        method:
          type: string
          const: Target.openDevTools
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        target_id:
          type: string
          maxLength: 128
          description: >
            Opaque target identifier. Clipped to 128 characters; a longer
            value is not a real identifier.
        panel_id:
          type: string
          maxLength: 128
          description: >
            DevTools panel opened. Clipped to 128 characters; a longer value
            is not a real identifier.
    BrowserCdpBrowserCancelDownloadCommandData:
      type: object
      description: >
        Sanitized `Browser.cancelDownload` arguments. Canonical input:
        `Browser.cancelDownload` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method, download_guid]
      properties:
        method:
          type: string
          const: Browser.cancelDownload
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        download_guid:
          type: string
          maxLength: 128
          description: >
            Opaque identifier of the download that was cancelled. Clipped to
            128 characters; a longer value is not a real identifier.
        browser_context_id:
          type: string
          maxLength: 128
          description: >
            Opaque browser context identifier. Clipped to 128 characters; a
            longer value is not a real identifier.
    BrowserCdpBrowserCloseCommandData:
      type: object
      description: >
        Sanitized `Browser.close` arguments. Canonical input:
        `Browser.close` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method]
      properties:
        method:
          type: string
          const: Browser.close
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
    BrowserCdpBrowserSetWindowBoundsCommandData:
      type: object
      description: >
        Sanitized `Browser.setWindowBounds` arguments. Canonical input:
        `Browser.setWindowBounds` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method, window_id]
      properties:
        method:
          type: string
          const: Browser.setWindowBounds
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        window_id:
          type: integer
          description: >
            Browser window identifier.
        left:
          type: integer
          description: >
            Window x position in screen coordinates.
        top:
          type: integer
          description: >
            Window y position in screen coordinates.
        width:
          type: integer
          description: >
            Window width in DIP.
        height:
          type: integer
          description: >
            Window height in DIP.
        window_state:
          $ref: "#/components/schemas/BrowserCdpWindowState"
          description: >
            Window state requested (`normal`, `minimized`, `maximized`,
            `fullscreen`). A value the protocol does not define is reported
            as `other`.
    BrowserCdpBrowserSetContentsSizeCommandData:
      type: object
      description: >
        Sanitized `Browser.setContentsSize` arguments. Canonical input:
        `Browser.setContentsSize` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method, window_id]
      properties:
        method:
          type: string
          const: Browser.setContentsSize
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        window_id:
          type: integer
          description: >
            Browser window identifier.
        width:
          type: integer
          description: >
            Contents width in DIP.
        height:
          type: integer
          description: >
            Contents height in DIP.
    BrowserCdpAutofillTriggerCommandData:
      type: object
      description: >
        Sanitized `Autofill.trigger` arguments. Canonical input:
        `Autofill.trigger` in devtools-protocol@2d019e73, pinned at
        https://github.com/ChromeDevTools/devtools-protocol/blob/2d019e73eb371d1d6985d26d395d78bd8f8a22ba/json/browser_protocol.json.
        Every argument of this command has a retained or redacted decision
        in lib/devtoolsproxy/testdata/cdp_arguments.yaml.
      additionalProperties: false
      required: [method, field_id]
      properties:
        method:
          type: string
          const: Autofill.trigger
        session_id:
          type: string
          maxLength: 128
          description: >
            CDP session identifier the command was addressed to. Absent for
            browser-level commands. Clipped to 128 characters.
        command_id:
          type: integer
          format: int64
          description: >
            The command's JSON-RPC id, so the command can be joined to the
            result the browser returned for it. Absent when the client sent
            none.
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies the CDP proxy connection the command arrived on,
            matching `cdp_connect` and `cdp_disconnect`. Two clients driving
            the same browser are told apart by this.
        field_id:
          type: integer
          description: >
            Opaque backend node identifier of the field that was autofilled.
        frame_id:
          type: string
          maxLength: 128
          description: >
            Opaque frame identifier. Clipped to 128 characters; a longer
            value is not a real identifier.
        mode:
          $ref: "#/components/schemas/BrowserCdpAutofillMode"
          description: >
            What was filled: `card` or `address`. The values themselves are
            never captured.
        address_field_count:
          type: integer
          description: >
            Number of address fields the command filled. Their names and
            values are never captured.
    BrowserCdpCommandEventData:
      description: >
        Per-command payload for `cdp_command` events, discriminated by
        `method`. Each variant carries only the arguments approved for that
        command: values that could hold a secret — typed and composition
        text, URLs, referrers, scripts, templates, file paths, drag contents
        and autofill values — are replaced by a length, a count, a presence
        flag, an enum or a URL scheme and host.
      oneOf:
        - $ref: "#/components/schemas/BrowserCdpInputDispatchMouseEventCommandData"
        - $ref: "#/components/schemas/BrowserCdpInputDispatchKeyEventCommandData"
        - $ref: "#/components/schemas/BrowserCdpInputInsertTextCommandData"
        - $ref: "#/components/schemas/BrowserCdpInputImeSetCompositionCommandData"
        - $ref: "#/components/schemas/BrowserCdpInputDispatchTouchEventCommandData"
        - $ref: "#/components/schemas/BrowserCdpInputDispatchDragEventCommandData"
        - $ref: "#/components/schemas/BrowserCdpInputCancelDraggingCommandData"
        - $ref: "#/components/schemas/BrowserCdpInputEmulateTouchFromMouseEventCommandData"
        - $ref: "#/components/schemas/BrowserCdpInputSynthesizePinchGestureCommandData"
        - $ref: "#/components/schemas/BrowserCdpInputSynthesizeScrollGestureCommandData"
        - $ref: "#/components/schemas/BrowserCdpInputSynthesizeTapGestureCommandData"
        - $ref: "#/components/schemas/BrowserCdpDomSetFileInputFilesCommandData"
        - $ref: "#/components/schemas/BrowserCdpDomFocusCommandData"
        - $ref: "#/components/schemas/BrowserCdpDomScrollIntoViewIfNeededCommandData"
        - $ref: "#/components/schemas/BrowserCdpPageBringToFrontCommandData"
        - $ref: "#/components/schemas/BrowserCdpPageCaptureScreenshotCommandData"
        - $ref: "#/components/schemas/BrowserCdpPageCaptureSnapshotCommandData"
        - $ref: "#/components/schemas/BrowserCdpPageHandleJavaScriptDialogCommandData"
        - $ref: "#/components/schemas/BrowserCdpPageNavigateCommandData"
        - $ref: "#/components/schemas/BrowserCdpPageNavigateToHistoryEntryCommandData"
        - $ref: "#/components/schemas/BrowserCdpPageReloadCommandData"
        - $ref: "#/components/schemas/BrowserCdpPagePrintToPdfCommandData"
        - $ref: "#/components/schemas/BrowserCdpPageStartScreencastCommandData"
        - $ref: "#/components/schemas/BrowserCdpPageStopScreencastCommandData"
        - $ref: "#/components/schemas/BrowserCdpPageStopLoadingCommandData"
        - $ref: "#/components/schemas/BrowserCdpPageCloseCommandData"
        - $ref: "#/components/schemas/BrowserCdpPageSetWebLifecycleStateCommandData"
        - $ref: "#/components/schemas/BrowserCdpTargetActivateTargetCommandData"
        - $ref: "#/components/schemas/BrowserCdpTargetCloseTargetCommandData"
        - $ref: "#/components/schemas/BrowserCdpTargetCreateTargetCommandData"
        - $ref: "#/components/schemas/BrowserCdpTargetCreateBrowserContextCommandData"
        - $ref: "#/components/schemas/BrowserCdpTargetDisposeBrowserContextCommandData"
        - $ref: "#/components/schemas/BrowserCdpTargetOpenDevToolsCommandData"
        - $ref: "#/components/schemas/BrowserCdpBrowserCancelDownloadCommandData"
        - $ref: "#/components/schemas/BrowserCdpBrowserCloseCommandData"
        - $ref: "#/components/schemas/BrowserCdpBrowserSetWindowBoundsCommandData"
        - $ref: "#/components/schemas/BrowserCdpBrowserSetContentsSizeCommandData"
        - $ref: "#/components/schemas/BrowserCdpAutofillTriggerCommandData"
      discriminator:
        propertyName: method
        mapping:
          Input.dispatchMouseEvent: "#/components/schemas/BrowserCdpInputDispatchMouseEventCommandData"
          Input.dispatchKeyEvent: "#/components/schemas/BrowserCdpInputDispatchKeyEventCommandData"
          Input.insertText: "#/components/schemas/BrowserCdpInputInsertTextCommandData"
          Input.imeSetComposition: "#/components/schemas/BrowserCdpInputImeSetCompositionCommandData"
          Input.dispatchTouchEvent: "#/components/schemas/BrowserCdpInputDispatchTouchEventCommandData"
          Input.dispatchDragEvent: "#/components/schemas/BrowserCdpInputDispatchDragEventCommandData"
          Input.cancelDragging: "#/components/schemas/BrowserCdpInputCancelDraggingCommandData"
          Input.emulateTouchFromMouseEvent: "#/components/schemas/BrowserCdpInputEmulateTouchFromMouseEventCommandData"
          Input.synthesizePinchGesture: "#/components/schemas/BrowserCdpInputSynthesizePinchGestureCommandData"
          Input.synthesizeScrollGesture: "#/components/schemas/BrowserCdpInputSynthesizeScrollGestureCommandData"
          Input.synthesizeTapGesture: "#/components/schemas/BrowserCdpInputSynthesizeTapGestureCommandData"
          DOM.setFileInputFiles: "#/components/schemas/BrowserCdpDomSetFileInputFilesCommandData"
          DOM.focus: "#/components/schemas/BrowserCdpDomFocusCommandData"
          DOM.scrollIntoViewIfNeeded: "#/components/schemas/BrowserCdpDomScrollIntoViewIfNeededCommandData"
          Page.bringToFront: "#/components/schemas/BrowserCdpPageBringToFrontCommandData"
          Page.captureScreenshot: "#/components/schemas/BrowserCdpPageCaptureScreenshotCommandData"
          Page.captureSnapshot: "#/components/schemas/BrowserCdpPageCaptureSnapshotCommandData"
          Page.handleJavaScriptDialog: "#/components/schemas/BrowserCdpPageHandleJavaScriptDialogCommandData"
          Page.navigate: "#/components/schemas/BrowserCdpPageNavigateCommandData"
          Page.navigateToHistoryEntry: "#/components/schemas/BrowserCdpPageNavigateToHistoryEntryCommandData"
          Page.reload: "#/components/schemas/BrowserCdpPageReloadCommandData"
          Page.printToPDF: "#/components/schemas/BrowserCdpPagePrintToPdfCommandData"
          Page.startScreencast: "#/components/schemas/BrowserCdpPageStartScreencastCommandData"
          Page.stopScreencast: "#/components/schemas/BrowserCdpPageStopScreencastCommandData"
          Page.stopLoading: "#/components/schemas/BrowserCdpPageStopLoadingCommandData"
          Page.close: "#/components/schemas/BrowserCdpPageCloseCommandData"
          Page.setWebLifecycleState: "#/components/schemas/BrowserCdpPageSetWebLifecycleStateCommandData"
          Target.activateTarget: "#/components/schemas/BrowserCdpTargetActivateTargetCommandData"
          Target.closeTarget: "#/components/schemas/BrowserCdpTargetCloseTargetCommandData"
          Target.createTarget: "#/components/schemas/BrowserCdpTargetCreateTargetCommandData"
          Target.createBrowserContext: "#/components/schemas/BrowserCdpTargetCreateBrowserContextCommandData"
          Target.disposeBrowserContext: "#/components/schemas/BrowserCdpTargetDisposeBrowserContextCommandData"
          Target.openDevTools: "#/components/schemas/BrowserCdpTargetOpenDevToolsCommandData"
          Browser.cancelDownload: "#/components/schemas/BrowserCdpBrowserCancelDownloadCommandData"
          Browser.close: "#/components/schemas/BrowserCdpBrowserCloseCommandData"
          Browser.setWindowBounds: "#/components/schemas/BrowserCdpBrowserSetWindowBoundsCommandData"
          Browser.setContentsSize: "#/components/schemas/BrowserCdpBrowserSetContentsSizeCommandData"
          Autofill.trigger: "#/components/schemas/BrowserCdpAutofillTriggerCommandData"
    BrowserCdpCommandEvent:
      type: object
      description: >
        A browser-control command a client sent over the CDP WebSocket proxy:
        input gestures, navigation, dialog handling, file selection and
        screenshots. Configuration commands and the DOM/Runtime traffic a client
        library issues on the caller's behalf are not reported.

        One event per browser-control command that reached the browser. The
        command stream is not sampled, coalesced or reordered. An event is lost
        only when the method is excluded by telemetry configuration, when the
        command's arguments do not decode, or when classification cannot keep
        up. Exclusions are counted in `cdp_disconnect.telemetry_excluded`; the
        rest in `cdp_disconnect.telemetry_dropped`.
      required: [ts, type, category, source, data]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: cdp_command
        category:
          type: string
          const: control
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserCdpCommandEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserCdpConnectEventData:
      type: object
      description: Per-connection payload for `cdp_connect` events.
      additionalProperties: false
      properties:
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies this CDP proxy connection, matching the `connection_id`
            on the `cdp_command` events that arrived on it. Two clients driving
            the same browser are told apart by this.
    BrowserCdpConnectEvent:
      type: object
      description: An external client (e.g. customer SDK, Playwright, Puppeteer) connected to the CDP WebSocket proxy on this VM.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: cdp_connect
        category:
          type: string
          const: connection
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserCdpConnectEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserCdpDisconnectEventData:
      type: object
      description: Per-disconnect payload for `cdp_disconnect` events.
      additionalProperties: false
      required: [duration_ms, message_count, reason]
      properties:
        connection_id:
          type: string
          maxLength: 128
          description: >
            Identifies this CDP proxy connection, matching the `connection_id`
            on the `cdp_command` events that arrived on it. Two clients driving
            the same browser are told apart by this.
        duration_ms:
          type: number
          description: Wall-clock duration of the connection in milliseconds.
        message_count:
          type: integer
          description: Number of CDP messages relayed across the connection in either direction.
        telemetry_excluded:
          type: integer
          description: >
            Number of forwarded client commands that produced no `cdp_command`
            event because their method is listed in
            `control.cdp.excluded_methods`. Configuration rather than loss, so
            it is counted apart from `telemetry_dropped`.
        telemetry_dropped:
          type: integer
          description: >
            Number of supported browser-control commands that were forwarded
            to the browser but never classified, because the queue was full
            or classification panicked. Every increment is a real lost
            command — unsupported and excluded methods are filtered before
            admission and never count toward this total. Telemetry loss
            only; every command was still relayed to the browser. Always
            present on images that report it; absent on images predating the
            field, which is not the same as zero.
        reason:
          type: string
          description: >
            Why the connection ended. `client_close`: the client initiated the close.
            `upstream_changed`: Chromium restarted mid-session and the proxy tore down
            so the client could reconnect against the new upstream. `upstream_error`:
            upstream dial or message pump errored. `context_cancelled`: the request
            context was cancelled (typically server shutdown).
          enum:
            - client_close
            - upstream_changed
            - upstream_error
            - context_cancelled
    BrowserCdpDisconnectEvent:
      type: object
      description: An external client disconnected from the CDP WebSocket proxy on this VM. Pair with the immediately preceding `cdp_connect` on the same stream.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: cdp_disconnect
        category:
          type: string
          const: connection
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserCdpDisconnectEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserLiveViewConnectEventData:
      type: object
      description: Per-session payload for `live_view_connect` events.
      additionalProperties: false
      required: [session_id]
      properties:
        session_id:
          type: string
          description: Live view session identifier. Stable across reconnects, so a transient network blip can emit two events with the same `session_id`.
    BrowserLiveViewConnectEvent:
      type: object
      description: A live view client connected to the headful browser's WebRTC server (Neko). Headful only; not emitted for headless images.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: live_view_connect
        category:
          type: string
          const: connection
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserLiveViewConnectEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserLiveViewDisconnectEventData:
      type: object
      description: Per-session payload for `live_view_disconnect` events.
      additionalProperties: false
      required: [session_id, duration_ms]
      properties:
        session_id:
          type: string
          description: Live view session identifier; matches the corresponding `live_view_connect` event.
        duration_ms:
          type: number
          description: Wall-clock duration of the connection in milliseconds.
    BrowserLiveViewDisconnectEvent:
      type: object
      description: A live view client disconnected from the headful browser's WebRTC server (Neko). Pair with `live_view_connect` by `session_id`.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: live_view_disconnect
        category:
          type: string
          const: connection
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserLiveViewDisconnectEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserCaptchaSolveResultEventDataCaptchaType:
      type: string
      description: >
        Captcha kind reported by the solver. Provider-specific task names are
        normalized into this set: enterprise variants of reCAPTCHA collapse
        into their version bucket (v2 / v3), `press_and_hold` covers
        press-and-hold challenges, and unlisted kinds are reported as `other`.
      enum:
        - hcaptcha
        - recaptcha_v2
        - recaptcha_v3
        - turnstile
        - geetest
        - press_and_hold
        - other
    BrowserCaptchaType:
      $ref: "#/components/schemas/BrowserCaptchaSolveResultEventDataCaptchaType"
    BrowserCaptchaChallengeID:
      type: string
      description: >
        Opaque identifier shared by events from one visible challenge. An
        image-grid captcha can retry with a fresh task per attempt, so one
        `challenge_id` may be associated with multiple `task_id` values. A
        producer may preserve it across a page reload when that reload is part
        of the same challenge episode. It does not indicate task ordering or
        challenge completion.
    BrowserCaptchaSolveStartedEventData:
      type: object
      description: >
        Per-task payload for `captcha_solve_started` events. Emitted when a
        solver accepts a task, once per solver task. A single visible challenge
        can produce many tasks, so consumers must not assume one started event
        per challenge or pair events by arrival order. When present, `task_id`
        correlates this event with a terminal `captcha_solve_result`, and
        `challenge_id` groups tasks from one visible challenge. Delivery is not
        ordered: a started event may arrive after its result, and either event
        may be missing. Arrival or absence therefore does not indicate current
        solve state.
      additionalProperties: false
      required: [captcha_type]
      properties:
        captcha_type:
          $ref: "#/components/schemas/BrowserCaptchaType"
        task_id:
          type: string
          description: >
            Opaque producer-assigned identifier shared with the matching
            `captcha_solve_result`.
        challenge_id:
          $ref: "#/components/schemas/BrowserCaptchaChallengeID"
        website_host:
          type: string
          description: Host of the page where the captcha is being solved. May be empty for solver tasks that carry no page URL (e.g. image-grid sub-tasks).
        website_path:
          type: string
          description: Path of the page where the captcha is being solved. Query string excluded.
    BrowserCaptchaSolveStartedEvent:
      type: object
      description: A captcha solver accepted a task.
      required: [ts, type, category, source, data]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: captcha_solve_started
        category:
          type: string
          const: captcha
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserCaptchaSolveStartedEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserCaptchaSolveResultEventData:
      type: object
      description: Per-attempt payload for `captcha_solve_result` events.
      additionalProperties: false
      required: [captcha_type, status, duration_ms]
      properties:
        captcha_type:
          $ref: "#/components/schemas/BrowserCaptchaType"
        status:
          type: string
          description: >
            Terminal outcome. `success`: solver returned a usable solution.
            `failure`: solver returned an error (see `error_code`).
            `timeout`: solver did not return within the caller's wait budget.
            `abandoned`: caller cancelled or the page navigated away mid-solve.
          enum:
            - success
            - failure
            - timeout
            - abandoned
        duration_ms:
          type: number
          description: >
            Wall-clock duration from solve start to terminal outcome. This is
            the authoritative solve timing; do not derive duration from the
            gap to a `captcha_solve_started` event, whose delivery and
            sequencing are not guaranteed.
        task_id:
          type: string
          description: Opaque producer-assigned identifier shared with the matching `captcha_solve_started`.
        challenge_id:
          $ref: "#/components/schemas/BrowserCaptchaChallengeID"
        website_host:
          type: string
          description: Host of the page where the captcha was solved.
        website_path:
          type: string
          description: Path of the page where the captcha was solved. Query string excluded.
        error_code:
          type: string
          description: Solver-specific error code on failure (e.g. `ERROR_CAPTCHA_UNSOLVABLE`). Absent on success.
    BrowserCaptchaSolveResultEvent:
      type: object
      description: A captcha solve attempt reached a terminal outcome.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: captcha_solve_result
        category:
          type: string
          const: captcha
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserCaptchaSolveResultEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserCaptchaChallengeResultEventData:
      type: object
      description: Per-challenge payload for `captcha_challenge_result` events.
      additionalProperties: false
      required: [captcha_type, status, challenge_id, duration_ms]
      properties:
        captcha_type:
          $ref: "#/components/schemas/BrowserCaptchaType"
        status:
          type: string
          description: >
            Terminal outcome of the visible challenge. `solved`: the page
            observed the challenge clear after a solver attempt. For token
            captchas this means a response token was present; for
            press-and-hold it means the challenge page navigated away. `failure`:
            the producer stopped after an explicit terminal solver failure or
            exhausted its attempts while the challenge remained. `timeout`:
            the producer's challenge-level wait budget expired while the
            challenge remained. `abandoned`: observation ended without a
            solved signal or an explicit terminal solver outcome, for example
            because the widget was dismissed or the page unloaded.

            `solved` describes the challenge, not the flow around it. A site
            can still reject a valid token, and tokens expire, so this is not
            proof that whatever the challenge was guarding succeeded.
          enum:
            - solved
            - failure
            - timeout
            - abandoned
          x-enum-varnames:
            - ChallengeSolved
            - ChallengeFailure
            - ChallengeTimeout
            - ChallengeAbandoned
        challenge_id:
          $ref: "#/components/schemas/BrowserCaptchaChallengeID"
        duration_ms:
          type: number
          description: >
            Wall-clock duration from the challenge appearing to its terminal
            outcome, covering every solver attempt in between.
        website_host:
          type: string
          description: Host of the page the challenge appeared on.
        website_path:
          type: string
          description: Path of the page the challenge appeared on. Query string excluded.
    BrowserCaptchaChallengeResultEvent:
      type: object
      description: >
        A visible captcha challenge reached a terminal outcome. Emitted once
        per challenge. Wait for this event, rather than a task result, to
        determine the overall challenge outcome. `captcha_solve_started` and
        `captcha_solve_result` describe individual solver tasks and may occur
        multiple times within one challenge.
      required: [ts, type, category, source, data]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: captcha_challenge_result
        category:
          type: string
          const: captcha
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserCaptchaChallengeResultEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserSystemOomKillEventData:
      type: object
      description: Per-kill payload for `system_oom_kill` events.
      additionalProperties: false
      required: [process_name, pid, rss_kb]
      properties:
        process_name:
          type: string
          description: Comm of the killed process as reported by the kernel (max 15 chars, truncated by the kernel).
        pid:
          type: integer
          description: PID of the killed process.
        rss_kb:
          type: integer
          description: Resident set size of the killed process in KiB (sum of anon-rss, file-rss, and shmem-rss). This is the physical memory the process was using at the time of the kill.
        trigger_process_name:
          type: string
          description: >
            Comm of the process whose allocation request caused the
            kernel to invoke the OOM-killer. Often the same as
            `process_name` (the kernel killed the requester) but can
            differ when the kernel chose a different victim. Max 15
            chars, truncated by the kernel.
        trigger_pid:
          type: integer
          description: >
            PID of the triggering process. Absent if the kernel did
            not emit the standard `CPU: N PID: N Comm:` header line.
        constraint:
          type: string
          description: >
            Why the kernel decided to OOM-kill. `none` means global memory
            exhaustion; `memcg` means a cgroup memory limit was hit;
            `cpuset` / `memory_policy` are NUMA/policy-driven kills.
            Absent on kernels older than 5.0 which did not emit the
            structured `oom-kill:` line.
          enum:
            - none
            - memcg
            - cpuset
            - memory_policy
        mem_total_kb:
          type: integer
          description: >
            Total system memory in KiB at the time of the kill, derived
            from the `N pages RAM` line in the kernel's Mem-Info dump.
            Assumes a 4 KiB page size. Absent if the kernel did not
            emit a parseable Mem-Info section.
        mem_free_kb:
          type: integer
          description: >
            Free system memory in KiB at the time of the kill, derived
            from the `free:N` field in the kernel's Mem-Info dump.
            Assumes a 4 KiB page size. Does not include reclaimable
            caches, so a small value with a large `mem_total_kb` may
            still mean the system was not under hard pressure. Absent
            if the kernel did not emit a parseable Mem-Info section.
        top_tasks:
          type: array
          maxItems: 5
          description: >
            Top processes by resident-set-size at the moment of the
            kill, sorted descending. Sourced from the kernel's
            `Tasks state` table. Empty if the kernel did not emit the
            table. Capped at 5 entries to bound payload size.
          items:
            $ref: "#/components/schemas/BrowserSystemOomKillTask"
    BrowserSystemOomKillTask:
      type: object
      description: A single process entry from the kernel's `Tasks state` dump.
      additionalProperties: false
      required: [pid, name, rss_kb]
      properties:
        pid:
          type: integer
          description: PID of the process.
        name:
          type: string
          description: Comm of the process (max 15 chars, truncated by the kernel).
        rss_kb:
          type: integer
          description: Resident set size in KiB at the moment of the kill.
    BrowserSystemOomKillEvent:
      type: object
      description: >
        The Linux kernel OOM-killer terminated a process inside the VM. Sourced from
        `/dev/kmsg`. Fires for any process killed by the kernel due to memory
        exhaustion, including Chrome renderer subprocesses that are not supervised.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: system_oom_kill
        category:
          type: string
          const: system
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserSystemOomKillEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    BrowserServiceCrashedEventData:
      type: object
      description: >
        Per-crash payload for `service_crashed` events. Exit code and
        signal are not exposed by the underlying process manager on this
        channel, so only the service identity, the lifecycle phase the
        crash occurred in, and (when available) the PID are reported.
      additionalProperties: false
      required: [service_name, phase]
      properties:
        service_name:
          type: string
          description: Program name of the crashed service (e.g. `chromium`, `mutter`, `kernel-images-api`).
        pid:
          type: integer
          description: PID of the crashed process. Absent when the process manager gave up after exhausting restart attempts and is no longer tracking a live PID.
        phase:
          type: string
          description: >
            Lifecycle phase the crash occurred in.
            `startup` means the process died before it ever reached a
            healthy running state. `running` means a previously healthy
            process died unexpectedly. `gave_up` means the process
            manager exhausted its restart attempts and stopped trying;
            no further `service_crashed` events will fire for this
            service until something restarts it.
          enum:
            - startup
            - running
            - gave_up
    BrowserServiceCrashedEvent:
      type: object
      description: >
        A managed service exited unexpectedly. Intentional stops (e.g.
        operator-initiated shutdown) do not produce this event — only
        unexpected exits and terminal restart-give-up transitions do.
      required: [ts, type, category, source]
      properties:
        ts:
          type: integer
          format: int64
          description: Event timestamp in Unix microseconds.
        type:
          type: string
          const: service_crashed
        category:
          type: string
          const: system
        source:
          $ref: "#/components/schemas/BrowserEventSource"
        data:
          $ref: "#/components/schemas/BrowserServiceCrashedEventData"
        truncated:
          type: boolean
          description: True if the data field was truncated due to size limits.
    KnownBrowserTelemetryEvent:
      description: >
        Discriminated union of browser telemetry events emitted by the Kernel
        image. This is a structural taxonomy: any event on the telemetry stream
        whose `data` conforms to one of the variants below (selected by `type`)
        is a `KnownBrowserTelemetryEvent`, regardless of who published it.
        Caller-published events via POST /telemetry/events are not constrained
        to this union; see `TelemetryEvent` for the wire shape. Validation of
        caller payloads against this taxonomy is the consumer's responsibility.
      oneOf:
        - $ref: "#/components/schemas/BrowserConsoleLogEvent"
        - $ref: "#/components/schemas/BrowserConsoleErrorEvent"
        - $ref: "#/components/schemas/BrowserNetworkRequestEvent"
        - $ref: "#/components/schemas/BrowserNetworkResponseEvent"
        - $ref: "#/components/schemas/BrowserNetworkLoadingFailedEvent"
        - $ref: "#/components/schemas/BrowserNetworkIdleEvent"
        - $ref: "#/components/schemas/BrowserProxyErrorEvent"
        - $ref: "#/components/schemas/BrowserPageNavigationEvent"
        - $ref: "#/components/schemas/BrowserPageDomContentLoadedEvent"
        - $ref: "#/components/schemas/BrowserPageLoadEvent"
        - $ref: "#/components/schemas/BrowserPageTabOpenedEvent"
        - $ref: "#/components/schemas/BrowserPageCrashedEvent"
        - $ref: "#/components/schemas/BrowserPageLayoutShiftEvent"
        - $ref: "#/components/schemas/BrowserPageLcpEvent"
        - $ref: "#/components/schemas/BrowserPageLayoutSettledEvent"
        - $ref: "#/components/schemas/BrowserPageNavigationSettledEvent"
        - $ref: "#/components/schemas/BrowserInteractionClickEvent"
        - $ref: "#/components/schemas/BrowserInteractionKeyEvent"
        - $ref: "#/components/schemas/BrowserInteractionScrollSettledEvent"
        - $ref: "#/components/schemas/BrowserMonitorScreenshotEvent"
        - $ref: "#/components/schemas/BrowserMonitorDisconnectedEvent"
        - $ref: "#/components/schemas/BrowserMonitorReconnectedEvent"
        - $ref: "#/components/schemas/BrowserMonitorReconnectFailedEvent"
        - $ref: "#/components/schemas/BrowserMonitorInitFailedEvent"
        - $ref: "#/components/schemas/BrowserApiCallEvent"
        - $ref: "#/components/schemas/BrowserPlatformApiCallEvent"
        - $ref: "#/components/schemas/BrowserCdpCommandEvent"
        - $ref: "#/components/schemas/BrowserCdpConnectEvent"
        - $ref: "#/components/schemas/BrowserCdpDisconnectEvent"
        - $ref: "#/components/schemas/BrowserLiveViewConnectEvent"
        - $ref: "#/components/schemas/BrowserLiveViewDisconnectEvent"
        - $ref: "#/components/schemas/BrowserCaptchaSolveStartedEvent"
        - $ref: "#/components/schemas/BrowserCaptchaSolveResultEvent"
        - $ref: "#/components/schemas/BrowserCaptchaChallengeResultEvent"
        - $ref: "#/components/schemas/BrowserSystemOomKillEvent"
        - $ref: "#/components/schemas/BrowserServiceCrashedEvent"
      discriminator:
        propertyName: type
        mapping:
          console_log: "#/components/schemas/BrowserConsoleLogEvent"
          console_error: "#/components/schemas/BrowserConsoleErrorEvent"
          network_request: "#/components/schemas/BrowserNetworkRequestEvent"
          network_response: "#/components/schemas/BrowserNetworkResponseEvent"
          network_loading_failed: "#/components/schemas/BrowserNetworkLoadingFailedEvent"
          network_idle: "#/components/schemas/BrowserNetworkIdleEvent"
          proxy_error: "#/components/schemas/BrowserProxyErrorEvent"
          page_navigation: "#/components/schemas/BrowserPageNavigationEvent"
          page_dom_content_loaded: "#/components/schemas/BrowserPageDomContentLoadedEvent"
          page_load: "#/components/schemas/BrowserPageLoadEvent"
          page_tab_opened: "#/components/schemas/BrowserPageTabOpenedEvent"
          page_crashed: "#/components/schemas/BrowserPageCrashedEvent"
          page_layout_shift: "#/components/schemas/BrowserPageLayoutShiftEvent"
          page_lcp: "#/components/schemas/BrowserPageLcpEvent"
          page_layout_settled: "#/components/schemas/BrowserPageLayoutSettledEvent"
          page_navigation_settled: "#/components/schemas/BrowserPageNavigationSettledEvent"
          interaction_click: "#/components/schemas/BrowserInteractionClickEvent"
          interaction_key: "#/components/schemas/BrowserInteractionKeyEvent"
          interaction_scroll_settled: "#/components/schemas/BrowserInteractionScrollSettledEvent"
          monitor_screenshot: "#/components/schemas/BrowserMonitorScreenshotEvent"
          monitor_disconnected: "#/components/schemas/BrowserMonitorDisconnectedEvent"
          monitor_reconnected: "#/components/schemas/BrowserMonitorReconnectedEvent"
          monitor_reconnect_failed: "#/components/schemas/BrowserMonitorReconnectFailedEvent"
          monitor_init_failed: "#/components/schemas/BrowserMonitorInitFailedEvent"
          api_call: "#/components/schemas/BrowserApiCallEvent"
          platform_api_call: "#/components/schemas/BrowserPlatformApiCallEvent"
          cdp_command: "#/components/schemas/BrowserCdpCommandEvent"
          cdp_connect: "#/components/schemas/BrowserCdpConnectEvent"
          cdp_disconnect: "#/components/schemas/BrowserCdpDisconnectEvent"
          live_view_connect: "#/components/schemas/BrowserLiveViewConnectEvent"
          live_view_disconnect: "#/components/schemas/BrowserLiveViewDisconnectEvent"
          captcha_solve_started: "#/components/schemas/BrowserCaptchaSolveStartedEvent"
          captcha_solve_result: "#/components/schemas/BrowserCaptchaSolveResultEvent"
          captcha_challenge_result: "#/components/schemas/BrowserCaptchaChallengeResultEvent"
          system_oom_kill: "#/components/schemas/BrowserSystemOomKillEvent"
          service_crashed: "#/components/schemas/BrowserServiceCrashedEvent"
    TelemetryState:
      type: object
      description: Current telemetry configuration.
      required:
        - config
        - seq
      properties:
        config:
          $ref: "#/components/schemas/BrowserTelemetryConfig"
        seq:
          type: integer
          format: int64
          description: >-
            Process-monotonic sequence number of the last published event.
            Does not reset across configuration changes.
          minimum: 0
        dropped_events:
          type: integer
          format: int64
          description: >-
            Cumulative number of buffered events a consumer missed because it
            fell behind the ring, summed across consumers and configuration
            changes. A rising count means the stream is being produced faster
            than it is being read; a steady one means nothing has been lost.
            Always present on images that report it; absent on an image
            predating the field, which is not the same as zero.
          minimum: 0
        applied_at:
          type: string
          format: date-time
          description: >-
            Wall-clock time at which the current configuration was applied.
            Omitted when telemetry is not configured.
      additionalProperties: false
    WebMCPToolAnnotations:
      type: object
      description: |
        Tool-provided behavioral hints from the [MCP tool specification](https://modelcontextprotocol.io/specification/2025-11-25/server/tools#tool)
        and the [WebMCP ToolAnnotations definition](https://webmachinelearning.github.io/webmcp/#dictdef-toolannotations).
        These hints are untrusted and are not enforced by Kernel.
      additionalProperties: false
      properties:
        readOnlyHint:
          type: boolean
        destructiveHint:
          type: boolean
        idempotentHint:
          type: boolean
        openWorldHint:
          type: boolean
        consequentialHint:
          type: boolean
        untrustedContentHint:
          type: boolean
        autosubmit:
          type: boolean
    WebMCPToolFrame:
      type: object
      additionalProperties: false
      required: [frame_id, url]
      properties:
        frame_id:
          type: integer
          minimum: 1
          description: Monotonically increasing identifier for this embedded frame during the current browser process.
        url:
          type: string
          description: Current frame URL with the fragment omitted.
    WebMCPCustomToolSource:
      type: object
      additionalProperties: false
      required: [id, namespace]
      properties:
        id:
          type: string
          pattern: "^ct_[a-z][a-z0-9]{23}$"
        namespace:
          type: string
    WebMCPToolSource:
      type: object
      additionalProperties: false
      required: [window_id, tab_id, page_title, page_url, frame]
      properties:
        window_id:
          type: integer
          minimum: 1
          description: Monotonically increasing identifier for the browser window during the current browser process.
        tab_id:
          type: integer
          minimum: 1
          description: Monotonically increasing identifier for the tab during the current browser process.
        target_id:
          type: string
          description: CDP target ID for a custom tool's registration tab; omitted for page-provided tools.
        page_title:
          type: string
          description: Current title of the top-level page.
        page_url:
          type: string
          description: Current URL of the top-level page with the fragment omitted.
        frame:
          allOf:
            - $ref: "#/components/schemas/WebMCPToolFrame"
          nullable: true
          description: Embedded frame that registered the tool, or null when the top-level page registered it.
        custom:
          $ref: "#/components/schemas/WebMCPCustomToolSource"
    WebMCPToolMetadata:
      type: object
      description: |
        Tool metadata follows the [MCP Tool definition](https://modelcontextprotocol.io/specification/2025-11-25/server/tools#tool)
        and the [WebMCP RegisteredTool definition](https://webmachinelearning.github.io/webmcp/#dictdef-registeredtool).
        outputSchema is optional for page and custom tools.
      additionalProperties: false
      required: [name, description, inputSchema]
      properties:
        name:
          type: string
        title:
          type: string
        description:
          type: string
        inputSchema:
          type: object
          additionalProperties: true
        outputSchema:
          type: object
          additionalProperties: true
        annotations:
          $ref: "#/components/schemas/WebMCPToolAnnotations"
    WebMCPTool:
      type: object
      additionalProperties: false
      required: [tool_ref, tool, source]
      properties:
        tool_ref:
          type: string
          description: Opaque reference for invoking this exact live registration. It becomes invalid when its document or browser process is replaced.
        tool:
          $ref: "#/components/schemas/WebMCPToolMetadata"
        source:
          $ref: "#/components/schemas/WebMCPToolSource"
    WebMCPToolsResponse:
      type: object
      additionalProperties: false
      required: [tools]
      properties:
        tools:
          type: array
          items:
            $ref: "#/components/schemas/WebMCPTool"
    AddCustomWebMCPToolsRequest:
      type: object
      required: [namespace, source]
      properties:
        namespace:
          type: string
          pattern: "^[A-Za-z0-9_.-]{1,128}$"
        source:
          type: string
          maxLength: 8000000
          description: JavaScript expression that evaluates to a non-empty array of custom tool definitions.
        force_overwrite_namespace:
          type: boolean
          default: false
          description: Atomically replace all existing tools in this namespace with this batch when true.
      additionalProperties: false
    CustomWebMCPMatch:
      type: object
      required: [url_patterns]
      properties:
        url_patterns:
          type: array
          minItems: 1
          items:
            type: string
      additionalProperties: false
    CustomWebMCPDefinition:
      type: object
      required: [id, namespace, kind, match, tool]
      properties:
        id:
          type: string
          pattern: "^ct_[a-z][a-z0-9]{23}$"
        namespace:
          type: string
        kind:
          type: string
          pattern: "^(page|cdp)$"
        match:
          $ref: "#/components/schemas/CustomWebMCPMatch"
        tool:
          $ref: "#/components/schemas/WebMCPToolMetadata"
      additionalProperties: false
    CustomWebMCPToolsResponse:
      type: object
      required: [tools]
      properties:
        tools:
          type: array
          items:
            $ref: "#/components/schemas/CustomWebMCPDefinition"
      additionalProperties: false
    WebMCPInvokeRequest:
      type: object
      additionalProperties: false
      required: [tool_ref, input]
      properties:
        tool_ref:
          type: string
          minLength: 1
          maxLength: 128
        input:
          type: object
          description: Tool input, limited to 1 MiB after JSON serialization.
          additionalProperties: true
        timeout_sec:
          type: integer
          minimum: 1
          maximum: 120
          default: 60
    WebMCPInvocationResult:
      type: object
      additionalProperties: false
      required: [invocation_id, status]
      properties:
        invocation_id:
          type: string
        status:
          type: string
          enum: [completed, canceled, error, awaiting_submission]
          description: awaiting_submission means a non-autosubmit declarative form was populated but not submitted.
        output:
          description: Untrusted page-provided output. Callers must treat it as potentially malicious input.
        error_text:
          type: string
    WebMCPInvocationFailure:
      type: object
      additionalProperties: false
      required: [code, message]
      properties:
        code:
          type: string
          enum: [outcome_unknown]
        message:
          type: string
        invocation_id:
          type: string
    StartRecordingRequest:
      type: object
      properties:
        maxFileSizeInMB:
          type: integer
          description: Maximum file size in MB (overrides server default)
          minimum: 10
          maximum: 10000
        framerate:
          type: integer
          description: Recording framerate in fps (overrides server default)
          minimum: 1
          maximum: 20
        maxDurationInSeconds:
          type: integer
          description: Maximum recording duration in seconds (overrides server default)
          minimum: 1
        recordAudio:
          type: boolean
          description: Capture audio alongside video. Requires the server to have an audio source and PulseAudio socket configured (the image sets both by default). When false the recording is video-only.
          default: false
        id:
          type: string
          description: Optional identifier for this recording session, used to target it from the other /recording endpoints (stop, mark, download, delete) and allowing multiple concurrent recordings. Alphanumeric or hyphen. When omitted, the default recording session is started.
          pattern: "^[a-zA-Z0-9-]+$"
      additionalProperties: false
    StopRecordingRequest:
      type: object
      properties:
        forceStop:
          type: boolean
          description: Immediately stop without graceful shutdown. This may result in a corrupted video file.
          default: false
        id:
          type: string
          description: Identifier of the recording session to stop, as passed to /recording/start. Alphanumeric or hyphen. When omitted, the default recording session is stopped.
          pattern: "^[a-zA-Z0-9-]+$"
      additionalProperties: false
    Error:
      type: object
      required: [message]
      properties:
        message:
          type: string
    ChromiumConfigureError:
      type: object
      description: Failure from batched chromium configure — includes which phase failed.
      required: [phase, message]
      additionalProperties: false
      properties:
        phase:
          type: string
          enum:
            - configure_phase
            - navigate_phase
          description: configure_phase maps to restart/filesystem/policy/extension/profile/display work; navigate_phase is retained for compatibility.
        message:
          type: string
        step:
          type: string
          description: Optional configure step that failed.
          enum:
            - stop_chromium
            - start_chromium
            - chrome_policies
            - extensions
            - display
            - chromium_flags
            - profile
    RecorderInfo:
      type: object
      required: [id, isRecording]
      properties:
        id:
          type: string
        isRecording:
          type: boolean
        started_at:
          type: [string, "null"]
          format: date-time
          description: Timestamp when recording started
        finished_at:
          type: [string, "null"]
          format: date-time
          description: Timestamp when recording finished
    ClickMouseRequest:
      type: object
      required:
        - x
        - y
      properties:
        button:
          type: string
          description: Mouse button to interact with
          enum: [left, right, middle, back, forward]
        click_type:
          type: string
          description: Type of click action
          enum: [down, up, click]
        x:
          type: integer
          description: X coordinate of the click position
        y:
          type: integer
          description: Y coordinate of the click position
        hold_keys:
          type: array
          description: Modifier keys to hold during the click
          items:
            type: string
        num_clicks:
          type: integer
          description: Number of times to repeat the click
          default: 1
      additionalProperties: false
    MoveMouseRequest:
      type: object
      required:
        - x
        - y
      properties:
        x:
          type: integer
          description: X coordinate to move the cursor to
        y:
          type: integer
          description: Y coordinate to move the cursor to
        hold_keys:
          type: array
          description: Modifier keys to hold during the move
          items:
            type: string
        smooth:
          type: boolean
          description: Use human-like Bezier curve path instead of instant mouse movement.
          default: true
        duration_ms:
          type: integer
          description: Target total duration in milliseconds for the mouse movement when smooth=true. Omit for automatic timing based on distance.
          minimum: 50
          maximum: 5000
      additionalProperties: false
    ScreenshotRegion:
      type: object
      required:
        - x
        - y
        - width
        - height
      properties:
        x:
          type: integer
          description: X coordinate of the region's top-left corner
        y:
          type: integer
          description: Y coordinate of the region's top-left corner
        width:
          type: integer
          description: Width of the region in pixels
        height:
          type: integer
          description: Height of the region in pixels
      additionalProperties: false
    ScreenshotRequest:
      type: object
      properties:
        region:
          $ref: "#/components/schemas/ScreenshotRegion"
      additionalProperties: false
    SetCursorRequest:
      type: object
      required:
        - hidden
      properties:
        hidden:
          type: boolean
          description: Whether the cursor should be hidden
      additionalProperties: false
    MousePositionResponse:
      type: object
      required:
        - x
        - y
      properties:
        x:
          type: integer
          description: X coordinate of the cursor
        y:
          type: integer
          description: Y coordinate of the cursor
    TypeTextRequest:
      type: object
      required:
        - text
      properties:
        text:
          type: string
          description: Text to type on the host computer
        delay:
          type: integer
          description: Delay in milliseconds between keystrokes. Ignored when smooth is true.
          minimum: 0
          default: 0
        smooth:
          type: boolean
          description: |
            Use human-like variable keystroke timing instead of a fixed delay.
            Defaults to true (same as moveMouse/dragMouse). Set to false for
            xdotool typing with an optional fixed delay between keys (delay=0 is instant).
            When true, text is typed in word-sized chunks with variable intra-word delays
            and natural inter-word pauses. The delay field is ignored when smooth is true.
          default: true
        typo_chance:
          type: number
          description: |
            Per-character typo injection rate; mistakes are corrected with backspace.
            Default 0. Only applies when smooth is true (silently ignored when
            smooth is false).
          minimum: 0
          maximum: 0.10
          default: 0
      additionalProperties: false
    ClipboardContent:
      type: object
      required:
        - text
      properties:
        text:
          type: string
          description: Current clipboard text content
      additionalProperties: false
    WriteClipboardRequest:
      type: object
      required:
        - text
      properties:
        text:
          type: string
          description: Text to write to the system clipboard
      additionalProperties: false
    PressKeyRequest:
      type: object
      required:
        - keys
      properties:
        keys:
          type: array
          description: |
            List of key symbols to press. Each item should be a key symbol supported by xdotool
            (see X11 keysym definitions). Examples include "Return", "Shift", "Ctrl", "Alt", "F5".
            Items in this list could also be combinations, e.g. "Ctrl+t" or "Ctrl+Shift+Tab".
            Use X11 names for punctuation in combinations, such as "Ctrl+minus" or "Ctrl+plus".
            A literal hyphen is also accepted as an alias, so "Ctrl+-" is normalized to "Ctrl+minus".
          items:
            type: string
        duration:
          type: integer
          description: Duration to hold the keys down in milliseconds. If omitted or 0, keys are tapped.
          minimum: 0
          default: 0
        hold_keys:
          type: array
          description: Optional modifier keys to hold during the key press sequence.
          items:
            type: string
      additionalProperties: false
    ScrollRequest:
      type: object
      required:
        - x
        - y
      properties:
        x:
          type: integer
          description: X coordinate at which to perform the scroll
        y:
          type: integer
          description: Y coordinate at which to perform the scroll
        delta_x:
          type: integer
          description: Horizontal scroll amount. Positive scrolls right, negative scrolls left.
          default: 0
        delta_y:
          type: integer
          description: Vertical scroll amount. Positive scrolls down, negative scrolls up.
          default: 0
        hold_keys:
          type: array
          description: Modifier keys to hold during the scroll
          items:
            type: string
      additionalProperties: false
    DragMouseRequest:
      type: object
      required:
        - path
      properties:
        path:
          type: array
          description: Ordered list of [x, y] coordinate pairs to move through while dragging. Must contain at least 2 points.
          minItems: 2
          items:
            type: array
            minItems: 2
            maxItems: 2
            items:
              type: integer
        button:
          type: string
          description: Mouse button to drag with
          enum: [left, middle, right]
        delay:
          type: integer
          description: Delay in milliseconds between button down and starting to move along the path.
          minimum: 0
          default: 0
        steps_per_segment:
          type: integer
          description: Number of relative move steps per segment in the path. Ignored when smooth=true. Minimum 1.
          minimum: 1
          default: 10
        step_delay_ms:
          type: integer
          description: Delay in milliseconds between relative steps while dragging. Ignored when smooth=true.
          minimum: 0
          default: 50
        smooth:
          type: boolean
          description: Use human-like Bezier curves between path waypoints instead of linear interpolation. When true, steps_per_segment and step_delay_ms are ignored.
          default: true
        duration_ms:
          type: integer
          description: Target total duration in milliseconds for the entire drag movement when smooth=true. Omit for automatic timing based on total path length.
          minimum: 50
          maximum: 10000
        hold_keys:
          type: array
          description: Modifier keys to hold during the drag
          items:
            type: string
      additionalProperties: false
    StartFsWatchRequest:
      type: object
      required:
        - path
      properties:
        path:
          type: string
          description: Directory to watch.
        recursive:
          type: boolean
          description: Whether to watch recursively.
          default: false
      additionalProperties: false
    FileSystemEvent:
      type: object
      description: Filesystem change event.
      required: [type, path]
      properties:
        type:
          type: string
          enum: [CREATE, WRITE, DELETE, RENAME]
          description: Event type.
        name:
          type: string
          description: Base name of the file or directory affected.
        path:
          type: string
          description: Absolute path of the file or directory.
        is_dir:
          type: boolean
          description: Whether the affected path is a directory.
    DeleteRecordingRequest:
      type: object
      properties:
        id:
          type: string
          description: Identifier of the recording session to delete, as passed to /recording/start. Alphanumeric or hyphen. When omitted, the default recording session is deleted.
          pattern: "^[a-zA-Z0-9-]+$"
      additionalProperties: false
    MarkRecordingRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
          description: Name of the marker, used as the MP4 chapter title.
          minLength: 1
          maxLength: 200
        id:
          type: string
          description: Identifier of the recording session to mark, as passed to /recording/start. Alphanumeric or hyphen. When omitted, the marker is added to the default recording session.
          pattern: "^[a-zA-Z0-9-]+$"
      additionalProperties: false
    MarkRecordingResult:
      type: object
      required: [name, offset_ms]
      properties:
        name:
          type: string
          description: Name of the recorded marker.
        offset_ms:
          type: integer
          format: int64
          description: >-
            Provisional offset of the marker from the recording start, in
            milliseconds, measured against the start time at mark time. The
            authoritative offset is the chapter start written at finalize.
      additionalProperties: false
    FileInfo:
      type: object
      required: [name, path, size_bytes, is_dir, mod_time, mode]
      properties:
        name:
          type: string
          description: Base name of the file or directory.
        path:
          type: string
          description: Absolute path.
        size_bytes:
          type: integer
          description: Size in bytes. 0 for directories.
        is_dir:
          type: boolean
          description: Whether the path is a directory.
        mod_time:
          type: string
          format: date-time
          description: Last modification time.
        mode:
          type: string
          description: File mode bits (e.g., "drwxr-xr-x" or "-rw-r--r--").

    ListFiles:
      type: array
      description: Array of file or directory information entries.
      items:
        $ref: "#/components/schemas/FileInfo"

    CreateDirectoryRequest:
      type: object
      required: [path]
      properties:
        path:
          type: string
          description: Absolute directory path to create.
          pattern: "^/.*"
        mode:
          type: string
          description: Optional directory mode (octal string, e.g. 755). Defaults to 755.
          pattern: "^[0-7]{3,4}$"
      additionalProperties: false

    DeletePathRequest:
      type: object
      required: [path]
      properties:
        path:
          type: string
          description: Absolute path to delete.
          pattern: "^/.*"
      additionalProperties: false

    SetFilePermissionsRequest:
      type: object
      required: [path, mode]
      properties:
        path:
          type: string
          description: Absolute path whose permissions are to be changed.
          pattern: "^/.*"
        owner:
          type: string
          description: New owner username or UID.
        group:
          type: string
          description: New group name or GID.
        mode:
          type: string
          description: File mode bits (octal string, e.g. 644).
          pattern: "^[0-7]{3,4}$"
      additionalProperties: false

    MovePathRequest:
      type: object
      required: [src_path, dest_path]
      properties:
        src_path:
          type: string
          description: Absolute source path.
          pattern: "^/.*"
        dest_path:
          type: string
          description: Absolute destination path.
          pattern: "^/.*"
      additionalProperties: false
    ProcessExecRequest:
      type: object
      description: Request to execute a command synchronously.
      required: [command]
      properties:
        command:
          type: string
          description: Executable or shell command to run.
        args:
          type: array
          description: Command arguments.
          items:
            type: string
          default: []
        cwd:
          type: [string, "null"]
          description: Working directory (absolute path) to run the command in.
          pattern: "^/.*"
        env:
          type: object
          description: Environment variables to set for the process.
          additionalProperties:
            type: string
          default: {}
        as_user:
          type: [string, "null"]
          description: Run the process as this user.
        as_root:
          type: boolean
          description: Run the process with root privileges.
          default: false
        timeout_sec:
          type: [integer, "null"]
          description: Maximum execution time in seconds.
      additionalProperties: false
    ProcessExecResult:
      type: object
      description: Result of a synchronous command execution.
      properties:
        exit_code:
          type: integer
          description: Process exit code.
        stdout_b64:
          type: string
          description: Base64-encoded stdout buffer.
        stderr_b64:
          type: string
          description: Base64-encoded stderr buffer.
        duration_ms:
          type: integer
          description: Execution duration in milliseconds.
      additionalProperties: false
    ProcessSpawnRequest:
      allOf:
        - $ref: "#/components/schemas/ProcessExecRequest"
        - type: object
          properties:
            allocate_tty:
              type: boolean
              description: Allocate a pseudo-terminal (PTY) for the process to enable interactive shells.
              default: false
            rows:
              type: integer
              description: Initial terminal rows when allocate_tty is true.
              minimum: 1
            cols:
              type: integer
              description: Initial terminal columns when allocate_tty is true.
              minimum: 1
    ProcessSpawnResult:
      type: object
      description: Information about a spawned process.
      properties:
        process_id:
          type: string
          format: uuid
          description: Server-assigned identifier for the process.
        pid:
          type: integer
          description: OS process ID.
        started_at:
          type: string
          format: date-time
          description: Timestamp when the process started.
      additionalProperties: false
    ProcessResizeRequest:
      type: object
      description: Resize a PTY-backed process.
      required: [rows, cols]
      properties:
        rows:
          type: integer
          minimum: 1
          description: New terminal rows.
        cols:
          type: integer
          minimum: 1
          description: New terminal columns.
      additionalProperties: false
    ProcessStatus:
      type: object
      description: Current status of a process.
      properties:
        state:
          type: string
          enum: [running, exited]
          description: Process state.
        exit_code:
          type: [integer, "null"]
          description: Exit code if the process has exited.
        cpu_pct:
          type: number
          description: Estimated CPU usage percentage.
        mem_bytes:
          type: integer
          description: Estimated resident memory usage in bytes.
      additionalProperties: false
    ProcessStdinRequest:
      type: object
      description: Data to write to the process standard input.
      required: [data_b64]
      properties:
        data_b64:
          type: string
          description: Base64-encoded data to write.
      additionalProperties: false
    ProcessStdinResult:
      type: object
      description: Result of writing to stdin.
      properties:
        written_bytes:
          type: integer
          description: Number of bytes written.
      additionalProperties: false
    ProcessKillRequest:
      type: object
      description: Signal to send to the process.
      required: [signal]
      properties:
        signal:
          type: string
          enum: [TERM, KILL, INT, HUP]
          default: TERM
          description: Signal to send.
      additionalProperties: false
    ProcessStreamEvent:
      type: object
      description: SSE payload representing process output or lifecycle events.
      properties:
        stream:
          type: string
          description: Source stream of the data chunk.
          enum: [stdout, stderr]
        data_b64:
          type: string
          description: Base64-encoded data from the process stream.
        event:
          type: string
          description: Lifecycle event type.
          enum: [exit]
        exit_code:
          type: integer
          description: Exit code when the event is "exit".
      additionalProperties: false
    PatchDisplayRequest:
      type: object
      properties:
        width:
          type: integer
          minimum: 320
          description: Display width in pixels
        height:
          type: integer
          minimum: 240
          description: Display height in pixels
        refresh_rate:
          type: integer
          enum: [60, 30, 25, 10]
          description: Display refresh rate in Hz. If omitted, uses the highest available rate for the resolution.
        require_idle:
          type: boolean
          description: If true, refuse to resize when live view or recording/replay is active.
          default: true
        restart_chromium:
          type: boolean
          description: If true, restart Chromium after resolution change to ensure it adapts to new size. Default is false for headful, true for headless.
      additionalProperties: false
    DisplayConfig:
      type: object
      properties:
        width:
          type: integer
          description: Current display width in pixels
        height:
          type: integer
          description: Current display height in pixels
        refresh_rate:
          type: integer
          description: Current display refresh rate in Hz (may be null if not detectable)
      additionalProperties: false
    LogEvent:
      type: object
      description: A log entry from the application.
      required: [message, timestamp]
      properties:
        timestamp:
          type: string
          format: date-time
          description: Time the log entry was produced.
        message:
          type: string
          description: Log message text.
    OkResponse:
      type: object
      description: Generic OK response.
      required: [ok]
      properties:
        ok:
          type: boolean
          description: Indicates success.
          default: true
      additionalProperties: false
    ExecutePlaywrightRequest:
      type: object
      description: Request to execute Playwright code
      required: [code]
      properties:
        code:
          type: string
          description: |
            TypeScript/JavaScript code to execute. The code has access to 'page', 'context', and 'browser' variables.
            Example: "await page.goto('https://example.com'); return await page.title();"
        timeout_sec:
          type: integer
          description: Maximum execution time in seconds. Default is 60.
          default: 60
          minimum: 1
          maximum: 300
      additionalProperties: false
    ExecutePlaywrightResult:
      type: object
      description: Result of Playwright code execution
      required: [success]
      properties:
        success:
          type: boolean
          description: Whether the code executed successfully
        result:
          description: The value returned by the code (if any)
        error:
          type: string
          description: Error message if execution failed
        stdout:
          type: string
          description: Standard output from the execution
        stderr:
          type: string
          description: Standard error from the execution
      additionalProperties: false
    BrowserReplRequest:
      type: object
      description: Request to execute code in the Browser REPL
      required: [code]
      properties:
        code:
          type: string
          description: |
            JavaScript evaluated in a persistent Node.js runtime.
            Top-level bindings persist until the API process exits, the REPL is
            reset, or the REPL is terminated after a crash or timeout. Persistent names
            are live context-global accessors: closures and timers observe later-cell
            assignments. Function declarations use the same accessor path, including
            same-cell closures and assignments. Braceless multi-declarator `var`
            statements retain their single-statement control-flow semantics. `var` in
            top-level nested statements persists; function and nested-block locals do
            not. Function `.name` is preserved; `Function.prototype.toString()` may
            expose the generated internal alias. Lexical names are reserved after
            linking, so retry a failed declaration with a new name or reset the REPL.
            A failed lexical initializer leaves that name in the TDZ; assignments
            cannot initialize it. Static top-level imports are rejected; use dynamic
            `import()`. Expression values are not returned automatically. Output is
            optional; code may produce no content, call `repl.write(...)` or
            `repl.emitImage(...)`, use console methods, or combine those mechanisms.
            May be empty only when reset is true. The HTTP body is limited to 8 MiB,
            and the API rejects a fully encoded daemon request over the daemon's 8 MiB
            request-line limit without terminating the REPL.
        timeout_sec:
          type: integer
          description: Maximum execution time in seconds. Default is 60.
          default: 60
          minimum: 1
          maximum: 300
        reset:
          type: boolean
          description: Terminate the current REPL, start a fresh one, and then evaluate code.
          default: false
      additionalProperties: false
    BrowserReplResult:
      type: object
      description: Result of Browser REPL code execution
      required: [success, repl_id]
      properties:
        success:
          type: boolean
          description: Whether the code executed successfully
        repl_id:
          type: string
          description: |
            CUID2 identifying the exact state-holding REPL process used for this
            execution. Stable across calls and Chromium reconnects; changes after
            an API restart, explicit reset, execution timeout, or REPL crash.
        error:
          type: string
          description: Error message if execution failed
        stack:
          type: string
          description: Stack trace if execution failed
        content:
          type: array
          description: Optional ordered text/image output produced by the execution; omitted or empty when no output was produced
          items:
            $ref: "#/components/schemas/BrowserReplContent"
        content_truncated:
          type: boolean
          description: True if text or image output was dropped or truncated due to response limits, including the 1,000-item cap on stray output buffered between executions
        repl_terminated:
          type: boolean
          description: |
            True if the REPL identified by repl_id was terminated by this request
            (timeout, protocol corruption, or a REPL crash/uncaught exception).
            The next request lazily starts a fresh REPL with a new repl_id.
        duration_ms:
          type: integer
          description: Wall-clock execution time in milliseconds
      additionalProperties: false
    BrowserReplContent:
      description: Ordered discriminated union of execution output items.
      oneOf:
        - $ref: "#/components/schemas/BrowserReplTextContent"
        - $ref: "#/components/schemas/BrowserReplImageContent"
      discriminator:
        propertyName: type
        mapping:
          text: "#/components/schemas/BrowserReplTextContent"
          image: "#/components/schemas/BrowserReplImageContent"
    BrowserReplTextContent:
      type: object
      required: [type, channel, text]
      properties:
        type:
          type: string
          enum: [text]
        channel:
          type: string
          description: >-
            write = repl.write, stdout = console.log/info/debug,
            stderr = console.warn/error
          enum: [write, stdout, stderr]
        text:
          type: string
      additionalProperties: false
    BrowserReplImageContent:
      type: object
      required: [type, mime_type, data_b64]
      properties:
        type:
          type: string
          enum: [image]
        mime_type:
          type: string
          pattern: "^image/"
        data_b64:
          type: string
          contentEncoding: base64
      additionalProperties: false
    SleepAction:
      type: object
      description: Pause execution for a specified duration.
      required: [duration_ms]
      properties:
        duration_ms:
          type: integer
          description: Duration to sleep in milliseconds.
          minimum: 0
          maximum: 30000
      additionalProperties: false
    ComputerAction:
      type: object
      description: |
        A single computer action to execute as part of a batch. The `type` field selects which
        action to perform, and the corresponding field contains the action parameters.
        Exactly one action field matching the type must be provided.
      required: [type]
      properties:
        type:
          type: string
          description: The type of action to perform.
          enum: [click_mouse, move_mouse, type_text, press_key, scroll, drag_mouse, set_cursor, sleep]
        click_mouse:
          $ref: "#/components/schemas/ClickMouseRequest"
        move_mouse:
          $ref: "#/components/schemas/MoveMouseRequest"
        type_text:
          $ref: "#/components/schemas/TypeTextRequest"
        press_key:
          $ref: "#/components/schemas/PressKeyRequest"
        scroll:
          $ref: "#/components/schemas/ScrollRequest"
        drag_mouse:
          $ref: "#/components/schemas/DragMouseRequest"
        set_cursor:
          $ref: "#/components/schemas/SetCursorRequest"
        sleep:
          $ref: "#/components/schemas/SleepAction"
      additionalProperties: false
    BatchComputerActionRequest:
      type: object
      description: A batch of computer actions to execute sequentially.
      required: [actions]
      properties:
        actions:
          type: array
          description: Ordered list of actions to execute. Execution stops on the first error.
          minItems: 1
          items:
            $ref: "#/components/schemas/ComputerAction"
      additionalProperties: false
  responses:
    BadRequestError:
      description: Bad Request
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    ConflictError:
      description: Conflict
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    NotFoundError:
      description: Not Found
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    InternalError:
      description: Internal Server Error
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
