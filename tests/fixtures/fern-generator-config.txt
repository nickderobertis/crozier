# Per-fixture non-default Fern generator settings, one row per fixture, read by
# scripts/generate-fern-fixture.sh. Not listed here: `pydantic_config.enum_type:
# python_enums`, which that script applies to EVERY fixture unconditionally (see
# tests/fixtures/AGENTS.md); a fixture cannot opt out of it from this table.
# fixture|audiences|audience_strict|client_class_name|extra_fields
audience-filter|public|false||
audience-filter-strict|public|true||
client-class-name||false|AcmeClient|
pydantic-extra-fields||false||ignore
