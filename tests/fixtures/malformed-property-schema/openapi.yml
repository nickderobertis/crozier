openapi: 3.1.0
info:
  title: fern
  version: 1.0.0
paths:
  /widgets/search:
    get:
      operationId: searchWidgets
      tags:
        - Widgets
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  widgets:
                    type: array
                    items:
                      type: string
                  required:
                    - widgets
