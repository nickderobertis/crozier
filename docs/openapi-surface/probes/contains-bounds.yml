openapi: 3.1.0
info:
  title: contains-bounds
  version: 1.0.0
servers:
  - url: https://probe.invalid
paths:
  /basket:
    get:
      operationId: getBasket
      responses:
        "200":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Basket"
components:
  schemas:
    Basket:
      title: Basket
      type: object
      properties:
        withContains:
          type: array
          contains:
            type: string
          maxContains: 3
          minContains: 1
        withoutContains:
          type: array
      required:
        - withContains
        - withoutContains
