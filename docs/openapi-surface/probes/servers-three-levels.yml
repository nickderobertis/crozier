openapi: 3.0.3
info:
  title: servers-three-levels
  version: 1.0.0
servers:
- url: https://root.invalid
paths:
  /probe:
    get:
      operationId: probe
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProbeResult'
      servers:
      - url: https://operation.invalid
    servers:
    - url: https://path.invalid
components:
  schemas:
    ProbeResult:
      type: object
      properties:
        ok:
          type: boolean
      required:
      - ok
