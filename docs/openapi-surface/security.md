# OpenAPI surface coverage — Security

Classified entries for the `security` region, against the boundary in
`## Scope` below.

**How this region's feature list was enumerated.** Not by reading the corpus and
writing down what it holds — that would classify only what is already covered.
The list comes off the specification, in five mechanical steps a reader can
repeat against [OAS 3.0.3](https://spec.openapis.org/oas/v3.0.3) and
[OAS 3.1.1](https://spec.openapis.org/oas/v3.1.1):

1. Take the objects this region owns from its `## Scope` row: Security Scheme,
   OAuth Flows, OAuth Flow, Security Requirement, and `components.securitySchemes`.
2. For each object, take **every row of its fixed-field table**, in both
   versions. The two tables are identical field for field, which is why no row
   here reads `3.0` and every field row reads `both`.
3. Where a field's value is drawn from a set the specification enumerates, take
   **one feature per member** — every member, with no catch-all, since a member no
   source declares is exactly what this document exists to surface — and diff
   *those* lists across the two versions; that diff is the region's only `3.1`
   row. `type` has 5 members (3.0 names four and 3.1 adds `mutualTLS`) and `in`
   has 3, both closed in the specification's own text. `scheme` is the open one:
   the specification defers to the [IANA HTTP Authentication Scheme
   Registry](https://www.iana.org/assignments/http-authschemes/http-authschemes.xhtml),
   so the member list is fetched from the registry's own machine-readable copy —
   `curl -sS https://www.iana.org/assignments/http-authschemes/authschemes.csv` —
   which names **14** schemes today: `basic`, `bearer`, `concealed`, `digest`,
   `dpop`, `gnap`, `hoba`, `mutual`, `negotiate`, `oauth`, `privatetoken`,
   `scram-sha-1`, `scram-sha-256`, `vapid`. Re-fetching the CSV is how a future
   reader picks up a scheme the registry has added since.
4. The Security Requirement Object has **no fixed fields** — its keys are scheme
   names — so its features come from the value shapes its own prose defines (a
   scheme with scopes, a scheme with an empty scope list, the empty requirement
   `{}` that makes authentication optional, an empty `security: []`, several
   requirements as alternatives, several schemes in one requirement as a
   conjunction) crossed with the **two positions** the specification allows the
   list to appear in (the OpenAPI Object's `security`, the Operation Object's
   `security`). Six shapes × two positions = twelve features. The `{}` row is the
   one whose two positions land in different categories, which is exactly why the
   cross is taken rather than assumed.
5. Drop what another region owns, cross-linking instead of duplicating — see
   *Region boundaries* under `## Method notes`.

Steps 2–4 are what makes this a walk rather than a survey: ten of the fourteen
IANA schemes, an OAuth Flows Object declaring two flows, an operation-level `{}`
and a `$ref`-valued security scheme are all features no corpus reading would have
produced, because nothing in the corpus declares them. Thirteen of the fifty rows
below are that kind of row.

## Scope

Security Scheme, OAuth Flows, OAuth Flow and Security Requirement objects, and
`components.securitySchemes`.

## Entries

| key | oas | spec location | category | evidence | crozier sites | why bytes could move | settlement |
|---|---|---|---|---|---|---|---|
| `securityscheme-type-apikey` | both | `Security Scheme Object.type` = `apiKey` | golden | census `securityScheme.type=apiKey`: 30 golden-bearing sources, 45 sites — `amazonaws.com-cloudformation`, `amazonaws.com-cloudfront`, `apideck.com-accounting`, `apideck.com-ats`, `apideck.com-connector`, `apideck.com-crm`(3), `apideck.com-customer-support`, `apideck.com-ecommerce`, `apideck.com-file-storage`, `apideck.com-hris`, `apideck.com-issue-tracking`, `apideck.com-lead`(3), `apideck.com-proxy`(2), `apideck.com-sms`, `apideck.com-vault`(2), `apideck.com-webhook`, `appwrite.io-client`(3), `appwrite.io-server`(4), `auth-schemes`, `buildrelay`, `bungie.net`, `byautomata.io`, `exa-gate`, `netbox.dev`, `openepcis-dpp-ready`(3), `openfigi.com`, `portfoliooptimizer.io`, `slurmdb-rest`(2), `squareup.com`, `tlon-notes`(2) |  |  |  |
| `securityscheme-type-http` | both | `Security Scheme Object.type` = `http` | golden | census `securityScheme.type=http`: 35 golden-bearing sources, 42 sites — `airbyte.local-config`, `apache.org`(2), `apache.org-airflow`(2), `auth-schemes`(2), `basic-auth`, `client-class-name`, `cookie-parameters`, `discriminated-unions`, `error-responses`, `exa-gate`, `exhaustive`, `form-bodies`, `gambitcomm.local-mimic`, `http-toolkit`(2), `inline-array-request`, `inline-request-response`, `integer-enums`, `khoainats`, `letta`, `livepeer-ai-runner`, `maif.local-otoroshi`, `openepcis-dpp-ready`(2), `pydantic-extra-fields`, `redhat.com-catalog_inventory`, `redocly.com-museum`, `sac-backend`, `schema-constraints`, `servers-webhooks`, `sse-streaming`, `tamoss`(2), `traccar.org`, `twilio.com-twilio_messaging_v1`, `twilio.com-twilio_voice_v1`, `worldcoin-signup-sequencer`(2), `writeonly-fields` |  |  |  |
| `securityscheme-type-oauth2` | both | `Security Scheme Object.type` = `oauth2` | golden | census `securityScheme.type=oauth2`: 10 golden-bearing sources, 11 sites — `auth-schemes`, `bungie.net`, `free5gc-namf-communication`, `free5gc-pdu-session`, `microcks.local`, `oauth-client-credentials`, `openbanking.org.uk-account-info-openapi`(2), `reverb.com`, `squareup.com`, `xero.com-xero-payroll-au` |  |  |  |
| `securityscheme-type-openidconnect` | both | `Security Scheme Object.type` = `openIdConnect` | golden | census `securityScheme.type=openIdConnect`: 4 golden-bearing sources, 4 sites — `apache.org`, `apache.org-airflow`, `khoainats`, `openepcis-dpp-ready` |  |  |  |
| `mutualTLS` | 3.1 | `Security Scheme Object.type` = `mutualTLS` | golden | census `securityScheme.type=mutualTLS`: 1 golden-bearing source, 1 site — `openbankingproject-ch-kundenbeziehung`, [`CORPUS.md`](../../tests/fixtures/CORPUS.md) row 98, whose `mTLS` scheme declares it beside a supported `bearer` one; measured on the 129-source walk this change pins ([`document-paths.md`](document-paths.md#snapshot-reconciliation)) rather than the 124-source walk the rest of this file's counts rest on. Its Fern 5.20.0 golden byte-matches with no exclusions, so what it pins is that crozier drops the scheme in exactly the places Fern does; `fern-limitations.md` key `mutualTLS`, verdict `discards + supply` |  |  |  |
| `securityscheme-description` | both | `Security Scheme Object.description` | golden | census `securityScheme.description`: 32 golden-bearing sources, 51 sites — `amazonaws.com-cloudformation`, `amazonaws.com-cloudfront`, `apideck.com-accounting`, `apideck.com-ats`, `apideck.com-connector`, `apideck.com-crm`(3), `apideck.com-customer-support`, `apideck.com-ecommerce`, `apideck.com-file-storage`, `apideck.com-hris`, `apideck.com-issue-tracking`, `apideck.com-lead`(3), `apideck.com-proxy`(2), `apideck.com-sms`, `apideck.com-vault`(2), `apideck.com-webhook`, `appwrite.io-client`(3), `appwrite.io-server`(4), `buildrelay`, `bungie.net`(2), `byautomata.io`, `microcks.local`, `openbanking.org.uk-account-info-openapi`(2), `openepcis-dpp-ready`(3), `portfoliooptimizer.io`, `sac-backend`, `slurmdb-rest`(2), `tamoss`(2), `tlon-notes`(2), `traccar.org`, `worldcoin-signup-sequencer`(2), `xero.com-xero-payroll-au` |  |  |  |
| `securityscheme-name` | both | `Security Scheme Object.name` (the `apiKey` parameter name) | golden | census `securityScheme.name`: 30 golden-bearing sources, 45 sites — `amazonaws.com-cloudformation`, `amazonaws.com-cloudfront`, `apideck.com-accounting`, `apideck.com-ats`, `apideck.com-connector`, `apideck.com-crm`(3), `apideck.com-customer-support`, `apideck.com-ecommerce`, `apideck.com-file-storage`, `apideck.com-hris`, `apideck.com-issue-tracking`, `apideck.com-lead`(3), `apideck.com-proxy`(2), `apideck.com-sms`, `apideck.com-vault`(2), `apideck.com-webhook`, `appwrite.io-client`(3), `appwrite.io-server`(4), `auth-schemes`, `buildrelay`, `bungie.net`, `byautomata.io`, `exa-gate`, `netbox.dev`, `openepcis-dpp-ready`(3), `openfigi.com`, `portfoliooptimizer.io`, `slurmdb-rest`(2), `squareup.com`, `tlon-notes`(2) |  |  |  |
| `apikey-header` | both | `Security Scheme Object.in` = `header` | golden | census `securityScheme.in=header`: 30 golden-bearing sources, 44 sites — `amazonaws.com-cloudformation`, `amazonaws.com-cloudfront`, `apideck.com-accounting`, `apideck.com-ats`, `apideck.com-connector`, `apideck.com-crm`(3), `apideck.com-customer-support`, `apideck.com-ecommerce`, `apideck.com-file-storage`, `apideck.com-hris`, `apideck.com-issue-tracking`, `apideck.com-lead`(3), `apideck.com-proxy`(2), `apideck.com-sms`, `apideck.com-vault`(2), `apideck.com-webhook`, `appwrite.io-client`(3), `appwrite.io-server`(4), `auth-schemes`, `buildrelay`, `bungie.net`, `byautomata.io`, `exa-gate`, `netbox.dev`, `openepcis-dpp-ready`(3), `openfigi.com`, `portfoliooptimizer.io`, `slurmdb-rest`(2), `squareup.com`, `tlon-notes`. This is the ledger's unnamed *control* row, so it joins no key |  |  |  |
| `apiKey-query` | both | `Security Scheme Object.in` = `query` | limitations | `fern-limitations.md` key `apiKey-query`, verdict `discards + supply`; census `securityScheme.in=query`: 0 golden-bearing sources. Its three registered sources — `bbci.co.uk`, `esgenterprise.com`, `etherpad.local` — are all `CORPUS.md` **DROPPED** rows with no golden |  |  |  |
| `apiKey-cookie` | both | `Security Scheme Object.in` = `cookie` | golden | census `securityScheme.in=cookie`: 1 golden-bearing sources, 1 sites — `tlon-notes`; `fern-limitations.md` key `apiKey-cookie`, verdict `discards`. `tlon-notes` pairs `eyreCookie` with a header `apiKey`, which is the ledger's *"pair one with a supported scheme"* case, so the golden pins the discard rather than the scheme |  |  |  |
| `http-basic` | both | `Security Scheme Object.scheme` = `basic` (RFC 7617) | golden | census `securityScheme.scheme=basic`: 15 golden-bearing sources, 15 sites — `apache.org`, `apache.org-airflow`, `auth-schemes`, `basic-auth`, `gambitcomm.local-mimic`, `http-toolkit`, `maif.local-otoroshi`, `openepcis-dpp-ready`, `redhat.com-catalog_inventory`, `redocly.com-museum`, `tamoss`, `traccar.org`, `twilio.com-twilio_messaging_v1`, `twilio.com-twilio_voice_v1`, `worldcoin-signup-sequencer` |  |  |  |
| `http-bearer` | both | `Security Scheme Object.scheme` = `bearer` (RFC 6750) | golden | census `securityScheme.scheme=bearer`: 25 golden-bearing sources, 25 sites — `airbyte.local-config`, `auth-schemes`, `client-class-name`, `cookie-parameters`, `discriminated-unions`, `error-responses`, `exa-gate`, `exhaustive`, `form-bodies`, `http-toolkit`, `inline-array-request`, `inline-request-response`, `integer-enums`, `khoainats`, `letta`, `livepeer-ai-runner`, `openepcis-dpp-ready`, `pydantic-extra-fields`, `sac-backend`, `schema-constraints`, `servers-webhooks`, `sse-streaming`, `tamoss`, `worldcoin-signup-sequencer`, `writeonly-fields` |  |  |  |
| `http-digest` | both | `Security Scheme Object.scheme` = `digest` (RFC 7616) | limitations | `fern-limitations.md` key `http-digest`, verdict `discards + licence`; census `securityScheme.scheme=digest` and `securityScheme.scheme=Digest` both report *(declared by no registered source)* over all 124 registered sources |  |  |  |
| `http-negotiate` | both | `Security Scheme Object.scheme` = `negotiate` (RFC 4559) | golden | census `securityScheme.scheme=negotiate`: 2 golden-bearing sources, 2 sites — `apache.org`, `apache.org-airflow` |  |  |  |
| `http-concealed` | both | `Security Scheme Object.scheme` = `concealed` (RFC 9729) | gap | census `securityScheme.scheme=concealed` and `securityScheme.scheme=Concealed` both report *(declared by no registered source)* over all 129 registered sources, re-measured on the 129-source walk this change pins ([`document-paths.md`](document-paths.md#snapshot-reconciliation)) rather than the 124-source walk the rest of this file's counts rest on, both spellings asked for because the field is case-insensitive. No `fern-limitations.md` row names it — that file's only `http` `scheme` row is `http-digest`. The world-wide search for a real-world document declaring it is the `http-concealed` row of [Witness search (issue #188)](#witness-search-issue-188) below, whose outcome is `none-found`: no candidate was verified as declaring it. | `src/openapi.rs` — 1 (`HttpAuthScheme`'s `#[serde(other)]` fallback, `src/openapi.rs:219`, collapses every scheme that is not `bearer` or `basic` into `Other`) and `src/ir.rs` — 1 (the `matches!(scheme.scheme, Some(Bearer \| Basic))` predicate in `auth_model`, `src/ir.rs:370`, which never selects it) | With `concealed` unselectable, `auth_model` falls through to `Auth::Bearer { required: false }`, so `client.py` takes an optional `token`, `core/client_wrapper.py` sends `Authorization: Bearer` and `reference.md` documents that signature — all of it wrong if Fern refuses the document or emits a different credential | `PROBE` on **witness-supply** grounds rather than structural ones — **and not on the four-dropped-schemes finding.** That finding no longer holds a row of this family open: `http-dpop` is now `golden` on three paired real-world witnesses whose byte-matching goldens pin exactly where the unsupported scheme is dropped, so a paired document is both registrable and evidence-bearing. What holds this row is that no document declaring it was found at all. The witness-search row names all six sources with the exact query put to each and its result: **APIs.guru** `grep -rilE '"?scheme"?[[:space:]]*:[[:space:]]*"?concealed"?[[:space:],]*$' APIs/` over 4,138 documents → 0 files; **GitHub** `gh search code concealed --filename openapi.yaml` (→ 30), `--filename openapi.json` (→ 20) and `--filename swagger.yaml` (→ 3), all 53 fetched and grepped → 0; **Sourcegraph** `content:"scheme: concealed"` and its JSON spelling → 0; **SwaggerHub** `GET /specs?query=concealed&limit=20` → 163 results, the 20 returned documents fetched → 0; **Postman** `queryText: "concealed"` over `adp.api` → 0; **vendor portals** Cloudflare's two published schemas, Okta, Ory Hydra and Ory Kratos → 0 each. Until a witness appears, only a locally authored probe declaring `concealed` alone and then beside a supported scheme, carried through `fern check` and `fern generate`, settles it in [`../fern-limitations.md`](../fern-limitations.md) |
| `http-dpop` | both | `Security Scheme Object.scheme` = `dpop` (RFC 9449) | golden | census `securityScheme.scheme=dpop`: 1 golden-bearing source, 1 site — `blackadi-oauth2`, [`CORPUS.md`](../../tests/fixtures/CORPUS.md) row 96, whose `dpopAuth` scheme declares it beside a `bearer` and a `basic` one; `securityScheme.scheme=DPoP`: 2 golden-bearing sources, 2 sites — `mosip-esignet` (1), row 97, and `openbankingproject-ch-kundenbeziehung` (1), row 98. Both spellings are asked for because the field is case-insensitive, and the corpus now declares each of them. Both re-measured on the 129-source walk this change pins ([`document-paths.md`](document-paths.md#snapshot-reconciliation)) rather than the 124-source walk the rest of this file's counts rest on. No `fern-limitations.md` row names it. **What the goldens pin, and what they do not.** Fern accepts all three documents at their pinned refs (`fern check` exit 0, `All checks passed`), imports a `bearer` scheme from each, and emits nothing anywhere in any of the three SDKs derived from the DPoP scheme — and all three goldens byte-match with no exclusions, so what is pinned is that crozier drops the scheme in exactly the places Fern does, across `client.py`, `core/client_wrapper.py` and `reference.md`, in the lowercase spelling and in the registry's mixed-case one alike. What it does not pin is what Fern does with a document declaring `dpop` **alone**: the one such document the search found, `zerkerlabs/treeship`, is refused by `fern check` with ten `Endpoint requires auth, but no auth is defined` errors — the ledger's four-dropped-schemes finding reproduced on a real document, and still a probe |  |  |  |
| `http-gnap` | both | `Security Scheme Object.scheme` = `gnap` (RFC 9635) | gap | census `securityScheme.scheme=gnap` and `securityScheme.scheme=GNAP` both report *(declared by no registered source)* over all 129 registered sources, re-measured on the 129-source walk this change pins ([`document-paths.md`](document-paths.md#snapshot-reconciliation)) rather than the 124-source walk the rest of this file's counts rest on, both spellings asked for because the field is case-insensitive. No `fern-limitations.md` row names it — that file's only `http` `scheme` row is `http-digest`. The world-wide search for a real-world document declaring it is the `http-gnap` row of [Witness search (issue #188)](#witness-search-issue-188) below, whose outcome is `none-found`: its one candidate, Interledger Open Payments, names a scheme `GNAP` but declares it `type: apiKey` with `in: header`, which is not this field. | `src/openapi.rs` — 1 (`HttpAuthScheme`'s `#[serde(other)]` fallback, `src/openapi.rs:219`, collapses every scheme that is not `bearer` or `basic` into `Other`) and `src/ir.rs` — 1 (the `matches!(scheme.scheme, Some(Bearer \| Basic))` predicate in `auth_model`, `src/ir.rs:370`, which never selects it) | With `gnap` unselectable, `auth_model` falls through to `Auth::Bearer { required: false }`, so `client.py` takes an optional `token`, `core/client_wrapper.py` sends `Authorization: Bearer` and `reference.md` documents that signature — all of it wrong if Fern refuses the document or emits a different credential | `PROBE` on **witness-supply** grounds rather than structural ones — **and not on the four-dropped-schemes finding.** That finding no longer holds a row of this family open: `http-dpop` is now `golden` on three paired real-world witnesses whose byte-matching goldens pin exactly where the unsupported scheme is dropped, so a paired document is both registrable and evidence-bearing. What holds this row is that no document declaring it was found at all. The witness-search row names all six sources with the exact query put to each and its result: **APIs.guru** `grep -rilE '"?scheme"?[[:space:]]*:[[:space:]]*"?gnap"?[[:space:],]*$' APIs/` over 4,138 documents → 0 files; **GitHub** `gh search code gnap --filename openapi.yaml` (→ 30), `--filename openapi.json` (→ 30) and `--filename swagger.yaml` (→ 12), all 72 fetched and grepped → 0; **Sourcegraph** `content:"scheme: gnap"` and its JSON spelling → 0; **SwaggerHub** `GET /specs?query=gnap&limit=20` → 736 results, the 20 returned documents fetched → 0; **Postman** `queryText: "gnap"` over `adp.api` → 0; **vendor portals** GNAP's one deployed implementation, Interledger Open Payments, declares the name as an `apiKey` scheme rather than as this field, and Ory Hydra and Ory Kratos → 0. Until a witness appears, only a locally authored probe declaring `gnap` alone and then beside a supported scheme, carried through `fern check` and `fern generate`, settles it in [`../fern-limitations.md`](../fern-limitations.md) |
| `http-hoba` | both | `Security Scheme Object.scheme` = `hoba` (RFC 7486) | gap | census `securityScheme.scheme=hoba` and `securityScheme.scheme=HOBA` both report *(declared by no registered source)* over all 129 registered sources, re-measured on the 129-source walk this change pins ([`document-paths.md`](document-paths.md#snapshot-reconciliation)) rather than the 124-source walk the rest of this file's counts rest on, both spellings asked for because the field is case-insensitive. No `fern-limitations.md` row names it — that file's only `http` `scheme` row is `http-digest`. The world-wide search for a real-world document declaring it is the `http-hoba` row of [Witness search (issue #188)](#witness-search-issue-188) below, whose outcome is `witness-blocked`: the SwaggerHub `Auth Test` document does declare `scheme_7` with `type: http` and `scheme: hoba`, so a real-world declaration exists. | `src/openapi.rs` — 1 (`HttpAuthScheme`'s `#[serde(other)]` fallback, `src/openapi.rs:219`, collapses every scheme that is not `bearer` or `basic` into `Other`) and `src/ir.rs` — 1 (the `matches!(scheme.scheme, Some(Bearer \| Basic))` predicate in `auth_model`, `src/ir.rs:370`, which never selects it) | With `hoba` unselectable, `auth_model` falls through to `Auth::Bearer { required: false }`, so `client.py` takes an optional `token`, `core/client_wrapper.py` sends `Authorization: Bearer` and `reference.md` documents that signature — all of it wrong if Fern refuses the document or emits a different credential | `FIXTURE` — **a witness exists and is blocked by this corpus's own registration rules rather than by Fern, and a second search for a *usable* one found no other declarer in any document body it could read.** The second pass recorded on the [`http-hoba` witness-search line](#witness-search-issue-188) read **both** complete corpora whole — 0 of APIs.guru's 4,138 documents and 0 of `jentic/jentic-public-apis`' 74,240 — fetched **1,291** GitHub documents of which **1,240** parsed (51 unparseable), **142** SwaggerHub documents and four identity-vendor portals (Okta, Ory Hydra, Ory Kratos, Keycloak), and found no declaration in any of those bodies but the one already recorded. **One candidate is unresolved rather than negative:** Postman's `adp.api` index returns one API for `hoba` — `hoba-backend` by `warped-meteor-692351`, which the index itself labels `openapi:3` — and its document body is credential-gated (`GET https://api.getpostman.com/apis/<id>` → **HTTP 401**), so whether it declares the scheme is unread rather than known to be no. That document, SwaggerHub `Auth Test` 1.0.0, is not redistributable (it declares no `info.license` and SwaggerHub publishes no licence property), has no immutable ref (its `1.0.0` version reference is editable in place by its owner), and its URL does not end `.json`, `.yaml` or `.yml` as [`../fern-goldens.md`](../fern-goldens.md) requires of a direct spec URL — so `fern check` was never run on it and Fern has refused nothing here. Settling it still takes a redistributable copy of that one document at a pinnable ref, and because the same document carries all three of `http-hoba`, `http-scram-sha-1` and `http-scram-sha-256`, unblocking it settles three rows at once |
| `http-mutual` | both | `Security Scheme Object.scheme` = `mutual` (RFC 8120) | golden | census `securityScheme.scheme=mutual`: 2 registered sources and **1 golden-bearing** — `cyberark-conjur-api` (1), [`CORPUS.md`](../../tests/fixtures/CORPUS.md) row 99, whose `conjurKubernetesMutualTls` scheme declares it beside a `basic` scheme and an `apiKey` one, and `conjur.local` (1), still the **DROPPED** row it always was; `securityScheme.scheme=Mutual` reports *(declared by no registered source)*, both spellings asked for because the field is case-insensitive. Both re-measured on the 130-source walk this change pins ([`document-paths.md`](document-paths.md#snapshot-reconciliation)) rather than the 124-source walk the rest of this file's counts rest on. No `fern-limitations.md` row names it — its `mutualTLS` row is the `type` of that name, not this `scheme`. **What the golden pins, and what it does not.** Fern accepts row 99's document at its pinned ref (`fern check` exit 0, `All checks passed`, nothing on stderr but the CLI's upgrade banner), imports the `basic` scheme as the client's `username`/`password` pair and the `apiKey` one as an `Authorization` constructor field, and emits nothing anywhere in the SDK derived from the `mutual` scheme — and the golden byte-matches with no exclusions, so what is pinned is that crozier drops the scheme in exactly the places Fern does, across `client.py`, `core/client_wrapper.py` and `reference.md`. What it does not pin is what Fern does with a document declaring `mutual` **alone**: no such document was found, and row 99 pairs the scheme as `conjur.local` did. That earlier witness's **DROPPED** verdict was re-examined for this change at the generator its own `CORPUS.md` provenance records — CLI `5.67.1` over `fernapi/fern-python-sdk` `5.20.0` — and still holds: `fern check` exits 0 printing `All checks passed` over `[api]: Failed to parse openapi document Conjur` and `[api]: Failed to resolve #/paths/~1authn~1%7Baccount%7D~1%7Blogin%7D~1authenticate/post/responses/200`, a `CliError` out of `resolveResponseReference`. Row 99 is a different document at a different source and ref, and Fern parses it |  |  |  |
| `http-oauth` | both | `Security Scheme Object.scheme` = `oauth` (RFC 5849) | gap | census `securityScheme.scheme=oauth` and `securityScheme.scheme=OAuth` both report *(declared by no registered source)* over all 129 registered sources, re-measured on the 129-source walk this change pins ([`document-paths.md`](document-paths.md#snapshot-reconciliation)) rather than the 124-source walk the rest of this file's counts rest on, both spellings asked for because the field is case-insensitive. No `fern-limitations.md` row names it — that file's only `http` `scheme` row is `http-digest`. The world-wide search for a real-world document declaring it is the `http-oauth` row of [Witness search (issue #188)](#witness-search-issue-188) below, whose outcome is `fern-rejected`: the Noun Project API 2.0.0 bundle in `jentic/jentic-public-apis` declares `OAuth1` with `type: http` and `scheme: oauth` as its only security scheme — read out of the document fetched at commit `9d36c7e3808ebe65d69e81d3c3250598927a575c`, which also shows it carries no `info.license` of its own and takes the repository's CC0-1.0 — and `fern check` refuses it at **exit 1**, re-run here at CLI `5.67.1` over `fernapi/fern-python-sdk` `5.20.0` and recorded in [`../../tests/fixtures/AGENTS.md`](../../tests/fixtures/AGENTS.md#specs-already-tried-and-rejected-do-not-re-attempt-without-a-fix-upstream). | `src/openapi.rs` — 1 (`HttpAuthScheme`'s `#[serde(other)]` fallback, `src/openapi.rs:219`, collapses every scheme that is not `bearer` or `basic` into `Other`) and `src/ir.rs` — 1 (the `matches!(scheme.scheme, Some(Bearer \| Basic))` predicate in `auth_model`, `src/ir.rs:370`, which never selects it) | With `oauth` unselectable, `auth_model` falls through to `Auth::Bearer { required: false }`, so `client.py` takes an optional `token`, `core/client_wrapper.py` sends `Authorization: Bearer` and `reference.md` documents that signature — all of it wrong if Fern refuses the document or emits a different credential | `FIXTURE` — **Fern refuses the one redistributable witness, and the second search found no readable document that would avoid the refusal.** The refusal is a property of the candidate rather than of the shape: the Noun Project bundle declares `scheme: oauth` **alone**, so Fern's importer drops its only scheme and leaves the service with no auth. What a usable second candidate needs is therefore exact — `scheme: oauth` *paired* with a scheme Fern supports, under an Apache-2.0/MIT/BSD/CC0 licence, at an immutable ref — and the [`http-oauth` witness-search line](#witness-search-issue-188) records that no such document exists in either complete corpus (2 declarations in APIs.guru's 4,138 and 15 across 3 APIs in `jentic/jentic-public-apis`' 74,240, every one of them the HERE or X document or the recorded witness), in the **1,240** of **1,291** fetched GitHub documents that parsed (51 unparseable), in **142** SwaggerHub documents, or in the OAuth 1.0a-era vendor portals (Trello, Bitbucket, Xero) — every one of which is a body this search read. **Sixteen candidates are unresolved rather than negative:** Postman's `adp.api` index returns 16 APIs for `oauth` and serves none of their document bodies without a credential (`GET https://api.getpostman.com/apis/<id>` → **HTTP 401**), so none of the sixteen is verified to declare a paired `scheme: oauth` and none is verified not to. The one document the second pass added, `ads-api.twitter.com/main/12`, is a third copy of the X document already blocked on the *X Developer Agreement and Policy*. Settling it takes that paired document |
| `http-privatetoken` | both | `Security Scheme Object.scheme` = `privatetoken` (RFC 9577) | gap | census `securityScheme.scheme=privatetoken` and `securityScheme.scheme=PrivateToken` both report *(declared by no registered source)* over all 129 registered sources, re-measured on the 129-source walk this change pins ([`document-paths.md`](document-paths.md#snapshot-reconciliation)) rather than the 124-source walk the rest of this file's counts rest on, both spellings asked for because the field is case-insensitive. No `fern-limitations.md` row names it — that file's only `http` `scheme` row is `http-digest`. The world-wide search for a real-world document declaring it is the `http-privatetoken` row of [Witness search (issue #188)](#witness-search-issue-188) below, whose outcome is `none-found`: no candidate was verified as declaring it. | `src/openapi.rs` — 1 (`HttpAuthScheme`'s `#[serde(other)]` fallback, `src/openapi.rs:219`, collapses every scheme that is not `bearer` or `basic` into `Other`) and `src/ir.rs` — 1 (the `matches!(scheme.scheme, Some(Bearer \| Basic))` predicate in `auth_model`, `src/ir.rs:370`, which never selects it) | With `privatetoken` unselectable, `auth_model` falls through to `Auth::Bearer { required: false }`, so `client.py` takes an optional `token`, `core/client_wrapper.py` sends `Authorization: Bearer` and `reference.md` documents that signature — all of it wrong if Fern refuses the document or emits a different credential | `PROBE` on **witness-supply** grounds rather than structural ones — **and not on the four-dropped-schemes finding.** That finding no longer holds a row of this family open: `http-dpop` is `golden` on three paired real-world witnesses whose byte-matching goldens pin exactly where the unsupported scheme is dropped, so a paired document is both registrable and evidence-bearing. What holds this row is that no document declaring it was found at all. The witness-search row names all six sources with the exact query put to each and its result: **APIs.guru** `grep -rilE '"?scheme"?[[:space:]]*:[[:space:]]*"?privatetoken"?[[:space:],]*$' APIs/` over 4,138 documents → 0 files; **GitHub** `gh search code privatetoken --filename openapi.yaml` (→ 8), `--filename openapi.json` (→ 2) and `--filename swagger.yaml` (→ 0), all 10 fetched and grepped → 0; **Sourcegraph** `content:"scheme: privatetoken"` and its JSON spelling → 0; **SwaggerHub** `GET /specs?query=privatetoken&limit=20` → 2,885 results, the 20 returned documents fetched → 0; **Postman** `queryText: "privatetoken"` over `adp.api` → 0; **vendor portals** the two issuers RFC 9577 names — Cloudflare's published `openapi.json` and `openapi.yaml` schemas → 0 each, and Apple, which publishes no OpenAPI document for Private Access Tokens at all. Until a witness appears, only a locally authored probe declaring `privatetoken` alone and then beside a supported scheme, carried through `fern check` and `fern generate`, settles it in [`../fern-limitations.md`](../fern-limitations.md) |
| `http-scram-sha-1` | both | `Security Scheme Object.scheme` = `scram-sha-1` (RFC 7804) | gap | census `securityScheme.scheme=scram-sha-1` and `securityScheme.scheme=SCRAM-SHA-1` both report *(declared by no registered source)* over all 129 registered sources, re-measured on the 129-source walk this change pins ([`document-paths.md`](document-paths.md#snapshot-reconciliation)) rather than the 124-source walk the rest of this file's counts rest on, both spellings asked for because the field is case-insensitive. No `fern-limitations.md` row names it — that file's only `http` `scheme` row is `http-digest`. The world-wide search for a real-world document declaring it is the `http-scram-sha-1` row of [Witness search (issue #188)](#witness-search-issue-188) below, whose outcome is `witness-blocked`: the SwaggerHub `Auth Test` document declares `scheme_11` with `type: http` and `scheme: scram-sha-1` — read out of the document fetched at that reference — among thirteen schemes that also cover `basic` and `bearer`, so a real-world **paired** declaration exists. It is the same document `http-scram-sha-256` and `http-hoba` are settled on. | `src/openapi.rs` — 1 (`HttpAuthScheme`'s `#[serde(other)]` fallback, `src/openapi.rs:219`, collapses every scheme that is not `bearer` or `basic` into `Other`) and `src/ir.rs` — 1 (the `matches!(scheme.scheme, Some(Bearer \| Basic))` predicate in `auth_model`, `src/ir.rs:370`, which never selects it) | With `scram-sha-1` unselectable, `auth_model` falls through to `Auth::Bearer { required: false }`, so `client.py` takes an optional `token`, `core/client_wrapper.py` sends `Authorization: Bearer` and `reference.md` documents that signature — all of it wrong if Fern refuses the document or emits a different credential | `FIXTURE` — **a witness exists, blocked by this corpus's registration rules rather than by Fern, and the second search found no other declarer in any document body it could read.** The [`http-scram-sha-1` witness-search line](#witness-search-issue-188) records this change re-reading both complete corpora whole — 0 of APIs.guru's 4,138 and 0 of `jentic/jentic-public-apis`' 74,240 — parsing **1,240** of the **1,291** GitHub documents it fetched (51 unparseable) (every `scram-sha-*` match among those 1,240 is a Kafka or MongoDB SASL mechanism enum value or a prose mention, not a Security Scheme Object `scheme`), **142** SwaggerHub documents and four database-vendor portals, and finding only the same SwaggerHub `Auth Test` document that carries `http-hoba`. **Two candidates are unresolved rather than negative:** Postman's `adp.api` index returns two APIs for `scram-sha-1`, both workspace-share links whose document bodies are credential-gated (`GET https://api.getpostman.com/apis/<id>` → **HTTP 401**), so whether either declares the scheme is unread rather than known to be no. It declares no `info.license`, has no immutable ref, and its URL does not end `.json`, `.yaml` or `.yml` — so `fern check` was never run on it and Fern has refused nothing here. Settling it still takes a redistributable copy of that one document at a pinnable ref, and because the same document carries all three of `http-hoba`, `http-scram-sha-1` and `http-scram-sha-256`, unblocking it settles three rows at once |
| `http-scram-sha-256` | both | `Security Scheme Object.scheme` = `scram-sha-256` (RFC 7804) | gap | census `securityScheme.scheme=scram-sha-256` and `securityScheme.scheme=SCRAM-SHA-256` both report *(declared by no registered source)* over all 129 registered sources, re-measured on the 129-source walk this change pins ([`document-paths.md`](document-paths.md#snapshot-reconciliation)) rather than the 124-source walk the rest of this file's counts rest on, both spellings asked for because the field is case-insensitive. No `fern-limitations.md` row names it — that file's only `http` `scheme` row is `http-digest`. The world-wide search for a real-world document declaring it is the `http-scram-sha-256` row of [Witness search (issue #188)](#witness-search-issue-188) below, whose outcome is `witness-blocked`: the SwaggerHub `Auth Test` document declares `scheme_12` with `type: http` and `scheme: scram-sha-256` — read out of the document fetched at that reference — among thirteen schemes that also cover `basic` and `bearer`, so a real-world **paired** declaration exists. It is the same document `http-scram-sha-1` and `http-hoba` are settled on. | `src/openapi.rs` — 1 (`HttpAuthScheme`'s `#[serde(other)]` fallback, `src/openapi.rs:219`, collapses every scheme that is not `bearer` or `basic` into `Other`) and `src/ir.rs` — 1 (the `matches!(scheme.scheme, Some(Bearer \| Basic))` predicate in `auth_model`, `src/ir.rs:370`, which never selects it) | With `scram-sha-256` unselectable, `auth_model` falls through to `Auth::Bearer { required: false }`, so `client.py` takes an optional `token`, `core/client_wrapper.py` sends `Authorization: Bearer` and `reference.md` documents that signature — all of it wrong if Fern refuses the document or emits a different credential | `FIXTURE` — **the same one document, the same two blockers, and the same empty second search.** The [`http-scram-sha-256` witness-search line](#witness-search-issue-188) records both complete corpora read whole (0 of 4,138 and 0 of 74,240), **1,240** of the **1,291** GitHub documents it fetched parsed (51 unparseable) — including the eight that one query's first attempt was refused for and a retry returned, all eight copies of one `clearblade.com` document whose `SCRAM-SHA-256` sits in a Markdown table of database authentication methods inside a description rather than in a Security Scheme Object `scheme` — **142** SwaggerHub documents and the database-vendor portals, all at zero. The only declaration any of those readable bodies holds is `scheme_12` of the SwaggerHub `Auth Test` document — and unlike its two neighbours this row has no unread candidate behind it, Postman returning **0** APIs for `scram-sha-256`. That document which declares no `info.license`, has no immutable ref, and whose URL does not end `.json`, `.yaml` or `.yml` — so `fern check` was never run on it and Fern has refused nothing here. Settling it still takes a redistributable copy of that one document at a pinnable ref, and because the same document carries all three of `http-hoba`, `http-scram-sha-1` and `http-scram-sha-256`, unblocking it settles three rows at once |
| `http-vapid` | both | `Security Scheme Object.scheme` = `vapid` (RFC 8292) | gap | census `securityScheme.scheme=vapid` and `securityScheme.scheme=VAPID` both report *(declared by no registered source)* over all 130 registered sources, re-measured on the 130-source walk this change pins ([`document-paths.md`](document-paths.md#snapshot-reconciliation)) rather than the 124-source walk the rest of this file's counts rest on, both spellings asked for because the field is case-insensitive. No `fern-limitations.md` row names it — that file's only `http` `scheme` row is `http-digest`. The world-wide search for a real-world document declaring it is the `http-vapid` row of [Witness search (issue #188)](#witness-search-issue-188) below, whose outcome is `none-found`: no candidate was verified as declaring it. | `src/openapi.rs` — 1 (`HttpAuthScheme`'s `#[serde(other)]` fallback, `src/openapi.rs:219`, collapses every scheme that is not `bearer` or `basic` into `Other`) and `src/ir.rs` — 1 (the `matches!(scheme.scheme, Some(Bearer \| Basic))` predicate in `auth_model`, `src/ir.rs:370`, which never selects it) | With `vapid` unselectable, `auth_model` falls through to `Auth::Bearer { required: false }`, so `client.py` takes an optional `token`, `core/client_wrapper.py` sends `Authorization: Bearer` and `reference.md` documents that signature — all of it wrong if Fern refuses the document or emits a different credential | `PROBE` on **witness-supply** grounds rather than structural ones — **and not on the four-dropped-schemes finding.** That finding no longer holds a row of this family open: `http-dpop` is `golden` on three paired real-world witnesses and `http-mutual` on a fourth, whose byte-matching goldens pin exactly where the unsupported scheme is dropped, so a paired document is both registrable and evidence-bearing. What holds this row is that no document declaring it was found at all. The witness-search row names all six sources with the exact query put to each and its result: **APIs.guru** `grep -rilE '"?scheme"?[[:space:]]*:[[:space:]]*"?vapid"?[[:space:],]*$' APIs/` over 4,138 documents → 0 files; **GitHub** `gh search code vapid --filename openapi.yaml` (→ 30), `--filename openapi.json` (→ 30) and `--filename swagger.yaml` (→ 22), all 82 fetched and grepped → 0; **Sourcegraph** `content:"scheme: vapid"` and its JSON spelling → 0; **SwaggerHub** `GET /specs?query=vapid&limit=20` → 117 results, the 20 returned documents fetched → 0; **Postman** `queryText: "vapid"` over `adp.api` → 0; **vendor portals** the Web Push domain RFC 8292 implies — `gotify/server` `docs/spec.json` → 0, `binwiederhier/ntfy` publishing no OpenAPI or Swagger document at all, and `mozilla-services/autopush-rs` `docs/openapi.yaml` and `pushpad/pushpad-api` `openapi.yaml` both 404. Until a witness appears, only a locally authored probe declaring `vapid` alone and then beside a supported scheme, carried through `fern check` and `fern generate`, settles it in [`../fern-limitations.md`](../fern-limitations.md) |
| `securityscheme-bearerformat` | both | `Security Scheme Object.bearerFormat` | golden | census `securityScheme.bearerFormat`: 5 golden-bearing sources, 5 sites — `airbyte.local-config`, `openepcis-dpp-ready`, `sac-backend`, `tamoss`, `worldcoin-signup-sequencer` |  |  |  |
| `securityscheme-openidconnecturl` | both | `Security Scheme Object.openIdConnectUrl` | golden | census `securityScheme.openIdConnectUrl`: 4 golden-bearing sources, 4 sites — `apache.org`, `apache.org-airflow`, `khoainats`, `openepcis-dpp-ready` |  |  |  |
| `securityscheme-flows` | both | `Security Scheme Object.flows` (the OAuth Flows Object) | golden | census `securityScheme.flows`: 10 golden-bearing sources, 11 sites — `auth-schemes`, `bungie.net`, `free5gc-namf-communication`, `free5gc-pdu-session`, `microcks.local`, `oauth-client-credentials`, `openbanking.org.uk-account-info-openapi`(2), `reverb.com`, `squareup.com`, `xero.com-xero-payroll-au` |  |  |  |
| `oauth2-implicit` | both | `OAuth Flows Object.implicit` | limitations | `fern-limitations.md` key `oauth2-implicit`, verdict `supply` (a qualifier-only cell — see *Two verdict cells* below); census `securityScheme.flows.implicit` reports *(declared by no registered source)* over all 124 registered sources |  |  |  |
| `oauth2-password` | both | `OAuth Flows Object.password` | limitations | `fern-limitations.md` key `oauth2-password`, verdict `supply` (a qualifier-only cell — see *Two verdict cells* below); census `securityScheme.flows.password` reports *(declared by no registered source)* over all 124 registered sources |  |  |  |
| `oauth2-clientcredentials` | both | `OAuth Flows Object.clientCredentials` | golden | census `securityScheme.flows.clientCredentials`: 7 golden-bearing sources, 7 sites — `auth-schemes`, `free5gc-namf-communication`, `free5gc-pdu-session`, `microcks.local`, `oauth-client-credentials`, `openbanking.org.uk-account-info-openapi`, `reverb.com` |  |  |  |
| `oauth2-authorizationcode` | both | `OAuth Flows Object.authorizationCode` | golden | census `securityScheme.flows.authorizationCode`: 4 golden-bearing sources, 4 sites — `bungie.net`, `openbanking.org.uk-account-info-openapi`, `squareup.com`, `xero.com-xero-payroll-au` |  |  |  |
| `oauth2-multiple-flows` | both | `OAuth Flows Object` declaring more than one of its four flows | gap | shape read `oauthFlows:multiple-flows`: 0 of the 107 golden-bearing sources declare it — each of the 11 declared OAuth Flows Objects names exactly one flow (`clientCredentials` 7, `authorizationCode` 4) — and 0 of all 124 registered sources. No `fern-limitations.md` row names it: `oauth2-implicit` and `oauth2-password` are about a flow standing alone, not about two standing together | `src/ir.rs` — 1 place: the `authorization_code.or(client_credentials).or(implicit).or(password)` chain in `oauth_scope_enum` (`src/ir.rs:418`) | `src/<pkg>/types/oauth_scope.py`'s `OauthScope` members are read from whichever flow that chain reaches first, so a Fern that prefers a different flow emits a different member set, and `types/__init__.py` and `reference.md` move with it | `FIXTURE` — **the search found a witness that clears all three screens, so what is left is registration.** The [`oauth2-multiple-flows` witness-search line](#witness-search-issue-188) records the first search this row has had: **9** declarations across APIs.guru's 4,138 documents and **265** across 250 files of `jentic/jentic-public-apis`' 74,240, and among them `OpenBanking-Brasil/specs-directory` `openapi.yaml` at commit `37423cbc93982e05eac255db6226ed599a5baace` — MIT in its own `info.license`, at an immutable ref, and accepted by Fern at **both** stages (`fern check` exit 0; a real generate at `fernapi/fern-python-sdk` 5.20.0 exit 0, 323 files). Its one `oauth2` scheme declares `clientCredentials` and `authorizationCode` with **disjoint** scope sets, which is exactly the *"the flows must differ"* this cell used to ask for: a golden over it says which flow `oauth_scope_enum`'s `.or(…)` chain reached. `discord.com`'s bundle in the same corpus is a second accepted witness that also declares `security-optional-requirement-operation`, so one corpus row could settle both rows. Register one of them and the byte-compare settles the precedence |
| `oauthflow-authorizationurl` | both | `OAuth Flow Object.authorizationUrl` | golden | census `securityScheme.flows.authorizationCode.authorizationUrl`: 4 golden-bearing sources, 4 sites — `bungie.net`, `openbanking.org.uk-account-info-openapi`, `squareup.com`, `xero.com-xero-payroll-au`. The field's other position, `flows.implicit`, is `oauth2-implicit`'s |  |  |  |
| `oauthflow-tokenurl` | both | `OAuth Flow Object.tokenUrl` | golden | census `securityScheme.flows.authorizationCode.tokenUrl` + `securityScheme.flows.clientCredentials.tokenUrl`: 10 distinct golden-bearing sources, 11 sites — `auth-schemes`, `bungie.net`, `free5gc-namf-communication`, `free5gc-pdu-session`, `microcks.local`, `oauth-client-credentials`, `openbanking.org.uk-account-info-openapi`(2), `reverb.com`, `squareup.com`, `xero.com-xero-payroll-au` |  |  |  |
| `oauthflow-refreshurl` | both | `OAuth Flow Object.refreshUrl` | golden | census `securityScheme.flows.clientCredentials.refreshUrl`: 1 golden-bearing sources, 1 sites — `microcks.local`. `…authorizationCode.refreshUrl` is declared once, by `asana.com`, which is **DROPPED** |  |  |  |
| `oauthflow-scopes` | both | `OAuth Flow Object.scopes` | golden | census `securityScheme.flows.authorizationCode.scopes` + `securityScheme.flows.clientCredentials.scopes`: 10 distinct golden-bearing sources, 11 sites — `auth-schemes`, `bungie.net`, `free5gc-namf-communication`, `free5gc-pdu-session`, `microcks.local`, `oauth-client-credentials`, `openbanking.org.uk-account-info-openapi`(2), `reverb.com`, `squareup.com`, `xero.com-xero-payroll-au`. Both value shapes are covered — 9 sites carry a non-empty map, and 2 (`auth-schemes`, `oauth-client-credentials`) carry the empty map the specification allows |  |  |  |
| `security-requirement-scopes-document` | both | `OpenAPI Object.security` → `Security Requirement Object`, scheme → non-empty scope list | golden | shape read `document:with-scopes`: 2 golden-bearing sources, 2 sites — `free5gc-namf-communication`, `free5gc-pdu-session` |  |  |  |
| `security-requirement-scopes-operation` | both | `Operation Object.security` → `Security Requirement Object`, scheme → non-empty scope list | golden | shape read `operation:with-scopes`: 7 golden-bearing sources, 451 sites — `bungie.net`(57), `khoainats`, `microcks.local`(32), `openbanking.org.uk-account-info-openapi`(29), `reverb.com`(108), `squareup.com`(195), `xero.com-xero-payroll-au`(29) |  |  |  |
| `security-requirement-noscopes-document` | both | `OpenAPI Object.security` → `Security Requirement Object`, scheme → `[]` | golden | shape read `document:noscopes`: 32 golden-bearing sources, 47 sites — `amazonaws.com-cloudformation`, `amazonaws.com-cloudfront`, `apideck.com-accounting`, `apideck.com-ats`, `apideck.com-connector`, `apideck.com-crm`(3), `apideck.com-customer-support`, `apideck.com-ecommerce`, `apideck.com-file-storage`, `apideck.com-hris`, `apideck.com-issue-tracking`, `apideck.com-lead`(3), `apideck.com-proxy`(2), `apideck.com-sms`, `apideck.com-vault`(2), `apideck.com-webhook`, `buildrelay`, `exa-gate`(2), `gambitcomm.local-mimic`, `letta`, `microcks.local`, `netbox.dev`, `openepcis-dpp-ready`(6), `openfigi.com`, `portfoliooptimizer.io`, `redhat.com-catalog_inventory`, `redocly.com-museum`, `sac-backend`, `slurmdb-rest`(2), `tamoss`(2), `tlon-notes`(2), `traccar.org` |  |  |  |
| `security-requirement-noscopes-operation` | both | `Operation Object.security` → `Security Requirement Object`, scheme → `[]` | golden | shape read `operation:noscopes`: 43 golden-bearing sources, 873 sites — `apideck.com-accounting`(53), `apideck.com-ats`(5), `apideck.com-connector`(8), `apideck.com-crm`(40), `apideck.com-customer-support`(5), `apideck.com-ecommerce`(7), `apideck.com-file-storage`(32), `apideck.com-hris`(27), `apideck.com-issue-tracking`(15), `apideck.com-lead`(5), `apideck.com-proxy`(6), `apideck.com-sms`(5), `apideck.com-vault`(17), `apideck.com-webhook`(9), `appwrite.io-client`(117), `appwrite.io-server`(229), `auth-schemes`(4), `basic-auth`, `client-class-name`(2), `cookie-parameters`, `discriminated-unions`, `error-responses`(2), `exa-gate`, `exhaustive`(54), `form-bodies`(2), `http-toolkit`(4), `inline-array-request`, `inline-request-response`(2), `integer-enums`, `khoainats`, `livepeer-ai-runner`(10), `maif.local-otoroshi`(101), `oauth-client-credentials`, `pydantic-extra-fields`(2), `sac-backend`(13), `schema-constraints`, `servers-webhooks`, `squareup.com`(4), `sse-streaming`, `twilio.com-twilio_messaging_v1`(45), `twilio.com-twilio_voice_v1`(32), `worldcoin-signup-sequencer`(4), `writeonly-fields` |  |  |  |
| `security-optional-requirement-document` | both | `OpenAPI Object.security` containing `{}` (authentication optional) | golden | shape read `document:optional-empty-object`: 5 golden-bearing sources, 5 sites — `airbyte.local-config`, `free5gc-namf-communication`, `free5gc-pdu-session`, `openepcis-dpp-ready`, `openfigi.com` |  |  |  |
| `security-optional-requirement-operation` | both | `Operation Object.security` containing `{}` (authentication optional for that operation) | gap | shape read `operation:optional-empty-object`: 0 of the 107 golden-bearing sources declare it. The one registered source that does, `atlassian.com-jira` (221 sites), is a `CORPUS.md` **DROPPED** row with no golden, so nothing pins it. No `fern-limitations.md` row names it — `operation-security-alternatives` is about several requirements, not about the empty one | `src/ir.rs` — 3 places: `all_operations_authenticated` (`src/ir.rs:627`), `body_response_same_ref` (`src/ir.rs:2544`) and `operation_uses_basic_auth` (`src/ir.rs:2569`), each folding `op.security` over `doc.security` and testing `!r.is_empty()` | An operation-level `{}` makes that one operation unauthenticated, which flips `all_operations_authenticated` and so decides whether the credential in `client.py`/`core/client_wrapper.py` (and its signature in `reference.md`) is required or `typing.Optional[...] = None` for the **whole** SDK | `FIXTURE` — **the search found three witnesses that clear all three screens, and settled the registered declarer that could not.** The [`security-optional-requirement-operation` witness-search line](#witness-search-issue-188) records the first search this row has had: **83** declarations across APIs.guru's 4,138 documents and **4,518** across 183 files of `jentic/jentic-public-apis`' 74,240. Three of those clear licence, ref and Fern together, each at commit `eb9d12a2684b0fbcb5aecf51e8ae54dba0929743`: `braintrust.dev` (Apache 2.0, **148** declarations against a single Fern-supported `bearer` scheme, check exit 0 and a real 5.20.0 generate exit 0), `discord.com` (MIT, **22**, which also declares `oauth2-multiple-flows`) and `api.openverse.org` (MIT, **6**). The registered declarer `atlassian.com-jira` — [`CORPUS.md`](../../tests/fixtures/CORPUS.md) row 27, 221 declarations — was screened on all three and fails exactly one: its licence is Apache 2.0 and it is reachable at the versioned APIs.guru reference the manifest pins, but `fern check` exits **1** and a real 5.20.0 generate exits **1** on three errors (an unnameable `~` enum member, a duplicate `ProjectFeatureState`, and a colliding `id`), none of them about security. Register one of the three and the byte-compare settles whether Fern reads `{}` as opting the operation out |
| `security-empty-list-document` | both | `OpenAPI Object.security` = `[]` | golden | shape read `document:empty-list`: 5 golden-bearing sources, 5 sites — `apache.org`, `apache.org-airflow`, `apideck.com-ecosystem`, `apis.guru`, `prometheus-x-edge-computing` |  |  |  |
| `security-empty-list-operation` | both | `Operation Object.security` = `[]` (opting out of the document default) | golden | shape read `operation:empty-list`: 5 golden-bearing sources, 10 sites — `airbyte.local-config`, `apideck.com-vault`(3), `exa-gate`(3), `sac-backend`(2), `squareup.com` |  |  |  |
| `security-alternatives-document` | both | `OpenAPI Object.security` holding several Security Requirement Objects (alternatives) | golden | shape read `document:alternatives`: 7 golden-bearing sources, 7 sites — `exa-gate`, `free5gc-namf-communication`, `free5gc-pdu-session`, `openepcis-dpp-ready`, `openfigi.com`, `tamoss`, `tlon-notes` |  |  |  |
| `operation-security-alternatives` | both | `Operation Object.security` holding several Security Requirement Objects (alternatives) | golden | shape read `operation:alternatives`: 2 golden-bearing sources, 3 sites — `khoainats`, `worldcoin-signup-sequencer`(2); `fern-limitations.md` key `operation-security-alternatives`, verdict `ignores + supply` |  |  |  |
| `security-conjunction-document` | both | `OpenAPI Object.security` → one Security Requirement Object naming several schemes (conjunction) | golden | shape read `document:conjunction`: 6 golden-bearing sources, 6 sites — `apideck.com-crm`, `apideck.com-lead`, `apideck.com-proxy`, `apideck.com-vault`, `openepcis-dpp-ready`, `slurmdb-rest` |  |  |  |
| `security-conjunction-operation` | both | `Operation Object.security` → one Security Requirement Object naming several schemes (conjunction) | golden | shape read `operation:conjunction`: 2 golden-bearing sources, 151 sites — `appwrite.io-client`(56), `appwrite.io-server`(95) |  |  |  |
| `components-securityschemes` | both | `Components Object.securitySchemes` | golden | census `components.securitySchemes`: 69 golden-bearing sources, 69 sites — `airbyte.local-config`, `amazonaws.com-cloudformation`, `amazonaws.com-cloudfront`, `apache.org`, `apache.org-airflow`, `apideck.com-accounting`, `apideck.com-ats`, `apideck.com-connector`, `apideck.com-crm`, `apideck.com-customer-support`, `apideck.com-ecommerce`, `apideck.com-file-storage`, `apideck.com-hris`, `apideck.com-issue-tracking`, `apideck.com-lead`, `apideck.com-proxy`, `apideck.com-sms`, `apideck.com-vault`, `apideck.com-webhook`, `appwrite.io-client`, `appwrite.io-server`, `auth-schemes`, `basic-auth`, `buildrelay`, `bungie.net`, `byautomata.io`, `client-class-name`, `cookie-parameters`, `discriminated-unions`, `error-responses`, `exa-gate`, `exhaustive`, `form-bodies`, `free5gc-namf-communication`, `free5gc-pdu-session`, `gambitcomm.local-mimic`, `http-toolkit`, `inline-array-request`, `inline-request-response`, `integer-enums`, `khoainats`, `letta`, `livepeer-ai-runner`, `maif.local-otoroshi`, `microcks.local`, `netbox.dev`, `oauth-client-credentials`, `openbanking.org.uk-account-info-openapi`, `openepcis-dpp-ready`, `openfigi.com`, `portfoliooptimizer.io`, `pydantic-extra-fields`, `redhat.com-catalog_inventory`, `redocly.com-museum`, `reverb.com`, `sac-backend`, `schema-constraints`, `servers-webhooks`, `slurmdb-rest`, `squareup.com`, `sse-streaming`, `tamoss`, `tlon-notes`, `traccar.org`, `twilio.com-twilio_messaging_v1`, `twilio.com-twilio_voice_v1`, `worldcoin-signup-sequencer`, `writeonly-fields`, `xero.com-xero-payroll-au` |  |  |  |
| `securityscheme-ref` | both | `Components Object.securitySchemes` holding a Reference Object instead of a Security Scheme Object | gap | shape read `securityScheme:$ref`: 0 of the 113 golden-bearing sources declare it, and 0 of all 130 registered sources, re-measured on the 130-source walk this change pins ([`document-paths.md`](document-paths.md#snapshot-reconciliation)). No `fern-limitations.md` row names it — `pathitem-ref` and `relative-file-ref` are the Reference features that file rules on, and both are [`document-paths.md`](document-paths.md)'s. The world-wide search for a real-world document declaring it is the `securityscheme-ref` row of [Witness search (issue #188)](#witness-search-issue-188) below, whose outcome is `fern-rejected`: `Open-EO/openeo-api` `extensions/workspaces/openapi.yaml` declares `components.securitySchemes.Bearer` as `$ref: '../../openapi.yaml#/components/securitySchemes/Bearer'` — read out of the document fetched at commit `1881dae18b3c2c417f1305774cf295c81d60d400`, which also carries `info.license` `Apache 2.0` — and Fern never parses it, recorded in [`../../tests/fixtures/AGENTS.md`](../../tests/fixtures/AGENTS.md#specs-already-tried-and-rejected-do-not-re-attempt-without-a-fix-upstream). | `none` — `SecurityScheme` (`src/openapi.rs:142`) declares no `$ref` field and `src/refs.rs` resolves references for `components.schemas` only, so the node deserializes to the default scheme and its `type` becomes `SecuritySchemeType::Other` | With no scheme recognised, `auth_model` falls through to `Auth::Bearer { required: false }`, so `client.py` takes an optional `token` and `core/client_wrapper.py` sends `Authorization: Bearer` — where a Fern that resolves the reference would emit the referenced scheme's credential (an `api_key` and its own header, say) | `FIXTURE` — **Fern never parses any witness, and the second search found no candidate that would let it.** The refusal is a property of the candidates rather than of the shape: every document found `$ref`s **across** documents, which `resolveSecuritySchemeReference` will not follow, so a usable candidate would have to keep its reference inside its own document. The [`securityscheme-ref` witness-search line](#witness-search-issue-188) records that no such document exists in either complete corpus — **0** of APIs.guru's 4,138 and **0** of `jentic/jentic-public-apis`' 74,240, the sharpest zero any row of this region has — nor among the **1,240** of the **1,291** GitHub documents it fetched parsed (51 unparseable), whose two hits are one more cross-document `$ref` (`technology-platforms/vcfa-api-docs`) and the OpenAPI 3.1 meta-schema. Both new candidates reproduce the same false success, now measured on the *generate* as well: exit **0** having written 35 files, a client with no endpoints and no types, where a document Fern did parse yields hundreds. Licence and pinning block none of them, which is why this is a fixture gap rather than a probe |

## Method notes

### What was measured, and with what

Two instruments, and the second is the first one imported rather than a new one.

**The census, for every feature the selector grammar can name.** Run in full once,
and re-run per selector for the absences the `gap` and `limitations` rows cite:

```
just surface-census --json                                    # 407 selectors, 603390 sites, 124 sources
just surface-census --selector securityScheme.type=mutualTLS \
                    --selector securityScheme.scheme=digest \
                    --selector securityScheme.flows.implicit \
                    --selector securityScheme.flows.password
```

That second invocation prints each of the four as `(declared by no registered
source)`, which is the evidence those rows cite — the instrument reports an
absence as an absence rather than as nothing.

The fourteen IANA `scheme` rows are asked the same way, in **both spellings**: an
`auth-scheme` is case-insensitive per RFC 7235, while the census records the
literal string a document wrote, so `Digest` and `digest` are two selectors and
only asking for both proves an absence.

```sh
just surface-census $(for n in Concealed DPoP GNAP HOBA Mutual OAuth PrivateToken \
                               SCRAM-SHA-1 SCRAM-SHA-256 vapid; do
  printf ' --selector securityScheme.scheme=%s --selector securityScheme.scheme=%s' \
         "$n" "$(printf %s "$n" | tr A-Z a-z)"
done)
```

Every one of those twenty printed `(declared by no registered source)` on the
124-source walk except `securityScheme.scheme=mutual`, which printed
`conjur.local 1` — a DROPPED row, so `http-mutual` was a `gap` all the same.
**Three of the twenty have since moved**: on the 130-source walk
[`document-paths.md`](document-paths.md#snapshot-reconciliation) now pins,
`securityScheme.scheme=dpop` prints `blackadi-oauth2 1`,
`securityScheme.scheme=DPoP` prints `mosip-esignet 1` and
`openbankingproject-ch-kundenbeziehung 1`, and `securityScheme.scheme=mutual`
prints `cyberark-conjur-api 1` beside that DROPPED `conjur.local 1` — corpus rows
96, 97, 98 and 99, the witnesses the `http-dpop` and `http-mutual` entries are
`golden` on. So the field takes six values across the registered sources —
`basic`, `bearer`, `dpop`, `DPoP`, `mutual`, `negotiate`, the DPoP scheme in both
its spellings — and eight of the fourteen registry names are `gap` rows rather
than ten. Each carries its own site,
artifact and settlement cells rather than sharing a catch-all — a catch-all would
have read `golden` off `negotiate` alone and hidden eight unmeasured schemes
behind it. Every other count in this file is the 124-source walk's, which is what
each cell says; refreshing them whole is a separate measurement.

**The census imported, for the shapes the grammar cannot name.** A Security
Requirement Object's keys are *scheme names*, so the grammar stops at
`openapi.security` / `operation.security` and emits no selector for what the list
holds: `[]` and `[{}]` and `[{a: []}, {b: []}]` are one selector with one count.
Twelve of this file's rows are about exactly that difference, and two more
(`securityscheme-ref`, `oauth2-multiple-flows`) turn on a shape the grammar
flattens the same way. All fourteen are measured by importing the census script's
own loader and registered-source list and reading the parsed object model — never
by opening a fixture. The whole read is repeatable
from the repository root:

```sh
"$(./scripts/census-python.sh)" - <<'PY'
import collections, importlib.util, sys
from pathlib import Path
root = Path.cwd()
spec = importlib.util.spec_from_file_location("census", root / "scripts/openapi-surface-census.py")
census = importlib.util.module_from_spec(spec); sys.modules["census"] = census
spec.loader.exec_module(census)
fx = root / "tests/fixtures"
aliases = census.corpus_aliases(fx)
sources = census.registered_sources(fx, root / ".local/corpus", False)
golden = {s.fixture for s in sources if (fx / aliases.get(s.fixture, s.fixture) / "expected").is_dir()}
hits = collections.defaultdict(collections.Counter)
METHODS = {"get", "put", "post", "delete", "options", "head", "patch", "trace"}
FLOWS = ("implicit", "password", "clientCredentials", "authorizationCode")

def requirements(node, where, name):
    if not isinstance(node, list):
        return
    if not node:
        hits[where + ":empty-list"][name] += 1
        return
    if len(node) > 1:
        hits[where + ":alternatives"][name] += 1
    for req in node:
        if not isinstance(req, dict):
            continue
        if not req:
            hits[where + ":optional-empty-object"][name] += 1
            continue
        if len(req) > 1:
            hits[where + ":conjunction"][name] += 1
        for scopes in req.values():
            hits[where + (":with-scopes" if scopes else ":noscopes")][name] += 1

for source in sources:
    if source.path is None or source.fixture not in golden:
        continue
    doc = census.load_document(source.path)
    requirements(doc.get("security"), "document", source.fixture)
    for item in (doc.get("paths") or {}).values():
        if not isinstance(item, dict):
            continue
        for method, op in item.items():
            if method in METHODS and isinstance(op, dict):
                requirements(op.get("security"), "operation", source.fixture)
    for scheme in ((doc.get("components") or {}).get("securitySchemes") or {}).values():
        if not isinstance(scheme, dict):
            continue
        if "$ref" in scheme:
            hits["securityScheme:$ref"][source.fixture] += 1
        flows = scheme.get("flows")
        if isinstance(flows, dict) and len([f for f in FLOWS if f in flows]) > 1:
            hits["oauthFlows:multiple-flows"][source.fixture] += 1
KEYS = [f"{where}:{shape}"
        for where in ("document", "operation")
        for shape in ("with-scopes", "noscopes", "optional-empty-object",
                      "empty-list", "alternatives", "conjunction")]
KEYS += ["securityScheme:$ref", "oauthFlows:multiple-flows"]
for key in KEYS:  # a fixed list, so a shape no source declares prints as 0 rather than vanishing
    counts = hits[key]
    print(f"{key:34} sources={len(counts):3} sites={sum(counts.values()):5}")
PY
```

### A registered source is not always a golden

The census's registered sources are **not** all goldens, and rule 1 of the
[category rules](../openapi-surface-coverage.md#the-category-rules) is about *a
registered golden fixture's own source document*. Seventeen registered sources
carry no committed `expected/` tree — sixteen are `CORPUS.md` **DROPPED** rows
(Fern refused or failed on them) and `calorieninjas.com` is the accepted-exception
row `AGENTS.md` names. So every count in this file is filtered to the
**golden-bearing sources**, `tests/fixtures/<dir>/expected` being the test, with
`corpus-aliases.tsv` mapping manifest names to fixture directories. That was 107
of 124 on the walk most of this file's counts rest on, and is 113 of 130 on the
one this change pins.

The filter is not cosmetic: it moves rows. `apiKey-query` is declared by three
registered sources and by no golden — all three are DROPPED — so it classifies
`limitations`, matching the ledger instead of contradicting it. `http-mutual`
was the filter's other worked example until this change: its only witness was
`conjur.local`, DROPPED because Fern falsely returned success on it, so the
scheme was a `gap` while its registry neighbour `http-negotiate` was `golden` on
`apache.org` and `apache.org-airflow`. It is `golden` now — not because the
filter loosened, but because corpus row 99 supplies a second witness that does
carry a committed golden, which is exactly what the filter was asking for.
`oauthflow-refreshurl` loses `asana.com` and rests on `microcks.local`. Read
unfiltered, two rows change category outright — `apiKey-query` would claim golden
coverage no golden provides, and `security-optional-requirement-operation` would
stop being a gap on the strength of `atlassian.com-jira` — and several more would
cite a source whose Fern output does not exist.

### Two verdict cells that are qualifiers, not verdicts

`oauth2-implicit` and `oauth2-password` carry `supply` in the verdict column of
[`fern-limitations.md`](../fern-limitations.md#what-round-3-did-not-register-and-why)'s
gap table — a *qualifier* under that file's own [*How to read a
verdict*](../fern-limitations.md#how-to-read-a-verdict), which lists `supply`
beside `licence` and `pipeline` as things joined to a verdict rather than as one.
Both rows quote the ledger's cell verbatim rather than inventing the verdict it
omits. What the ledger does measure for both flows is in its auth probe table:
Fern emits `token` → `Authorization: Bearer`, required when every operation is
secured. That is an `implements`-shaped result the gap table's supply cell does
not carry, and only a probe recorded in that file can promote it.

### Region boundaries

Five surfaces touch this region and none is classified here:

- **`OpenAPI Object.security` and `Operation Object.security` as *fields***, i.e.
  whether a document declares them at all, belong to
  [`document-paths.md`](document-paths.md), which owns the OpenAPI and Operation
  objects. This region owns what the *list* holds — the Security Requirement
  Object and its value shapes — which is why its twelve requirement rows all name
  a shape and none of them names the bare field. The census measures the fields as
  `openapi.security` (48 sources) and `operation.security` (58 sources, 1748
  sites) over all 124 registered sources; those two numbers are that region's to
  classify, filter included.
- **`x-` extensions on a Security Scheme Object** belong to
  [`oas31-extensions.md`](oas31-extensions.md). The census finds two:
  `securityScheme.x-amazon-apigateway-authtype` and `securityScheme.x-appwrite`.
- **The Reference Object itself** (`$ref`, `summary`, `description`) is
  [`document-paths.md`](document-paths.md)'s. `securityscheme-ref` above is not
  that object — it is the Security Scheme *position* holding one, which only this
  region can classify.
- **`Security Scheme Object.in`** reuses the Parameter Object's `in` vocabulary
  but is its own field on its own object; `parameter.in` is
  [`parameters.md`](parameters.md)'s.
- **No JSON Schema keyword appears in this region.** OAuth `scopes` is a
  `string → string` map, not a schema, so nothing here belongs to
  [`schemas.md`](schemas.md).

### Deliberate exclusions

Enumeration step 3 takes a feature per member of a value set the specification
enumerates; it does **not** take a feature per *cardinality of a free-keyed map*.
So "a document declaring several security schemes" (24 registered sources do) and
"a document declaring two `oauth2` schemes" (`openbanking.org.uk-account-info-openapi`
does) are not rows here: the keys of `components.securitySchemes` are names, which
is the same rule that keeps them out of the census. `oauth2-multiple-flows` is a
row despite looking similar because the OAuth Flows Object's four flows are
**fixed fields**, not map keys, so declaring two of them is a shape of the object
rather than a count of names.

### One instrument artifact, found while measuring

The census reports `securityRequirement.x-api-key`, 1 site, in
`calorieninjas.com`. It is not an extension: that document names its security
scheme `x-api-key`, and a Security Requirement Object's keys are scheme names. The
walk tests a key for the `x-` prefix before it tests whether the enclosing object
keys its map by name, so a scheme whose name starts with `x-` is recorded as a
vendor extension of the requirement. No row in this file rests on it — the
requirement rows are measured by the shape read above, not by that selector — and
the source is a golden-less accepted-exception row either way. Fixing it belongs
to `scripts/openapi-surface-census.py`, not to this file.

### What this region could not settle

`etherpad.local` and `appng-rest-api` each declare a **sole** `apiKey` scheme in
`query` and `cookie` respectively, with a document-level `security` that secures
every operation — precisely the two rows
[`fern-limitations.md`](../fern-limitations.md#the-importer-drops-four-security-schemes-outright)
measured as `fern check` **exit 1**. Both are DROPPED in `CORPUS.md`, and
`etherpad.local`'s reason is recorded as a *check* failure with five fatal
diagnostics, which is consistent. Whether the sole-scheme refusal is what dropped
them is not settled here: settling it means running Fern, which this pass does
not do.

### Witness search (issue #188)

This table was written when ten of this region's eleven `PROBE` rows were IANA
`scheme` members whose `settlement` cells rested on the ledger's
four-dropped-schemes finding. **No row rests on it now.** All four of the
region's remaining `PROBE` rows are IANA `scheme` members and all four stay
`PROBE` on witness supply — `http-concealed`, `http-gnap`, `http-privatetoken`
and `http-vapid`; `http-dpop` and `http-mutual` are `golden`; and `http-hoba`,
`http-oauth`, `http-scram-sha-1` and `http-scram-sha-256` are `FIXTURE` gaps.
The eleven rows of this table's first pass are settled on the evidence below and
recorded in **What this table's first four rows became**, **What this table's
second four rows became** and **What this table's last three rows became** after
the table; the two rows a second pass adds, and the five it re-searched, are
recorded in **What this change's second pass found** after those.
That four-dropped-schemes
finding is a claim about **Fern** — an importer-unsupported scheme cannot be
isolated, because alone Fern refuses the document and paired with a supported
scheme it leaves no trace in the output. It is not a claim about whether any
real-world document *declares* the scheme, and this table answers only the
second question: for each row, was a document found in the world that declares
the feature, and what is true of that document.

Nothing in the table itself moves a `category`, a `settlement` or a count: it
records what is true of the world, and the three **What this table's … became**
sections after it record what the corpus then did with that. Every row it covers
has now been reclassified, so no row below is left reading `witness-found` beside
a `settlement` of `PROBE`.

`outcome` is one of the five words the index's [settlement rule](../openapi-surface-coverage.md#the-settlement-rule-as-amended)
defines. No row below reads `search-incomplete`; the other four, at this
region's bar, are these. `witness-found` — a named real-world document
declares the feature, at a credential-free HTTPS URL ending `.json`, `.yaml` or
`.yml` (the direct-spec-URL rule [`fern-goldens.md`](../fern-goldens.md) states),
pinned to an immutable ref, under a redistribution-compatible license, accepted
by `fern check`, and under a name that is neither registered in
[`CORPUS.md`](../../tests/fixtures/CORPUS.md) nor listed DROPPED or REJECTED
there or in [`tests/fixtures/AGENTS.md`](../../tests/fixtures/AGENTS.md).
`fern-rejected` — all of that, and Fern refuses the raw document. `witness-blocked`
— a document declares it and can be recorded as neither, for the reason the row
names. `none-found` — every source below was searched and no candidate was
verified as declaring it. Each row records the **best** outcome its whole
candidate set reached, names the document it is recorded on, and names the other
candidates and what became of each.

**Reading the `fern check` cell.** Fern's exit code is not by itself its verdict:
on three of the `securityscheme-ref` candidates it printed `Failed to parse
openapi document` and a `CliError` out of `resolveSecuritySchemeReference`, then
printed `All checks passed` and exited 0 — the same false success `CORPUS.md`
records against `conjur.local` and `dapr`. A document Fern failed to parse is not
a document Fern accepted, so those rows read `fern-rejected` and the cell carries
both what Fern printed and the exit code. Every check ran the local `fern` CLI
against the workspace shape `scripts/generate-fern-fixture.sh` scaffolds — CLI
`5.67.1` pinned in `fern.config.json`, `fernapi/fern-python-sdk` `5.20.0`,
`pydantic_config.enum_type: python_enums` — over the document fetched at the ref
the row names.

**A second pass, for issue #188's `FIXTURE` backlog.** Everything above is the
first pass. This change re-searched the seven `security` rows of the ranked
`FIXTURE` backlog — `http-hoba`, `http-oauth`, `http-scram-sha-1`,
`http-scram-sha-256`, `oauth2-multiple-flows`,
`security-optional-requirement-operation` and `securityscheme-ref` — and the
table now covers **thirteen** rows rather than eleven. Five of the seven already
carried a completed search that had found a document this corpus cannot use, so
for those five the question this pass answers is a different one: **is there a
*second*, usable witness anywhere.** Their lines below are not a restatement of
the first pass; each one's cell carries the first pass's query and count and then
this pass's own, and what the two new rows record is the whole of a first search.
The two Fern refused were read first, because each was refused for a property of
the *candidate* rather than of the shape, and that is what says what a usable
second candidate would have to look like: `http-oauth`'s witness declares
`scheme: oauth` **alone**, so a usable one pairs it with a scheme Fern's importer
supports; `securityscheme-ref`'s three witnesses all `$ref` **across documents**,
so a usable one would have to keep its reference inside the same document, where
`resolveSecuritySchemeReference` can follow it. The search found neither, and
[**What this change's second pass found**](#what-this-changes-second-pass-found-and-what-it-settles)
after the table records that with the candidates it screened.

**How each source was queried.** Six sources, the same six for every row, with
only the searched value changing. Each row's last cell gives the exact query text
with that value substituted, and the count it returned.

* **APIs.guru / `openapi-directory`, and `jentic/jentic-public-apis`** — the two
  complete document corpora, both searched whole rather than sampled, and both
  named under one label because a row's cell records them in one segment.
  `git clone --depth 1 https://github.com/APIs-guru/openapi-directory` at
  `f04b8d0bcd39c52e1cf3ad7a5fe744709832ae49`, which is **4,138** spec documents
  under 701 provider directories, then one `grep -rilE` per row over `APIs/`.
  The second corpus is this change's addition: `git clone --depth 1
  https://github.com/jentic/jentic-public-apis` at
  `eb9d12a2684b0fbcb5aecf51e8ae54dba0929743`, **74,240** documents under
  `apis/openapi/`, read the same way. **Neither corpus's count rests on its
  grep.** A literal is a superset for a row whose shape is a count or a
  structure, and it is not even that for a row whose shape is a value: the
  inherited `scheme` query anchors on end-of-line and so cannot see the one
  minified document this table already records as a witness. So every document
  of both corpora — 4,138 and 74,240 — is parsed and walked over its object
  model, and the grep is recorded as the re-runnable query rather than as the
  measurement.
* **GitHub code search** — `gh search code <value> --filename openapi.yaml`, then
  `--filename openapi.json`, then `--filename swagger.yaml`. Two forms were tried
  first and are recorded because they mislead: `gh api -X GET search/code -f
  q='dpop path:openapi.yaml'` returned **HTTP 403** *"API rate limit exceeded for
  user ID …"* at 19:39 UTC while `gh api rate_limit` reported `code_search` 10/10
  and `search` 30/30 remaining — the secondary limiter, which no polling waits
  out — and `gh search code securitySchemes path:openapi.yaml` returns `[]` where
  `gh search code securitySchemes --filename openapi.yaml` returns hits, so the
  `path:` qualifier is not honoured through that API. The engine behind `gh` is
  also the legacy one, which does not do phrase search, so the searched term is
  the bare scheme value and the result set is filtered **afterwards**: all
  **726** distinct `(repo, path)` results across the 30 queries were fetched from
  `https://raw.githubusercontent.com/<repo>/HEAD/<path>` and grepped with the same
  regex, so each row's GitHub count is read out of documents rather than out of a
  result snippet. Queries ran one per 20s and none was refused.
* **Sourcegraph public code search** — a second credential-free index over the
  same file names: `GET https://sourcegraph.com/.api/search/stream?v=V3` with
  `q=file:(openapi|swagger)\.(yaml|yml|json) content:"scheme: <value>" count:100`
  and the JSON spelling `content:"\"scheme\": \"<value>\""`. Only the literal form
  is used: its regexp forms fail open, returning 0 even for a control
  (`content:/scheme:\s*bearer/` → 0 where `content:"scheme: bearer"` → 100).
* **SwaggerHub public registry** — `GET
  https://api.swaggerhub.com/specs?query=<value>&limit=20`. That search matches
  API name, description and tags rather than document content (`query=petstore` →
  129,570, `query=zzqqxxnonsense` → 0), so a row records the count it returned and
  the top names, **and** the top 20 returned documents were fetched from their own
  `X-Swagger` URLs and grepped.
* **Postman public API network** — `POST https://www.postman.com/_api/ws/proxy`
  with body `{"service":"search","method":"POST","path":"/search-all","body":
  {"queryIndices":["adp.api"],"queryText":"<value>","size":3,"from":0}}`; the row
  records `meta.total` for `api`, `apiDefinition`, `specification` and
  `collection`. The `apinetwork.collection` and `apinetwork.api` indices are
  refused by that endpoint (*"The following indices are not allowed"*), so
  `adp.api` — the API/definition index — is the one asked.
* **Vendor developer portals** — each row names the portals its own feature
  domain implies. Every one was fetched and grepped rather than read, and a
  vendor that publishes no OpenAPI document at all is recorded as such.

| key | outcome | witness | immutable ref | license | fern check | sources searched and the exact query used against each |
|---|---|---|---|---|---|---|
| `http-concealed` | `none-found` | none — no candidate was verified as declaring it. The nearest, the SwaggerHub `Auth Test` document that carries this table's `hoba` and `scram` rows, declares nine other IANA schemes and not `concealed` | — | — | not run — no candidate to run it on | **APIs.guru** `grep -rilE '"?scheme"?[[:space:]]*:[[:space:]]*"?concealed"?[[:space:],]*$' APIs/` over the 4,138 documents at `f04b8d0b` → 0 files. **GitHub** `gh search code concealed --filename openapi.yaml` → 30 results, `--filename openapi.json` → 20, `--filename swagger.yaml` → 3; all 53 fetched and grepped → 0 declare it. **Sourcegraph** `content:"scheme: concealed"` → 0 matches; `content:"\"scheme\": \"concealed\""` → 0. **SwaggerHub** `GET /specs?query=concealed&limit=20` → 163 results, top three all `1Password Connect`; the 20 returned documents fetched and grepped → 0 declare it. **Postman** `queryText: "concealed"` over `adp.api` → api 0, apiDefinition 0, specification 0, collection 0. **Vendor portals** RFC 9729 concealed authentication ships today only in MASQUE/relay products, so Cloudflare's published schema was fetched — `raw.githubusercontent.com/cloudflare/api-schemas/main/openapi.json` (24,855,060 bytes) and its `openapi.yaml` twin (18,358,009 bytes) → 0 — with the identity portals nearest the HTTP-auth-scheme domain: `okta/okta-management-openapi-spec` `dist/legacy-v1-swagger/spec.json` (633,378 bytes), `ory/hydra` `spec/api.json` (189,711 bytes) and `ory/kratos` `spec/api.json` (398,714 bytes) → 0 |
| `http-dpop` | `witness-found` | `mosip/esignet` — `docs/esignet-openapi.yaml`, at `https://raw.githubusercontent.com/mosip/esignet/201264c86e98113762451f4a306163233fa79e24/docs/esignet-openapi.yaml`. Fetched and read at that exact reference: **1** declaration, `Authorization-DPoP` with `type: http` and `scheme: DPoP`, beside four `bearer` schemes. The name is in neither `CORPUS.md` nor either DROPPED/REJECTED list | commit `201264c86e98113762451f4a306163233fa79e24` | MPL-2.0 — declared in the document (`info.license.name: MPL-2.0`) and as the repository's own license | **exit 0**, `All checks passed`, nothing on stderr but the CLI's upgrade banner | **APIs.guru** `grep -rilE '"?scheme"?[[:space:]]*:[[:space:]]*"?dpop"?[[:space:],]*$' APIs/` over the 4,138 documents at `f04b8d0b` → 0 files. **GitHub** `gh search code dpop --filename openapi.yaml` → 30, `--filename openapi.json` → 27, `--filename swagger.yaml` → 28; fetched and grepped, **7** documents declare it. **Sourcegraph** `content:"scheme: dpop"` → 0; `content:"\"scheme\": \"dpop\""` → 0 (its index does not hold these repositories). **SwaggerHub** `GET /specs?query=dpop&limit=20` → 710 results whose top names are unrelated (`7pace Timetracker API Reference`); the 20 returned documents fetched and grepped → 0 declare it. **Postman** `queryText: "dpop"` over `adp.api` → api 0, apiDefinition 0, specification 0, collection 0. **Vendor portals** `okta/okta-management-openapi-spec` `dist/legacy-v1-swagger/spec.json`, `ory/hydra` `spec/api.json`, `ory/kratos` `spec/api.json` → 0 each; Curity publishes no OpenAPI document at a public repository path (`curityio/openapi-specifications` → 404, and `gh search repos "curity openapi"` → no results). **The other candidates** `blackadi/OAUTH2.0` `server/src/routes/openapi.json` at `b6e4cfa1fb060ca5ca3e32185f4a5d88c27163e3` (repository MIT; `scheme: dpop` beside `bearer` and `basic`; `fern check` exit 0 `All checks passed`) and `openbankingproject-ch/Open-API-Kundenbeziehung-Legacy` `api/openapi.yaml` at `c7439ba67f5790d901d1d20943cae5cb48e8e7fc` (the document declares `info.license: MIT`, the repository carries no license file; `scheme: DPoP` beside `bearer`; `fern check` exit 0) also reach `witness-found`, and are the MIT-licensed fallbacks should MPL-2.0 be judged outside the corpus's Apache/MIT/BSD/CC0 set. `zerkerlabs/treeship` `docs/content/docs/api/hub-openapi.yaml` at `322a55f11e6799cf79b3dcf1bc1c874eb2630099` (Apache-2.0) declares `DPoP` as its **only** scheme and `fern check` exits 1 with ten `Endpoint requires auth, but no auth is defined` errors — the four-dropped-schemes finding reproduced on a real document. `rapinoinfeliz/SDRNexus`, `mishrasanjeev/grantex` and `routerarchitects/mc-cds-ui` declare it under no license or `NOASSERTION` |
| `http-gnap` | `none-found` | none — the one candidate was fetched and read and does not declare the feature: Interledger Open Payments' authorization-server document names its scheme `GNAP` but declares it `type: apiKey` with `in: header`, not `Security Scheme Object.scheme`, at `https://raw.githubusercontent.com/interledger/open-payments/v1.0/openapi/auth-server.yaml`, in its resource-server twin, and in `interledger/open-payments-specifications` `openapi/auth-server.yaml` at `8bc9eb5ea500a17b55da614268dca563865e30d4` alike | — | — | not run — no candidate declares the feature | **APIs.guru** `grep -rilE '"?scheme"?[[:space:]]*:[[:space:]]*"?gnap"?[[:space:],]*$' APIs/` over the 4,138 documents at `f04b8d0b` → 0 files. **GitHub** `gh search code gnap --filename openapi.yaml` → 30, `--filename openapi.json` → 30, `--filename swagger.yaml` → 12; all 72 fetched and grepped → 0 declare it. **Sourcegraph** `content:"scheme: gnap"` → 0; `content:"\"scheme\": \"gnap\""` → 0. **SwaggerHub** `GET /specs?query=gnap&limit=20` → 736 results whose top names are unrelated (`XLS Alphabank Campaign API`); the 20 returned documents fetched and grepped → 0 declare it. **Postman** `queryText: "gnap"` over `adp.api` → api 0, apiDefinition 0, specification 0, collection 0. **Vendor portals** GNAP's deployed implementation is Interledger Open Payments, whose three documents above are the candidate recorded; `ory/hydra` `spec/api.json` and `ory/kratos` `spec/api.json` → 0 |
| `http-hoba` | `witness-blocked` | SwaggerHub `Auth Test` 1.0.0, published by user `0UH3TN5N_1` at `https://api.swaggerhub.com/apis/0UH3TN5N_1/query/1.0.0`. Fetched and read at that reference: **1** declaration, `scheme_7` with `type: http` and `scheme: hoba`, among thirteen schemes that also cover `basic`, `bearer`, `digest`, `mutual`, `negotiate`, `oauth`, `scram-sha-1` and `scram-sha-256` | none — SwaggerHub exposes only the mutable `1.0.0` version reference, which its owner can edit in place; there is no commit or digest to pin | none — the document declares no `info.license` and the SwaggerHub API carries no license property, so it is not redistributable | not run — three properties block the document before Fern's verdict could matter: no license, no immutable ref, and a reference that does not end `.json`, `.yaml` or `.yml` as `fern-goldens.md` requires of a direct spec URL | **APIs.guru** first pass `grep -rilE '"?scheme"?[[:space:]]*:[[:space:]]*"?hoba"?[[:space:],]*$' APIs/` over the 4,138 documents at `f04b8d0b` → 0 files. Re-read whole for this change at the same ref `f04b8d0bcd39c52e1cf3ad7a5fe744709832ae49`: the same query → **0** files, and all **4,138** documents read (4,090 parsed, 48 unparseable) and walked for a Security Scheme Object with `type: http` and `scheme` equal to `hoba` case-insensitively → **0** declarations. The second complete corpus this change reads, `jentic/jentic-public-apis` at commit `eb9d12a2684b0fbcb5aecf51e8ae54dba0929743`, was cloned and read the same way: `grep -rilE '"?scheme"?[[:space:]]*:[[:space:]]*"?hoba"?[[:space:],]*$' apis/` → **0** files, and all **74,240** of its documents parsed (0 unparseable) and walked → **0** declarations. The inherited query is **shown unable to find a known declarer**: run against the SwaggerHub `Auth Test` document this row already records — which is minified, one line for the whole document — it matches **0**, because its `[[:space:],]*$` anchor needs the value to end the line. So a second, unanchored form is recorded beside it, `grep -rilE '"?scheme"?[[:space:]]*:[[:space:]]*"?hoba"?([^-a-z0-9]|$)' <root>/`, which matches that same document **1** and matches a document written for the purpose in both the pretty-printed and the flow-style YAML spelling; over the two corpora it returns **0** and **0** files. Neither literal is what the count rests on: every document of both corpora was parsed and walked, which is what makes the zero a reading rather than a regex. **GitHub** first pass `gh search code hoba --filename openapi.yaml` → 30, `--filename openapi.json` → 30, `--filename swagger.yaml` → 18; all 78 fetched and grepped → 0. Re-run for this change at the higher cap: `gh search code hoba --filename openapi.yaml --limit 100 --json repository,path` → **59**, `gh search code hoba --filename openapi.json --limit 100 --json repository,path` → **49**, `gh search code hoba --filename swagger.yaml --limit 100 --json repository,path` → **18**. Those 126 results fall inside the **1,291** distinct `(repo, path)` union of this change's 21 queries; all 1,291 were fetched from `https://raw.githubusercontent.com/<repo>/HEAD/<path>` (1,291 of 1,291 at HTTP 200), of which **1,240** parsed (51 unparseable) and were walked → **0** of those 1,240 declare it. Two of the 21 queries were refused `HTTP 403` *"API rate limit exceeded for user ID …"* — the secondary limiter, while `gh api rate_limit` still reported `search` 30/30 — and both were retried later and answered; neither is a `hoba` query. **Sourcegraph** the literal query in both spellings, the form this table's preamble records as the only one that does not fail open, each run against the public streaming index at `GET https://sourcegraph.com/.api/search/stream?v=V3`: `file:(openapi|swagger)\.(yaml|yml|json) content:"scheme: hoba" count:100` → **0**; `file:(openapi|swagger)\.(yaml|yml|json) content:"\"scheme\": \"hoba\"" count:100` → **0**. The control `file:(openapi|swagger)\.(yaml|yml|json) content:"scheme: bearer" count:100`, run before the first query and again after every fifth, answered on 3 of its 3 runs and returned **97** — so a zero beside it is the index answering rather than refusing. All **241** distinct files the run's queries returned were fetched from `https://raw.githubusercontent.com/<repo>/HEAD/<path>` (241 of 241 at HTTP 200) and parsed (238 parsed, 3 unparseable): **0** declare it. No document was reached through it that either complete corpus did not already hold. **SwaggerHub** first pass `GET /specs?query=hoba&limit=20` → 726 results whose top names are unrelated; the 20 returned documents fetched and grepped → 0. Re-run for this change: `GET https://api.swaggerhub.com/specs?query=hoba&limit=20` → **726**, and this change's 11 SwaggerHub queries returned **142** distinct documents in all, every one fetched from its own `X-Swagger` URL and parsed → **1** declares it, and it is the `Auth Test` document this row already records. The control `GET /specs?query=zzqqxxnonsense&limit=20` → **0**, so the registry answered and discriminated. **Postman** `POST https://www.postman.com/_api/ws/proxy` with `{"service":"search","method":"POST","path":"/search-all","body":{"queryIndices":["adp.api"],"queryText":"hoba","size":3,"from":0}}` → api **1**, apiDefinition 0, specification 0, collection 0 — the same `hoba-backend` by `warped-meteor-692351` the first pass names. Its document body could not be read: `GET https://api.getpostman.com/apis/cbb0705c-0845-4499-b2ab-aef0ff308954` returns **HTTP 401** and the proxy refuses the `apis` service (`invalidServiceError`), so the index answers the *search* and exposes no document to parse. The controls `queryText: "petstore"` → api **401** and `queryText: "zzqqxxnonsense"` → api **0** show the index answering and discriminating. **Vendor portals** HOBA still has no vendor implementation with a developer portal, so the identity portals nearest the domain were fetched and parsed afresh: `okta/okta-management-openapi-spec` `dist/legacy-v1-swagger/spec.json` (633,378 bytes, `swagger: 2.0`), `ory/hydra` `spec/api.json` (189,711), `ory/kratos` `spec/api.json` (398,714) and, new to this pass, Keycloak's published `https://www.keycloak.org/docs-api/latest/rest-api/openapi.json` (561,395, `openapi: 3.0.3`) → **0** each |
| `http-mutual` | `witness-found` | `jentic/jentic-public-apis` — `apis/openapi/cyberark.com/conjur-api/5.3.2/openapi.json`, a redistribution of CyberArk Conjur 5.3.2, at `https://raw.githubusercontent.com/jentic/jentic-public-apis/9d36c7e3808ebe65d69e81d3c3250598927a575c/apis/openapi/cyberark.com/conjur-api/5.3.2/openapi.json`. Fetched and read at that exact reference: **1** declaration, `conjurKubernetesMutualTls` with `type: http` and `scheme: mutual`, beside `basicAuth` (`http`/`basic`) and an `apiKey` scheme. This is a different document at a different source and ref from `conjur.local`, APIs.guru's 5.3.0 conversion, which is a `CORPUS.md` **DROPPED** row and was not retried | commit `9d36c7e3808ebe65d69e81d3c3250598927a575c` | Apache-2.0 declared in the document (`info.license.name`), inside a CC0-1.0 repository | **exit 0**, `All checks passed`, nothing on stderr but the CLI's upgrade banner — so not the false success `CORPUS.md` records against `conjur.local` | **APIs.guru** `grep -rilE '"?scheme"?[[:space:]]*:[[:space:]]*"?mutual"?[[:space:],]*$' APIs/` over the 4,138 documents at `f04b8d0b` → 1 file, `APIs/conjur.local/5.3.0/openapi.yaml`, which is the DROPPED row. **GitHub** `gh search code mutual --filename openapi.yaml` → 30, `--filename openapi.json` → 30, `--filename swagger.yaml` → 30; fetched and grepped, 3 declare it — the DROPPED `conjur.local`, the witness above, and `konfig-dev/konfig` `sdks/db/intermediate-fixed-specs/cyberark/conjur/openapi.yaml`, a third copy of the same Conjur document. **Sourcegraph** `content:"scheme: mutual"` → 3 matches (`APIs-guru/openapi-directory`, `WebFuzzing/EvoMaster` and `konfig-dev/konfig`, all copies of Conjur); `content:"\"scheme\": \"mutual\""` → 1 (the witness). **SwaggerHub** `GET /specs?query=mutual&limit=3` → 4,599 results whose top names are unrelated (`0Wallet Game Service API`), a set its content-blind metadata search cannot narrow further; the `Auth Test` document reached through the `scram-sha-1` query also declares `scheme: mutual`, under no license. **Postman** `queryText: "mutual"` over `adp.api` → api 0, apiDefinition 0, specification 0, collection 0. **Vendor portals** CyberArk is the vendor RFC 8120 implies: upstream `cyberark/conjur-openapi-spec` `spec/openapi.yml` at `90442c4db65695666f1c5545824c76968c5a7339` (Apache-2.0) was fetched and declares it too, and `fern check` exits 0 on it — but its `paths` are relative-file `$ref`s into sibling documents, so no single direct spec URL carries the whole API, which is why the jentic redistribution is the one recorded. **One correction, made when the row was registered rather than searched.** That redistribution is *not* self-contained either, as this cell first recorded: all 36 of its `paths` entries are the same relative-file `$ref`s, which Fern discards without diagnosing. It is still the witness — it declares the scheme, Fern parses it and accepts it at exit 0, and its golden is what row 99 pins — but what that golden holds is an endpoint-free client, and [`document-paths.md`](document-paths.md)'s `pathitem-ref` becomes `golden` on the same 36 sites |
| `http-oauth` | `fern-rejected` | `jentic/jentic-public-apis` — `apis/openapi/thenounproject.com/noun-project-api/2.0.0/openapi.json`, at `https://raw.githubusercontent.com/jentic/jentic-public-apis/9d36c7e3808ebe65d69e81d3c3250598927a575c/apis/openapi/thenounproject.com/noun-project-api/2.0.0/openapi.json`. Fetched and read at that exact reference: **1** declaration, `OAuth1` with `type: http` and `scheme: oauth`, its only security scheme | commit `9d36c7e3808ebe65d69e81d3c3250598927a575c` | CC0-1.0, the repository's license; the document declares no `info.license` of its own | **exit 1** — `[sdk] 1 error`, `path: v2.yml -> service`, `issue: Service requires auth, but no auth is defined.`, `Found 1 error and 2 warnings` — the four-dropped-schemes finding reproduced on a real document that declares `oauth` alone | **APIs.guru** first pass `grep -rilE '"?scheme"?[[:space:]]*:[[:space:]]*"?oauth"?[[:space:],]*$' APIs/` over the 4,138 documents at `f04b8d0b` → 2 files, `APIs/here.com/tracking/2.1.192/openapi.yaml` and `APIs/twitter.com/current/2.62/openapi.yaml`. Re-read whole for this change at the same ref `f04b8d0bcd39c52e1cf3ad7a5fe744709832ae49`: the same query → **2** files, and all **4,138** documents parsed and walked for a Security Scheme Object with `type: http` and `scheme: oauth` → **2** declarations, in those same two documents and no others. `jentic/jentic-public-apis` at commit `eb9d12a2684b0fbcb5aecf51e8ae54dba0929743`: `grep -rilE '"?scheme"?[[:space:]]*:[[:space:]]*"?oauth"?[[:space:],]*$' apis/` → **33** files, and all **74,240** documents parsed and walked → **15** declarations, which are **3** distinct APIs each carried in five copies (`openapi.json` plus four `meta/import/` bundles): `thenounproject.com/noun-project-api/2.0.0` — this row's recorded witness — and `twitter.com/current/2.132` and `ads-api.twitter.com/main/12`, both under the X terms. The inherited query is **shown unable to find a known declarer**: run against the SwaggerHub `Auth Test` document this row already records — which is minified, one line for the whole document — it matches **0**, because its `[[:space:],]*$` anchor needs the value to end the line. So a second, unanchored form is recorded beside it, `grep -rilE '"?scheme"?[[:space:]]*:[[:space:]]*"?oauth"?([^-a-z0-9]|$)' <root>/`, which matches that same document **1** and matches a document written for the purpose in both the pretty-printed and the flow-style YAML spelling; over the two corpora it returns **2** and **33** files. Neither literal is what the count rests on: every document of both corpora was parsed and walked, which is what makes the zero a reading rather than a regex. **GitHub** first pass `gh search code oauth --filename openapi.yaml` → 30, `--filename openapi.json` → 30, `--filename swagger.yaml` → 30. Re-run for this change at the higher cap: `gh search code oauth --filename openapi.yaml --limit 100 --json repository,path` → **100**, `gh search code oauth --filename openapi.json --limit 100 --json repository,path` → **100**, `gh search code oauth --filename swagger.yaml --limit 100 --json repository,path` → **100** (each at the cap). Of the **1,291** distinct documents this change fetched, **1,240** parsed (51 unparseable) and were walked: **2** of those 1,240 declare it, the APIs.guru mirror of the X document and `konfig-sdks/openapi-examples` `x/openapi.yaml`, a third copy of that same X document. **Sourcegraph** the literal query in both spellings, each run against the public streaming index at `GET https://sourcegraph.com/.api/search/stream?v=V3`: `file:(openapi|swagger)\.(yaml|yml|json) content:"scheme: oauth" count:100` → **4**; `file:(openapi|swagger)\.(yaml|yml|json) content:"\"scheme\": \"oauth\"" count:100` → **5**. The control `file:(openapi|swagger)\.(yaml|yml|json) content:"scheme: bearer" count:100`, run before the first query and again after every fifth, answered on 3 of its 3 runs and returned **97** — so a zero beside it is the index answering rather than refusing. All **241** distinct files the run's queries returned were fetched and parsed (238 parsed, 3 unparseable): **8** declare it, in 8 files. The 9 distinct files they returned, read rather than counted: `APIs-guru/openapi-directory` `APIs/twitter.com/current/2.62/openapi.yaml`; `Alia5/steaminputdb.com` `openapi.yaml`; `DataFire/Integrations` `integrations/generated/twitter_current/openapi.json`; `WebFuzzing/EvoMaster` `core-tests/integration-tests/core-it/src/test/resources/APIs_guru/twitter.com/current/2.21/openapi.yaml`; `jentic/jentic-public-apis` `apis/openapi/ads-api.twitter.com/main/12/openapi.json`; `jentic/jentic-public-apis` `apis/openapi/thenounproject.com/noun-project-api/2.0.0/openapi.json`; `jentic/jentic-public-apis` `apis/openapi/twitter.com/current/2.132/openapi.json`; `konfig-dev/konfig` `sdks/db/intermediate-fixed-specs/x/openapi.yaml`; `xdevplatform/xdk` `specs/openapi.json`. Eight of the nine are copies or mirrors of documents the record already names — the X document in five places (`APIs-guru`, `WebFuzzing/EvoMaster`, `DataFire/Integrations`, `konfig-dev/konfig`, `xdevplatform/xdk`) and the `jentic` bundles of X, X Ads and the Noun Project witness — so the index returned no candidate this search had not already screened. **The ninth corrects the first pass.** `Alia5/steaminputdb.com` `openapi.yaml` is recorded there as declaring `scheme: OAuth` under AGPL-3.0; read rather than grepped, its `Steam Auth` scheme is `type: oauth2`, and `scheme` is not a field of an OAuth2 Security Scheme Object at all — so it never declared this row's feature, and the licence was never what disqualified it. **SwaggerHub** `GET https://api.swaggerhub.com/specs?query=oauth&limit=20` → **199,565** results, top names `Swagger Petstore - OpenAPI 3.0` and two OAuth2 sample projects — a set its content-blind metadata search cannot narrow. Of the **142** documents this change's 11 SwaggerHub queries returned and parsed, **1** declares it, and it is the same unlicensed `Auth Test` document. **Postman** `queryText: "oauth"` over `adp.api` → api **16**, apiDefinition 0, specification 0, collection 0; the three top hits are `oauth`, `OAuthService API` and `oauth2`, all name matches, and the index exposes no document body to parse — `GET https://api.getpostman.com/apis/<id>` returns **HTTP 401**. **Vendor portals** the OAuth 1.0a-era portals, fetched and parsed afresh: Trello `https://developer.atlassian.com/cloud/trello/swagger.v3.json` (261,671 bytes), Bitbucket `https://api.bitbucket.org/swagger.json` (863,310, `swagger: 2.0`) and Xero `XeroAPI/Xero-OpenAPI` `xero_accounting.yaml` (933,096) → **0** each; Tumblr publishes its API reference as Markdown rather than an OpenAPI document (`tumblr/docs` `api.md`, 192,174 bytes, fetched and confirmed not one) |
| `http-privatetoken` | `none-found` | none — no candidate was verified as declaring it | — | — | not run — no candidate to run it on | **APIs.guru** `grep -rilE '"?scheme"?[[:space:]]*:[[:space:]]*"?privatetoken"?[[:space:],]*$' APIs/` over the 4,138 documents at `f04b8d0b` → 0 files. **GitHub** `gh search code privatetoken --filename openapi.yaml` → 8 results, `--filename openapi.json` → 2, `--filename swagger.yaml` → 0; all 10 fetched and grepped → 0 declare it. **Sourcegraph** `content:"scheme: privatetoken"` → 0; `content:"\"scheme\": \"privatetoken\""` → 0. **SwaggerHub** `GET /specs?query=privatetoken&limit=20` → 2,885 results whose top names are unrelated (`LIVE PUBLIC REST API Docs v1.0.0`); the 20 returned documents fetched and grepped → 0 declare it. **Postman** `queryText: "privatetoken"` over `adp.api` → api 0, apiDefinition 0, specification 0, collection 0. **Vendor portals** RFC 9577 Private Access Tokens are issued by Cloudflare and Apple: Cloudflare's `raw.githubusercontent.com/cloudflare/api-schemas/main/openapi.json` (24,855,060 bytes) and `openapi.yaml` (18,358,009 bytes) → 0, and Apple publishes no OpenAPI document for Private Access Tokens at all |
| `http-scram-sha-1` | `witness-blocked` | SwaggerHub `Auth Test` 1.0.0, published by user `0UH3TN5N_1` at `https://api.swaggerhub.com/apis/0UH3TN5N_1/query/1.0.0`. Fetched and read at that reference: **1** declaration, `scheme_11` with `type: http` and `scheme: scram-sha-1` | none — SwaggerHub exposes only the mutable `1.0.0` version reference, which its owner can edit in place | none — the document declares no `info.license` and the SwaggerHub API carries no license property, so it is not redistributable | not run — the same three properties block it as for `http-hoba`: no license, no immutable ref, and a reference that does not end `.json`, `.yaml` or `.yml` | **APIs.guru** first pass `grep -rilE '"?scheme"?[[:space:]]*:[[:space:]]*"?scram-sha-1"?[[:space:],]*$' APIs/` over the 4,138 documents at `f04b8d0b` → 0 files. Re-read whole for this change at the same ref `f04b8d0bcd39c52e1cf3ad7a5fe744709832ae49`: the same query → **0** files, and all **4,138** documents parsed and walked for a Security Scheme Object with `type: http` and `scheme` equal to `scram-sha-1` → **0** declarations. `jentic/jentic-public-apis` at commit `eb9d12a2684b0fbcb5aecf51e8ae54dba0929743`: `grep -rilE '"?scheme"?[[:space:]]*:[[:space:]]*"?scram-sha-1"?[[:space:],]*$' apis/` → **0** files, and all **74,240** documents parsed (0 unparseable) and walked → **0** declarations. The inherited query is **shown unable to find a known declarer**: run against the SwaggerHub `Auth Test` document this row already records — which is minified, one line for the whole document — it matches **0**, because its `[[:space:],]*$` anchor needs the value to end the line. So a second, unanchored form is recorded beside it, `grep -rilE '"?scheme"?[[:space:]]*:[[:space:]]*"?scram-sha-1"?([^-a-z0-9]|$)' <root>/`, which matches that same document **1** and matches a document written for the purpose in both the pretty-printed and the flow-style YAML spelling; over the two corpora it returns **0** and **0** files. Neither literal is what the count rests on: every document of both corpora was parsed and walked, which is what makes the zero a reading rather than a regex. **GitHub** first pass `gh search code scram-sha-1 --filename openapi.yaml` → 0, `--filename openapi.json` → 0, `--filename swagger.yaml` → 8. Re-run for this change at the higher cap: `gh search code scram-sha-1 --filename openapi.yaml --limit 100 --json repository,path` → **0**, `gh search code scram-sha-1 --filename openapi.json --limit 100 --json repository,path` → **0**, `gh search code scram-sha-1 --filename swagger.yaml --limit 100 --json repository,path` → **8**. Of the **1,291** distinct documents this change fetched, **1,240** parsed (51 unparseable) and were walked: **0** of those 1,240 declare it — every match is a Kafka or MongoDB SASL mechanism enum value or a prose mention rather than a Security Scheme Object `scheme`. **Sourcegraph** the literal query in both spellings, each run against the public streaming index at `GET https://sourcegraph.com/.api/search/stream?v=V3`: `file:(openapi|swagger)\.(yaml|yml|json) content:"scheme: scram-sha-1" count:100` → **0**; `file:(openapi|swagger)\.(yaml|yml|json) content:"\"scheme\": \"scram-sha-1\"" count:100` → **0**. The control `file:(openapi|swagger)\.(yaml|yml|json) content:"scheme: bearer" count:100`, run before the first query and again after every fifth, answered on 3 of its 3 runs and returned **97** — so a zero beside it is the index answering rather than refusing. All **241** distinct files the run's queries returned were fetched and parsed (238 parsed, 3 unparseable): **0** declare it. **SwaggerHub** `GET https://api.swaggerhub.com/specs?query=scram-sha-1&limit=20` → **3**. Of the **142** documents this change's 11 SwaggerHub queries returned and parsed, **1** declares it, and it is the same unlicensed `Auth Test` document this row already records. **Postman** `queryText: "scram-sha-1"` over `adp.api` → api **2**, apiDefinition 0, specification 0, collection 0; both are workspace-share links exposing no OpenAPI document, and `GET https://api.getpostman.com/apis/<id>` returns **HTTP 401** for either. **Vendor portals** the database-vendor set, fetched and parsed afresh: `mongodb/openapi` `openapi/v2.json` (4,099,640 bytes), `confluentinc/kafka-rest` `api/v3/openapi.yaml` (175,554), `scylladb/scylladb` `api/api-doc/storage_service.json` (146,329) and, new to this pass, `redpanda-data/console` `proto/gen/openapi/openapi.json` (224,405) → **0** each; RabbitMQ still publishes its management API as hand-written HTML rather than an OpenAPI document (`deps/rabbitmq_management/priv/www/api/index.html`, 82,167 bytes, fetched and confirmed not one) |
| `http-scram-sha-256` | `witness-blocked` | SwaggerHub `Auth Test` 1.0.0, published by user `0UH3TN5N_1` at `https://api.swaggerhub.com/apis/0UH3TN5N_1/query/1.0.0`. Fetched and read at that reference: **1** declaration, `scheme_12` with `type: http` and `scheme: scram-sha-256` | none — SwaggerHub exposes only the mutable `1.0.0` version reference, which its owner can edit in place | none — the document declares no `info.license` and the SwaggerHub API carries no license property, so it is not redistributable | not run — the same three properties block it as for `http-hoba`: no license, no immutable ref, and a reference that does not end `.json`, `.yaml` or `.yml` | **APIs.guru** first pass `grep -rilE '"?scheme"?[[:space:]]*:[[:space:]]*"?scram-sha-256"?[[:space:],]*$' APIs/` over the 4,138 documents at `f04b8d0b` → 0 files. Re-read whole for this change at the same ref `f04b8d0bcd39c52e1cf3ad7a5fe744709832ae49`: the same query → **0** files, and all **4,138** documents parsed and walked for a Security Scheme Object with `type: http` and `scheme` equal to `scram-sha-256` → **0** declarations. `jentic/jentic-public-apis` at commit `eb9d12a2684b0fbcb5aecf51e8ae54dba0929743`: `grep -rilE '"?scheme"?[[:space:]]*:[[:space:]]*"?scram-sha-256"?[[:space:],]*$' apis/` → **0** files, and all **74,240** documents parsed (0 unparseable) and walked → **0** declarations. The inherited query is **shown unable to find a known declarer**: run against the SwaggerHub `Auth Test` document this row already records — which is minified, one line for the whole document — it matches **0**, because its `[[:space:],]*$` anchor needs the value to end the line. So a second, unanchored form is recorded beside it, `grep -rilE '"?scheme"?[[:space:]]*:[[:space:]]*"?scram-sha-256"?([^-a-z0-9]|$)' <root>/`, which matches that same document **1** and matches a document written for the purpose in both the pretty-printed and the flow-style YAML spelling; over the two corpora it returns **0** and **0** files. Neither literal is what the count rests on: every document of both corpora was parsed and walked, which is what makes the zero a reading rather than a regex. **GitHub** first pass `gh search code scram-sha-256 --filename openapi.yaml` → 16, `--filename openapi.json` → 15, `--filename swagger.yaml` → 8. Re-run for this change at the higher cap: `gh search code scram-sha-256 --filename openapi.yaml --limit 100 --json repository,path` → **16**, `gh search code scram-sha-256 --filename openapi.json --limit 100 --json repository,path` → **15**, `gh search code scram-sha-256 --filename swagger.yaml --limit 100 --json repository,path` → **8**. Of the **1,291** distinct documents this change fetched, **1,240** parsed (51 unparseable) and were walked: **0** of those 1,240 declare it — every match is a Kafka or MongoDB SASL mechanism enum value or a prose mention rather than a Security Scheme Object `scheme`. The `--filename swagger.yaml` query is the one of the 21 whose **first attempt was refused** `HTTP 403` *"API rate limit exceeded for user ID …"* while `gh api rate_limit` still reported `search` 30/30 — the secondary limiter — and it was retried and answered **8**, all eight of them copies of `APIs/clearblade.com/3.0/swagger.yaml`, fetched (8 of 8 at HTTP 200) and parsed → **0** declare it. **Sourcegraph** the literal query in both spellings, each run against the public streaming index at `GET https://sourcegraph.com/.api/search/stream?v=V3`: `file:(openapi|swagger)\.(yaml|yml|json) content:"scheme: scram-sha-256" count:100` → **0**; `file:(openapi|swagger)\.(yaml|yml|json) content:"\"scheme\": \"scram-sha-256\"" count:100` → **0**. The control `file:(openapi|swagger)\.(yaml|yml|json) content:"scheme: bearer" count:100`, run before the first query and again after every fifth, answered on 3 of its 3 runs and returned **97** — so a zero beside it is the index answering rather than refusing. All **241** distinct files the run's queries returned were fetched and parsed (238 parsed, 3 unparseable): **0** declare it. **SwaggerHub** `GET https://api.swaggerhub.com/specs?query=scram-sha-256&limit=20` → **689**. Of the **142** documents this change's 11 SwaggerHub queries returned and parsed, **1** declares it, and it is the same unlicensed `Auth Test` document this row already records. **Postman** `queryText: "scram-sha-256"` over `adp.api` → api **0**, apiDefinition 0, specification 0, collection 0. **Vendor portals** the database-vendor set, fetched and parsed afresh: `mongodb/openapi` `openapi/v2.json` (4,099,640 bytes), `confluentinc/kafka-rest` `api/v3/openapi.yaml` (175,554), `scylladb/scylladb` `api/api-doc/storage_service.json` (146,329) and, new to this pass, `redpanda-data/console` `proto/gen/openapi/openapi.json` (224,405) → **0** each; RabbitMQ still publishes its management API as hand-written HTML rather than an OpenAPI document (`deps/rabbitmq_management/priv/www/api/index.html`, 82,167 bytes, fetched and confirmed not one) |
| `http-vapid` | `none-found` | none — no candidate was verified as declaring it | — | — | not run — no candidate to run it on | **APIs.guru** `grep -rilE '"?scheme"?[[:space:]]*:[[:space:]]*"?vapid"?[[:space:],]*$' APIs/` over the 4,138 documents at `f04b8d0b` → 0 files. **GitHub** `gh search code vapid --filename openapi.yaml` → 30, `--filename openapi.json` → 30, `--filename swagger.yaml` → 22; all 82 fetched and grepped → 0 declare it. **Sourcegraph** `content:"scheme: vapid"` → 0; `content:"\"scheme\": \"vapid\""` → 0. **SwaggerHub** `GET /specs?query=vapid&limit=20` → 117 results whose top names are unrelated (`Commerce Admin REST endpoints`); the 20 returned documents fetched and grepped → 0 declare it. **Postman** `queryText: "vapid"` over `adp.api` → api 0, apiDefinition 0, specification 0, collection 0. **Vendor portals** RFC 8292 VAPID is the Web Push domain: `gotify/server` `docs/spec.json` (94,742 bytes) → 0; `binwiederhier/ntfy` publishes no OpenAPI or Swagger document at all — its whole tree at `main` holds 0 paths matching `openapi` or `swagger`; `mozilla-services/autopush-rs` `docs/openapi.yaml` → 404 and `pushpad/pushpad-api` `openapi.yaml` → 404, neither vendor publishing one |
| `oauth2-multiple-flows` | `witness-found` | `OpenBanking-Brasil/specs-directory` — `openapi.yaml`, at `https://raw.githubusercontent.com/OpenBanking-Brasil/specs-directory/37423cbc93982e05eac255db6226ed599a5baace/openapi.yaml`. Fetched and read at that exact reference (279,988 bytes, `openapi: 3.0.0`, 48 paths): **1** declaration — `components.securitySchemes.oAuth`, `type: oauth2`, whose OAuth Flows Object declares **two** of the four flows, `clientCredentials` (scopes `directory:admin` and `directory:software`) and `authorizationCode` (scope `directory:website`) — beside an `apiKey` scheme `authorizer`. The two scope sets are **disjoint**, which is what this row's entry asks of a witness: a golden over it can say which flow `oauth_scope_enum`'s `.or(…)` chain reached, where a document repeating one scope map across its flows cannot. It declares none of this region's other rows. The name is in neither [`CORPUS.md`](../../tests/fixtures/CORPUS.md) nor either rejected list | commit `37423cbc93982e05eac255db6226ed599a5baace` | MIT, declared by the document's own `info.license` (`https://mit-license.org`); the repository carries no licence file, which is the form `CORPUS.md` row 98 already records | **accepted at both stages.** `fern check` at CLI `5.67.1` prints `Found 0 errors and 17 warnings` and exits **0**, and a real generate at `fernapi/fern-python-sdk` **5.20.0** — `fern generate --group python-sdk --local --preview`, the invocation `scripts/generate-fern-fixture.sh` uses — exits **0** and writes a packaged SDK of **323** files. Neither stage printed `Failed to parse openapi document`, which is the false success this table's preamble warns about | **APIs.guru** the shape is a *count* rather than a literal, so the recorded query is a superset a reader can re-run and the count rests on parsing what it returns: `grep -rlE '"?(implicit|password|clientCredentials|authorizationCode)"?[[:space:]]*:' APIs/` over the 4,138 documents at `f04b8d0bcd39c52e1cf3ad7a5fe744709832ae49` → **906** files, and — rather than sampling those — all **4,138** documents were read (4,090 parsed, 48 unparseable) and walked for an OAuth Flows Object holding more than one of `implicit`, `password`, `clientCredentials`, `authorizationCode` → **9** declarations in **9** documents: three eBay APIs (`developer-analytics`, `sell-marketing`, `sell-metadata`), `fulfillment.com/2.0`, `gerermesaffaires.com/1.0.6`, `gettyimages.com/3`, `motaword.com/1.0`, `surevoip.co.uk/9dcb0dc8` and `vimeo.com/3.4`. `jentic/jentic-public-apis` at commit `eb9d12a2684b0fbcb5aecf51e8ae54dba0929743`, read the same way: `grep -rlE '"?(implicit|password|clientCredentials|authorizationCode)"?[[:space:]]*:' apis/` → **11,700** files, and all **74,240** documents parsed (0 unparseable) and walked → **265** declarations in **250** files, which are **50** distinct documents once each API's four `meta/import/` copies are set beside its `openapi.json`. The superset query is shown able to find the shape: run over `APIs/ebay.com/sell-marketing/v1.15.0/openapi.yaml`, `APIs/vimeo.com/3.4/openapi.yaml`, `OpenBanking-Brasil/specs-directory` `openapi.yaml` and `discord.com`'s jentic bundle — four documents independently confirmed to declare two or more flows — it matches every one, and it matches a document written for the purpose in both the YAML and the JSON spelling. **GitHub** the two flow names a multi-flow object must carry are the searchable tokens: `gh search code clientCredentials --filename openapi.yaml --limit 100 --json repository,path` → **100**, `gh search code clientCredentials --filename openapi.json --limit 100 --json repository,path` → **100**, `gh search code clientCredentials --filename swagger.yaml --limit 100 --json repository,path` → **100**, and the same three for `authorizationCode` → **100**, **100**, **100** (all six at the cap). Of the **1,291** distinct documents this change fetched, **1,240** parsed (51 unparseable) and were walked: **28** of those 1,240 declare it, carrying **31** declarations. **Sourcegraph** no literal expresses *two* flows, so the query is the conjunction of two flow names, in the YAML and the JSON spelling. The pair is `clientCredentials` and `authorizationCode` because it is the one both this row's recorded witness and the `discord.com` bundle declare, so the query is shown able to find the shape rather than assumed to; the endpoint's rate limiting is why it is that pair rather than all six, each run against the public streaming index at `GET https://sourcegraph.com/.api/search/stream?v=V3`: `file:(openapi|swagger)\.(yaml|yml|json) content:"clientCredentials:" AND content:"authorizationCode:" count:100` → **33**; `file:(openapi|swagger)\.(yaml|yml|json) content:"\"clientCredentials\":" AND content:"\"authorizationCode\":" count:100` → **45**. The control `file:(openapi|swagger)\.(yaml|yml|json) content:"scheme: bearer" count:100`, run before the first query and again after every fifth, answered on 3 of its 3 runs and returned **97** — so a zero beside it is the index answering rather than refusing. All **241** distinct files the run's queries returned were fetched and parsed (238 parsed, 3 unparseable): **54** declarations in **40** files. **SwaggerHub** `GET https://api.swaggerhub.com/specs?query=clientCredentials&limit=20` → **14,511**, `GET https://api.swaggerhub.com/specs?query=authorizationCode&limit=20` → **19,907** and `GET https://api.swaggerhub.com/specs?query=oauth2&limit=20` → **802,968** — its metadata search is content-blind and cannot narrow a set that size. The 60 documents those three queries returned are inside the **142** this change fetched from their own `X-Swagger` URLs and parsed → **0** declare it. **Postman** `queryText: "clientCredentials"` over `adp.api` → api **0**, apiDefinition 0, specification 0, collection 0; `queryText: "authorizationCode"` → api **0**, apiDefinition 0, specification 0, collection 0. The controls `queryText: "petstore"` → api **401** and `queryText: "zzqqxxnonsense"` → api **0** show the index answering and discriminating on the same request shape. **Vendor portals** the OAuth2 providers whose own portals publish several grant types, fetched and parsed: Microsoft Graph `microsoftgraph/msgraph-metadata` `openapi/v1.0/openapi.yaml` (44,146,442 bytes, `openapi: 3.0.4`), PayPal `paypal/paypal-rest-api-specifications` `openapi/checkout_orders_v2.json` (971,284), Stripe `stripe/openapi` `openapi/spec3.yaml` (6,409,430), Keycloak `https://www.keycloak.org/docs-api/latest/rest-api/openapi.json` (561,395), Ory Hydra `spec/api.json` (189,711) and WSO2 `wso2/carbon-apimgt` `publisher-api.yaml` (681,434) → **0** each; GitLab publishes `doc/api/openapi/openapi_v2.yaml` (2,445,929) as **Swagger 2.0**, which has no OAuth Flows Object at all, so it can declare the shape neither way and is recorded as such |
| `security-optional-requirement-operation` | `witness-found` | `jentic/jentic-public-apis` — `apis/openapi/braintrust.dev/main/1.0.0/openapi.json`, at `https://raw.githubusercontent.com/jentic/jentic-public-apis/eb9d12a2684b0fbcb5aecf51e8ae54dba0929743/apis/openapi/braintrust.dev/main/1.0.0/openapi.json`. Fetched and read at that exact reference (1,032,825 bytes, 71 paths): **148** Operation Objects whose `security` array holds `{}`, against a single security scheme `bearerAuth` (`type: http`, `scheme: bearer`) that Fern's importer supports — so the document exercises the row without also tripping the ledger's dropped-scheme refusal. It declares none of this region's other rows. **A second accepted witness declares this row *and* `oauth2-multiple-flows` together:** `apis/openapi/discord.com/main/10/openapi.json` at the same commit (818,803 bytes, `openapi: 3.1.0`, 128 paths) carries **22** operation-level `{}` requirements *and* **1** OAuth Flows Object declaring three flows whose scopes differ pairwise, so one corpus row could settle both. A third, `apis/openapi/api.openverse.org/main/1.0.0/openapi.json` (34,522 bytes, 17 paths), carries **6**. None of the three names is in `CORPUS.md` or either rejected list | commit `eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` | Apache 2.0 for `braintrust.dev` and MIT for `discord.com` and `api.openverse.org`, each declared by the document's own `info.license`, inside the aggregating repository's own CC0-1.0 — the same two-licence form `CORPUS.md` row 99 records | **accepted at both stages, for all three.** `braintrust.dev`: `fern check` at CLI `5.67.1` → `Found 0 errors and 9 warnings`, exit **0**; real generate at `fernapi/fern-python-sdk` **5.20.0** → exit **0**, **747** files. `discord.com`: check exit **0** (`Found 0 errors and 65 warnings`), generate exit **0**, **566** files. `api.openverse.org`: check exit **0** (`Found 0 errors and 5 warnings`), generate exit **0**, **74** files. No stage of any of the three printed `Failed to parse openapi document` | **APIs.guru** the empty Security Requirement Object is a literal, but one that reads the same at document level and operation level, so the recorded query is again a superset and the count rests on the parse: `grep -rlE '^[[:space:]]*(-[[:space:]]*)?\{[[:space:]]*\},?[[:space:]]*$' APIs/` over the 4,138 documents at `f04b8d0bcd39c52e1cf3ad7a5fe744709832ae49` → **29** files, and all **4,138** documents read (4,090 parsed, 48 unparseable) and walked for an *Operation Object* whose `security` array holds `{}` → **83** declarations in **8** documents: `openbankingproject.ch` 1.3.8 (34), `clever-cloud.com` 1.0.0 (19), `swaggerhub.com` 1.0.66 (16), `docker.com/hub` beta (9), `gsmtasks.com` 2.4.13 (2), `azure.com/imds` 2019-08-15 and 2019-11-01 (1 each) and `wowza.com` 1 (1). The gap between the 29 files the grep returns and the 8 the parse keeps is the empty object standing somewhere other than an Operation Object's `security`, which is why the count rests on the parse. The query is shown able to find the shape rather than assumed to: run over four documents independently confirmed to declare it — `atlassian.com-jira`, `Open-EO/openeo-api` `openapi.yaml`, `braintrust.dev` and `discord.com` — it matches every one, and it matches both spellings in a document written for the purpose (`- {}` under a YAML `security:`, and `{}` inside a pretty-printed JSON `"security": [ … ]`). `jentic/jentic-public-apis` at commit `eb9d12a2684b0fbcb5aecf51e8ae54dba0929743`, read the same way: `grep -rlE '^[[:space:]]*(-[[:space:]]*)?\{[[:space:]]*\},?[[:space:]]*$' apis/` → **617** files, and all **74,240** documents parsed (0 unparseable) and walked → **4,518** declarations in **183** files, which are **36** distinct documents once each API's `meta/import/` copies are set beside its `openapi.json`. **GitHub** the shape carries no token of its own — `{}` is punctuation the legacy index does not search — so the queried term is the `securitySchemes` map every declarer of an operation-level requirement also carries, and the result set is parsed rather than matched: `gh search code securitySchemes --filename openapi.yaml --limit 100 --json repository,path` → **100**, `gh search code securitySchemes --filename openapi.json --limit 100 --json repository,path` → **100**, `gh search code securitySchemes --filename swagger.yaml --limit 100 --json repository,path` → **100** (the third refused `HTTP 403` on the secondary limiter at the first attempt, retried and answered). Of the **1,291** distinct documents this change fetched, **1,240** parsed (51 unparseable) and were walked: **26** of those 1,240 declare it, carrying **113** declarations. **Sourcegraph** the empty requirement's two literal spellings, `- {}` for YAML and `[{}]` for compact JSON, each run against the public streaming index at `GET https://sourcegraph.com/.api/search/stream?v=V3`: `file:(openapi|swagger)\.(yaml|yml|json) content:"- {}" count:100` → **19**; `file:(openapi|swagger)\.(yaml|yml|json) content:"[{}]" count:100` → **22**. The control `file:(openapi|swagger)\.(yaml|yml|json) content:"scheme: bearer" count:100`, run before the first query and again after every fifth, answered on 3 of its 3 runs and returned **97** — so a zero beside it is the index answering rather than refusing. All **241** distinct files the run's queries returned were fetched and parsed (238 parsed, 3 unparseable): **141** declarations in **14** files. **Only the first of those two is a query that can find the shape, and this record says so rather than reporting the second's zero as evidence.** `- {}` is the real YAML spelling — it is what `Open-EO/openeo-api` `openapi.yaml` writes at two of its operations, and what a document written for the purpose writes. `[{}]` is not: pretty-printed JSON puts the empty object alone on its own line, and the literal appears in **none** of the four real JSON declarers checked (`atlassian.com-jira`, `braintrust.dev`, `discord.com`, `api.openverse.org`), while `{}` alone is no discriminator at all. So this index can express the YAML spelling and cannot express the JSON one, and the JSON-side evidence for this row is the two complete corpora, which were parsed rather than searched. **SwaggerHub** `GET https://api.swaggerhub.com/specs?query=security&limit=20` → **802,968** and `GET https://api.swaggerhub.com/specs?query=optional%20authentication&limit=20` → **26,119** — a content-blind metadata search over a set it cannot narrow. The 40 documents those two returned are inside the **142** this change fetched and parsed → **3** declare it (`novaxsolution/0WalletApi` 2, `novaxv1/0WalletApi` 2, `our-377/1-izocloud-nextjs-template` 1), each on a mutable SwaggerHub version reference and none declaring an `info.license`. **Postman** `queryText: "optional authentication"` over `adp.api` → api **0**, apiDefinition 0, specification 0, collection 0; `queryText: "security"` → api **20**, apiDefinition 0, specification 0, collection 0, whose three top hits are `securityapi`, `NOVA Security API` and Postman's own *Getting started with OpenAPI in Postman: Security* — name matches whose document bodies the index does not expose (`GET https://api.getpostman.com/apis/<id>` → **HTTP 401**). **Vendor portals** the portals whose APIs mix authenticated and anonymous endpoints, fetched and parsed: GitHub `github/rest-api-description` `descriptions/api.github.com/api.github.com.json` (12,927,758 bytes), Discourse `discourse/discourse_api_docs` `openapi.json` (708,343), Gitea `go-gitea/gitea` `templates/swagger/v1-openapi3.generated.json` (1,038,566), Sentry `getsentry/sentry-api-schema` `openapi-derefed.json` (4,015,570) and GitLab `doc/api/openapi/openapi_v2.yaml` (2,445,929) → **0** each |
| `securityscheme-ref` | `fern-rejected` | `Open-EO/openeo-api` — `extensions/workspaces/openapi.yaml`, at `https://raw.githubusercontent.com/Open-EO/openeo-api/1881dae18b3c2c417f1305774cf295c81d60d400/extensions/workspaces/openapi.yaml`. Fetched and read at that exact reference: **1** reference-valued scheme, `components.securitySchemes.Bearer` holding `$ref: '../../openapi.yaml#/components/securitySchemes/Bearer'` in place of a Security Scheme Object | commit `1881dae18b3c2c417f1305774cf295c81d60d400` | Apache-2.0, the repository's license | **not accepted, exit 0** — Fern printed `[api]: Failed to parse openapi document openEO API - Workspaces Extension` and `[api]: Failed to resolve ../../openapi.yaml#/components/securitySchemes/Bearer`, with a `CliError` raised from `resolveSecuritySchemeReference`, and then printed `All checks passed` and exited 0. That is the false success `CORPUS.md` records against `conjur.local` and `dapr`; a document Fern failed to parse is not one Fern accepted, so this is `fern-rejected` | **APIs.guru** first pass `grep -rl 'components/securitySchemes/' APIs/` over the 4,138 documents at `f04b8d0b` → 0 files. Re-read whole for this change at the same ref `f04b8d0bcd39c52e1cf3ad7a5fe744709832ae49` with the wider superset `grep -rl 'securitySchemes' APIs/` → **1,623** files, and all **4,138** documents read (4,090 parsed, 48 unparseable) and walked for a `$ref` standing in a Security Scheme Object position under `components.securitySchemes` (or Swagger 2.0's `securityDefinitions`) → **0** declarations. `jentic/jentic-public-apis` at commit `eb9d12a2684b0fbcb5aecf51e8ae54dba0929743`, read the same way: `grep -rl 'securitySchemes' apis/` → **42,570** files, and all **74,240** documents parsed (0 unparseable) and walked → **0** declarations. That is the whole of both corpora and neither holds one. The superset query is shown able to find the shape: run over `Open-EO/openeo-api` `extensions/workspaces/openapi.yaml`, `apify/apify-docs` `apify-api/openapi/openapi.yaml`, `apache/gravitino` `docs/open-api/idp/openapi.yaml` and a document written for the purpose, it matches every one, and the parse behind it reports the reference-valued scheme each of them carries. **GitHub** first pass `gh search code securitySchemes --filename openapi.yaml` → 30, `--filename openapi.json` → 30, `--filename swagger.yaml` → 30. Re-run for this change at the higher cap: `gh search code securitySchemes --filename openapi.yaml --limit 100 --json repository,path` → **100**, `gh search code securitySchemes --filename openapi.json --limit 100 --json repository,path` → **100**, `gh search code securitySchemes --filename swagger.yaml --limit 100 --json repository,path` → **100** (the third refused `HTTP 403` on the secondary limiter at the first attempt, retried and answered). Of the **1,291** distinct documents this change fetched, **1,240** parsed (51 unparseable) and were walked: **2** of those 1,240 hold a `$ref` in a scheme position — `technology-platforms/vcfa-api-docs` `openapi.yaml` (**1**, `./components/securitySchemes.yaml#/bearerAuth`) and `zaghaghi/openapi-tui` `openapi-31/openapi-in-openapi.json` (**1**), the second a false positive — it is the OpenAPI 3.1 meta-schema, whose `security-scheme-or-reference` `$ref` describes the shape rather than declaring it. **Sourcegraph** the reference's two literal spellings, the wider of which (`securitySchemes/`) matches `Open-EO/openeo-api`'s workspaces extension and the two vendor documents this row's candidate ledger names, each run against the public streaming index at `GET https://sourcegraph.com/.api/search/stream?v=V3`: `file:(openapi|swagger)\.(yaml|yml|json) content:"#/components/securitySchemes/" count:100` → **20**; `file:(openapi|swagger)\.(yaml|yml|json) content:"securitySchemes/" count:100` → **27**. The control `file:(openapi|swagger)\.(yaml|yml|json) content:"scheme: bearer" count:100`, run before the first query and again after every fifth, answered on 3 of its 3 runs and returned **97** — so a zero beside it is the index answering rather than refusing. All **241** distinct files the run's queries returned were fetched and parsed (238 parsed, 3 unparseable): **23** declarations in **21** files. **SwaggerHub** `GET https://api.swaggerhub.com/specs?query=securitySchemes&limit=20` → **154,353** results, top names `Swagger Petstore - OpenAPI 3.0` and `0x API` — a set its content-blind metadata search can neither narrow nor enumerate. The 20 documents it returned are inside the **142** this change fetched and parsed → **0** declare it, so the source answered this pass where the first recorded that it could not. **Postman** `queryText: "securitySchemes"` over `adp.api` → api **0**, apiDefinition 0, specification 0, collection 0, beside the controls `queryText: "petstore"` → api **401** and `queryText: "zzqqxxnonsense"` → api **0**. **Vendor portals** the portals whose documents are split across files, all re-fetched: `apify/apify-docs` `apify-api/openapi/openapi.yaml` at `522de71b25a299e348ba1da8f12b2608093b5712` → **2**, `apache/gravitino` `docs/open-api/idp/openapi.yaml` at `dad9a08dcdf397ed9558e4c36d57bdd49b75f3bf` → **1**, `flightctl/flightctl` `api/imagebuilder/v1alpha1/openapi.yaml` → **2** — where the first pass measured **0**, because that document has since moved its two schemes to `$ref`s into `../../core/v1beta1/openapi.yaml` — and `ory/hydra` `spec/api.json` (189,711 bytes) → **0** |

**What this table's first four rows became, and why.** `http-dpop` is now a
`golden` row, and **all three** documents this table's `http-dpop` cell records as
`witness-found` are registered, because each is reachable at an immutable ref, is
redistributable, and is accepted by `fern check` at exit 0:
[`CORPUS.md`](../../tests/fixtures/CORPUS.md) row 96 `blackadi/OAUTH2.0`
(repository MIT), row 97 `mosip/esignet` (MPL-2.0, in the document's
`info.license` and in the repository's `LICENSE` at the pinned commit) and row 98
`openbankingproject-ch/Open-API-Kundenbeziehung-Legacy` (MIT in the document's
`info.license`, which is the redistribution-compatible licence metadata
`CORPUS.md` asks of a row; its repository carries no licence file, and the
manifest row says so). **MPL-2.0 is redistribution-compatible**, so the recorded
witness is registered on its own terms rather than set aside for the two
MIT-licensed fallbacks this table names — those are registered too. All three
goldens byte-match with no exclusions. Registering them moved the census from 126
registered sources to 129 and 109 golden-bearing to 112, took
`securityScheme.scheme=dpop` and `securityScheme.scheme=DPoP` from absences to one
and two sites, and — through row 98's `mTLS` scheme and row 97's two
`format: duration` fields — moved `mutualTLS` here and
[`schemas.md`](schemas.md)'s `format-duration` to `golden` as well.
`http-hoba` becomes a `gap` whose settlement is `FIXTURE`, on this table's
`witness-blocked`: a real-world declaration exists and the two properties
blocking it are the corpus's own (redistributability and an immutable ref), not
Fern's. `http-concealed` and `http-gnap` stay `PROBE`, and their settlement cells
now say so on **witness supply** — the six sources below found no document
declaring either — rather than on the four-dropped-schemes finding, which the
`http-dpop` goldens have now shown does not hold a paired witness out of the
corpus.

**What this table's second four rows became, and why.** None of the four is
registrable, for three reasons rather than four: one witness is refused by Fern,
two rows share a single blocked document, and one row has no witness at all.
`http-oauth` becomes a `gap` whose settlement is `FIXTURE` on this table's
`fern-rejected`: its witness — the Noun Project API 2.0.0 bundle in
`jentic/jentic-public-apis`, reached at commit
`9d36c7e3808ebe65d69e81d3c3250598927a575c` and redistributable under that
repository's CC0-1.0 — declares `scheme: oauth` as its only security scheme, and
`fern check` refuses it at exit 1 (`[sdk] 1 error`, `path: v2.yml -> service`,
`issue: Service requires auth, but no auth is defined.`), re-run for this change
at CLI `5.67.1` over `fernapi/fern-python-sdk` `5.20.0` against the document
fetched at that commit. The document is now listed in
[`tests/fixtures/AGENTS.md`](../../tests/fixtures/AGENTS.md#specs-already-tried-and-rejected-do-not-re-attempt-without-a-fix-upstream)
so the next agent does not spend a generate on it. `http-scram-sha-1` and
`http-scram-sha-256` become `FIXTURE` gaps on this table's `witness-blocked`,
and on one and the same document: the SwaggerHub `Auth Test` document that also
carries `http-hoba`'s witness, which is not redistributable — it declares no
`info.license` and SwaggerHub publishes no license property, so no terms
permitting redistribution can be established — and which separately has no
immutable ref, its `1.0.0` version reference being editable in place by its
owner. Both rows are blocked by that one document rather than by two independent
findings, which is why unblocking it would settle three rows at once.
`http-privatetoken` stays `PROBE` on **witness supply**, which is a different
state again: no document declaring it was found at all, so there is nothing to
block. Its settlement cell now names the six sources and the exact query put to
each, rather than resting on the four-dropped-schemes finding.

**What this table's last three rows became, and why.** `http-mutual` becomes
`golden`, on this table's `witness-found`: the CyberArk Conjur 5.3.2 bundle in
`jentic/jentic-public-apis`, reached at commit
`9d36c7e3808ebe65d69e81d3c3250598927a575c` under the `Apache-2.0` its own
`info.license` declares, is registered as [`CORPUS.md`](../../tests/fixtures/CORPUS.md)
row 99 and its Fern 5.20.0 golden byte-matches with no exclusions. What that
golden pins is written into the entry row: crozier drops `mutual` exactly where
Fern does, and nothing about a document declaring `mutual` alone. The row's older
witness `conjur.local` is unchanged — its **DROPPED** verdict was re-run at the
generator its own provenance records and reproduces, false success and all — so
the row moves on the new document rather than on a re-reading of the old one.
Registering row 99 moved the census from 129 registered sources to 130 and 112
golden-bearing to 113, and — through the 36 relative-file Path Item `$ref`s Fern
discards from it — moved [`document-paths.md`](document-paths.md)'s `pathitem-ref`
from `limitations` to `golden` as well.
`securityscheme-ref` becomes a `gap` whose settlement is `FIXTURE`, on this
table's `fern-rejected`: all three of its Apache-2.0 witnesses at immutable refs
are documents Fern never parses, each re-run for this change and each recorded in
[`tests/fixtures/AGENTS.md`](../../tests/fixtures/AGENTS.md#specs-already-tried-and-rejected-do-not-re-attempt-without-a-fix-upstream).
Licence and pinning are not what blocks them, which is why the row is a fixture
gap rather than a probe. `http-vapid` stays `PROBE` on **witness supply**: no
document declaring it was found at all, so its settlement cell now names the six
sources and the exact query put to each rather than resting on the
four-dropped-schemes finding.

**What that evidence did to the four-dropped-schemes finding.** Its *alone* half
survives contact with a real document: `fern check` exits 1 on the Noun Project
bundle, which declares `oauth` by itself, as it did on `zerkerlabs/treeship`,
which declares `dpop` by itself. Its *paired* half does not: the three
registered `http-dpop` witnesses are paired documents whose goldens byte-match,
and what each pins is that crozier drops the unsupported scheme exactly where
Fern does, which is byte-level verification of the shape rather than a
formality. So it is the *paired* half alone that no longer keeps a row of this
family out of the corpus — `http-mutual` is the second row to prove it, `golden`
on a paired document whose golden byte-matches. No row of this region cites the
finding in its own cell any longer; the four that remain `PROBE` cite witness
supply instead. Of the four rows settled here, `http-oauth`, `http-scram-sha-1` and
`http-scram-sha-256` are open for want of a paired document that is both
redistributable and pinnable, and `http-privatetoken` for want of any document
at all — none of them because the shape cannot be isolated.

Every count above was read out of a document fetched at the reference its row
names, never out of a search-result snippet, and every date-sensitive refusal is
recorded as it was observed rather than as a claim about the service. Re-running
a row means re-running the six queries its own cell spells out; nothing here is a
guarantee that the world holds no witness beyond those six sources, only a record
of what they answered.

### What this change's second pass found, and what it settles

**Two rows are now `witness-found`, and both witnesses are screened end to end.**
`oauth2-multiple-flows` had never been searched: the census reports each of the
eleven OAuth Flows Objects across the registered sources naming exactly one flow,
which says nothing about the world. Reading both corpora whole finds **9**
declarations in APIs.guru and **265** across **250** files in
`jentic/jentic-public-apis`, and the one recorded on the row —
`OpenBanking-Brasil/specs-directory` `openapi.yaml` at commit
`37423cbc93982e05eac255db6226ed599a5baace`, MIT — clears all three screens:
licence inside the Apache-2.0/MIT/BSD/CC0 set [`CORPUS.md`](../../tests/fixtures/CORPUS.md)
states, an immutable commit ref, and Fern accepting it at **both** stages, `fern
check` exit 0 and a real generate at `fernapi/fern-python-sdk` 5.20.0 exit 0.
`security-optional-requirement-operation` had never been searched either, and its
one registered declarer is a **DROPPED** row; the search finds **83**
declarations in APIs.guru and **4,518** across **183** files in the jentic
corpus, and **three** documents that clear all three screens. Registering either
row's witness retires it from this backlog outright.

**One document settles both of the new rows at once.**
`apis/openapi/discord.com/main/10/openapi.json` in `jentic/jentic-public-apis` at
commit `eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` is MIT in its own
`info.license`, declares **22** operation-level `{}` security requirements *and*
**1** OAuth Flows Object naming three flows whose scope sets differ pairwise, and
is accepted by `fern check` and by a real 5.20.0 generate. It is recorded on
`security-optional-requirement-operation` and named again on
`oauth2-multiple-flows`, so the node that registers these can spend one corpus row
where the table's own primaries would spend two. The primaries are named as they
are for one reason each: `braintrust.dev` carries **148** declarations of the
optional requirement against a single Fern-supported `bearer` scheme, and
`OpenBanking-Brasil/specs-directory`'s two flows carry **disjoint** scope sets,
which is the sharpest discriminator this region's flow-precedence question can
have.

**The registered declarer of `security-optional-requirement-operation` is
unregistrable, and this pass says which of the three screens it fails.**
`atlassian.com-jira` is [`CORPUS.md`](../../tests/fixtures/CORPUS.md) row 27 and
declares the shape **221** times. Its licence is Apache 2.0, inside the corpus's
set; it is reachable at the versioned APIs.guru reference the manifest already
pins, `https://api.apis.guru/v2/specs/atlassian.com/jira/1001.0.0-SNAPSHOT/openapi.json`.
It is **Fern** that refuses it, and this pass measured that rather than inheriting
it: `fern check` at CLI `5.67.1` exits **1** with `[sdk] 3 errors` — `Enum name  for
value ~ is not suitable for code generation`, `ProjectFeatureState is already
declared in projectFeatures.yml`, and `Multiple request properties have the name
id` — and a real generate at `fernapi/fern-python-sdk` **5.20.0** exits **1** on
the same three, reproducing the *"3 fatal diagnostics"* the batch-4 ledger
records. None of the three touches security, which is why the row is settled by a
different document rather than by an argument about this one.

**The five rows that already had a search keep their outcomes, and now record why
no better one was reachable.** For `http-hoba`, `http-scram-sha-1` and
`http-scram-sha-256` the second pass is a clean zero everywhere it could read: the
two complete corpora hold **0** declarations between them across 78,378 documents,
and of the **1,291** GitHub documents this pass fetched, the **1,240** that parsed (51 unparseable) hold **0**. The
only declaration any *readable* document body holds is the same unlicensed,
unpinnable SwaggerHub `Auth Test` document, reached again through the same
neighbour query. **Three candidates stay unresolved rather than negative**, all
of them Postman's: one API returned for `hoba` and two for `scram-sha-1`, whose
bodies `api.getpostman.com` serves only to a credential (**HTTP 401**). They are
recorded as evidence this search could not read, not as absence — so all three
rows stay `witness-blocked` on that one document, and what would unblock three
rows at once is still a redistributable copy of it at a pinnable reference.
`http-oauth` stays `fern-rejected`: the corpora add exactly one document the first
pass did not name, `ads-api.twitter.com/main/12` in the jentic corpus, and it is a
third copy of the X document already blocked on the *X Developer Agreement and
Policy* — no document pairing `scheme: oauth` with a Fern-supported scheme under a
redistributable licence exists in either corpus, in the 1,240 of 1,291 fetched GitHub documents that
parsed, in 142 SwaggerHub documents, or in the OAuth 1.0a-era vendor portals.
`securityscheme-ref` stays `fern-rejected` for the sharpest reason of the five:
**both complete corpora hold zero** — 0 of 4,138 and 0 of 74,240 — and the only two
new candidates anywhere both `$ref` across documents exactly as the three recorded
ones do, and both reproduce the same false success. The property a usable
candidate would need — a `$ref` that stays inside its own document, where
`resolveSecuritySchemeReference` can follow it — was searched for and found
nowhere.

**Every required source answered, and Sourcegraph is the one that nearly did not.**
The public streaming index answers `HTTP 429` behind a Cloudflare managed
challenge after roughly six queries from one address, and — this is the trap —
the challenge body parses to *no matches*, so a refusal reads exactly like a
zero unless the HTTP status is read. A first attempt at this pass recorded
fourteen such zeros before a control query returned 0 where it had returned 97
minutes earlier; those results were discarded rather than written down. The
record below therefore rests on a query runner that reads the status, treats a
non-200 as `unanswered`, paces at 90–120 seconds and re-runs only what has not
yet answered at 200, and interleaves a `content:"scheme: bearer"` control before
the first query and after every fifth. All **17** queries in the final run
answered `HTTP 200`, and the control returned **97** on each of its three runs.
The set was narrowed to what each row needs — one conjunction pair for
`oauth2-multiple-flows` rather than all six — because every extra query against a
rate-limited endpoint costs the queries that matter, and the pair chosen is one
this row's own witness declares. The **241** distinct files those queries
returned were then fetched from the CDN and parsed, so every count below is read
out of a document rather than out of a match snippet.

**What that parse found that no grep could.** Twenty-one files hold a `$ref` in
a Security Scheme Object position, carrying **23** references between them, and
sorting those by *where the reference points* is what settles
`securityscheme-ref`. **Ten** point inside their own document —
`#/components/securitySchemes/…`, the one form `resolveSecuritySchemeReference`
could follow — and one of the ten is the `zaghaghi/openapi-tui` meta-schema this
row's ledger records as a false positive. **Every one of the other nine is a
tooling test fixture**, across four projects: `getkin/kin-openapi` (2),
`yaklang/yaklang` (2), `aaronchen2k/deeptest` (2) and three `swagger-api/apidom`
refractor fixtures — none of them a deployed API's document. The remaining
**13** references, which are every one a real-world document carries, all point
across documents. Every *real-world* declarer found anywhere
references across documents, which is precisely the property Fern refuses. That
is not a gap in the search; it is the search's answer.

**What Fern's exit code did *not* mean, twice more.** `flightctl/flightctl` and
`technology-platforms/vcfa-api-docs` both exit **0** from `fern check` *and* from
a real 5.20.0 generate, and neither is accepted: each prints `Failed to parse
openapi document` and a `CliError` out of `resolveSecuritySchemeReference` before
printing `All checks passed`. What the generate then wrote is the measurement the
first pass could not take — **35** files each, an empty client with no endpoints
and no types, against `braintrust.dev`'s 747 from a document Fern did parse. A
generate that exits 0 having emitted a client for nothing is the same false
success as the exit-0 check, one stage along.

#### The candidate ledger

Every document this pass screened and did not record as a witness, with what it
declares and why. Each is what a repeat of this search finds first.

| candidate | ref | licence | what it declares, and how many | what became of it |
|---|---|---|---|---|
| `atlassian.com-jira` — [`CORPUS.md`](../../tests/fixtures/CORPUS.md) row 27, `https://api.apis.guru/v2/specs/atlassian.com/jira/1001.0.0-SNAPSHOT/openapi.json` | the versioned APIs.guru reference the manifest pins, `1001.0.0-SNAPSHOT` | Apache 2.0 — inside the corpus's Apache-2.0/MIT/BSD/CC0 set | `security-optional-requirement-operation` **221**; none of this node's other rows | **not a witness — Fern refuses it, and only Fern.** Licence and reachability both pass; `fern check` at CLI `5.67.1` exits **1** with `[sdk] 3 errors` and a real generate at `fernapi/fern-python-sdk` **5.20.0** exits **1** on the same three, none of them about security. It stays **DROPPED** |
| `Redocly/openapi-starter` `openapi/openapi.yaml` | commit `0d8aeeaef7d2953a0dbc39fa679543a68a5720d1` | MIT, in the repository and in `info.license` | `oauth2-multiple-flows` **1** (`OAuth2`: `authorizationCode` + `clientCredentials`) | **screened clean and still not recorded.** Fern accepts it at both stages (check exit **0**, generate exit **0**, 38 files), but its two flows carry the **same** five scopes, so a golden over it cannot tell which flow `oauth_scope_enum` reached — and it is Redocly's published *starter template* rather than a deployed API's own document |
| `api-evangelist/visier` `openapi/_original/visier-data-in-apis-openapi.yaml` | commit `e5e7c36e68c702580895ef4c3ded6b10330bae6a` | Apache License, Version 2.0 in `info.license`; the repository carries no licence file | `oauth2-multiple-flows` **1** (`OAuth2Auth`: `authorizationCode` + `password`, same scopes) | **not a witness — Fern refuses it.** `fern check` exits **1** and the 5.20.0 generate exits **1**, both on `path: tablesource.yml -> service -> endpoints -> regeneratematerializedview`, `issue: Multiple request properties have the name tableId` |
| `Open-EO/openeo-api` `openapi.yaml` — the **main** document, not the workspaces extension already in the rejected list | commit `a2efa3464c28242e98d72973db09f7eda9767583` | Apache 2.0 in `info.license`; the repository is Apache-2.0 | `security-optional-requirement-operation` **12**; `securityscheme-ref` **0** (the reference-valued scheme is in the extension document, not this one) | **not a witness — the generator refuses it.** `fern check` exits **0**, and the real 5.20.0 generate exits **1** on `OSError: [Errno 36] File name too long` writing `process_graph_json_schema_returns_returns_…_subtype.py`, a recursive type-name explosion with nothing to do with security |
| `dns3l/dns3l` `openapi.yaml` | commit `32ea4062af76a2752092639e416a2396d9e28ded` | Apache 2.0 in `info.license`; the repository is MIT | `security-optional-requirement-operation` **15** | **not a witness — the generator refuses it.** `fern check` exits **0**; the 5.20.0 generate exits **1** with `fernapi/fern-python-sdk Unknown OAuth type: 'undefined'` |
| `flightctl/flightctl` `api/imagebuilder/v1alpha1/openapi.yaml` | commit `3537fca43bcf06f2ac760de29979fa0bfd377cf0` | Apache-2.0 (repository) | `securityscheme-ref` **2**, both `$ref`s into `../../core/v1beta1/openapi.yaml` | **not a witness — Fern never parses it.** `Failed to parse openapi document Flight Control Image Builder API`, `Failed to resolve …#/components/securitySchemes/bearerAuth` out of `resolveSecuritySchemeReference`, then `All checks passed` at exit **0** — and the generate exits **0** too, writing **35** files with no endpoints and no types. The first pass measured this document at 0 declarations; it has since moved its schemes to `$ref`s |
| `technology-platforms/vcfa-api-docs` `openapi.yaml` | commit `e5a164adcb58acf955263097651cd443c3196c2c` | Apache-2.0 (repository); the document declares no `info.license` | `securityscheme-ref` **1**, `./components/securitySchemes.yaml#/bearerAuth` | **not a witness — the same false success**, `Failed to parse openapi document VCF Automation — Undocumented API` then `All checks passed` at exit **0**, generate exit **0** and **35** files |
| `zaghaghi/openapi-tui` `openapi-31/openapi-in-openapi.json` | — | — | apparent `securityscheme-ref` **1** | **false positive.** It is the OpenAPI 3.1 meta-schema; its `security-scheme-or-reference` `$ref` *describes* the shape rather than declaring it, which is why the row's count is read off a parse that knows where `components.securitySchemes` is |
| `openbankingproject.ch` 1.3.8 (APIs.guru) | `1.3.8_2020-12-14 - Swiss edition 1.3.8.1-CH` | **Creative Commons Attribution 4.0 International Public License** | `security-optional-requirement-operation` **34** — the largest count in APIs.guru | **not a witness — licence outside the set.** The corpus states its set as Apache-2.0/MIT/BSD/CC0 in `CORPUS.md`'s own `assemblyai-autosdk` rejection, and no registered row is CC BY. Whether to widen the set is the registering node's call, not this search's |
| `ads-api.twitter.com/main/12` (jentic) | commit `eb9d12a2684b0fbcb5aecf51e8ae54dba0929743` | *Twitter/X Developer Agreement and Policy* | `http-oauth` **1** | **not a witness — licence outside the set.** It is the one `scheme: oauth` document in either corpus the first pass did not already name, and it is a third copy of the X document that pass blocked on the same terms |
| the five unlicensed APIs.guru multi-flow documents — `fulfillment.com/2.0`, `gerermesaffaires.com/1.0.6`, `gettyimages.com/3`, `motaword.com/1.0`, `vimeo.com/3.4` | their APIs.guru version references | **none declared.** `APIs-guru/openapi-directory` is itself CC0-1.0 and its README says contributed definitions are | `oauth2-multiple-flows` **1** each; `gerermesaffaires.com` is the only one of the five whose flows carry different scopes | **not witnesses — no licence this corpus can rest on.** Of the 67 registered `api-guru` rows, the **46** whose document is in the pinned clone were checked and **all 46** declare an `info.license` of their own, so registering one on the aggregator's CC0 alone would be a new precedent rather than an existing one. `ebay.com` ×3 (*eBay API License Agreement*) and `surevoip.co.uk` (*Proprietry*) are blocked outright |
| `ChurchSuite/churchsuite-api` `src/openapi.yaml` | HEAD at the time of the search | **none** — the document declares no `info.license` and the repository publishes no `LICENSE` file (fetched, HTTP 404) | `oauth2-multiple-flows` **12** — the largest count this search found in any single document | **not a witness — no licence this corpus can rest on.** Its twelve `oauth2` schemes each pair `clientCredentials` with `authorizationCode` over one shared scopes map, so even licensed it could not discriminate the precedence chain; it also declares `openapi: 3.2.0`, which is outside this document's scope rows |
| `getkin/kin-openapi`, `yaklang/yaklang`, `aaronchen2k/deeptest` and three `swagger-api/apidom` refractor fixtures | HEAD at the time of the search | various | `securityscheme-ref` **1** each — nine references in all, and **the only ones anywhere whose `$ref` points inside its own document** (`#/components/securitySchemes/…`) other than the `openapi-tui` meta-schema below | **not witnesses — none is a real-world API document.** They are the parser test fixtures those four projects ship. They matter because they are the *only* documents found with the one property that would let Fern resolve the reference, which is what makes `securityscheme-ref`'s blocker a property of the world's real documents rather than of this search |
| `Open-EO/openeo-api` `extensions/processing-parameters/openapi.yaml` | HEAD at the time of the search | Apache 2.0 | `securityscheme-ref` **1** (`../../openapi.yaml#/components/securitySchemes/Bearer`); `security-optional-requirement-operation` **1** | **not a witness — a fourth cross-document declarer**, the same repository and the same `$ref` shape as the workspaces extension already in the rejected list, so the same `resolveSecuritySchemeReference` refusal applies |
| `Alia5/steaminputdb.com` `openapi.yaml` | HEAD at the time of the search | AGPL-3.0 | **nothing this node owns** — see the verdict | **a correction to the first pass, not a candidate.** That pass records it as declaring `scheme: OAuth` under AGPL-3.0. Read rather than grepped, its `Steam Auth` scheme is `type: oauth2` and carries `scheme: OAuth` as a stray key; `scheme` is not a field of an OAuth2 Security Scheme Object, so the document never declared `http-oauth` and its licence was never what disqualified it |
| `novaxsolution/0WalletApi` 0.0.1, `novaxv1/0WalletApi` 0.0.1 and `our-377/1-izocloud-nextjs-template` 1.0.0 (SwaggerHub) | none — SwaggerHub exposes only the mutable version reference, which its owner can edit in place | none — no `info.license`, and SwaggerHub publishes no licence property | `security-optional-requirement-operation` **2**, **2** and **1** | **not witnesses — the same two blockers as the `Auth Test` document**: no licence, and no immutable reference. They are the only declarers of any of this node's rows that SwaggerHub's 142 fetched documents hold besides `Auth Test` |
